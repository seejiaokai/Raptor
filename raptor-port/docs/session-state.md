# Session handoff — [LW-MONTHJUMP-PHONE]: two tests fixed, the checks moved to his PC; PR #428 NOT merged (23 Sep 26)

**Something IS unfinished.** The full handoff is `HANDOFF-NEXT.md` at the repo root — read that; this file
exists only so `CLAUDE.md`'s promise (absent = nothing pending) stays true, and deliberately does not repeat it.

- **Branch:** `claude/lw-monthjump-phone`, pushed, PR #428 open, NOT merged (owner, D152). Start there, not `main`.
- **First job:** fix `[LW-MONTHJUMP-PHONE]` and `[LW-HBAR-RESYNC]` on that branch, then delete the repo variable
  `CI_ON_GITHUB` so the checks run on his PC, then merge on his "merge live".

The previous content of this file (`[ALL-AVAIL-WINDOW]` built, not bug-checked) described work that has since
merged and gone live — overwritten, not appended.
