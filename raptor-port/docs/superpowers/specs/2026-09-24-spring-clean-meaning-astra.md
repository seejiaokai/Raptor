# Spring clean — Astra's meaning check (D138, D70), 24 Sep 26

Tier 3. Astra (Codex, gpt-5.6-sol, high, read-only) on `claude/spring-clean` at `316e2c55` (before Fable's fixes, `5f0c...` later), against `main` `d0a1bb94`; brief: `raptor-port/docs/superpowers/briefs/2026-09-24-spring-clean-meaning-brief.md`. Saved verbatim; dispositions follow in the next commit or in `HANDOFF.md`'s `claude/spring-clean` block.

Against committed `HEAD` `316e2c55` only; later uncommitted worktree edits were excluded.

1. **HIGH — superseded OIL lock remains live.** New ruling: `HEAD:.claude/rules/decisions/oil.md:39`; conflicting live text: `HEAD:OUTSTANDING.md:102-104,132-134`; originals: `main:OUTSTANDING.md:245-248,276-278`. D142 says a later published amendment/EOD may remove past-day OIL—“no lock, no clock”—but the backlog still preserves the worked-day lock and treats that removal as a defect. **Exact fix:** replace lines 102–104’s OIL clause with “OIL for a day comes from its latest published version—the latest amendment, or the EOD if latest—however old the day; no lock, no clock (D142),” and delete REV5-04 lines 132–134 as superseded.

2. **HIGH — a performance owner-approval condition is archive-only.** New live destination: `HEAD:raptor-port/docs/performance.md:653-666`; archive-only copy: `HEAD:raptor-port/docs/archive/handoff-2026-09-24.md:210-214`; original: `main:HANDOFF.md:374-379`. Performance sessions will not load the archive, so they miss that the residuals are unbuilt/deferred and that two require the owner because they change wording or feel. **Exact fix:** restore in `performance.md`: “**The performance thread — residuals recorded, not built.** Several speed wins are measured and deferred (the seven day-strings sort, the JS-bound drop, hover-boundary repaints, the `body.dnd` decorations, the one `validate` call); two are the owner’s call because they change wording or feel.” Also file a live `[PERF-RESIDUALS]` backlog item and priority pointer.

3. **HIGH — D140 routes truly global rules to a file root-only sessions do not load.** New text: `HEAD:.claude/rules/doc-structure.md:30`; contradictory loading definition: `HEAD:.claude/rules/doc-structure.md:10-13` and `HEAD:raptor-port/docs/doc-budget.md:16`; original assumption: `main:raptor-port/docs/doc-budget.md:16`; controlling ruling: `HEAD:.claude/rules/decisions/how-we-work.md:15`. A future “EVERY task” rule placed only in `raptor-port/CLAUDE.md` is missed before any `raptor-port/` Read. **Exact fix:** change line 30 to: “a rule EVERY task needs, whatever the area | an unscoped file under `.claude/rules/`—or `decisions/how-we-work.md` for a general ruling; `raptor-port/CLAUDE.md` carries only its project-side pointer/copy.”

4. **HIGH — the Tracker repository-data safeguard moved out of automatic reach.** New location: `HEAD:raptor-port/docs/file-map.md:144`, with another non-automatic copy at `HEAD:raptor-port/docs/tracker/known-gaps.md:309-311`; original: `main:HANDOFF.md:913`. The widened Tracker area file only points toward those documents (`HEAD:.claude/rules/decisions/tracker.md:24-35`), so a session opening `src/tracker/data/**` need not load “no student name, mark or date may enter the repository.” **Exact fix:** add to `tracker.md` after line 29: “**No student name, mark or date may enter the repository.** The repository has been private since 23 Sep 26 (D59), but the rule stands because collaborators can read it.” Correct file-map line 144’s obsolete “public repo” reason likewise.

5. **LOW — the handoff skill still tells reviewers to check the obsolete home.** New: `HEAD:.claude/skills/session-handoff/SKILL.md:139-140`; original: `main:.claude/skills/session-handoff/SKILL.md:136-137`. The sentence was valid when the file map and open issues lived in `HANDOFF.md`; after D140 they live in `docs/file-map.md` and `OUTSTANDING.md`. The following bullets name them, so the safeguard is blurred rather than lost. **Exact fix:** replace lines 139–140 with: “For each path it reports, confirm—by reading the applicable live record (`HANDOFF.md`, `OUTSTANDING.md`, or `raptor-port/docs/file-map.md`), not from memory.”

Nothing else: shipping; the remaining HANDOFF/HANDOFF-NEXT rewrite; the other eight archived backlog items; CLAUDE/scheduler/leave-war area moves; gates, data-schema, engine-rules and pointer edits; or the skill’s remaining safeguards and size.

