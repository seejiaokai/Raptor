# Brief — the D114 change: a request taken off (or put on) a published day is ONE pending change (25 Sep 26)

You are reviewing ONE small change on branch `claude/amendment-batch`, commit `a95afcbe` (read it with
`git show a95afcbe`). You did not write it. Do NOT edit any file, and do NOT run the test suite or any build — read the
code and the tests. Your final message is your whole report.

## The owner's rulings it implements (the rulings file `.claude/rules/decisions/scheduler.md`, rows D109, D113, D114)

- **D109** (25 Sep 26): every count of a published day's waiting changes reads ONE body (`publish.ts dayPendingItems`),
  counted in the unit a person counts in — a man moved on the same day is one; a swap two; only taken off or only
  added one. The stored diff (what goes out) is unchanged.
- **D113** ("5 yes"): a replacement in one seat is one. Nothing built for it — it was already so.
- **D114** ("6 yes"): taking an accepted request off a published day (✕ on its row) is ONE change, not two (the row
  removed + the request's filing). The agent's reading, told to the owner: the mirror (accepting a request onto a
  published day) is also one; a filing with no row of its own on that day stays its own one change.

The register line: `raptor-port/docs/superpowers/specs/2026-09-24-amendment-behaviour-register.md` AM23.
Background: `raptor-port/docs/engine-rules.md` (§the pending count's unit), `raptor-port/docs/ui-contracts.md`
(§The pending list). The counting body: `raptor-port/src/engine/publish.ts` (`dayPendingItemsIn`, `alIssue`,
`retireIssued`), `raptor-port/src/engine/canonical.ts` (`canonicalUnits`, the structural add/delete addresses),
`raptor-port/src/ui/pendlist.ts` (the words), `raptor-port/src/ui/ALPanel.tsx` (an issued AL's line). Where a request
lands as a row: `raptor-port/src/engine/slots.ts` (`acceptInput`, `unacceptInput`, `reconcileDayFiling`).

## What to do — two jobs

1. **Scenarios first (what is MISSING):** list the concrete situations in which this pairing would count WRONG —
   two items where the owner would see one act, or one item that hides two acts — or word the line wrongly, or break
   an invariant: "the item list is empty exactly when `dayDelta` is"; every count agrees (day head, board, ⓘ panel,
   Amendments panel, "Discard N edits", the pending list, the publish message, an issued AL's count and its line).
   Think about: a request covering several days; a request whose row sits on ANOTHER day; "→ Unavail" on a row; a row
   removed by a plain delete rather than ✕; the request edited or deleted from the Inputs page; plans (a parked plan as
   the working copy); a load of an older version; Undo / Redo; an AL after an AL; two requests for one man; a row
   whose `src` no longer matches any request; the ORIGINAL vs an AL as the issued version compared against.
2. **Read the code** for defects in those situations and any other. For each finding give: the exact scenario (steps a
   person takes in the app), what the app says, what it should say, whether it is NEW in `a95afcbe` or older, and an
   exact step-by-step fix (file, function, what to change) with the test that would pin it.

**Not a finding (owner, D56):** a problem that lives ONLY in data already stored AND where the code is already right
going forward — e.g. an AL issued before this commit has no `ukinds` and falls back to its diff's split. The whole
store is demo data, wiped before the database. Do not report those.

Also say, briefly, what you checked and found RIGHT, so the absence of a finding means something.

## Round 2 — the run that counts (25 Sep 26, afternoon; the first Astra run was stopped unfinished, so both run fresh)

You are an independent reviewer; another model reads the same change blind. **Do not look for or read any other
reviewer's report** (any `*d114*read*` file under `raptor-port/docs/handpass/` other than your own). Do not edit any
file; run nothing that changes anything (reading and grepping are fine).

**Read first — the host's roll-call and door check:** `raptor-port/docs/handpass/2026-09-25-amendment-batch.md` §9
(the tier, the rules that apply, every surface that draws the count or its words, every door that makes the pair).
Use it to look for ABSENCES — a surface, a reader, a door, an order it does not list.

**The rules files a Codex session does not load by itself** — read the rows named above in
`.claude/rules/decisions/scheduler.md` (D98, D103, D109, D113, D114, D119), and `.claude/rules/decisions/oil.md`,
`.claude/rules/decisions/how-we-work.md` (D56, D67), `.claude/rules/raptor-executor.md`.

**Already found by the host — do NOT re-report these** (§9.5): D114-1, "Discard N edits" (`publish.ts dayDiscardCount`)
counts the load's filing put-back apart from its row, so it reads 2 beside the day head's 1; D114-2, a request deleted
from the Inputs page pairs into one line that reads only "A request · on the programme → not on the programme". Their
fixes are the host's. Look past them.

**The finder brief (the standing order's wording):** Do not merely review the changed code. Starting from the user
promise and the applicable rulings, enumerate every qualifying object, renderer, visible door, writer, reader,
downstream consumer, role, overlay and meaningful order of actions. **Assume every existing line may be correct and the
defect may be a MISSING call site.** For each item, state where the visible sign and the working gesture should exist in
the production app. Then rank concrete failure scenarios with setup, action, expected result, and the observation that
would disprove correctness. Start with the least-shared or most specialised surface.

**Your report** (your final message is the whole report): (1) the scenarios, ranked; (2) each finding — the steps a
person takes in the app, what the app says, what it should say, NEW in `a95afcbe` or older (compare with `main`,
`301a11fc`), and an exact step-by-step fix (file, function, what to change) with the test that pins it; (3) the
explicit negatives — what you checked and found RIGHT.
