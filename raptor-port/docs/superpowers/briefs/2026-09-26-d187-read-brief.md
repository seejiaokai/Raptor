# Brief — D187: a look at a published version wears its warnings (26 Sep 26) — a focused read, blind

You are an independent reviewer. You did not write this code. **Do not edit any file. Do not run the full test suite**
(the owner's machine is running its gates); reading tests is fine.

**The ruling:** `.claude/rules/decisions/scheduler.md` D187 ("Q5 it should") — the edit week's and the board's 👁 look at
a PUBLISHED version shows its warnings, as View-only Sched does: the day's current version exactly as View-only Sched
draws it; an older version the warnings it went out with; a parked plan none. Context: D183–D185, D188 (what stays live
on a published face), `raptor-port/docs/engine-rules.md` §Publishing, `raptor-port/docs/ui-contracts.md` §Version
preview.

**The change:** `git diff c9fb2fd8~2..c9fb2fd8~1 -- raptor-port/src` (commit e53f9786): `src/engine/validate.ts`
(`versionFaceWarn`, `withVersionWarn`, `rewordSlice`), `src/ui/html.ts` (`withVersionFlags`, `dayPreviewHTML`,
`puckMarks`, `withChipWorld`), `src/ui/SchedBoard.tsx`, `src/ui/board.ts` (`boardWarnHTML(di, look)`), `src/state/view.ts`
(`displayedBundle`); pins `src/ui/latepub.test.tsx` "D187 …", `src/ui/html.test.ts` (the preview pin updated).

**Your job:** assume every existing line may be correct and the defect may be a MISSING call site. Every surface that
draws a look at a version (the edit week day, the board's seats and panels, its checks, the ALL AVAIL window opened from
a chip in the look, the ⓘ panel, the warning tap and the puck-select highlight, the phone widths), every kind of version
(current, older, plan, one kept before the freeze), and every order (look → edit behind → look again; AL out while
looking; unpublish while looking). Where can the look and View-only Sched disagree, a tap land on the wrong warning, a
write surface reappear, or OFW leak past the look? **Not a finding (D56):** harm only in data already stored when the
code is right going forward.

**Hand back:** findings ranked by consequence (setup / action / expected / observed) with exact fix steps, then explicit
negatives. Plain English, tight.
