# The code read — D176 (a request "taken off" when the day was published, since deleted or re-dated off it), 25 Sep 26

You are an independent reviewer of ONE small change in the Raptor repo (`C:\Users\User\projects\Raptor`), branch
`claude/request-one-row`: `git diff d13162dc 4947c004 -- raptor-port/src` (`src/engine/publish.ts` — `filingSame` gains
`present`, read by `filingDelta`; `filingKey`'s exclusion widened — and its tests in `src/ui/reqonerow.test.tsx`, the
`D176` block). Opus 5.5 wrote it. Another reviewer of a different provider reads it at the same time; do not open any
file named `2026-09-25-d176-*-read.md` but your own. Read-only: edit nothing but your report; one test file at a time
from `raptor-port/` is fine (`npx vitest run <file>`); no full suites, builds, browser tests or servers.

**The ruling:** `.claude/rules/decisions/scheduler.md` row **D176** (and D174 beside it, whose rule this extends; D98,
D103). The rule as written: `raptor-port/docs/engine-rules.md` §Publishing, "A REQUEST'S ROW AND ITS FILING ON A
PUBLISHED DAY". Everything else on the branch was already read (`2026-09-25-req-one-row-{fable,astra}-read.md`) — do not
re-read it unless this change reaches it.

**How to read:** do not merely review the changed lines. Enumerate every reader of the filing comparison and of the
signature binding, and every way a request can stop covering a published day (deleted, re-dated, retyped, re-assigned
to another person, a multi-day request shortened, a week navigation, undo / redo, a load, Unpublish) and ask of each:
does the day now read what its published face and its working copy actually show? **Assume every existing line may be
correct and the defect may be a MISSING case.** Rank concrete failure scenarios with setup, action, expected result and
the observation that would disprove correctness. For each finding: NEW or OLDER, severity, exact fix steps, the test.

This app is pre-promulgation and its entire stored world is DEMO DATA that will be CLEARED before the database step.
**Do not report a problem whose harm exists only in data already stored when the code is already correct going
forward** — no migration, no back-compat. If the app would do it again to NEW data, report it.

Give **explicit negatives** ("I checked X and found nothing"). Write your report to
`raptor-port/docs/handpass/2026-09-25-d176-<you>-read.md` (`<you>` = `fable` or `astra`). Keep it tight.
