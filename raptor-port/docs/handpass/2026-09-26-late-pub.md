# Evidence — `[LEAVE-LATE-PUBLISHED]`: a published day keeps what it went out with (D177, D178, D179; what stays live: D183, D184, D185) — 26 Sep 26, overnight (D181) and morning

Branch `claude/leave-late-published`. Plan `docs/superpowers/plans/2026-09-25-late-published-plan.md`. Built by Opus 5.5;
scenarios and plan attacks by Fable 5.1 and Astra (`docs/handpass/2026-09-25-late-pub-{fable,astra}-{scenarios,plan}.md`).

## 1. The eight questions and the tier
1 Money — YES (a request's OIL window and the ALL AVAIL crowd move the OIL axis; folded, not changed). 2 The published record —
YES (the whole point). 3 Saved data — YES (two new snapshot fields, persisted). 4 A shared drawer — YES (the Unavailable block,
the board's panels, the puck marks on every face). 5 A new gesture — NO (no control added). 6 A new surface — NO (the
pending list gains two kinds of line). 7 Roles — NO change of who may do what (a member still files his own; the admin
publishes). 8 The warning list — YES (the issued face's warnings freeze). **Tier: FULL.**

## 2. The rulings swept
D177 (a late leave pends, the four fall, the face keeps what it had), D178 (every member input change), D179 (medical,
quals, rules, neighbour days freeze — provisional), D183 (the dotted next-day crew-rest mark stays live), D184 (the 7-day
run warning live too — and, by the agent's reading, a crew-rest breach on the day), D185 (a lapsed qualification live;
medical frozen still — the agent's reading widened at the build to the Qualification-flag family and the crew-rest
tight turn, recorded on D185's row), D44/D45 (the freeze-and-pending half), D103 (any pending change wipes
the sign-offs), D98 / AM20 (back to issued reads 0), D174 / D176 (absent ↔ taken off reads 0), D109 / D113 / D114 (one act,
one item), D48 (a rule change waits for a re-issue — held for OIL; now for warnings too), D19 (the no-period advisory stays
live), D142 (OIL from the latest published version — untouched), 14 Sep 26 (a rename is a label), 7 Aug 26 (no rule
versioning — see §8, the clash put to him).

## 3. The roll-call — every place a published day's inputs and warnings are drawn

| Surface | Shows the issued inputs? | Shows the issued warnings? | Pending reads it? | Proof |
|---|---|---|---|---|
| View-only Sched, issued face — Unavailable block | YES (`withDaySnap` installs `snap.inp`) | YES (`faceWarn`) | — | walk A1, B1, C1 close-ups; `latepub.test` "filed", "edited", "deleted" |
| View-only Sched — the ⓘ panel from the issued face | YES (`withChipWorld` → `withDaySnap`) | YES | — | code path; `availwin.test` issued-face cases green |
| View-only Sched — the ALL AVAIL window from the issued face | YES (the same world replay) | YES | — | `availwin.test` (48 green) |
| View-only Sched — "Working draft" (VWORK) | NO, because a reader's own choice (D179 leaves it live) | NO, because the same | — | unchanged path (`viewDayHTML`) |
| Edit week — 👁 preview of an issued version | YES (`withDaySnap`, which also installs the issued CAT / brief lead / roster) | no flags on a past version (PV), as before — `[PV-NO-FLAGS]` | — | `withDaySnap` is the one swap |
| Board — 👁 preview of an issued version: Personal Inputs / Unavailable / SANS panels | YES (`inputsOn` through `withDaySnap`, with the issued CAT / brief lead) | input-row pucks now read the gated marks (none on a preview — Fable F1); no flags at all — `[PV-NO-FLAGS]` | — | `board-html.ts sbInpRow` / `sbInputsHTML` |
| Edit week / board — the working copy | NO, because it is where the admin works | NO, because the same (WORKING) | YES — every count | walk A1/B1/C1 counts (week, board, both ⓘ, Amendments panel) |
| The pending list | the frozen copy names a deleted input | "Warnings on this day" | YES | walk A1/B1 pictures; `latepub.test` |
| The official pass (validator) — a published date | YES (`withIssuedWeek` → `withFrozenInputs`, + `windowInputs` for neighbours) | it IS the detector | the warnings axis | `official-flags.test` (26 green, CRPF-001 and §4 flipped) |
| Print / CSV | content only (no input reader) — *superseded by the morning row below: they draw the issued CAT and brief lead* | — | — | Fable's explicit negative |
| A man's CAT letter / colour on a puck; a blank brief's printed time | **NO — still drawn live** | — | YES (`pa`, `rv` compared) | `[LATE-PUB-FACE-LIVE]` — his call (§8) |
| The next-day crew-rest mark (dotted ring) | — | **NO — stays live by design** (Fable F4) | NO (out of the key) | `[LATE-PUB-FACE-LIVE]` item 3 — shown to him |
| The Leave War's "no period" advisory | — | **live by design** (`LIVE_ON_FACE`, D19/D179) | NO | Fable F5 |
| Desktop next-week preview (`peek.ts`) | NO — shows next week's working copy, as before this build | — | — | outside the item; noted in `[LATE-PUB-FACE-LIVE]` |
| **(morning) What stays LIVE on the issued face (D183–D185)** — a crew-rest breach / tight turn, the 7-day run, the Qualification-flag warnings, the OIL warnings, with their rings and flags | — | **YES, live** — `faceWarn` lays the official pass's `lv` marks and live warnings over the frozen slice | **NO** — out of `warnSliceOf` / `warnSliceKey` (never stored, never compared) | `latepub.test` "D184 / D185 …" (6 pins), `official-flags` §4 (2), `amendretest-lw` F3; walks N·, L·, Q·, LW1 (§4c) |
| (morning) A man's CAT / seat / posting on a puck; a blank brief's printed time (Astra #2) | **YES, frozen** — `faceattrs.ts` draws `snap.pa` / `snap.rv` on the face, CSV and print | — | YES (`pa`, `rv`) | `latepub.test` "Astra's code read" (4 pins); walks Q4, L4 |
| (morning) The pending list's input line — its tap target (F4) and one man's takeover as one edit (F2) | — | — | YES | `latepub.test` F2, "accepted request edited …"; host walk H1, H3 |
| (morning) The load's message, both doors (F5: "already at" and the load itself) | — | — | YES | `latepub-load.test` (2, through the App's button); host walk H4 |
| (morning) The Amendments panel's words ("N input change(s)", the AL tag's "what the day shows" — F6) | — | — | YES | `latepub.test` "a leave FILED …" |
| (second reads) The ALL AVAIL window from an issued face — its columns (pilots / WSOs) and order, for a man behind the placeholder only | YES — `seatShown` / `byCrewShown` over `snap.pa`, which now holds the frozen crowd | its flags as before (the face) | YES (`pa`) | `availwin.test` "the issued window keeps a crowd-only man …" |
| (second reads) The day panel's "free all day" on the issued face | the roster as issued (`snap.ros`, `rosterShown`) | — | NO (drawn, never compared) | `latepub.test` "a man posted out who is nowhere on the day …" |
| (second reads) Week Insights | NO — the working copy's week, for everyone, as on `main` | NO — the working warnings | — | pre-existing; `[INSIGHTS-WORKING-COPY]` |

## 4. The walk — `scripts/handpass/am/late-pub-walk.mjs`, the production build on 4173, desktop 1440×900 and phone 390×844
Pictures: `docs/img/handpass/2026-09-26-late-pub/{desktop,phone}/`. Fresh demo world per section, made through the app's own
controls (the board's sign-off selects and Publish, the Inputs page's form, the board's in-place remarks box, the top bar's
Undo, Publish AL, View-only Sched).

| Order | What the screen said | Result |
|---|---|---|
| A0 publish Monday, sign again | ORIG; four signed | PASS |
| A1 Ranger's LL filed after publishing (Inputs page) | "1 pending" on the week, the board, both ⓘ ("1 unpublished edit"), the Amendments panel; four boxes blank, "4 to sign"; the list "Ranger · LL — filed"; the working copy's Unavailable shows the row (LATE); View-only Sched's Monday text **byte-for-byte as before** | PASS (desktop + phone) |
| A2 Undo (top bar) | the leave gone; every count 0; the four boxes back as signed | PASS (desktop + phone) |
| B0 publish Wednesday | the face shows Taipan's OL "off island" | PASS |
| B1 the OL's remarks edited in place (board) | every count 1; the list "Cobra · OL “Overseas leave — off island” → “EDITED AFTER PUBLISH WALK”"; the face still "off island" | PASS |
| B2 sign, Publish AL1 | tag AL1; the face shows the new words; every count 0 | PASS |
| C0 publish Monday | ORIG | PASS |
| C1 ATT C filed for Saber (flying Monday's first line) | every count 1; the working copy rings him red and lists the downchit; the published face's text **byte-for-byte as before**, its puck class unchanged | PASS |
| console errors | none | PASS |

**Walk totals:** desktop 22 PASS / 0 FAIL, phone 10 PASS / 0 FAIL (the phone runs world A; B and C are desktop-only — see §7).

## 4b. The morning re-walk and the host's walk of the fixes — the production build of `fcfbf198` on 4173
- **The re-walk** (`late-pub-walk.mjs`, `HP_REWALK` → `docs/img/handpass/2026-09-26-late-pub/rewalk/`): desktop **22 PASS / 0
  FAIL**, phone **10 PASS / 0 FAIL**, no console errors. The Amendments panel now reads "Mon · 1 change · 1 input change".
- **The host walk** (`late-pub-host-walk.mjs`, pictures `…/host/{desktop,phone}/`), through the Inputs page's own form,
  its takeover dialog ("Keep them", "Save"), its upchit dialog ("Save upchit"), its ✎ editor and the pending list:

| World | What the screen said | Desktop | Phone |
|---|---|---|---|
| H1 F2 — ATT C Mon–Fri, Wednesday published, the MO files OML for Tuesday ("Keep them") | Wednesday "1 pending"; the list "Vandal · ATT C “LONG DOWNCHIT” → “LONG DOWNCHIT till 17 Jul”" — one line; the face byte-for-byte as before | PASS | PASS |
| H2 F1 — ATT C Mon–Fri, Monday and Thursday published, an upchit on Thursday | Thursday "Vandal · ATT C moved off this day"; Monday ONE line "“WEEK DOWN” → “WEEK DOWN till 15 Jul”" (the note the trim writes — look card Q4); Monday's face keeps "WEEK DOWN" | PASS | PASS |
| H3 F4 — Yeti's accepted Appointment re-timed on the Inputs page, Monday published | "1 pending"; ONE line "Bolt · Appointment 10:00–11:00 → 15:00–16:00", a button; the tap flashed the row | PASS | PASS |
| H3b the LATE badge — Bruise's "Fly with" (on time when published) edited today | working copy LATE; the published face no LATE; after AL1 the face LATE with the new words | PASS | PASS |
| H4 F5 — a member's leave the only change; the Original previewed; Load | **FAIL on `fcfbf198`**: "Monday is already at Original" — the load short-circuits and its own sentence never runs, beside "1 pending". **Fixed** (the "already at" message says it) — pinned through the App's button (`latepub-load.test`); re-walked: "Monday is already at Original · 1 member input change stays pending — a load cannot put back a member's own record" | FAIL → PASS | (desktop only) |
| H5 (second reads) — Monday published; a man who is nowhere on Monday posted out with the Quals page's ✕ | the published day panel's "Free all day" 17 → 17; the working copy's 17 → 16; Monday 0 pending | PASS | PASS |
| H6 (second reads) — Friday (no flying lines) gets an SC wave through the board's "+ Wave", published; the Logic page's brief lead 140 → 150 | Friday 0 pending — its standby line prints no brief | PASS | PASS |

**The host walk's totals, on the final build (`05abfd71`):** desktop **24 PASS / 0 FAIL**, phone **22 PASS / 0 FAIL**, no
console errors (H4 is desktop only).

## 4c. The four parallel walks (D16) — the production build of `2c665ad9`, each on its own port, fresh worlds, both widths
Each walker is an Opus agent with the brief `scratchpad/walker-brief.md` (the fixture only through the app's own controls,
assertions of the right behaviour, pictures looked at). Their scripts are the re-walk: `scripts/handpass/am/late-pub-{quals,
logic,neighbour,leavewar}-walk.mjs`; pictures `docs/img/handpass/2026-09-26-late-pub/<route>/`. Every finding was reproduced
by the host before it entered this sheet (the dispositions below).

| Route (port) | What it drove | Desktop | Phone | Console |
|---|---|---|---|---|
| Quals (4174) | Q1b a CAT change raising a crew-pairing warning (frozen: face "A", no ring; 1 pending "Drifter · CAT A → OCU" + the warning "— new"; put back 0). Q2 DAAR unticked under a published AAR line: the face rings Warden red with "Q" at once, the summary 4 → 5, **0 pending, the four stand**; the member sees the same; ticked back → gone. Q3 an SC shift built with "+ Wave → SC", Saint on its seat, SC DAY unticked: live on the face, 0 pending. Q4 a letter-only CAT change: the face keeps "IP", 1 pending "Saber · CAT IP → A". Q5 a man posted out (the row's ✕): 1 pending "posted out no → yes"; Restore → 0 | 66 / 66 | 66 / 66 | none |
| Logic (4175) | L1 crew rest 12h → 14h under a published Tuesday: three new breaches ringed on the face at once, the summary 4 → 7, Monday's dotted marks follow; **0 pending, the four stand**, through a reload; back → gone. L2 the 7-day limit 6 → 1: 17 men ringed "7" on the face at once, 0 pending. L3 the long-day limit 12h → 6h: the face byte-for-byte as issued, the working copy 26 issues, **1 pending** listing the long days "— new", the four fall; back → 0. L4 the brief lead 2h20 → 2h30: the face keeps 10:20, the working copy 10:10, 1 pending "A blank brief — its suggested lead 140 min → 150 min" | 61 / 61 | 61 / 61 | none |
| Neighbour day (4176) | N1 Hex put on draft Monday's evening SDO desk: Tuesday's published face rings him red "R" at once, the summary 4 → 5, **0 pending, the four stand** — through the board's undo, redo and a reload; Monday draws the dotted mark on both his pucks; Forge back → gone. N2 Monday published too: Monday 1 pending, Tuesday's face waits for Monday's AL1, then shows the breach — Tuesday never pending. N3 Outlaw's landing moved earlier in steps: the breach's words follow the times at once, then "TT" (the tight turn — live, D185 reading (e)), then clear — Tuesday 0 throughout. N4 Echo put on draft Tuesday's early line: Monday's published face draws his dotted mark at once, 0 pending | 54 / 54 | 54 / 54 | none |
| Leave War (4177) | LW1 a PH declared on the war after Tuesday went out: EXACTLY 1 pending ("what this day earns changed"), the face says "This day started earning OIL after it was published …" at once (F3), and "No OIL earned — the ground programme has no usable times"; deleted → 0. LW1b the other order (a holiday lifted after publishing): 1, "stopped being a holiday" live; back → 0. LW2 a leave approved / acked / cleared on the war: Monday 1 / 0 / 1 / 0, the face never shows it. LW2b a leave over a man flying the published day: the working copy rings him "On leave + flying", the face unchanged. LW3 a war leave stretched Mon–Tue → Wed: **Mon and Tue each read 1** — the war rewrote its note "till 14 Jul" → "till 15 Jul", which the faces print (see F1 below); Wed "filed". LW4 the published-stage remarks sheet: 1 pending with the new words, the face keeps the old | 65 / 69 | 65 / 69 | none |

**Dispositions of what the walks raised:**
- **LW3 (4 checks, both widths) — the war's "till <date>" note.** Reproduced by the host (unit pin "F1, as the app writes it";
  host walk H2 through the Inputs page). Not a code fault: every door that re-dates an input rewrites that note, and the
  face prints the remarks, so the covered published days' words DID move and D178 reads them pending. The walker's
  expectation came from the host's brief, written before the note was understood. **His question — look card Q4.**
- **The board's 👁 look at a published version draws no warnings at all** (Quals, Logic, neighbour) — reproduced in the code
  (`html.ts`: a version preview "reads and does not check"; the same on `main`). Filed `[PV-NO-FLAGS]`; look card Q5.
- **A rule change that re-words a warning read cleared + new** (Logic L4: seven lines for one change) — **fixed**
  (`pendlist.ts faceWords` pairs a cleared and a new warning of the same code on the same men as one "changed" line),
  pinned (`latepub.test` "the brief lead changed …", break test B17), re-walked (`…/logic-rewalk/`, L4 16 / 16 each width).
- **No door makes a scheduled pilot ground crew, a WSO or CAT IW** (Quals O1) — as designed (the Quals page's CAT list is
  filtered by seat); the illegal-seat member of the Qualification family is pinned in unit tests through `PEOPLE`.
- **Older, outside this item:** the Quals page's Save toast says "prototype …" (`[QUALS-PROTO-TOAST]`); the week's "‹"
  arrow covers an opened warning list on a desktop (`[VIEW-ARROW-OVER-LIST]`).
- **A phone tap on the war's event-sheet Delete that did not take once** under a parallel desktop run — not reproduced in
  three probes and the final run: the driver under load.

## 5. Break tests (one red per wired surface)
Run by `breaks.py` (scratch): each wire cut once, its test files run, the source restored byte-for-byte.

| # | Wire cut | Named test(s) that went red |
|---|---|---|
| B1 | `withDaySnap` installs the frozen inputs (the issued face) | 5 — "a leave FILED after publishing …", "EDITED …", "a downchit filed after publishing …", F1 … |
| B2 | `inputAxes` — the input details axis | 12 — every input-change pin |
| B3 | `faceWarn` — a published day shows its frozen warnings | "a lapsed SC currency … a warning that FREEZES still reads pending" |
| B4 | `faceattrs` — the issued CAT on a puck | "a man's CAT changed on the Quals side …" |
| B5 | `inpShow` — medical freezes (the old downchit exemption put back) | **none** — a second guard behind the frozen inputs: a late downchit is absent from `snap.inp`, so it never reaches `inpShow` on a published date (B1 turns its pin red); the line is reached only by a version issued before the freeze — stored demo data (D56). Observation #257 |
| B6 | `faceWarn` lays the LIVE marks over the frozen ones | 3 — the §4 QUAL / RULE pins, "a late duty put on the DRAFT day before …" |
| B7 | the live-class tag on the crew-rest ring | 3 — incl. "every ring a live warning raises is filed live …" |
| B8 | `warnSliceOf` stores only the frozen-class marks | 5 — AAR, SC, the draft-day duty, the 7-day run, the published-day-before |
| B9 | `LIVE_ON_FACE` — a lapsed qualification | 4 |
| B10 | F1 — the far end in the details key | "F1: the dates alone …" |
| B11 | F2 — one man's takeover pairs as one edit | "F2: a medical takeover …" |
| B12 | F4 — the folded line keeps its place | "an accepted request edited after publishing is ONE change …" |
| B13 | F5 — the load says why it stays pending (`inputsLeftSaid`) | the load pin — "Load onto working copy" leaves a member's late leave alone … |
| B14 | F3 — the holiday advisory live, not compared | "declared: one item …" (`amendretest-lw`) |
| B15 | F6 — "input change", not "filing" | first none (the old pin only said NOT "filing"); a positive pin added — then "a leave FILED after publishing …" |
| B16 | F5's "already at" door | "only the member's leave pending: …" (`latepub-load`) |
| B17 | a re-worded warning as one "changed" line (the Logic walker's find) | "the brief lead changed on the Logic side …" |
| B18 | a live warning's mark site names its code (the source pin, Fable's second read #4) — SC_QUAL's chip untagged | "a live warning's marks name its code …" (`liveface-marks.test`) |
| B19 | a person slot, never free text (Astra's second read #4) | "free text that spells a man's id is not that man …" |
| B20 | the frozen crowd is one of the day's men (Astra #1) | "the issued window keeps a crowd-only man in the column he went out in …" (`availwin.test`) |
| B21 | the window's column from the seat shown (Fable #2) | the same `availwin.test` pin |
| B22 | "free all day" counts the roster the face shows (Astra #3) | "a man posted out who is nowhere on the day …" |
| B23 | the brief lead compared only where a flying line prints it (Fable #1) | "a blank B on a STANDBY line only …" |

## 6. Gates
**The final code** (`2cfceae2` — the source of `05abfd71`, after both second reads' fixes), one run on a quiet PC: unit
5977 / 5978 — the Inputs calendar's chip-tap test, which this branch does not touch, failed inside the run and passes alone
3 / 3 (`[INPUTSCAL-TAP-FLAKY]`; Fable's first read saw it once too) — then the unit suite re-run alone: **5978 / 5978**
(368 files) · **build** clean · **tfin 728 / 0** · **e2e 471 passed**, 48 skipped · **smoke 442 / 0** · **rulecheck OK** ·
**docsize OK** — every record accounted for; over by 128, deferred (D29).
Before the second reads — one run, on a quiet PC (every preview and walker stopped; the checks runner off — the repo is public, D106), on `be38feff`
(no source change since), 26 Sep 26 morning: **unit 5972 / 5972** (367 files) · **build** clean · **tfin 728 / 0** · **e2e 471
passed**, 48 skipped · **smoke 442 / 0** · **rulecheck OK** · **docsize OK** — every record accounted for; over by 116,
deferred (D29: a change that touches `raptor-port/src` never trims a document).
Earlier, under the walkers' load (`2c665ad9`): unit 5969 / 5971 — two Leave War figure-column tests timed out at 20 s and
passed alone (24 / 24), the known slow-machine family (`[LW-FIGSEL-SLOW]`).

## 7. What was NOT walked, and why
- *(Walked this morning, §4c: the Quals page, the Logic page, a neighbour day, the Leave War's approve / ack / clear, its
  event row and its published-stage remarks.)*
- **The illegal-seat Qualification warning after publishing** — no door can make a scheduled pilot ground crew, a WSO or
  CAT IW (the Quals page's CAT list is filtered by seat); pinned in unit tests through `PEOPLE` (`official-flags` §4,
  `latepub` "a posting change …").
- **"AAR instructor not cleared"** — needs a pilot in a back seat; no demo line has one. Same Qualification family, same
  mark sites; the invariant pin covers its class tags.
- **The war's Move of an approved leave, the tap list on a two-record day, drag-select approval, decisions at CLOSED, a
  member's own remarks at PUBLISHED** — the Leave War walker's time; the input axis they land on is the same one LW2 / LW4
  walked.
- **Undo / redo of a Logic change, "Reset to standard"** — the Logic walker used the row's own box both ways.
- **A short screen** (a phone on its side) — nothing here changes a layout.
- **B and C at phone width** — the counting and face bodies are width-independent; A proves the phone surfaces.
- **The owner's look** — his, in the morning (§10).

## 8. What the reviews found before the build, and each disposition
From the plan attacks (Astra, Fable): D174/D176 kept (already built); the OIL answer out of the details key; a rename is a
label (callsigns keyed out, the face re-words); the retired snapshot keeps every new field; one act one item (the re-landed
row, the ground order it alone moved, the OIL line it alone moved — said on its line); the trace and the no-period advisory
stay live; the board's input-row pucks read the gated marks; comments corrected. **Put to him (not decided by the agent):**
a man's CAT on a puck and a printed rule value still draw live (freezing them is a per-version copy of the roster and of the
rules — the clash between D179 and the 7 Aug 26 "no rule versioning"), and the next-day crew-rest mark stays live
(`OUTSTANDING.md` `[LATE-PUB-FACE-LIVE]`).

## 9. The reads, fixes and re-walk
**The first reads** (blind, both providers — `docs/handpass/2026-09-26-late-pub-{astra,fable}-read.md`), every finding
dispositioned against the rulings:
- **Astra #1** (freeze the next-day mark) — applied overnight, then **set aside by his D183** (the mark stays live) and built back.
- **Astra #2** (a man's CAT / seat / posting and the blank brief's time frozen on the face, the CSV and the print; the man
  on the Unavailable list only) — applied (`engine/faceattrs.ts`); his look card Q1 asks whether to keep it.
- **Astra #3, #4** (rename-proof list words; structured "what this day shows" lines; the AL panel's words) — applied.
- **Fable F1** (no dates in the details key) — applied; the real app's doors also rewrite the "till" note, so a stretched
  input still reads pending on the covered days (walked, §4c) — look card Q4.
- **Fable F2** (one man's takeover as one edit) — applied, walked (H1). **F3** (the OIL warnings live) — applied, widened to
  every OIL warning after the Leave War walk showed "no usable times" moving with a holiday too; walked (LW1, LW1b).
  **F4** (the folded line taps) — applied, walked (H3). **F5** (the load's sentence) — applied; the walk found the load's
  OTHER door ("already at") silent, fixed and pinned through the App's button (H4). **F6** (words) — applied, pinned.
- **D184 / D185**, arriving after both reads — built this morning (§3, the live rows), walked by all four walkers.
- **The walks' own find** — a re-worded warning as one "changed" line (§4c).

**The second reads** (blind, both providers, the brief `docs/superpowers/briefs/2026-09-26-late-published-read2-brief.md`,
the code as of `be38feff`; `docs/handpass/2026-09-26-late-pub-{astra,fable}-read2.md`). Both found every live warning's
mark site correct and no other writer of the rings and flags; each finding reproduced in the code and dispositioned:
- **Astra #1 (high) / Fable #2 (low)** — a man behind a placeholder only was not one of the day's men, and the issued
  ALL AVAIL window split and sorted by today's seat and CAT. **Confirmed-new** (the window and `pa` came with this item) —
  **fixed**: the frozen crowd joins `pa`; the window reads `seatShown` / `byCrewShown`; pinned (`availwin.test`), B20, B21.
- **Astra #2 (high)** — Week Insights reads the working copy for everyone. **Confirmed-pre-existing** (the same on `main`;
  Fable's read calls it by design) — not this item's surface; filed `[INSIGHTS-WORKING-COPY]` as his question.
- **Astra #3 (medium)** — the issued day panel's "free all day" read today's roster. **Confirmed** — **fixed**: `snap.ros`,
  drawn and never compared (comparing would make every published day pending when a man joins the squadron); pinned, B22,
  walked (H5).
- **Astra #4 (medium)** — free text spelling a person's id was taken for that man. **Confirmed-new** — **fixed**: person
  slots only; the comparison now reads the version's own men (`peopleAttrsNow`) instead of re-deriving them; pinned, B19.
- **Astra #5 (low)** — the load's words. **Fixed** ("1 member input change stays pending"), both pins assert the sentence.
- **Fable #1 (medium)** — a brief-lead change made a published day with a blank STANDBY B pending for nothing on screen.
  **Confirmed-new** — **fixed**: compared only where a flying line prints a suggested brief; the CSV's standby column stays
  frozen by `rv` (so its computed standby brief never moves silently — the CSV's own behaviour unchanged); pinned, B23,
  walked (H6).
- **Fable #3 (docs)** — the stale comments (`publish.ts freezeWarn`, `html.ts dayTraceHTML`) and the roll-call's rows 37 /
  32 / 33 — **fixed**.
- **Fable #4 (hardening)** — **done**: `engine/liveface-marks.test.ts` reads every mark site; B18.

**Re-walked after the fixes** (the production build of `05abfd71`): the host walk H1–H6, both widths (§4b). The Logic
walker's L4 re-walk (`…/logic-rewalk/`) was on the build before these fixes; nothing it walked changed.

## 10. His look card
**Look here (five minutes, on the Vercel preview; pictures first: `docs/img/handpass/2026-09-26-late-pub/`):**
1. Publish Tuesday. On Monday (not published) put Hex on the evening SDO desk → View-only Sched: Tuesday rings Hex red with
   the crew-rest flag at once, and Tuesday still says nothing is pending (`neighbour/d-N1-2-*`).
2. On the Quals page untick a pilot's DAAR where a published line's remarks say AAR → his red ring and "Q" show at once;
   nothing pending, the four stay signed (`quals/desktop/Q2-*`).
3. File a leave for someone on a published Monday → Monday reads "1 pending", the four fall, View-only Sched unchanged;
   Publish AL1 → it shows (`rewalk/`, A1–B2).
4. Declare a public holiday on the Leave War for a published weekday → exactly 1 pending, and the published day says it
   "started earning OIL after it was published" (`leavewar/desktop/desktop-LW1-*`).

**His questions (each recommended answer is what is built):**
- **Q1** — A man's CAT letter on his puck and a blank line's suggested brief time now stay as they were published, like
  everything else; a change to either reads pending. Keep it? *(Built that way; it stores the brief time with the day —
  your 7 Aug 26 "no rule versioning" is why this is asked.)* **ANSWERED 26 Sep 26 — D186: "Q1 yes".**
- **Q4** — When a leave or a downchit is stretched or cut short (the Inputs page, the Leave War, an upchit), the app
  rewrites the "till <date>" at the end of its remarks. For each published day the leave still covers, the ADMIN's
  working copy of that day (Edit Schedule, the board, the ⓘ panel, the Amendments panel) then shows "1 pending" and its
  four sign-offs fall; the published schedule itself (View-only Sched) never shows "pending" — it keeps printing the old
  words ("till 14 Jul") until the next AL. Keep that, or have the app ignore its own "till" note (those days would then
  show nothing pending, and their published schedule would keep printing the old end date until something else is
  published)? *(Recommended and built: keep.)* *(Worded this way after he asked, 26 Sep 26, whether "the published days
  read 1 pending" meant the published schedule shows it — it never does.)*
- **Q5** — The board's 👁 look at a published version shows no warnings at all (as it always has); View-only Sched shows
  them. Show them there too? *(Not built — `OUTSTANDING.md` `[PV-NO-FLAGS]`; a small build if yes.)*
- **Q6** — My reading of D185: "a lapsed qualification" is live — SC currency, AAR currency, AAR instructor clearance and
  an illegal seat; the crew-pairing warnings (an OCU with no instructor, no IR examiner, an unauthorised pairing) stay
  frozen and read pending. And the "tight turning — crew rest" note is live with the crew-rest break (the same check).
  Right? *(Built that way.)*
