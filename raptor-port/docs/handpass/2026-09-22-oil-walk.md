# EVIDENCE SHEET — the OIL walk (22 Sep 26)

Branch `claude/oil-auto-remove-design`, PR #424. **Nothing merged.** Companion to
`2026-09-21-oil.md`, which holds the first pass; this file holds the walk that pass said was
still owed, plus the seven screen-level wording items.

The order followed is `docs/bug-check-order.md`. Pictures: `docs/img/handpass/2026-09-21-oil/`
(55 added by this walk, 408 in the folder).

---

## 1. The eight questions, and the tier

| # | Question | Answer |
|---|---|---|
| 1 | Money — what a man is owed or paid | **YES** — OIL is earned leave (D25), and it is owed the same way |
| 2 | The published record | **YES** — publishing, amendments, the issued copy, undo of a publish |
| 3 | Saved data | **YES** — decisions persist on the day |
| 4 | A shared drawer | **YES** — the puck, drawn from every surface |
| 5 | A new gesture or mode | **YES** — the OIL Earn mode |
| 6 | A new surface | NO — no new screen |
| 7 | Roles | **YES** — scheduler vs member |
| 8 | The warning list | **YES** — three checks added or changed |

**TIER: FULL.** Most of FULL was already spent on 21–22 Sep: the gates, the rules sweep, and both
providers reading the code independently (Fable and Codex, blind to each other), every finding
closed. What was owed was the WALK — the one check that sees absence — and the re-walk of what the
fixes touched.

---

## 2. THE DOOR THAT WAS NOT FOUND — closed

The 21 Sep session filed a blocker: a request's own edit button (`data-inpedit`) "is NOT in the DOM"
on the board, with the edit-mode hook reading false. Every remaining scenario that edits a request
was stuck behind it.

**The button was never missing.** The board's PERSONAL INPUTS panel folds to a one-line summary by
default — *"3 inputs · 3 on programme · show ⌄"* — and folded it renders **no rows at all**, so the
button is absent rather than hidden. The header itself is the toggle. Pressing it: **0 → 3** edit
buttons.

Two traps written into `scripts/handpass/lib.mjs` (`openInputs`) so nobody pays for them again:

- the toggle attribute rides the header in **both** states, so its presence says nothing about which
  way the panel is folded — a naive "is it shut?" check closes an open one. Read the ROWS.
- the fold is `!readOnly`, which is why the rows DO appear once the OIL mode is on — and why the edit
  button there is correctly replaced by the OIL cell.

Pictures: `door-01-panel-folded.png`, `door-02-panel-open.png`.

---

## 3. The roll-call, re-marked

Only the rows this walk changed or re-tested. The full 22-row table is §2a of `2026-09-21-oil.md`.

| Where a man is drawn | Shows the edge | Tap works | Painted over |
|---|---|---|---|
| SC SPARE seat | NO, correctly — a spare earns nothing **(until D24)** | one switch per FORMATION now, not per row — job 7 verified | — |
| AVALON line / desk, BB line | NO, correctly — exempt **(until D24)** | no switch offered, correctly **(until D24)** | — |
| A landed request row — the requester | YES | YES | — |
| A landed request row — a SECOND man | **WAS: no, and nothing said why. NOW: YES** — his own figure, his own switch | **YES** — and taking one off never touches the other | — |
| Personal Inputs echo of a request | YES | YES | the row's NAME no longer contradicts the puck |
| Common Programme, ALL AVAIL (board) | YES — **and now the count** | opens into real people in the mode | — |
| The viewer's OWN puck | **WAS: no stripe at all. NOW: YES** | YES | the "this is you" fill no longer wipes it |

**D24 note:** the owner ruled on 22 Sep that SC SPARE, AVALON and BB must start OFFERING the switch,
still earning nothing by default. Not built here — queued as `[OIL-EXEMPT-CREDITABLE]`. Until then
those three rows read as above and are correct.

---

## 4. The jobs, driven

| Job | What was driven | Result |
|---|---|---|
| **1** — a request handed to another man pays him nothing | Opened the Training request on the board, changed the holder from Talisman to Ace, saved | **PASS.** The question came up naming **Ace**; his answer was taken; Talisman's refusal was **cleared**, not inherited (decisions `{haowen: deny}` → `{}`) |
| **2** — a multi-day request pays nothing on the day it was answered for | Filed Fri 17 → Sun 19 from the Inputs page (the board's form has no date field), answered the **Saturday only** | **PASS, on the money not the screen.** Friday — the day the single row lands on — pays NOTHING. **Saturday pays FO.** Sunday, refused, pays nothing |
| **4** — a day that stopped being a holiday says so | Named an event on Friday 17, tagged it PH, put a man on a row, signed all four roles, published through *Publish day*, then tagged the day Work | **PASS.** The day now reads *"This day stopped being a holiday after it was published — publish it again to withdraw the OIL"* |
| **5** — the mode is read-only, Undo stops at its door | The claim rows inside the mode; then a schedule change, two OIL decisions, four presses of the board's own Undo | **Read-only half PASS.** Boxes shut, Accept gone, late mark inert, switch still live. **Undo half FAILED — see §5** |
| **6** — a weekend no leave war period covers | Stepped to Sat 6 Feb 28, put a man down to earn, read the checks, pressed the way out | **PASS, after a fix.** The day names the year and the publish reminder stays quiet. The **action was missing on the board — see §5** |
| **7** — one switch per formation | Counted real switches in the mode | **PASS.** 14 switches over 14 distinct items, none duplicated — its own figure. The other 16 wearing the class are the deliberate "nothing here can earn" cells, which carry no item and no switch |
| **8** — a second man on a request row earns | Dragged a man the day paid NOTHING onto Talisman's landed Training row through the row's own drop zone | **PASS on the money, FAILED on the screen — see §5** |

**A trap worth the line:** for job 8, using a man who was already earning proves nothing. Ace was
already on HO from elsewhere on that Saturday, so the first measurement was worthless. Repeated with
Cobra, who earned NOTHING before the drag and HO after — that is the only version that says anything.

---

## 5. WHAT THE WALK FOUND — three defects, all fixed, all re-walked

Every one was invisible to the gates, to both code reads, and to the existing tests.

### 5.1 Undo's boundary was wired to a button nobody can press

Fix 5 says the press that would reach past the mark leaves the mode instead of changing the day. It
does — from `#undoBtn`, the **page's** Undo, which is the only caller of the guard. That button is
covered by the board's own sticky top bar whenever the board is open (measured with
`elementFromPoint`: it returns the top bar, not the button), and **the board is the only place the
mode exists**. The board's own Undo went straight to the real undo.

Driven as a person drives it, the third press walked back a ground-row change underneath a screen
that says the schedule cannot change — silently, mode still open.

**Why every test passed.** `oilundo.test.tsx`'s helper is
`pressUndo = () => (oilUndoBoundary() ? … : globalUndo())` — it reimplements the handler, so the file
would pass identically if no control anywhere called the guard.

Fixed: both buttons carry the same two steps. `oilundo-button.test.tsx` presses the real button on a
rendered app; red first. **Re-walked:** press 1 and 2 take back the OIL decisions ("Undid: a change
to the schedule"), press 3 says *"Left OIL Earn — the next undo would change the day itself"* and the
schedule is untouched, press 4 — outside — undoes it normally.

**Also noticed, not fixed:** presses 1 and 2 say *"Undid: a change to the schedule"* when what they
undid was an OIL decision. Filed, not on this branch.

### 5.2 A claim row said the opposite of the puck beside it

Four rows on the everything-Saturday carried both at once: the name cell *"Nothing on this row can
earn OIL, so there is nothing to switch off"* and, on the same line, *"Talisman earns half a day"*.

`oilCapableItems` is derived from the SCHEDULE walk, and a request's money never goes through it — the
requester is paid from his own answer and D18's extras are each decided on their own puck. So a live
claim is absent from that set for a reason that has nothing to do with being unable to earn.

Fixed: a live claim now says *"A request is answered for each person on it — tap a puck on this row,
not the row itself"*. Cancelled, ⓘ, withdrawn and unreadable-week claims **keep** the old wording,
because there it is true — asserted, so a fix that merely deleted the sentence would not pass.
`oilclaimrow.test.tsx` asserts the invariant over every row the board draws. **Re-walked:
contradicting rows 4 → 0.**

### 5.3 D18's second man was paid and told he earned nothing

Cobra earned nothing on the Saturday. Dragged onto Talisman's landed Training row, the day paid him
HO — job 8's engine half works. His puck in the mode was drawn **inert**, titled *"Cobra — nothing
measurable to earn from here"*, carrying no item and **no switch**. The screen contradicted the money
about a man's entitlement and offered no way to change it.

Two bodies: the money moved to `oilEarnedWork` (where D18 was built) while `oilEligible` still asked
the work walk, whose ground pass skips every claim row whole. Fixed by asking the same body — and
asked WITHOUT the decision, so a man switched off does not go inert and become unswitchable.

**Re-walked:** the row reads "TRAINING Talisman HO **Cobra HO**", his puck is live and says *"Cobra
earns half a day — tap to take him off this event"*. And D18's other half holds: taking the second man
off leaves the requester on HO; taking the requester off leaves the second man on HO.

### 5.4 D19's way out was missing on the board

The check that names the missing leave war period is drawn on two surfaces. Only the week's rows
carried the action. The board — where a scheduler works the day and publishes it — named what was
missing and left him to go and find the Leave War himself.

Fixed: the board's rows carry the same button, from the same helper, so the button and the sentence
can never name different years. `oilnoperiod-board.test.tsx`; red first.

**Operated end to end:** pressed on the board → lands on the Leave War, says *"The 2028 leave war
period is created — set its bidding window here"*, and the period appears. **At 390px:** present,
134 × 20px, inside its row, no horizontal page overflow, pressable. **A member is offered nothing.**

**A false start worth recording.** The first attempt looked for an uncovered year by stepping to the
first 2027 week and reading the Leave War's grid, which draws 353 cells all in 2026. That reads
exactly like "the war stops at 2026" — but the PERIOD SELECTOR carries **two** periods, Jan–Dec 26
and Jan–Dec 27, and the grid only ever draws the selected one. 2027 is covered, the silence there was
correct, and reporting it as a defect would have been wrong. **Read the selector, not the cells.**

---

## 6. The seven screen-level wording items

| # | What it said | Settled |
|---|---|---|
| **8** | two buttons reading "✓ Done" at once | **FIXED.** The mode's is now "✓ OIL done". The old lesson said "✓ Done with OIL" overflowed at 390px; this is five characters shorter and was MEASURED before changing — 90px wide, right edge at 348 of 390, no page overflow. On the desktop the two labels are now distinct |
| **9** | the viewer's OWN puck never draws its green stripe | **FIXED.** `.puck.me` used the `background` shorthand with `!important`, which resets `background-image` — and the stripe IS a background image. Now `background-color`. Measured: the classes were always right and the computed image was `none`; it is now the gradient. Known, wanted consequence: a `me` puck in the crew palette that is busy now shows the grey blend it used to hide |
| **10** | "tap to see each one" does nothing | **NOT A DEFECT.** Tapping it lists all 27 men — *"27 behind this puck — Ace half day, Warden half day, …"*, 458 characters, all 27 named, not clipped. The promise is kept. The 21 Sep pass missed it because the message element FADES rather than being removed, so a probe looking for a live one finds nothing |
| **11** | the board never shows the count | **FIXED.** The Common Programme builder drew its own seat and took only the bar from the decoration, throwing the count away. The board's ALL AVAIL row now reads "ALL AVAIL **27**" |
| **12** | every tappable puck in the mode is 15px tall on a phone | **CONFIRMED, NOT FIXED — the owner's call.** 71 targets at 390px: shortest 15px, median 15px, 57 under 24px, all 71 under 44px, narrowest 20px wide. There is no cheap fix — the rows are 15px apart, so growing only the hit box makes neighbours overlap. Three options and a recommendation filed as `[OIL-PHONE-TARGETS]` in `OUTSTANDING.md` |
| **13** | a claim row's name says the opposite of the puck beside it | **FIXED** — §5.2 |
| **17** | no toast or acknowledgement after any OIL gesture | **NOT A DEFECT.** Every gesture acknowledges itself: entering the mode, switching a whole item off (*"FAMILY DAY earns nobody any OIL"*), taking one man off (*"Saber earns nothing from MASS BRIEF"*), and the whole-day blanket. Same measurement error as 10 |

Already closed and not re-opened: 15 (an empty Saturday no longer nags) and 16 (settled by D21, no
code).

---

## 7. What was NOT walked, and why

- **The `[OIL-PHONE-TARGETS]` fix** — not built; it is a product decision (item 12 above).
- **A real phone.** 390 × 844 emulation only, as on 21 Sep.
- **The fifth ALL AVAIL state** (a window nobody is free for) — still not reachable through the app's
  own controls without making all 45 men busy.
- **Taking a man off a ground row by dragging his puck away** — still no button or keyboard
  equivalent. Adding one THIS way round now works from a script (the row's own drop zone), which is
  how job 8 was driven.
- **Job 4 on a day other than Friday.** A publish is refused while a day carries hard conflicts, and
  Mon–Thu all do in the demo (2, 2, 2, 3). Friday is the only clean weekday, so it is the only day
  this fixture can be built on through the app's own publish control. Worth knowing, not a defect.

---

## 8. The gates

| Gate | Result |
|---|---|
| unit | **5449 passed / 0 failed** (335 files) — see the note below |
| build | green |
| reference parity | **728 / 0**, no errors |
| rulecheck | **OK** (53 rulings named by a test, 7 by none — unchanged baseline) |
| docsize | **OK** — `OUTSTANDING.md` went 92 lines over its ceiling with three new items; the items are trimmed and the DONE `[CMDL-FINISH]` block compressed 42 → 14 lines, keeping its PRs and its open threads |
| tracker smoke | **425 passed / 0 failed** |
| e2e (geometry) | **447 passed / 0 failed** (3.4 min) — a FULLY clean run, on this branch after the last fix. Deliberately re-run rather than carried over from 21 Sep, because the CSS in items 9 and 12 and the new warning-row button are exactly what this gate exists to catch. It is also better than 21 Sep's two runs (446/1, then 445/2): the `[LW-SCRUBBER-FLAKY]` family did not fire this time, which supports the same load reading as the unit flake below |

**THE UNIT GATE'S FLAKE, dispositioned.** Two runs of the full suite reported failures — one unnamed
with 7 unhandled errors, then two cases in `src/ui/stsaved.test.tsx`. Both of those runs were taken
while the tracker smoke and the browser gate were running alongside. Taken alone the suite is
**5449 / 5449, twice**; `stsaved.test.tsx` is **6 / 6 in isolation**; and run in one process straight
after the three new OIL test files it is **16 / 16**, which rules out pollution from them.

`HANDOFF.md` already records this suite as flaky under load (6 Sep 26, against `odds.test.tsx`), and
`stsaved.test.tsx` is the expected shape — its whole subject is a SELF-EXPIRING 1.2-second window, so
a loaded machine expires it before the assertion. **Disposition: confirmed pre-existing, load-caused,
and the load was mine for running three gates at once.** The HANDOFF note now names it as a family
rather than one test, with the rule: do not run the unit gate beside the browser gates, and never read
a failure from a concurrent run as a finding without re-running it alone.

---

## 9. The Walk line

`Walk: docs/handpass/2026-09-22-oil-walk.md · 55 pictures · 7 surfaces re-marked · 7 jobs + 7 wording items · MISSING: 4 fixed with red-first tests, 1 filed ([OIL-PHONE-TARGETS]), 2 were never defects`

**The owner's sixty-second check, answered:**

1. **Is there a sheet?** This file, plus `2026-09-21-oil.md` for the first pass.
2. **Can he see the everything-day?** `j78-01-mode-on.png`, `j8-02-mode-two-men.png`,
   `w12-phone-targets.png` (390px), `j6a-02-period-created.png`.
3. **Does every roll-call row have a mark?** §3 re-marks the seven rows this walk changed; the other
   fifteen are unchanged in §2a of the 21 Sep sheet, no blanks.
4. **Both orders, and after publishing?** Job 4 crosses the publish boundary in both directions. Job
   8 was walked in both orders (second man off / requester off). Undo was walked across its boundary
   in four presses and back out the other side.
5. **One red break test per wired surface?** Four fixes, four tests, each proved RED before its fix:
   `oilundo-button.test.tsx`, `oilclaimrow.test.tsx` (2 of 4 red), `oilnoperiod-board.test.tsx`. Items
   9 and 11 are paint and markup — proved by measurement in the built app, before and after, because
   the unit environment has no layout engine.

---

## 10. What this walk cost the method

Per §11 of the order — every defect found is a method failure before it is a bug, and pays a line.

1. **A test that reimplements a handler's steps proves the rule, never the wiring.** §5.1. A guard
   with fewer call sites than triggering controls is a reachability bug. New anti-pattern:
   **the handler re-enacted** — a test that calls a guard and then the action, instead of pressing
   the control that should call both.
2. **Absence of a DOM node is not evidence a feature is unbuilt.** §2. Read what IS rendered there
   first — a folded panel, an empty state, a "show N more" summary all present as a missing node.
3. **Two bodies answering one question is the defect, not the symptom.** §5.3. When money moves to a
   new body, every reader of the old one is a roll-call row.
4. **A list that draws the same check on two surfaces is two surfaces.** §5.4. The roll-call has to
   cover the WARNING LIST as a drawer, not only the puck.
5. **A measurement against a faded element reads as absence.** Items 10 and 17 were reported as
   defects for a day because the message element fades instead of being removed. Read state, not
   liveness.
6. **A control that proves nothing when the subject already has the thing.** §4's trap line — job 8
   with a man already earning. The fixture has to start from the negative.
