# [HUMAN-RETEST] the Tracker — the FULL-tier evidence sheet (23 Sep 26)

Branch `claude/tracker-human-retest-8d3411` (from `main` at `65129783`). Re-test of the Tracker AS
BUILT — no new feature — by the bug-check order (`docs/bug-check-order.md`). D85/D86: this chat runs
in parallel with the amendment system's; ports 4180 (preview) / 4181 (smoke) / 4182 (e2e).
Pictures: `docs/img/handpass/2026-09-23-tracker/`. Walk drivers: `scripts/handpass/trk-*.mjs`
(`trk-lib.mjs` is the driver; results per script in `docs/handpass/parts/tracker/`).
Scenario design: `2026-09-23-tracker-fable-scenarios.md`, `2026-09-23-tracker-astra-scenarios.md`
(brief: `docs/superpowers/briefs/2026-09-23-tracker-retest-scenarios-brief.md`).

**Written DURING the walk (D68).** Sections fill as the work reaches them; a blank section means
"not reached yet", never "nothing found".

## 0. Where this stands (kept current — read this first after a compaction or in a new chat)

- **Done:** survey; the rulings sweep (`2026-09-23-tracker-rulings.md`, R1–R123); both scenario
  lists (Fable, Astra); host walks 0–6 (`scripts/handpass/trk-0*.mjs`); **first-round fixes
  committed** `5322db46` — F1–F7, F9–F11, F13 and the hidden flash line, pinned red-first in
  `src/tracker/retest.test.tsx` (Tracker vitest 191/191; every resetSession test 326/326; tsc clean);
  stale docs corrected `f191844c`.
- **In flight:** three parallel walkers (brief `docs/superpowers/briefs/2026-09-23-tracker-walker-brief.md`)
  write `parts/tracker/w1.md` (round trip + File menu), `w2.md` (marking, students, undo),
  `w3.md` (surfaces, phone, seams, courses, charts). Their scripts are `scripts/handpass/trk-w*.mjs`.
- **Next:** reproduce and disposition each walker finding (§7/§8) → second-round fixes, red first
  (candidates already read in the code: Export with unsaved edits, Add/Duplicate/Import discarding
  them silently, never-placed balls exported to one corner, no tick-all in Export, the Export
  window's refusals shown behind it, the details bubble outliving the tab, lull × with no question)
  → rebuild → full gates (check first that no other chat is mid-run, D86) → the two code reads
  (Fable + Astra, blind, with this sheet) → re-walk what the fixes touched → finish §9–§12 → the
  owner's look. **The preview on 4180 must not be rebuilt while walkers drive it.**
- **For the owner (asked at the end, with a recommendation each):** event details shared across
  charts by event code, or per chart (Astra #3/#5)? A deleted built-in returning after the wipe
  (F8) — carry deletions or leave it? Course delete (F12) — a restore door or an honest delete?
  Unsaved chart edits at logout — warn the person logging out? (F10 keeps them behind Save changes.)

## 1. The eight questions → FULL

| # | Question | Answer |
|---|---|---|
| 1 | Money / earned leave / manning | **NO** — the Tracker records training progress only; nothing it stores reaches the Leave War, OIL or a manning count (`pid` carries nothing back yet) |
| 2 | The published record | **NO** — the Tracker has no publish, sign or amendment; its Export is a file copy, walked under 3 |
| 3 | Saved data | **YES** — marks, dates, students and charts save themselves; Import/Export read and write files; D120 makes the export → wipe → import round trip the one path his charts survive by |
| 4 | A shared drawer | **YES** — a student's marks are drawn on the balls, in Show All, on the side cards, in the details bubble |
| 5 | A gesture / mode | **YES** — marking, arrange mode, ball and line drags, zoom, search, undo |
| 6 | A surface | **YES** — the whole Tracker is in scope |
| 7 | Roles | **YES** — everyone edits; Import/Export are the admin's (owner, 7 Sep 26) |
| 8 | Rules | **YES** — what a student can do next (prerequisites), pace, currency, down days |

## 2. Scope ruling this session

**D120 (owner, 23 Sep 26):** his charts reach the database by export → wipe → import. So NOT walked:
stored-data-only harm, the start-up converters for older stored data, older file formats. Walked
FIRST: the current export → wipe → import round trip. Everything done to NEW data still walked.

## 3. Findings so far (running list — dispositions in §8 once fixed/ruled/filed)

| # | Where | What a person sees | Found by | State |
|---|---|---|---|---|
| F1 | Students card, the key ball | its centre prints the course's internal code (`cmudq0…`) instead of the course name (`26ABSG`) — every course, every visit | the survey picture `00-desk-admin.png`; `core.js renderKeyBall` prints `course`, which became an id on 13 Sep 26 (course ids) | open |
| F2 | Syllabus ✎ menu → Duplicate syllabus | the hover text promises "including every student's marks"; the copy carries NO marks (the 13 Sep 26 design: flow + layout, empty student layer) | reading the menu against `dupSyl` | open |
| F3 | Syllabus ✎ menu → Add syllabus | the hover text says "from the current structure"; it makes an EMPTY chart (its own prompt says so) | reading the menu against `addSyl` | open |
| N1 | `Shell.tsx` comment on the Tracker tab | says "edits are the admin's"; the owner's 7 Sep 26 second word is everyone edits, only the File menu is the admin's (code obeys the ruling; the comment is stale) | reading | open (comment) |
| F4 | Details mode (ⓘ), an event with no details | the bubble says "tap Edit details", but Details mode has no Edit details button — it lives in the marking pop-up, which Details mode switches off | walk 1 (`01-A-details-nodoor.png`) + `Pop.jsx` | open |
| F5 | Chart editor, the ball box (double-click / Text tool) on the SHORT course | changing only a ball's text on Tx rewrote 2026's BFM-3 crew line to Tx's own ("UP / IP" → "UP / IW") — silently, on every chart with that event code; the leak rides the export into the database. The ⓘ details window was fixed for exactly this ("one save of SA-5 on Tx did exactly that"); the ball box — the second writer — was not | walk 3 (`trk-03`, `03-A-2026-bfm3-after-tx-edit.png`); Astra #4 | open |
| F6 | **D120 round trip — chart ORDER** | set in the Syllabus order window and recorded in the file (Zulu blank · 2024 · Tx · A/G · Alpha copy · 2026), it comes back after export → wipe → import in the default order (2024 · 2026 · Tx · A/G · Zulu blank · Alpha copy): Import applies each chart alone and never the file's order | walk 3; Astra #2 | open |
| F7 | D120 round trip — the "✎ edited" mark | every chart comes back marked edited after the import, including built-ins that were never touched (2024, A/G) — the mark stops telling him which charts he changed | walks 1 and 3 | open |
| F8 | D120 round trip — a DELETED built-in | a built-in chart he deleted comes back after the wipe (the app ships it); its edits were deleted with it, so nothing of his is lost — it reappears | walk 1; Astra #1 | open — his call |

| F9 | A course with NO students — tap a ball | the full marking pop-up opens, titled "ST-01 ·" (no name), with every grade, the failure counter and both dates live-looking; DCO closes it silently and writes nothing. The Students card beside it already says "No students on this course yet" | walk 4 (`04-empty-course-tap.png`); Astra #13 | open |
| **F10** | **Crossing a login — admin logs out, a member signs in on the same browser** | the member's ↶ reads "Undo the mark on ST-01 for STUDENT A" and pressing it DELETES the admin's mark; Details mode is still on; the admin's UNSAVED chart edits (a moved ball, a new ball) are on screen in edit mode with "✓ Save changes" offered to the member, and the unsaved ball already counts in the member's totals. Breaks the standing ruling that undo is per login session and never reaches another user. Logout resets only the Tracker's file lock | walk 5 (`05-member-undid-admin-mark.png`, `05-member-after-admin-left-unsaved-edit.png`); Astra #9 | open |

| F11 | Show All → Edit (its own inline event editor) | the grey hint in its Type/format box still reads "e.g. Lecture, OFT/AMT" — D64 changed the details window's hint to "…, 2 x F-15" and missed this second editor | walk 6 B (`06-B-showall-editor.png`); Astra #14 | open |
| F12 | Course ✎ → Delete course | the question promises "(marks remain in storage)", but nothing on any screen brings them back: re-creating the course under the same name starts EMPTY (course ids, 13 Sep 26, gave it a new id) and no menu offers a restore. The records are orphaned — kept, invisible, not exported. known-gaps.md still says re-creating by name brings the roster back | walk 6 A (`06-A-recreated-course.png`); Astra #11 | open — his call (restore door, or an honest delete) |
| F13 | The marking pop-up, then a chart switch by KEYBOARD (focus the Syllabus box, arrow key) | the pop-up stays open over the new chart (its title loses the name — this course has no students on Tx, so its buttons could write nothing; on a chart WITH the same students they would grade that chart's same-code ball). The pointer route is closed (walk 4) | walk 6 D | open (a one-line belt) |

**The marking pop-up's lifetime (walk 4) — cleared, with pictures:** it closes on a press on empty
chart (the standing outside-click rule), on a chart switch made by pressing the dropdown, and on
removing its student by the chip's ×. Astra #7/#8 predicted from the code that it outlives those;
through the pointer it does not. (A keyboard-only switch was not walked — §9.)

**Round trip, what SURVIVES (walks 1 + 3, 23 Sep 26):** every ball's place and label, every line and
prerequisite arrow, the fonts (11.5px set by Select all), every event detail typed through ⓘ, a
renamed built-in's name, a duplicated chart, an empty chart with its one ball — five charts, 634
balls, 0 differing. The import asks "Replace it, or add as new?" once per chart already there.

## 4. The roll-call

## 5. The door check

## 6. Orders walked

## 7. Scenario dispositions (Fable + Astra)

## 8. Every finding, dispositioned — red first

Every fix has a test in `src/tracker/retest.test.tsx` that failed on the unfixed code for the defect
itself (a red on a broken premise was repaired before it counted).

| Finding | Disposition | Commit |
|---|---|---|
| F1 key ball printed the course's hidden code | **fixed** — names the course | `5322db46` |
| F2 / F3 Duplicate / Add syllabus words; the stale "hit ✎ Edit" guidance | **fixed** — the words say what they do | `5322db46` |
| F4 Details mode's "tap Edit details" | **fixed** — the bubble says how to reach it | `5322db46` |
| F5 the ball box leaked Tx's details onto every chart | **fixed** — one writer body (`writeInfo`) | `5322db46` |
| F6 import lost the chart ORDER (D120) | **fixed** — a file with 2+ charts restores its order; one chart keeps its place | `5322db46` |
| F7 untouched built-ins came back "✎ edited" | **fixed** — a shipped-identical chart is not stored as an override | `5322db46` |
| F9 empty course: a pop-up of dead grades | **fixed** — a flashed notice instead | `5322db46` |
| F10 the next login inherited undo, modes, windows | **fixed** — the session ends at login/logout; an unsaved chart edit stays behind Save changes | `5322db46` |
| F11 Show All's format hint missed D64 | **fixed** — one `FMT_HINT` | `5322db46` |
| F13 a keyboard chart switch left the pop-up open | **fixed** — loading a chart closes it | `5322db46` |
| Fable #8 flash messages written to a hidden line | **fixed** — the line shows while a flash is up | `5322db46` |
| Role gating (the File menu admin-only) | **superseded by D121** — the owner ruled admin and member the same; the lock is removed | `b95f7844` |
| Fable #6 Export's refusals drawn behind the window | **fixed** — the question box tops the ladder (101) | round two |
| Fable #5 no way to tick every chart in Export | **fixed** — All / None beside the list (default unchanged) | round two |
| Fable #2 Export left unsaved edits out, silently | **fixed** — asks to save first | round two |
| Fable #3 Add / Duplicate / Import dropped unsaved edits silently | **fixed** — each asks, as the chart switch does | round two |
| F8 a deleted built-in returns after the wipe | **his call** — asked at the end | — |
| F12 course delete's "marks remain in storage" has no way back | **his call** — asked at the end | — |

## 9. NOT walked, and why

## 10. Gates

## 11. The two code reads

## 12. Re-walk after the fixes
