# [HUMAN-RETEST] the Tracker — the FULL-tier evidence sheet (23 Sep 26)

Branch `claude/tracker-human-retest-8d3411` (from `main` at `65129783`). Re-test of the Tracker AS
BUILT — no new feature — by the bug-check order (`docs/bug-check-order.md`). D85/D86: this chat runs
in parallel with the amendment system's; ports 4180 (preview) / 4181 (smoke) / 4182 (e2e).
Pictures: `docs/img/handpass/2026-09-23-tracker/`. Walk drivers: `scripts/handpass/trk-*.mjs`
(`trk-lib.mjs` is the driver; results per script in `docs/handpass/parts/tracker/`).
Scenario design: `2026-09-23-tracker-fable-scenarios.md`, `2026-09-23-tracker-astra-scenarios.md`
(brief: `docs/superpowers/briefs/2026-09-23-tracker-retest-scenarios-brief.md`).

**Written DURING the walk (D68).** Sections fill as the work reaches them; a blank section means
"not reached yet", never "nothing found".

## 0. Where this stands (kept current — read this first after a compaction or in a new chat)

**RESUMED 23 Sep 26 (D125, superseding D155's pause)** beside the demo chat — the demo has first
call on the PC: check the load before any heavy run (gates, walkers), one heavy run at a time, pause
at a clean point if he says the demo slows. `main` merged in (D78). **The four open questions are
ANSWERED (D126–D129):** details per chart; the file remembers a deleted built-in; a course restore
door; ask at logout. The session that resumed works in this order: rebuild → re-walk every fixed
item → fix the "still to fix" lists + D122–D124 + D126–D129, red first → full gates → Fable + Astra
blind with this sheet → re-walk what the fixes touched → §9–§12 → his look card.
**Progress (kept current):** rebuilt, rounds one and two re-walked (they hold); round three BUILT —
every "still to fix" item and D122–D129, red first (commits `41e22411` `85a6562a` `01e0c1a6`
`245687a6` `060f3359`), re-walked green (§12: 21/21, 31/31, 18/18); §4–§9 written; the smaller notes
filed (`[TRK-RETEST-NOTES]`); full gates green (§10, G-1 found and fixed); the two reads done, every
finding fixed red-first and walked (§11, `267dab1e`); the final gates re-run. **NEXT:** his look card
(three questions for him in it) → his "merge live".

- **Done:** survey; the rulings sweep (`2026-09-23-tracker-rulings.md`, R1–R123); both scenario
  lists (Fable, Astra); host walks 0–6 (`scripts/handpass/trk-0*.mjs`); **first-round fixes
  committed** `5322db46` — F1–F7, F9–F11, F13 and the hidden flash line, pinned red-first in
  `src/tracker/retest.test.tsx` (Tracker vitest 191/191; every resetSession test 326/326; tsc clean);
  stale docs corrected `f191844c`.
- **In flight:** three parallel walkers (brief `docs/superpowers/briefs/2026-09-23-tracker-walker-brief.md`)
  write `parts/tracker/w1.md` (round trip + File menu), `w2.md` (marking, students, undo),
  `w3.md` (surfaces, phone, seams, courses, charts). Their scripts are `scripts/handpass/trk-w*.mjs`.
- **Next:** reproduce and disposition each walker finding (§7/§8) → second-round fixes, red first
  (candidates already read in the code: Export with unsaved edits, Add/Duplicate/Import discarding
  them silently, never-placed balls exported to one corner, no tick-all in Export, the Export
  window's refusals shown behind it, the details bubble outliving the tab, lull × with no question)
  → rebuild → full gates (check first that no other chat is mid-run, D86) → the two code reads
  (Fable + Astra, blind, with this sheet) → re-walk what the fixes touched → finish §9–§12 → the
  owner's look. **The preview on 4180 must not be rebuilt while walkers drive it.**
- **Walker 1 reported** (`parts/tracker/w1.md`, committed `f60f34fc`): W1-1/2/4/5 = the round-two
  fixes (`912ee458`, walked on the build before them — re-walk after a rebuild). **Still to fix:**
  W1-3 never-dragged balls export to one corner (D120 route; also a Reset-layout copy exported on
  screen comes back re-laid); W1-6 a rename collision refuses the whole post-wipe import; W1-7 the
  closing message never names a skipped chart; W1-4b "a full backup" said of a file holding 1 of 4
  charts; **W1-9 a one-chart import resets event details on OTHER charts — ruled D122: marks may
  reset, typed details never** (fix: export only real detail edits; import merges only those).
- **Walker 2 reported** (`parts/tracker/w2.md`): W2-F1 (N.A. "+" message hidden) = fixed by
  `5322db46`. **Still to fix:** W2-F2 retyping a done flight's "Done on" reads the briefly empty box
  as TODAY and sets Last Flown to today; W2-F4 lull × deletes with no question and no undo; W2-F5
  Copy to… lacks the "select all" row the approved 7 Aug design promises (R53); W2-F6 ↶ / ↷ jump the
  chart near its top (grading keeps the view, R62 — undo should too). Noted, not counted: entering
  edit mode shifts the chart 190–240 px; the Failures card counts failures on an N.A. event; a red
  failure tick takes the tap meant for another student's wedge. **Ruled 23 Sep 26:** W2-F3 → **D123**
  Last Flown = the latest day actually flown, derived from the done flights (a correction or un-mark
  pulls it back); W2-F7 → **D124** deleting a ball wipes its marks. Both fixes are next.
- **Walker 3 reported** (`parts/tracker/w3.md`, 18/18 walked). **Still to fix:** w3-F1 the Details
  bubble outlives the phone's Info tab and a page change (the logout half is fixed by F10's
  `hideDetailBubble` — re-walk it); w3-F2 a resize / phone rotation while on Info never re-fits the
  chart (stuck at 92%); w3-F3 landscape phone (844×390): the grading pop-up opens above the top of
  the screen, nothing scrolls it into reach, the chart zoom sits below the bottom; w3-F4 "Reset to
  doc" on a ball the user made empties its details with no question and no undo (both editors);
  w3-F5 at ~1200–1250 px the first chart edit pushes ✓ Save changes onto a second row and slides the
  chart 36 px under the pointer; w3-F6 on a phone the far-right spot goes to the status word, not
  ✓ Save changes (R97).
- **All three walkers drove the build from BEFORE the fixes** (preview built 14:22 from `58d9ec92`).
  So the NEXT CHAT: rebuild, re-walk every fixed item (round one, D121, round two), fix the "still to
  fix" lists above and D122–D124, then the full gates (check no other chat is mid-run), then the two
  code reads (Fable + Astra, blind, with this sheet), then the owner's look.
- **The four owner questions — ANSWERED 23 Sep 26, each as recommended:** event details per chart,
  never shared by code (**D126**, W1-8, Astra #3/#5); the file remembers a deleted built-in (**D127**,
  F8); a restore door for a deleted course (**D128**, F12); logout with unsaved chart edits asks
  Save / Discard / Stay (**D129**, replacing F10's keep-behind-Save).

## 1. The eight questions → FULL

| # | Question | Answer |
|---|---|---|
| 1 | Money / earned leave / manning | **NO** — the Tracker records training progress only; nothing it stores reaches the Leave War, OIL or a manning count (`pid` carries nothing back yet) |
| 2 | The published record | **NO** — the Tracker has no publish, sign or amendment; its Export is a file copy, walked under 3 |
| 3 | Saved data | **YES** — marks, dates, students and charts save themselves; Import/Export read and write files; D120 makes the export → wipe → import round trip the one path his charts survive by |
| 4 | A shared drawer | **YES** — a student's marks are drawn on the balls, in Show All, on the side cards, in the details bubble |
| 5 | A gesture / mode | **YES** — marking, arrange mode, ball and line drags, zoom, search, undo |
| 6 | A surface | **YES** — the whole Tracker is in scope |
| 7 | Roles | **YES** — everyone edits, the File menu too (D121, 23 Sep 26, superseding the 7 Sep "Import/Export are the admin's"); the login session still ends at logout (F10, D129) |
| 8 | Rules | **YES** — what a student can do next (prerequisites), pace, currency, down days |

## 2. Scope ruling this session

**D120 (owner, 23 Sep 26):** his charts reach the database by export → wipe → import. So NOT walked:
stored-data-only harm, the start-up converters for older stored data, older file formats. Walked
FIRST: the current export → wipe → import round trip. Everything done to NEW data still walked.

## 3. Findings so far (running list — dispositions in §8 once fixed/ruled/filed)

| # | Where | What a person sees | Found by | State |
|---|---|---|---|---|
| F1 | Students card, the key ball | its centre prints the course's internal code (`cmudq0…`) instead of the course name (`26ABSG`) — every course, every visit | the survey picture `00-desk-admin.png`; `core.js renderKeyBall` prints `course`, which became an id on 13 Sep 26 (course ids) | open |
| F2 | Syllabus ✎ menu → Duplicate syllabus | the hover text promises "including every student's marks"; the copy carries NO marks (the 13 Sep 26 design: flow + layout, empty student layer) | reading the menu against `dupSyl` | open |
| F3 | Syllabus ✎ menu → Add syllabus | the hover text says "from the current structure"; it makes an EMPTY chart (its own prompt says so) | reading the menu against `addSyl` | open |
| N1 | `Shell.tsx` comment on the Tracker tab | says "edits are the admin's"; the owner's 7 Sep 26 second word is everyone edits, only the File menu is the admin's (code obeys the ruling; the comment is stale) | reading | open (comment) |
| F4 | Details mode (ⓘ), an event with no details | the bubble says "tap Edit details", but Details mode has no Edit details button — it lives in the marking pop-up, which Details mode switches off | walk 1 (`01-A-details-nodoor.png`) + `Pop.jsx` | open |
| F5 | Chart editor, the ball box (double-click / Text tool) on the SHORT course | changing only a ball's text on Tx rewrote 2026's BFM-3 crew line to Tx's own ("UP / IP" → "UP / IW") — silently, on every chart with that event code; the leak rides the export into the database. The ⓘ details window was fixed for exactly this ("one save of SA-5 on Tx did exactly that"); the ball box — the second writer — was not | walk 3 (`trk-03`, `03-A-2026-bfm3-after-tx-edit.png`); Astra #4 | open |
| F6 | **D120 round trip — chart ORDER** | set in the Syllabus order window and recorded in the file (Zulu blank · 2024 · Tx · A/G · Alpha copy · 2026), it comes back after export → wipe → import in the default order (2024 · 2026 · Tx · A/G · Zulu blank · Alpha copy): Import applies each chart alone and never the file's order | walk 3; Astra #2 | open |
| F7 | D120 round trip — the "✎ edited" mark | every chart comes back marked edited after the import, including built-ins that were never touched (2024, A/G) — the mark stops telling him which charts he changed | walks 1 and 3 | open |
| F8 | D120 round trip — a DELETED built-in | a built-in chart he deleted comes back after the wipe (the app ships it); its edits were deleted with it, so nothing of his is lost — it reappears | walk 1; Astra #1 | open — his call |

| F9 | A course with NO students — tap a ball | the full marking pop-up opens, titled "ST-01 ·" (no name), with every grade, the failure counter and both dates live-looking; DCO closes it silently and writes nothing. The Students card beside it already says "No students on this course yet" | walk 4 (`04-empty-course-tap.png`); Astra #13 | open |
| **F10** | **Crossing a login — admin logs out, a member signs in on the same browser** | the member's ↶ reads "Undo the mark on ST-01 for STUDENT A" and pressing it DELETES the admin's mark; Details mode is still on; the admin's UNSAVED chart edits (a moved ball, a new ball) are on screen in edit mode with "✓ Save changes" offered to the member, and the unsaved ball already counts in the member's totals. Breaks the standing ruling that undo is per login session and never reaches another user. Logout resets only the Tracker's file lock | walk 5 (`05-member-undid-admin-mark.png`, `05-member-after-admin-left-unsaved-edit.png`); Astra #9 | open |

| F11 | Show All → Edit (its own inline event editor) | the grey hint in its Type/format box still reads "e.g. Lecture, OFT/AMT" — D64 changed the details window's hint to "…, 2 x F-15" and missed this second editor | walk 6 B (`06-B-showall-editor.png`); Astra #14 | open |
| F12 | Course ✎ → Delete course | the question promises "(marks remain in storage)", but nothing on any screen brings them back: re-creating the course under the same name starts EMPTY (course ids, 13 Sep 26, gave it a new id) and no menu offers a restore. The records are orphaned — kept, invisible, not exported. known-gaps.md still says re-creating by name brings the roster back | walk 6 A (`06-A-recreated-course.png`); Astra #11 | open — his call (restore door, or an honest delete) |
| F13 | The marking pop-up, then a chart switch by KEYBOARD (focus the Syllabus box, arrow key) | the pop-up stays open over the new chart (its title loses the name — this course has no students on Tx, so its buttons could write nothing; on a chart WITH the same students they would grade that chart's same-code ball). The pointer route is closed (walk 4) | walk 6 D | open (a one-line belt) |

**The marking pop-up's lifetime (walk 4) — cleared, with pictures:** it closes on a press on empty
chart (the standing outside-click rule), on a chart switch made by pressing the dropdown, and on
removing its student by the chip's ×. Astra #7/#8 predicted from the code that it outlives those;
through the pointer it does not. (A keyboard-only switch was not walked — §9.)

**Round trip, what SURVIVES (walks 1 + 3, 23 Sep 26):** every ball's place and label, every line and
prerequisite arrow, the fonts (11.5px set by Select all), every event detail typed through ⓘ, a
renamed built-in's name, a duplicated chart, an empty chart with its one ball — five charts, 634
balls, 0 differing. The import asks "Replace it, or add as new?" once per chart already there.

## 4. The roll-call

Round three (23 Sep 26, resumed — D125) changed four shared things. Every place each is drawn or
written, with a written answer. Pins: `src/tracker/retest.test.tsx` (RT), `eventDetails.test.tsx` (ED);
walks: `scripts/handpass/trk-r3-{a,b,c}-*.mjs` (A, B, C), pictures `docs/img/handpass/2026-09-23-tracker/rewalk/`.

**A — an event's details (D126: they belong to the chart they were typed on)**

| Place | Reads / writes the chart ON SCREEN only? | Evidence |
|---|---|---|
| Grading pop-up's details (`#popInfo`) | YES — `infoFor` (current chart) | walk A (the bubble / Show All rows read after each switch) |
| Details bubble (ⓘ mode, side-panel chip hover) | YES — `infoFor` | walk A, C |
| Show All row + its inline editor | YES read; YES write (`saveInfoFor → writeInfo`) | walk A `r3-a1-*`; RT D126 |
| ✎ Edit details window | YES read; YES write | walk B `r3-b7-*`; RT w3-F4 |
| Chart editor's ball box (crew / prerequisite note) | YES write (`writeInfo`, F5) | RT F5 |
| Failures list names (side panel) | YES — `infoFor` | reading |
| "Reset to doc" (both editors) | refills the boxes from THIS chart's document; saves nothing; absent where no document | walk B; RT w3-F4; smoke |
| Export | carries each exported chart's OWN edits (`eventInfoBySyl`) | walk A (the file's keys named) |
| Import | writes ONLY into the charts brought in; merge, file wins where it speaks | walk A (D122 ×3); RT D122 ×3 |
| ⧉ Duplicate | the copy takes what the source SHOWS (profile + edits) | RT D126 dup |
| 🗑 Delete — custom chart | its details go with it (it cannot come back) | reading |
| 🗑 Delete — built-in / ↺ Restore | MUST NOT drop them, because a restore should bring back what was typed | reading |
| "Revert edits only" | MUST NOT drop them, because typed details are not structure (D122's spirit: never wipe typed details) | reading — stated to the reviewers |
| 📋 Edit events (the raw list) | MUST NOT carry details, because it edits structure only | reading |
| The old one-table store (`v3:eventinfo`) | converted once onto every chart with the event; old key kept | ED (booted over it) |
| A file written before D126 (`eventInfo`) | its real edits land on the imported charts only | RT D122 old-file; smoke |

**B — a ball's marks when the ball goes (D124)**

| Door that takes an event off a chart | Wipes its marks? | Evidence |
|---|---|---|
| 🗑 Delete tool / Delete key / the ball editor's Delete ball → ✓ Save changes | YES, every course, at the save; ↶ before the save restores ball + marks | walk B; RT D124 |
| 📋 Edit events dropping an event → ✓ Save changes | YES (the same save) | reading (one body: `persistSyl`) |
| "Revert edits only" dropping an added event | YES | reading |
| ⇪ Import "Replace" dropping an event | MUST NOT, because a chart import never writes a mark (the 9 Sep rule the smoke pins; D122 lets an import reset marks but does not require it) | smoke's write-watch |
| 🗑 Delete syllabus | YES (its whole sweep, as before) | existing |
| Re-adding the code before the save | REFUSED with the way on (Save first, or Undo) | walk B |

**C — Last Flown (D123)**: every writer — a flight's grade (done, not done, N.A.), a flight re-dated, the
D124 wipe, the two boxes typed by hand (the hand marks), undo/redo (the whole dates record) — walked
or pinned (walk B ×7, RT ×3). A students import writes the file's dates as they were (MUST NOT settle:
the file's record is the record).

**D — logging out (D129)**: the top bar's Logout and the phone drawer's both call `ui/logout.ts` — walk B
(top bar, from Edit Schedule), RT (the function). A reload / closed tab keep the browser's own
leave-the-page question (unchanged, walked by w3 #18).

**E — the save corner (R96/R97/R115)**: desktop 1200 / 1250 / 1440 in and out of edit mode, phone — walk C.

**Rows the two code reads added (§11) — now answered:**

| Roll-call | Place / door | Answer | Evidence |
|---|---|---|---|
| A | A deleted built-in's typed details × Export / Import | ride a whole backup; merged home; Restore brings them back | RT; walk r4 |
| A | ⇪ Import "Add as new" of a built-in | the copy reads as the file's chart (profile laid under) | RT; walk r4 |
| A | A deleted ball's typed details | go with it at the save (D130) | RT D130 |
| B | 📋 Edit events re-adding a deleted-unsaved code | refused, as + Add | RT; walk r4 |
| B | A student's "last worked" pointer to a deleted ball | goes with the marks | RT |
| B | ⇪ Import Replace dropping a ball | keeps its marks, out of sight (D132) | smoke write-watch |
| C | A ball switched flight ↔ not-flight (ball box, raw list, Revert) | Last Flown re-settled at the save | RT; walk r4 |
| D | Logout while a Tracker question is up | waits for the answer | RT; walk r4 |
| D128 | A deleted course × the backup | not carried (D131); the question says "in this browser" | RT D128 |
| — | A rolled-back command (details, deleted courses) | the screen follows the rollback | RT (not reachable from a control) |

## 5. The door check

| Action the data allows | Its door, and state | Walked |
|---|---|---|
| Bring back a deleted course (D128) | ⇅ Reorder courses → "Deleted courses" → ↺ Restore | walk B |
| Bring back a built-in kept deleted by an import (D127) | ⇅ Reorder syllabi → ↺ Restore | walk A (listed) |
| Put a detail back to its document | Reset to doc → Save, in both editors — only where a document exists | walk B |
| Stop a logout with unsaved chart edits | the question's Stay | walk B |
| Skip one chart in an import | "Skip this one" (and a press outside) | walk A |
| Select every student in Copy to… | the Select all row (with two or more others) | walk B |

## 6. Orders walked

- **Import vs edit, both orders:** edit Tx after exporting 2026, then import 2026 → Tx kept (walk A);
  edit 2026 after exporting it, then import → the file's edit comes in, other typed details stay (RT).
- **Delete then undo / delete then save then re-add / delete then re-add before the save** (walk B, RT).
- **Last Flown:** newer then older flight; correct the newer earlier; un-mark each; hand-typed then an
  older flight, then a later one (walk B).
- **Logout:** Stay → Discard → sign in → Save them → sign in (walk B).
- **Phone halves:** bubble then Info; bubble then another page; turn on Info then Flow, both ways (walk C).

## 7. Scenario dispositions (Fable + Astra)

The two lists (`2026-09-23-tracker-fable-scenarios.md`, `…-astra-scenarios.md`) became the findings in §3
and the walkers' parts. Astra #1 → D127; #2 → F6; #3/#5 → D126/D122; #4 → F5; #6 → F2; #7 → F13; #8 →
cleared by walk 4 (pointer) and closed by F13's belt; #9 → F10 + D129; #10 (a whole import as ONE undo
step) → already filed in `OUTSTANDING.md` (the command layer's leftovers); #11 → D128; #12 → superseded by
D121; #13 → F9; #14 → F11; #15 → not a D120 finding (old-data converter). Fable's list: #2/#3/#5/#6/#8
fixed in round two; the rest walked by the walkers (their parts).

## 8. Every finding, dispositioned — red first

Every fix has a test in `src/tracker/retest.test.tsx` (or `eventDetails.test.tsx` / `app/sylIds.test.ts`)
that failed on the unfixed code for the defect itself (a red on a broken premise was repaired before it
counted). Where jsdom cannot show the defect (no layout), the walker's own measurement is the red and the
re-walk the green — named in the row.

| Finding | Disposition | Commit |
|---|---|---|
| F1 key ball printed the course's hidden code | **fixed** — names the course | `5322db46` |
| F2 / F3 Duplicate / Add syllabus words; the stale "hit ✎ Edit" guidance | **fixed** — the words say what they do | `5322db46` |
| F4 Details mode's "tap Edit details" | **fixed** — the bubble says how to reach it | `5322db46` |
| F5 the ball box leaked Tx's details onto every chart | **fixed** — one writer body (`writeInfo`) | `5322db46` |
| F6 import lost the chart ORDER (D120) | **fixed** — a file with 2+ charts restores its order; one chart keeps its place | `5322db46` |
| F7 untouched built-ins came back "✎ edited" | **fixed** — a shipped-identical chart is not stored as an override | `5322db46` |
| F9 empty course: a pop-up of dead grades | **fixed** — a flashed notice instead | `5322db46` |
| F10 the next login inherited undo, modes, windows | **fixed** — the session ends at login/logout; the logout now ASKS about unsaved chart edits (D129) | `5322db46`, `01e0c1a6` |
| F11 Show All's format hint missed D64 | **fixed** — one `FMT_HINT` | `5322db46` |
| F13 a keyboard chart switch left the pop-up open | **fixed** — loading a chart closes it | `5322db46` |
| Fable #8 flash messages written to a hidden line | **fixed** — the line shows while a flash is up | `5322db46` |
| Role gating (the File menu admin-only) | **superseded by D121** — the owner ruled admin and member the same; the lock is removed | `b95f7844` |
| Fable #6 / W1-5 Export's refusals drawn behind the window | **fixed** — the question box tops the ladder (101); re-walk: the old script could no longer click behind it | round two |
| Fable #5 / W1-4 no way to tick every chart in Export | **fixed** — All / None beside the list (default unchanged) | round two |
| Fable #2 / W1-1 Export left unsaved edits out, silently | **fixed** — asks to save first; re-walk PASS | round two |
| Fable #3 / W1-2 Add / Duplicate / Import dropped unsaved edits silently | **fixed** — each asks; re-walk PASS | round two |
| F8 a deleted built-in returns after the wipe | **fixed per D127** — a whole backup names it; the import keeps it deleted (restorable) and says so | `01e0c1a6` |
| F12 course delete's "marks remain in storage" has no way back | **fixed per D128** — a restore door in ⇅ Reorder courses | `01e0c1a6` |
| W1-3 never-dragged balls exported to one corner; a Reset copy re-laid | **fixed** — one place rule for the board and the export | `85a6562a` |
| W1-4b "a full backup" for 1 of 4 charts | **fixed** — "N of M charts … NOT a full backup" | `41e22411` |
| W1-6 a rename collision refused the whole import | **fixed** — the file's own rename frees the old name; labels given back after | `41e22411` |
| W1-7 "Cancel" skipped a chart and the report never said | **fixed** — "Skip this one"; the report names it | `41e22411` |
| W1-8 details typed on one chart showed on every chart | **fixed per D126** — per chart | `41e22411` |
| W1-9 a one-chart import reset other charts' details | **fixed per D122/D126** | `41e22411` |
| W2-F1 N.A. "cannot be failed" never shown | **fixed** (round one's flash line); re-walk PASS | `5322db46` |
| W2-F2 a retyped Done-on day read as today | **fixed** — only a whole day re-dates | `41e22411` |
| W2-F3 Last Flown could not come back down | **fixed per D123** — derived; hand-typed stands until a later flight | `41e22411` |
| W2-F4 a lull period's × removed it at once | **fixed** — asks first | `41e22411` |
| W2-F5 Copy to… had no Select all | **fixed** | `41e22411` |
| W2-F6 ↶ / ↷ threw the chart to its top | **fixed** — keeps the view. Red = walker 2 (4673 → 368); green = walk B (4673 → 4673); jsdom cannot see it | `41e22411` |
| W2-F7 a new ball with a deleted code came up graded | **fixed per D124** | `41e22411` |
| w3-F1 the details bubble outlived its chart | **fixed** — hidden on a phone half switch and when the tab leaves | `245687a6` |
| w3-F2 a turn on the Info half left the chart unfitted | **fixed** — re-fits on Flow. Red = walker 3 (92% / 842px in 390); green = walk C | `245687a6` |
| w3-F3 a phone on its side: pop-up above the screen; zoom control below it | **fixed** — held on screen, scrolls inside; the scroll room is a spacer, not padding. Red = walker 3; green = walk C | `245687a6`, `060f3359` |
| w3-F4 Reset to doc blanked a user ball, saved on the press | **fixed** — only where a doc exists; refills, Save decides | `41e22411` |
| w3-F5 1200–1250px: the first edit wrapped the bar | **fixed** — one fixed save corner. Red = walker 3 (51 → 87px); green = walk C | `245687a6` |
| w3-F6 a phone's far-right spot was the status, not Save | **fixed** — Save last. Green = walk C | `245687a6` |
| R-1 (re-walk) a fonts-only Save marked an untouched built-in "✎ edited" | **fixed** — F7's rule at the second writer | `41e22411` |
| The smaller notes (edit-mode shift, N1–N7, O-notes) | **filed** — `OUTSTANDING.md` `[TRK-RETEST-NOTES]`, three marked his call | — |
| The chart-baking script is broken | **filed** — `[TRK-BAKE-STALE]` | — |
| G-1 (the gates' smoke run) the no-details hint named a door F9 had removed | **fixed** — ☰ Show All → Edit on a chart with nobody on it | this commit |

## 9. NOT walked, and why

- **The real OS file pickers** — replaced the documented way (a download out, a file input in).
- **Older file formats and the start-up converters** — D120 (except the one-time details carry-over,
  which is NEW and protects his typed details: pinned by `eventDetails.test.tsx`, booted over the old key;
  NOT walked on his real browser data).
- **The D124 sweep across a SECOND course** — pinned for one course and two students (RT); the sweep
  walks every course namespace by the same body the syllabus delete uses.
- **iPhone Safari** — headless Chromium only, with touch emulation for the phone walks.
- **The member account** in round three — D121 made the bar and doors identical; walker 3's 27-step
  member walk re-ran green on the new build.

## 10. Gates

Run 23 Sep 26 on this branch, one at a time on a quiet PC (D125; no other chat's run, D86): unit tests
on half the cores (`--maxWorkers=8`), smoke on 4181, e2e on 4182.

| Gate | Result |
|---|---|
| `vitest run` (all) | **5809 / 5809** passed, 358 files |
| `npm run build` (typecheck + build) | clean |
| `node reference/tfin.js` | **728 / 0** |
| `npm run rulecheck` | OK (7 rulings named by nothing — the recorded baseline) |
| `npm run test:e2e` | **467 passed**, 48 skipped (the usual per-project skips) |
| `npm run smoke:tracker` | first run: 1 FAIL + 1 stop — both from this branch's own fixes (below); after: **427 / 427** |
| `npm run docsize` | OK; `docsize: OVER by 47, deferred (D29)` (a code change; the trim is its own pass). `[LW-MONTHJUMP-PHONE]` recorded with `Docs-guard-allow` — main archived it; the guard read this branch's pre-merge history (`[DOCSGUARD-MERGE]`) |

**The final run, on the finished code** (after G-1 and the two reads' fixes, `267dab1e`): unit
**5818 / 5818** · build clean · tfin **728 / 0** · rulecheck OK · smoke **427 / 427** · e2e **467 passed**,
48 skipped · docsize OK (`OVER by 47, deferred (D29)`).

**What the smoke run caught — G-1, a real finding:** since F9 a tap on a ball of a chart with nobody on
it opens no pop-up, yet F4's hint told a person on such a chart to "tap the ball and ✎ Edit details" —
two fixes pointing at each other. **Fixed, red first** (`retest.test.tsx` G-1): with no students the
hint and the "no students" notice both point at ☰ Show All → Edit. The smoke's two Tx steps that tapped
for details after a wipe now use the doors a studentless chart has (Show All; a student added with
+ Add). The other smoke failure was the R-1 fix itself: a check counted the untouched 2026 in the
stored list of EDITED charts; it now counts the chart as the app reads it.

## 11. The two code reads

Fable 5.1 (a subagent) and Astra (Codex, gpt-5.6-sol, high), blind to each other, both read-only, given
this sheet and `docs/superpowers/briefs/2026-09-23-tracker-final-read-brief.md`; neither is the model
that built it (D67). Reports verbatim/in full beside this sheet: `2026-09-23-tracker-fable-final-read.md`,
`2026-09-23-tracker-astra-final-read.md`. Every finding reproduced as a test that failed on the code
before the fix (`retest.test.tsx`, "the two code reads"), then walked (`trk-r4-review`, 8/8).

| Finding | Who | Disposition |
|---|---|---|
| A deleted built-in's typed details did not ride the whole backup (Restore after the wipe lost them) | BOTH (Fable F-A medium, Astra #1 high) | **fixed** — carried and merged home (D127's "exactly as before the wipe") |
| A ball switched between flight and not-flight did not re-settle Last Flown | BOTH (Fable F-G low, Astra #2 medium) | **fixed** — one chart sweep at Save / Revert |
| The raw event list could put back a deleted-but-unsaved code with its marks | Fable F-B | **fixed** — refused, as + Add |
| A built-in imported "as new" took the long course's wording | Fable F-C (pre-existing) | **fixed** — its profile laid under the file's edits |
| A keyboard Logout mid-question cancelled it; the import carried on into the next session | Fable F-D | **fixed** — the session waits for the answer |
| A half-typed year when DCO is pressed stored year 0002 | Fable F-E (pre-existing) | **fixed** — today |
| A rolled-back command left details / deleted courses on screen | Astra #3 | **fixed** — the global lets reload on restore |
| A deleted ball stayed a student's "last worked" event | Astra #4 | **fixed** — the pointer goes with the marks |
| `restoreCourse` / the record re-read could duplicate a live course | Fable (a belt) | **fixed** — not reachable from a control |
| A deleted course does not ride a backup | Fable F-A (his call) | **ruled D131: leave it** — the delete question now says "in this browser" (red first) |
| A deleted ball's typed details reappear on a later ball with its code | Fable F-F (his call) | **ruled D130: delete them too** — fixed, red first |
| An import that drops a ball keeps its marks (the 9 Sep chart-import rule vs D124's spirit) | Fable (roll-call B) | **ruled D132: keep them hidden** — today's behaviour, confirmed; comment at the rule |

**Where the two disagreed, settled by evidence:** Astra's "D129 — no defect found" against Fable's F-D —
F-D reproduced (red test, and walked: Logout by keyboard with the import's question up). Severity of the
shared details finding (Fable medium, Astra high) — high taken: it loses typed work on the D120 route.

## 12. Re-walk after the fixes

On the rebuilt preview (4180), pictures in `docs/img/handpass/2026-09-23-tracker/rewalk/`, results in
`docs/handpass/parts/tracker/rewalk/`. No console errors, page errors or failed requests in any run.

| What | Result |
|---|---|
| Host walks 0–6 re-run (round one's fixes) | hold; the leftovers were F8 / F10's draft / F12 → built as D127 / D129 / D128 |
| Walkers' round-two scripts re-run (w1-a, w1-c, w1-j, w2-01, w3-05b, w3-17) | round two holds (W1-1/2/4/5, W2-F1, the logout half of w3-F1); the FAILs are D121's superseded admin-only checks, and steps whose premise the fix consumed (re-walked fresh in A/B) |
| `trk-r3-a-files` — details per chart, one-chart import, never-dragged balls, rename collision, Skip, backup wording, deleted chart | **21 / 21** |
| `trk-r3-b-marking` — Done-on, Last Flown, undo view, delete a ball, lulls, Copy to…, Reset to doc, ✎ mark, course restore, logout | **31 / 31** |
| `trk-r3-c-surfaces` — bubble, re-fit, sideways pop-up + zoom control, save corner at 1200/1250/1440 and phone | **18 / 18** |
| `trk-w3-07-phone` re-run (the phone's room below the last event after the spacer change) | PASS — 516px, clear of the zoom control |
| `trk-r5-rulings` — D130 (a deleted ball's typed details go with it) and D131 (the delete question says "in this browser") | **2 / 2**, no errors |
| `trk-r4-review` — what the two code reads' fixes touched (backup of a deleted chart's details, type change, list refusal, as-new import, keyboard Logout mid-import, half-typed year) | **8 / 8**, no errors |

## 13. His five-minute look (the card handed to him)

On the branch's Vercel preview, signed in as admin, Tracker tab:

1. **Tx 2026** → ☰ Show All → Edit **BFM-5** → type a name → Save. Switch to **2026** → its BFM-5 keeps
   its own name. (Details belong to their chart — D126.)
2. Syllabus ✎ → Edit chart layout → 🗑 Delete a ball you have marked → the question says its marks go at
   ✓ Save changes → Save. Add a ball with the same code → it comes up **not graded**. (D124.)
3. Add a ball in Edit chart layout and do NOT save. Go to **Edit Schedule** and press **Logout** → the
   Tracker comes forward and asks **Save them / Discard them / Stay**. (D129.)
4. Course ✎ → Delete a course → ⇅ Reorder courses → **↺ Restore** → it is back with its students. (D128.)
5. On your phone, turned sideways: tap a ball → the whole grading pop-up is on the screen, and the chart's
   − % + control sits at the bottom edge. (w3-F3.)

**Three questions for him — ANSWERED 23 Sep 26:** a deleted ball's typed details go with it (**D130**, built);
an import never deletes anyone's marks (**D132**, today's behaviour confirmed — asked again in plainer words
after the first wording confused); a backup does not carry a deleted course (**D131**, the question now
says "in this browser").
