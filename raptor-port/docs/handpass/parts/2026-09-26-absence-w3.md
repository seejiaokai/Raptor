# [HUMAN-RETEST] the absence record — walker W3: the Leave War's own gestures (26 Sep 26)

**Ground** (plan §9, the brief): bulk by a real mouse drag over a mixed rectangle (AB2, AB7, old plan D1–D5); the one
window in every stage and its absence in a DRAFT war (N15; Fable S13, S19; Astra 37); the postings (R9a–c) and AB5's
other doors (Astra 34); awards (N19, Astra 30, Fable S40) and the read-only sheet's routing (Astra 38); switching wars
with each sheet open, undo after switching, the leave across 31 Dec (old plan E1–E4, Fable S14, Astra 11); AB3 on the
war side; the member (D166).

**The bundle walked:** the production build served at `http://localhost:4175`, `assets/index-CvfIG_EN.js`, built
26 Sep 26 18:34 (every script prints it). **It is OLDER than the host's working-tree edits** seen at 19:16
(`store.ts` now carries a `postingProblem` for AB5, and `SelectSheet.tsx`, `BidPicker.tsx`, `Matrix.tsx` are
modified) — none of those were in what I walked. **World:** a fresh demo per script (`openHi`, or a touch context for
the phone runs of W3-05/05b), made through the app's own controls; nothing written through `window`. **Widths:** desktop
1440×900 and phone 390×844 wherever the gesture exists (the bulk drag at desktop only — touch drag-select is W4's).
**Console / page errors / failed requests:** none in any run (every script's `errors` line is `[]`).

**Scripts** (`raptor-port/scripts/handpass/ab/`, run from `raptor-port/`; each prints PASS/FAIL, so re-running it IS
the re-walk; results beside this sheet as `2026-09-26-absence-w3-*.txt`):

| script | covers |
|---|---|
| `w3-lib.mjs` | helpers: the real mouse drag (`dragRect`), the selection sheet's buttons, the top bar's Undo/Redo, reload, the stage control, the war picker, the war's own records read for the table |
| `w3-01-bulk.mjs <fill\|delete\|approve\|refuse\|ack\|move\|moveclash>` | the mixed rectangle; each action with undo, redo and a reload |
| `w3-02-window.mjs [desktop\|phone]` | N15 in OPEN / CLOSED / PUBLISHED, the tap list, a DRAFT war made through + New |
| `w3-03-postings.mjs [desktop\|phone]`, `w3-03b-po-archive.mjs` | R9a–c, the posting sheets, AB5's four doors, the top bar's Undo after a posting, an archived man's row |
| `w3-04-awards.mjs [desktop\|phone]`, `w3-04c-tracker-n19.mjs`, `w3-04d-code-follows-days.mjs` | N19 on the bid sheet and the OIL tracker, an award beside a bid, S40, the read-only routing; the award's code |
| `w3-05-switch.mjs [desktop\|phone]`, `w3-05b-stale-sheet.mjs`, `w3-05c-tab-reach.mjs`, `w3-05d-newyear-charge.mjs` | switching wars with each sheet open; the published-lock bypass; undo after switching; 31 Dec; E3; E4 |
| `w3-06-till.mjs` | AB3 on the war side, four paths |
| `w3-07-member.mjs [desktop\|phone]` | the member (Ranger) |
| `w3-00*-*.mjs`, `w3-04b-tracker-probe.mjs` | read-only probes used to plan the walk (not part of the re-walk) |

**Pictures:** `raptor-port/docs/img/handpass/2026-09-26-absence/w3/` — 222, every one named in the tables below; the
ones a finding rests on were opened and looked at.

**Counts:** 78 scenarios walked (each at both widths where the gesture exists) — **60 PASS, 18 FAIL**, the 18 falling
into **10 findings** (3 already filed by the reviews — AB2, AB3, AB5 — and AB7 predicted by Fable, now shown; 6 new).
Result lines: 201 PASS / 34 FAIL; two of the FAILs (`W3-04-* N19-tracker`) are my driver's selector, not the app —
`w3-04c` walks the same check and passes.

---

## 1. The findings (every FAIL, one line)

| # | finding | rule | pictures |
|---|---|---|---|
| **W3-F1 = AB2** | Bulk **Delete** takes the OIL awards in the rectangle — Dash's and Fable's +OIL each drop by 1 — and its confirm ("Delete 3 days for 4 people? Tap Delete again.") never mentions them | Fable F2; an award has its own Remove | `w3-01-delete-a-before`, `-c-confirm`, `-d-note`, `-e-after` |
| **W3-F2 = AB7** | Bulk **Approve** says "3 decided" when 2 changed — Fable's already-approved leave is counted | Fable F7 / S16 | `w3-01-approve-d-note` |
| **W3-F3** (new) | Bulk **Delete** and bulk **Move** skip an undecided bid that shares its day with Inputs-filed leave (Ghost: morning filed, afternoon bid): Delete files it under "5 skipped — locked, owned by Raptor…", Move says "move 2 entries" and leaves it behind. Approve, Refuse and Ack DO reach it (the D1 fix) | old plan D3, D4; the D1 shape | `w3-01-delete-e-after`, `w3-01-move-c-hover`, `w3-01-move-e-after` |
| **W3-F4 = AB5** (other doors) | A posting that closes before it opens is refused SILENTLY at all four doors: the Post out sheet's date box and the Post in sheet's date box snap back with no word; the drag-selection's Post out and the bid sheet's PO close as though done | Fable F5 / S3; Astra 34 | `w3-03-{desktop,phone}-D1…D4-*` |
| **W3-F5** (new, small) | Moving the Post out sheet's date PAST the day you tapped swaps the sheet, under your hands, for a bid sheet on that day | the sheet's own promise: "commits on change — the sheet stays up" | `w3-03-desktop-C3-after-po-date-moved-past-tapped-day` |
| **W3-F6** (new) | An award's CODE does not follow a change of its days made on the **OIL tracker** or the **tap list**: a 0.5-day HO made 2 days still reads **HO** (and "HO · 2 days" on the sheet); a 1-day FO made 0.5 still reads **FO**. The OIL figure is right | N19 ("under a day reads HO, a day or more reads FO"); cf. `[OIL-AWARD-IS-A-GRANT]` (the three editors) | `w3-04d-{desktop,phone}-*` |
| **W3-F7** (new, serious) | A sheet left open across a **keyboard** war switch is re-read against the war now on screen: the PUBLISHED 26 war's note sheet on Basher's approved 16 Dec leave turns into a live bid sheet, and its **Clear deletes that published, war-approved leave** (Inputs row gone) while JAN–DEC 27 is on screen. The tap list and the read-only sheet also turn into bid sheets; the Post out sheet stays live | E1 / Astra 11 / Fable S14; owner 21 Sep 26 "approved and published is finished paperwork" | `w3-05b-desktop-1…4`, `w3-05c-after-keyboard-switch`, `w3-05-desktop-E1-*` |
| **W3-F8** (new) | **Redo** brings an undone bid back onto a day where a medical was filed after the Undo — the undecided LL stands on the ATT C day (amber `!`, "Two of these can't both stand…") | E3; B7 at every door, undo and redo included (plan §0) | `w3-05b-{desktop,phone}-5-redo-offered-after-medical`, `-6-after-redo-taplist` |
| **W3-F9 = AB3** (war side) | The war's own "till 31 Jul" stays on EVERY piece after a medical cut, the war's Refuse of a middle day, the war's Delete of a middle day, and a drag-Move of the last two days — the 3–4 Aug piece reads "till 31 Jul" | Fable F3 / S5; D189's reading | `w3-06-{a-medical,b-refuse,c-delete,d-move}-inputs-page`, `-after` |
| **W3-F10** (new, low — perhaps a question for him) | The member cannot open his OWN OIL award outside the bidding window: at OPEN his July award opens nothing (inside the window, Feb, it reads reason / given by / days); at CLOSED nothing opens at all. The OIL tracker still shows it | owner 21 Sep 26 (the BidPicker comment: "a member reading his own OIL is entitled to know why it is there and who gave it") | `w3-07-{desktop,phone}-M-own-award-open`, `-M-own-award-in-window` |

---

## 2. Each finding in full

### W3-F1 — AB2: bulk Delete takes the awards (REPRODUCED)
- **Steps (fresh world, admin, desktop):** publish Sat 18 Jul (board, four sign-offs, Publish day). On the war: Dash
  (spaceman) bid LL Thu 16; +OIL 1 day on Dash Sat 18 ("W3 award A") and on Fable (plasma) Thu 16 ("W3 award B").
  Drag with the mouse from Dash 16 to Grit (sufa) 18 → Delete → Delete again.
- **Saw:** the confirm "Delete 3 days for 4 people? Tap Delete again." (no word of awards); then "4 deleted. 5 skipped";
  both FO boxes empty; Dash OIL 3 → 2, Fable 2 → 1. The count is TRUE (4 boxes changed) — what it hides is WHAT went.
  One Undo brings both back; Redo and a reload keep the result.
- **Who it hurts:** the men whose awarded days vanish from their OIL balance, and the admin, who deleted a block of
  bids and did not mean to take anyone's owed days. Earned leave (D25).

### W3-F2 — AB7: "N decided" counts an already-approved leave (REPRODUCED)
- **Steps:** as W3-F1's rectangle (Fable Fri 17 bid LL, then the window's Approve), drag it, press Approve.
- **Saw:** "3 decided. 9 skipped"; only 2 boxes changed (Dash 16, Ghost 16 afternoon — both became Inputs). Fable's
  leave was already approved and nothing happened to it.
- **Who it hurts:** the admin, who reads the count to know what he just did.

### W3-F3 — bulk Delete / Move skip a bid sitting beside Inputs-filed leave (NEW)
- **Steps:** Ghost (riddler): file LL Thu 16 Jul **morning** on the Inputs page; on the war bid his free afternoon
  (the sheet offers only PM, naming the held morning). His box reads `<LL +1`. Drag the rectangle over it, then
  (a) Delete ×2, or (b) Move… and click Mon 20 Jul.
- **Saw:** (a) the afternoon bid survives (`request:LL*/pending`), counted among "5 skipped — locked, owned by
  Raptor, or outside the window"; (b) the banner reads "Tap a day to move **2** entries" (Dash's bid and Fable's
  approved leave) and Ghost's afternoon bid stays on the 16th. Approve / Refuse / Ack over the same rectangle DO act on
  it (old plan D1's fix) — Delete and Move ask only the day's TOP record (`mainAt`), which is the filed morning.
- **Rule:** old plan D3 ("delete removes the editable requests") and D4. **Who:** the admin clearing or moving a block —
  one man's bid is silently left where it was.

### W3-F4 — AB5 at the other three doors (REPRODUCED, both widths)
- **Steps:** Echo (freak): PI from 6 Jul (bid sheet → PI), moved to 8 Jul on the Post in sheet; PO from 10 Aug
  (bid sheet → PO), moved to 14 Aug on the Post out sheet. Then:
  - **D1** tap a greyed day (20 Aug) → Post out sheet → type 2026-07-01 in its date box → the box snaps back to
    2026-08-14, nothing is said (`w3-03-*-D1-postout-datebox-refused`);
  - **D2** tap a pre-PI day (3 Jul) → Post in sheet → type 2026-08-20 → snaps back to 2026-07-08, nothing said (`…-D2-…`);
  - **D3** drag three of his July days → Post out (PO)… → 2026-07-01 → Confirm post-out → the sheet CLOSES, nothing
    changed, nothing said (`…-D3-selection-po-fold`, `…-D3-selection-po-after`);
  - **D4** bid sheet PO 2026-07-01 → the sheet closes, nothing said (the host's H2 door, `…-D4-…`).
- **Who:** the admin, who believes a posting was changed. (The host's working tree carries a `postingProblem` sentence
  for this — not in the bundle I walked.)

### W3-F5 — the Post out sheet swaps under the admin's hands (NEW, small)
- **Steps:** Echo posted out from 10 Aug; tap 12 Aug (greyed) → Post out sheet → change its date to 2026-08-14.
- **Saw:** the posting moves (12–13 Aug are his again) and the sheet he was working in is replaced by a **bid sheet
  for 12 Aug** — because 12 Aug is now inside his time. The sheet's own comment promises it "stays up so the admin can
  see the grid move behind it". A date moved the other way (earlier) keeps the sheet (B2 on the Post in sheet).
- **Who:** the admin — a leave sheet appears where his posting sheet was; one careless tap places leave.

### W3-F6 — an award's code does not follow its days (NEW, both widths)
- **Steps:** (1) Sidewinder (mamba) 26 Jul: +OIL 0.5 → the box reads HO. OIL tracker → his 26 Jul credit's reason
  button → days 2 → Save. (2) Warden (nact) 27 Jul: +OIL 1 (FO) and a bid beside it → tap list → Edit… → days 0.5 → Save.
- **Saw:** (1) the grid still reads **HO**, the bid sheet "HO · 2 days", OIL = 2 (`w3-04d-*-grid-after-tracker`,
  `-tracker-2-days-sheet`); (2) the box and the line still read **FO** for half a day (`-taplist-half`). Only the bid
  sheet's +OIL derives the code from the number ("Give HO" / "Give FO").
- **Rule:** N19 (owner, 21 Sep 26 — the number is the fact, the code is derived from it). **Who:** everyone reading the
  grid: a man owed two days shows "half a day". Close to `[OIL-AWARD-IS-A-GRANT]` ("the three cell editors become one
  ledger edit") but not named there.

### W3-F7 — a sheet survives a keyboard war switch and writes past the published lock (NEW, serious)
- **Steps (fresh world, admin, desktop):** Basher (glass): bid LL Wed 16 Dec 26, Approve. Stage → BIDDING CLOSED →
  PUBLISHED. Switch to JAN–DEC 27 and move it to OPEN FOR BIDDING; back to JAN–DEC 26. Tap Basher 16 Dec → the note
  sheet (correct for a published war). **Now press Shift+Tab until the Period picker has the focus** (49 presses from
  the sheet — the sheet does not hold the focus, `w3-05c`) **and press ↓** to pick JAN–DEC 27. The sheet does not
  close: it becomes "Basher 2026-12-16" with the leave buttons and Clear (`w3-05b-desktop-2-after-switch-to-27`).
  Press **Clear**.
- **Saw:** the sheet closes; back on JAN–DEC 26 (still PUBLISHED) Basher's 16 Dec is empty and his Inputs row is gone
  (`…-4-back-in-26`). One Undo brings it back. The same switch turns the tap list and the read-only "Leave from Raptor"
  sheet into bid sheets for the other war's day, and leaves the Post out sheet live (its Undo post out works)
  (`w3-05-desktop-E1-*`); nothing is written INTO the 27 war.
- **Why:** the war switch clears the drag-selection but not the one-cell sheet, which is re-evaluated against the war
  now on screen — its stage decides the controls (27 is OPEN), while the write lands on the day's own war (26,
  PUBLISHED).
- **Not by finger or mouse:** on a touch phone the shade lets the finger reach the picker, and that tap closes the sheet
  first (`w3-05-phone`, `w3-05b-phone` — all PASS); on a desktop the mouse lands on the shade, which closes the sheet.
  The door is the KEYBOARD (and anything else that changes the picker while a sheet is up).
- **Who:** the published record — a finished, published leave removed with no reopening of the stage and no word.

### W3-F8 — Redo puts a bid back over a medical (NEW, both widths)
- **Steps:** Warden (nact) bid LL on a day (Tue 2 Feb 27 / Tue 8 Dec 26) → the war's Undo (bid gone) → Inputs page:
  file ATT C for Warden that day (certificate: none) → back to the war: Redo is still offered ("Redo — a change to the
  leave board") → press it.
- **Saw:** the undecided LL bid is back on the medical day: box `C` with the amber `!`; the tap list lists the ATT C and
  "LL — local leave · bid, not decided yet" with "Two of these can't both stand on the same time — an admin needs to
  change one" (`w3-05b-*-6-after-redo-taplist`). Filing the same medical while the bid existed replaces the bid with a
  notice (the Inputs door's rule); by Redo the rule is skipped. The Inputs filing also left the Redo available.
- **Who:** the man and the admin — a bid that the rules would have turned into a notice stands live on a sick day.

### W3-F9 — AB3 on the war side (REPRODUCED, four paths)
- **Steps:** for each of Talisman, Wildcard, Recon, Vector: drag Mon 27 – Fri 31 Jul on his row → LL → drag again →
  Approve → ONE Input "till 31 Jul" (PASS). Then: (a) file ATT C 29 Jul on the Inputs page; (b) tap 29 Jul → Refuse;
  (c) drag 29 Jul → Delete ×2; (d) drag 30–31 Jul → Move… → click Mon 3 Aug.
- **Saw:** every surviving piece still reads "till 31 Jul" — 27–28 Jul, 30–31 Jul, 27–29 Jul, and the moved **3–4 Aug**
  piece (`w3-06-d-move-inputs-page`).
- **Who:** anyone reading the Inputs page, the week and the board; and D189's "each published day it still covers reads
  pending" does not fire on these paths.

### W3-F10 — the member cannot read his own award outside the bidding window (NEW, low)
- **Steps:** admin gives Ranger (bane) +OIL on 24 Jul and on 12 Feb; sign in as `us`. Tap 24 Jul → nothing opens; tap
  12 Feb (inside the 1 Jan – 31 Mar window) → the bid sheet with REASON / GIVEN BY / DAYS at the foot. At CLOSED his
  own days open nothing. (His own war-approved July leave at OPEN also opens nothing.)
- **Rule:** the one-tap read-back (owner, 21 Sep 26), which the code's comment extends to a member's own OIL. The OIL
  tracker shows the same facts, so nothing is hidden — it is a door that does not open. **Put to him?** — whether a
  member's own FO/HO should open read-only outside the window.

---

## 3. The scenarios

### 3a. Bulk by a real mouse drag (`w3-01-bulk.mjs`, desktop) — the rectangle
Dash / Fable / Ghost / Grit × Thu 16 – Sat 18 Jul, war OPEN: an empty day, a pending bid, an Inputs-filed leave, a
medical (the demo's ATT C), a split day (filed morning + afternoon bid), two OIL awards, the schedule's automatic
credit (Fable, SDO on the published Saturday — `FO*`), a war-approved leave (`w3-01-*-a-before`).

| id | expected (rule) | saw | result | pictures |
|---|---|---|---|---|
| drag | a mouse drag over 4 rows × 3 days opens the selection sheet naming the block | "4 people 16 Jul 26 – 18 Jul 26 · 3 days", Fill / Decide / Delete / Move | PASS | `w3-01-*-b-sheet` |
| D2 fill OL | writes where allowed; "N written, M skipped" true | "7 written. 5 skipped" = 7 boxes changed; a bid lands beside an award (`+1`) and beside the automatic credit (`!` — the bid rule, deliberate) | PASS | `w3-01-fill-d-note`, `-e-after` |
| AB2 | Delete keeps the awards / says it takes them | both awards gone, confirm silent | **FAIL — W3-F1** | `w3-01-delete-*` |
| D3 | Delete never takes the medical or the automatic credit; takes the bid and the war-approved leave | as expected | PASS | `w3-01-delete-e-after` |
| D3 split | Delete takes Ghost's afternoon bid | skipped | **FAIL — W3-F3** | `w3-01-delete-e-after` |
| delete note | "4 deleted. 5 skipped" equals what changed | true | PASS | `w3-01-delete-d-note` |
| D1 | Approve decides the afternoon bid under the filed morning (writes an Input) | yes — a PM Input, lw | PASS | `w3-01-approve-e-after` |
| AB7 | "N decided" counts only what changed | "3 decided", 2 changed | **FAIL — W3-F2** | `w3-01-approve-d-note` |
| D5 refuse | Refuse over a war-approved leave turns it back into a refused bid; "3 decided" true | yes | PASS | `w3-01-refuse-*` |
| D5 ack | Ack over it → an acked bid; "3 decided" true | yes | PASS | `w3-01-ack-*` |
| D4 move | the block lands with its gaps (16→20, 17→21); the medical and a course never move | yes | PASS | `w3-01-move-c-hover`, `-e-after` |
| D4 split | Ghost's afternoon bid moves too | "move 2 entries", left behind | **FAIL — W3-F3** | `w3-01-move-c-hover` |
| D4 all-or-nothing | one clashing landing (Fable's approved leave onto his Inputs-filed 21 Jul) refuses the WHOLE move and names the day | "That lands on 2026-07-21 which is already booked — pick another day.", nothing moved, the mode stays | PASS | `w3-01-moveclash-c-hover`, `-d-after-click` |
| undo / redo / reload | after every action: one Undo restores the whole rectangle, Redo re-applies it exactly, a reload keeps it | yes, all seven actions (a refused move leaves nothing on the timeline — its Undo takes the previous filing) | PASS | `w3-01-*-f-undone`, `-g-reloaded` |

### 3b. The one window (`w3-02-window.mjs`, desktop and phone — identical)
| id | expected (rule) | saw | result | pictures |
|---|---|---|---|---|
| O1 | OPEN: a tap on an undecided bid shows Ack / Approve / Refuse / Move (N15) | yes (Move off until a date is typed) | PASS | `w3-02-*-O1-open-window` |
| O2 | Ack → acknowledged, Ack pressed next time; Undo / Redo | yes | PASS | `-O2-acked-window` |
| O3 | Move → lands UNDECIDED on 22 Jul, no moved mark while open; Undo / Redo | yes | PASS | `-O3-moved` |
| O4 | Approve → the war's Input ("on 22 Jul", lw), the bid goes | yes; a tap on it at OPEN opens the same four-answer window | PASS | `-O4-approved-tap` |
| O6 | Refuse keeps the bid as refused history | yes (red chip) | PASS | — |
| O7 | a day with an award + a bid opens the tap list, each with its own buttons | "FO — OIL award (…) Edit… Clear" / "LL … bid, not decided yet Approve Ack Refuse Clear" | PASS | `-O7-taplist-open` |
| C1 | CLOSED: the same window; a moved bid reads "moved from 2026-07-20" and wears the dotted mark | yes | PASS | `-C1-moved-from`, `-C1-grid-moved-mark` |
| C2 | CLOSED: Approve writes the Input | yes | PASS | `-C3-approved-tap-closed` |
| P1 | PUBLISHED: a war-approved leave opens the note sheet ONLY | "Remarks" + Save, no decision | PASS | `-P1-approved-remarks` |
| P2 | PUBLISHED: an undecided bid still opens the four answers (S19) | yes | PASS | `-P2-undecided-published` |
| P3 | PUBLISHED: on the tap list an approved leave has Note only; an undecided bid keeps Approve / Ack / Refuse | yes | PASS | `-P3-taplist-published` |
| P4 | reload: still PUBLISHED, the bid still there | yes | PASS | — |
| D0 | + New makes a DRAFT war and shows it | "W3 DRAFT 28" (Jan 2028), DRAFT | PASS | `-D0-new-war-sheet` |
| D1 | DRAFT: a placed bid opens with NO Ack / Approve / Refuse / Move (S13, Astra 37) | yes | PASS | `-D1-draft-bid-tapped` |
| D2 | DRAFT: the selection sheet has no Decide row | yes | PASS | `-D2-draft-selection` |

### 3c. Postings (`w3-03-postings.mjs` both widths — identical; `w3-03b-po-archive.mjs`)
| id | expected (rule) | saw | result | pictures |
|---|---|---|---|---|
| A1 | bid sheet PI from 6 Jul: days before hatched (N7) | yes | PASS | `-A1-pi-fold`, `-A1-pi-hatch` |
| A2 | the top bar's Undo after a posting (R28 — recorded) | "Undo — a change to the leave board" took the BID placed before the PI; the PI stayed (a posting is not on the timeline) | PASS (recorded) | `-A2-after-topbar-undo` |
| B1–B3 | an admin's tap before the PI opens the Post in sheet (date, Undo post in, Place leave or OIL here instead…); its date commits at once; Place hands the day to the bid sheet (N12) | yes | PASS | `-B1-postin-sheet` |
| B4 | leave placed before the PI shows LL on the hatched day (N8) | yes | PASS | `-B3-leave-before-pi` |
| C1–C3 | bid sheet PO from 10 Aug (archive off): 10 Aug on greyed, the last day in tagged PO; the Post out sheet; its date moves the posting at once | yes | PASS | `-C1-po-fold`, `-C1-po-grey`, `-C2-postout-sheet` |
| C3b | the Post out sheet stays up while its date changes | swapped for a bid sheet | **FAIL — W3-F5** | `-C3-after-po-date-moved-past-tapped-day` |
| C4 | clearing leave after the PO through "Place leave or OIL here instead…" | yes | PASS | `-C4-clearing-leave` |
| D1–D4 | AB5 at four doors: refused AND said | refused, never said | **FAIL ×4 — W3-F4** | `-D1…D4-*` |
| E1 | the drag-selection's Post out for one man | yes | PASS | `-E1-selection-po-ok` |
| E2 | a reload keeps both postings; each posting's own Undo clears it; the leave placed outside the dates stays | yes | PASS | `-E2-undone` |
| S37 | posted out from a date already past with Archive on: his row stays on the war with his July bid; Quals no longer lists him | yes (both doors) | PASS | `w3-03b-after-sel-po`, `w3-03b-quals` |

### 3d. Awards (`w3-04-awards.mjs`, `w3-04c-tracker-n19.mjs`, `w3-04d-…`, both widths — identical)
| id | expected (rule) | saw | result | pictures |
|---|---|---|---|---|
| N19 bid sheet | 0.25, 0.75, 1.25 → "Days come in halves — 1, 1.5, 2 …"; 0, −1, "abc" → "Type how many days — or leave it blank"; nothing written, never rounded; 0.5 → HO, 1.5 → FO land as typed | exactly | PASS | `w3-04-*-N19-0.25-refused`, `-N19-abc-refused` |
| N19 tracker | the same values on the OIL tracker's award editor refused, the award left at 0.5; 2 lands; one Undo takes it back | exactly | PASS | `w3-04c-*-tracker-0.25`, `-tracker-abc` |
| N19 code | the code follows the days on the tracker and the tap list | it does not | **FAIL — W3-F6** | `w3-04d-*` |
| beside a bid | a bid on an award day lands beside it (`+1`, both lines on the tap list) | yes | PASS | — |
| S40 | the tap list refuses 1.25; reason + giver + days edited together; ONE Undo takes all three back (OIL 2 → 1) | yes | PASS | `w3-04-*-S40-edit-three`, `-S40-after-one-undo` |
| per-record Clear | the tap list's Clear on the bid line takes the bid only | yes | PASS | — |
| R38 admin | Inputs-filed leave → read-only "Filed on the Inputs page … change it there"; schedule credit → "Earned off the published schedule — change the schedule…"; input credit → "Earned off a duty input that was accepted — change that input…" (the admin's credit days open the bid sheet with that line at its foot, so he can add an award — N16) | exactly | PASS | `w3-04-*-R38-admin-*` |
| R38 member | the same three on another man's row: the read-only sheet, its own sentence, no controls | exactly | PASS | `w3-04-*-R38-member-*` |

### 3e. Switching wars (`w3-05*`)
| id | expected (rule) | saw | result | pictures |
|---|---|---|---|---|
| E1 phone (finger) | every sheet (bid, tap list, Post out, read-only) closes when the finger reaches the picker; nothing written into 27; nothing re-opens back in 26 | yes (the finger tap closes the sheet first) | PASS | `w3-05-phone-E1-*-after-switch` |
| E1 desktop (keyboard) | the same | the selection sheet closes; the bid sheet stays for a 26 day; the tap list and the read-only sheet become bid sheets; the Post out sheet stays live — nothing written into 27 | **FAIL — W3-F7** | `w3-05-desktop-E1-*-after-switch` |
| published lock | a sheet surviving the switch cannot change the PUBLISHED war's approved leave | Clear removed it | **FAIL — W3-F7** | `w3-05b-desktop-1…4` |
| keyboard reach | (fact for W3-F7) the picker is reachable by Shift+Tab with a sheet open | 49 presses; the sheet does not hold the focus | recorded | `w3-05c-after-keyboard-switch` |
| E2 | Undo made in 27, pressed while 26 is shown, takes back the 27 bid, shows 27, touches nothing in 26; Redo puts it back | yes, both widths | PASS | `w3-05-desktop-E2-after-undo` |
| Q11 both wars | LL 30 Dec 26 – 2 Jan 27 (Inputs) shows 30–31 Dec in the 26 war and 1–2 Jan in the 27 war | yes | PASS | `w3-05-desktop-Q11-war26`, `-Q11-war27` |
| Q11 move | moving the war-approved 31 Dec leave to 4 Jan 27 is refused with a sentence | "2027-01-04 is not a day this leave can land on — pick a day inside the war." (the date box's max is 2026-12-31) | PASS | `w3-05-desktop-Q11-move-refused` |
| charges once | the 31 Dec leave charges its working days once (30, 31 Dec, 1 Jan): LVE TOT 4.5 → 7.5, the SAME in both wars — one running balance per counter, `[LEAVE-YEAR]` (filed) | yes | PASS | `w3-05d-war27-figures` |
| E3 | a redo the rules must refuse is refused by name | Redo re-applied the bid over the medical | **FAIL — W3-F8** | `w3-05b-*-5`, `-6` |
| E4 | after a reload the bid stays, Undo and Redo are both off | yes, both widths | PASS | `w3-05-desktop-E4-after-reload` |

### 3f. AB3 on the war side (`w3-06-till.mjs`, desktop)
| id | expected | saw | result |
|---|---|---|---|
| war writes till | five days dragged, filled, approved → ONE Input "till 31 Jul" | yes (×4) | PASS |
| a medical / b refuse / c delete / d move | each piece's "till" names its own last day | every piece "till 31 Jul" | **FAIL ×4 — W3-F9** |

### 3g. The member (`w3-07-member.mjs`, both widths — identical)
| id | expected (rule) | saw | result | pictures |
|---|---|---|---|---|
| other rows | another man's bid or empty day opens nothing | yes | PASS | — |
| other Inputs leave | another man's Inputs-filed leave: read-only only | yes | PASS | `-M-other-inputs-leave` |
| own, no admin controls | nothing on his row carries a decision, posting or award control | yes | PASS | — |
| own bid | in the window he bids on his own row; no Ack/Approve/Refuse/Move, no +OIL/PO/PI | yes | PASS | `-M-own-bid-sheet` |
| own award | his own award opens read-only outside the window too | only inside | **FAIL — W3-F10** | `-M-own-award-open`, `-M-own-award-in-window` |
| drag | a drag from his row across three rows selects his row only ("Ranger"), no Decide / PO; fill writes only his row; his Undo takes it back | yes (a drag that STARTS on another man's row selects nothing) | PASS | `-M-drag-sheet` |
| CLOSED | his own bid opens nothing; a drag selects nothing; no stage control | yes | PASS | `-M-closed-own-bid` |
| PUBLISHED | his own approved leave opens the note editor and his note saves; another man's leave read-only; another man's bid nothing | yes | PASS | `-M-published-own-approved` |

---

## 4. The roll-call rows in my ground (both widths, admin and member)

| row | SHOWS it? | can ACT there? | painted on the same pixels | walked |
|---|---|---|---|---|
| R5 grid cell | yes — one code by the ladder, `+1` grey, `!` amber, `FO*` for the schedule's credit, the dotted moved mark (closed), the PI hatch, the PO tag on the last day in | tap opens the right sheet per stage/role | the drag's landing paint (`.mvland`) during a move | admin + member, both widths |
| R6 tap list | yes — every record, ladder order | per-record Approve/Ack/Refuse/Clear (bid), Edit…/Clear (award), Note (published leave) | the clash line ("Two of these can't both stand…") | admin both widths; member not on a multi-record day of his own (**not walked**) |
| R7 bid sheet | yes | leave chips, Clear, the decision row (admin, not draft), +OIL/PO/PI (admin) | the award read-back (reason / given by / days) at its foot | both, both widths |
| R8 bulk | yes | fill / Ack / Approve / Refuse / Delete / Move / PO (one man); member: fill only, own row | the move banner at the foot | admin desktop (phone by mouse only in the DRAFT and member cases); touch = W4 |
| R9a PI / PO / +OIL fold | yes (admin) | yes; **AB5 silent (W3-F4)** | — | both widths |
| R9b Post in / Post out sheets | yes | date, archive, Undo, Place…; **AB5 silent**, **W3-F5** | — | both widths |
| R9c selection's Post out | yes (one man) | yes; **AB5 silent** | — | both widths |
| R10 inline award | yes | one number in halves, reason, giver; refusals said | the day's other records behind `+1` | both widths |
| R13 OIL tracker (award editor) | yes | days / reason / giver; refusals said; **code does not follow (W3-F6)** | — | both widths |
| R14 remarks sheet | yes (published, approved leave — admin any, member own) | Save | — | both, both widths |
| R15 war picker + stage control | yes | + New (admin), → / ← stage (admin; absent for the member) | **a one-cell sheet survives a keyboard switch (W3-F7)** | admin both widths |
| R25 read-only sheet | yes | nothing (✕ only) | its source sentence | admin + member, both widths |
| R28 Undo / Redo | yes | one timeline; a posting is not on it (recorded); **Redo skips the medical rule (W3-F8)**; both off after a reload | — | admin + member, both widths |

---

## 5. What I did NOT walk, and why
- **Touch drag-select and the phone's two-step move** — W4's ground; my phone runs used a mouse where a drag was
  needed (the DRAFT selection, the member's drag) and a real finger only for the war picker (W3-05/05b).
- **The tap list's Back to bid / Refuse / Delete / Move on an approved leave at OPEN and CLOSED** (a multi-record
  day) — walked only at PUBLISHED (absent, correct); the single-cell window was walked in every stage.
- **R27 the clash strip, R29 a notice's actor, R26 storage faults** — not in my ground (host).
- **Undo reverses only your own changes (D148)** — decided, not built; the member's Undo was only walked on his own
  change.
- **A published war's member drag** — `enabled` is admin-or-open; walked at CLOSED only (nothing selects).
- **Fable S17** (two same-day halves approved together) — seen in passing: Ghost's afternoon approval made a SECOND
  Input beside his morning one (the box `<LL +1`); left as Fable's question for him.

## 6. Console errors
None — no console error, page error or failed request in any run (every result file's `errors` line is `[]`).
