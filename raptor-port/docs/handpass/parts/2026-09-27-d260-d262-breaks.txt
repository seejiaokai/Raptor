{"break": "W1 D260 the store names no award (awardsIn empty)", "summary": "Tests  8 failed | 44 passed (52)", "red": ["|leavewar| src/leavewar/inputgate.test.ts > bulk Delete and Approve over mixed days (W3-F3, AB7) > Delete takes an OIL award beneath a WAR-approved leave, and beneath leave filed on the Inputs page", "|leavewar| src/leavewar/ui/awardclear.test.tsx > a dragged block’s Delete names the awards and takes them (D260) > a block holding ONLY awards still offers Delete — it removes everything in it", "|leavewar| src/leavewar/ui/awardclear.test.tsx > a dragged block’s Delete names the awards and takes them (D260) > one Undo brings every award back", "|leavewar| src/leavewar/ui/awardclear.test.tsx > a dragged block’s Delete names the awards and takes them (D260) > the confirm names each award BEFORE anything goes; the second tap takes them all", "|leavewar| src/leavewar/ui/awardclear.test.tsx > awardsIn — the awards a clear of these days would take > names each hand-given award in the cells, with its worth", "|leavewar| src/leavewar/ui/awardclear.test.tsx > the bid sheet’s Clear names the award and asks once (D260) > a range: Clear names every award in the span before it goes", "|leavewar| src/leavewar/ui/awardclear.test.tsx > the bid sheet’s Clear names the award and asks once (D260) > asked about one day, then a range picked: Clear asks again, naming the range’s awards (Fable’s S17)", "|leavewar| src/leavewar/ui/awardclear.test.tsx > the bid sheet’s Clear names the award and asks once (D260) > one day: the first Clear names the award and keeps it; the second takes it"]}
{"break": "W2 D260 the Delete confirm drops its award clause", "summary": "Tests  2 failed | 50 passed (52)", "red": ["|leavewar| src/leavewar/ui/awardclear.test.tsx > a dragged block’s Delete names the awards and takes them (D260) > a block holding ONLY awards still offers Delete — it removes everything in it", "|leavewar| src/leavewar/ui/awardclear.test.tsx > a dragged block’s Delete names the awards and takes them (D260) > the confirm names each award BEFORE anything goes; the second tap takes them all"]}
{"break": "W3 D260 the Clear never asks", "summary": "Tests  3 failed | 49 passed (52)", "red": ["|leavewar| src/leavewar/ui/awardclear.test.tsx > the bid sheet’s Clear names the award and asks once (D260) > a range: Clear names every award in the span before it goes", "|leavewar| src/leavewar/ui/awardclear.test.tsx > the bid sheet’s Clear names the award and asks once (D260) > asked about one day, then a range picked: Clear asks again, naming the range’s awards (Fable’s S17)", "|leavewar| src/leavewar/ui/awardclear.test.tsx > the bid sheet’s Clear names the award and asks once (D260) > one day: the first Clear names the award and keeps it; the second takes it"]}
{"break": "W4 D260 Delete leaves the award beside leave", "summary": "Tests  2 failed | 50 passed (52)", "red": ["|leavewar| src/leavewar/inputgate.test.ts > bulk Delete and Approve over mixed days (W3-F3, AB7) > Delete takes an OIL award beneath a WAR-approved leave, and beneath leave filed on the Inputs page", "|leavewar| src/leavewar/inputgate.test.ts > bulk Delete and Approve over mixed days (W3-F3, AB7) > on a published war Delete takes the award and COUNTS the approved leave it could not take"]}
{"break": "W5 D260 a block of awards alone offers no Delete", "summary": "Tests  2 failed | 50 passed (52)", "red": ["|leavewar| src/leavewar/ui/awardclear.test.tsx > a dragged block’s Delete names the awards and takes them (D260) > a block holding ONLY awards still offers Delete — it removes everything in it", "|leavewar| src/leavewar/ui/awardclear.test.tsx > a dragged block’s Delete names the awards and takes them (D260) > one Undo brings every award back"]}
{"break": "W6 S18 a published leave that stays is not counted", "summary": "Tests  1 failed | 51 passed (52)", "red": ["|leavewar| src/leavewar/inputgate.test.ts > bulk Delete and Approve over mixed days (W3-F3, AB7) > on a published war Delete takes the award and COUNTS the approved leave it could not take"]}
{"break": "W7 D261 his own award is not tappable", "summary": "Tests  4 failed | 3 passed (7)", "red": ["|leavewar| src/leavewar/ui/ownaward.test.tsx > his own award opens read only at every stage (D261) > CLOSED", "|leavewar| src/leavewar/ui/ownaward.test.tsx > his own award opens read only at every stage (D261) > OPEN, on a day outside the bidding window", "|leavewar| src/leavewar/ui/ownaward.test.tsx > his own award opens read only at every stage (D261) > PUBLISHED", "|leavewar| src/leavewar/ui/ownaward.test.tsx > his own award opens read only at every stage (D261) > its ✕ closes it"]}
{"break": "W8 D261 the read-only award sheet never mounts", "summary": "Tests  4 failed | 3 passed (7)", "red": ["|leavewar| src/leavewar/ui/ownaward.test.tsx > his own award opens read only at every stage (D261) > CLOSED", "|leavewar| src/leavewar/ui/ownaward.test.tsx > his own award opens read only at every stage (D261) > OPEN, on a day outside the bidding window", "|leavewar| src/leavewar/ui/ownaward.test.tsx > his own award opens read only at every stage (D261) > PUBLISHED", "|leavewar| src/leavewar/ui/ownaward.test.tsx > his own award opens read only at every stage (D261) > its ✕ closes it"]}
{"break": "W9 D262 the sheet's Move picks nothing up", "summary": "Tests  20 failed | 47 passed (67)", "red": ["|leavewar| src/leavewar/ui/deciding.test.tsx > shifting a bid > keeps the trail when the moved bid is then approved", "|leavewar| src/leavewar/ui/deciding.test.tsx > shifting a bid > marks the moved cell so the trail is visible on the grid", "|leavewar| src/leavewar/ui/deciding.test.tsx > shifting a bid > moves the bid and leaves it pending on the new date", "|leavewar| src/leavewar/ui/deciding.test.tsx > shifting a bid > says why, rather than doing nothing, when the destination is taken", "|leavewar| src/leavewar/ui/deciding.test.tsx > the moved mark waits for bidding to close > draws the moved edge once bidding has closed", "|leavewar| src/leavewar/ui/deciding.test.tsx > the moved mark waits for bidding to close > hides it again if the war is reopened for bidding", "|leavewar| src/leavewar/ui/deciding.test.tsx > the one input window > moves an input from ONE click — no drag-select needed", "|leavewar| src/leavewar/ui/deciding.test.tsx > the one input window > says WHY when a move cannot land, rather than doing nothing", "|leavewar| src/leavewar/ui/moveone.test.tsx > the one-day sheet’s Move (D262) > a click on another day lands it there, undecided — the old date empty", "|leavewar| src/leavewar/ui/moveone.test.tsx > the one-day sheet’s Move (D262) > a day that cannot take it is refused with its reason, and the move stays on", "|leavewar| src/leavewar/ui/moveone.test.tsx > the one-day sheet’s Move (D262) > a quick second click on the same spot (a double-click on Move) lands nothing", "|leavewar| src/leavewar/ui/moveone.test.tsx > the one-day sheet’s Move (D262) > its own day says so and keeps the move on — never \"Nothing to move\"", "|leavewar| src/leavewar/ui/moveone.test.tsx > the one-day sheet’s Move (D262) > once bidding has closed it lands with the dotted \"moved\" trail", "|leavewar| src/leavewar/ui/moveone.test.tsx > the one-day sheet’s Move (D262) > picks the chip up: the sheet closes and the move banner says one entry", "|leavewar| src/leavewar/ui/moveone.test.tsx > while the chip is picked up (D262) > Undo ends it (the existing guard)", "|leavewar| src/leavewar/ui/moveone.test.tsx > while the chip is picked up (D262) > a click on a control outside the grid does NOT cancel it, and neither does one on the grid’s own frame", "|leavewar| src/leavewar/ui/moveone.test.tsx > while the chip is picked up (D262) > a click on an empty area OUTSIDE the grid cancels it", "|leavewar| src/leavewar/ui/moveone.test.tsx > while the chip is picked up (D262) > a stage change ends it (the existing guard)", "|leavewar| src/leavewar/ui/moveone.test.tsx > while the chip is picked up (D262) > leaving the Leave War ends it — the chip is not carried onto another page", "|leavewar| src/leavewar/ui/moveone.test.tsx > while the chip is picked up (D262) > the month buttons work and the move stays on"]}
{"break": "W10 D262 no hover edge scroll", "summary": "Tests  3 failed | 64 passed (67)", "red": ["|leavewar| src/leavewar/ui/movewire.test.ts > the edge scroll while a chip is picked up (D262) > a pointer ALREADY resting in a band when the move begins does not scroll until it has left it", "|leavewar| src/leavewar/ui/movewire.test.ts > the edge scroll while a chip is picked up (D262) > the mouse at the grid’s RIGHT edge scrolls the days on; at the LEFT edge (past the names) back", "|leavewar| src/leavewar/ui/movewire.test.ts > the edge scroll while a chip is picked up (D262) > what the grid scrolled under the mouse is previewed there"]}
{"break": "W11 D262 a press-and-drag lands nothing", "summary": "Tests  2 failed | 65 passed (67)", "red": ["|leavewar| src/leavewar/ui/movewire.test.ts > a press-and-drag carries it and lands on the lift (D262) > a finger held, then dragged, stages the day it lifts over (a phone confirms it)", "|leavewar| src/leavewar/ui/movewire.test.ts > a press-and-drag carries it and lands on the lift (D262) > a held mouse dragged from one day to another lands on the day it is released over — once"]}
{"break": "W12 D262 an empty tap outside the grid does not cancel", "summary": "Tests  2 failed | 65 passed (67)", "red": ["|leavewar| src/leavewar/ui/moveone.test.tsx > while the chip is picked up (D262) > a click on an empty area OUTSIDE the grid cancels it", "|leavewar| src/leavewar/ui/movewire.test.ts > a click while a chip is picked up (D262) > on a day picks it; on an empty spot outside the grid cancels"]}
{"break": "W13 D262 no double-click guard", "summary": "Tests  2 failed | 65 passed (67)", "red": ["|leavewar| src/leavewar/ui/moveone.test.tsx > the one-day sheet’s Move (D262) > a quick second click on the same spot (a double-click on Move) lands nothing", "|leavewar| src/leavewar/ui/movewire.test.ts > a click while a chip is picked up (D262) > in the first moments of the move (the second click of a double-click) lands nothing"]}
{"break": "W14 D262 a long press cancels", "summary": "Tests  1 failed | 66 passed (67)", "red": ["|leavewar| src/leavewar/ui/movewire.test.ts > a press-and-drag carries it and lands on the lift (D262) > a finger’s LONG PRESS (sent as a right-click on Android) never cancels the move"]}
{"break": "W15 D262 leaving the war keeps the move", "summary": "Tests  1 failed | 66 passed (67)", "red": ["|leavewar| src/leavewar/ui/moveone.test.tsx > while the chip is picked up (D262) > leaving the Leave War ends it — the chip is not carried onto another page"]}
{"break": "W16 D262 its own day reads \"Nothing to move\"", "summary": "Tests  1 failed | 66 passed (67)", "red": ["|leavewar| src/leavewar/ui/moveone.test.tsx > the one-day sheet’s Move (D262) > its own day says so and keeps the move on — never \"Nothing to move\""]}
{"break": "W17 D262 a band the mouse starts in scrolls at once", "summary": "Tests  1 failed | 66 passed (67)", "red": ["|leavewar| src/leavewar/ui/movewire.test.ts > the edge scroll while a chip is picked up (D262) > a pointer ALREADY resting in a band when the move begins does not scroll until it has left it"]}
{"break": "W18 D262 the whole page counts as the grid", "summary": "Tests  1 failed | 66 passed (67)", "red": ["|leavewar| src/leavewar/ui/moveone.test.tsx > while the chip is picked up (D262) > a click on an empty area OUTSIDE the grid cancels it"]}
{"break": "W19 D262 a popup's shade cancels", "summary": "Tests  1 failed | 66 passed (67)", "red": ["|leavewar| src/leavewar/ui/movewire.test.ts > a click while a chip is picked up (D262) > on a control, or a sheet’s shade, outside the grid does not cancel"]}
DONE 19
