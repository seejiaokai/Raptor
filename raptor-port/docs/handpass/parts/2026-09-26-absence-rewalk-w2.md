# [HUMAN-RETEST] the absence record — RE-WALK W2: medical (26 Sep 26)

Re-walker W2 (Opus 5.5), brief `docs/superpowers/briefs/2026-09-26-absence-rewalk-brief.md`. The REBUILT app on
`http://localhost:4175` — every run checked the page loaded `index-KbvILaPW.js` (desktop AB4 run: the `bundle` line). Each
script its own fresh demo world (`openHi`), admin `ad` (Saber) unless noted, member `us` (Ranger). Desktop 1440×900; phone
390×844 at dpr 3, the calendar dragged by FINGER (CDP touch: hold past the pick-up, slide, lift). Expected = register §12.

**Scripts** (`scripts/handpass/ab/`, copies — the first walk's `w2-*.mjs` untouched): `rw-w2-01-ab4.mjs` (AB4, the
premise rewritten: the gesture now puts up the question, so each door is walked Cancel → every answer → one Undo; the
last answer also Redo and a reload), `rw-w2-02-cascade.mjs` (B2–B6 unchanged, plus AB3: B6's cut leave, AB3a / AB3w /
AB3b / AB3c), `rw-w2-03-hours.mjs` (unchanged), `rw-w2-05-published.mjs` (P6's last check with W1-F3's premise: another
man's chip must not lift for a member, watched mid-gesture). Probe: `rw-w2-00-probe-a3d.mjs` (below, "Driver slips").
**Results:** `docs/handpass/parts/2026-09-26-absence-rewalk-w2-0{1,2,3,5}-{desktop,phone}.txt`.
**Pictures:** `docs/img/handpass/2026-09-26-absence/rewalk/w2/` (158, `rw-w2-0N-<width>-…`).

## Tally

| script | ground | desktop | phone |
|---|---|---|---|
| rw-w2-01 | AB4 — four doors, both answers of every question; the control; the same-type refusal | 84 PASS · 0 FAIL | 70 PASS · 0 FAIL (no palette drag on a phone — its door is arm-then-tap) |
| rw-w2-02 | the cascade B2–B6 + AB3 (every piece of a cut leave says its own last day) | 32 PASS · 0 FAIL | 32 PASS · 0 FAIL |
| rw-w2-03 | real hours (§7, six-hour rule, overnight) — unchanged | 14 PASS · 0 FAIL | 14 PASS · 0 FAIL |
| rw-w2-05 | a medical on a PUBLISHED day; a member's own medical — unchanged | 19 PASS · 0 FAIL | 19 PASS · 0 FAIL |

**284 checks pass, 0 fail.** No console errors, page errors or failed requests in any run. Every picture of every AB4 and
AB3 row below was LOOKED at, at both widths.

## The re-walk table

| finding | what must happen now (register §12) | what I SAW | result | pictures (`rw-w2-…`) |
|---|---|---|---|---|
| **AB4 door 1** — calendar drag onto a DIFFERENT-type medical (A1: Reaper HL 20–24, ATT C 18–19 dragged by its 18th onto 24) | "A medical … MOVED asks what filing it asks … who holds the shared days … before anything is written"; no default (owner 27 Aug 26) | The drop puts up the clash sheet "ATT C — Reaper, Jul 24 – Jul 25 · HL Jul 20 – Jul 24 · both cover Jul 24", **"ATT C replaces" / "Keep HL till Jul 24"**, Save OFF, "Choose for each clash above" — word for word the edit window's sheet for the same move. Nothing written, nothing said, no ghost left. **Cancel**: sheet gone, chip back on 18, rows unchanged. **"ATT C replaces"** → ATT C 24–25, HL cut to 20–23 "…hospital till 23 Jul", "Moved to 24 Jul", war MED TOT 7 → 6. **"Keep HL till Jul 24"** → HL 20–24 whole, ATT C filed around it on 25 only, MED TOT 7 → 6 (but see new item 1). ONE Undo restores each; Redo and a reload keep the answer | **PASS** (both widths) | `01-desktop-A1-1a-sheet-up`, `-1b-after-cancel`, `-2a/2b/2c-replaces-*`, `-3a/3b/3c-keep-HL-*`; `01-phone-A1-1a…3c` |
| **AB4 door 2** — the drag INSIDE the other medical (A1f: Hunter HL 20–24, ATT C 18 dragged onto 22) | the forced "replaces" and the leftover shown with its Remove / Keep before it happens (owner 28 Aug 26) | Sheet: "ATT C replaces" pre-lit (no Keep-HL button — forced), "Left over after it: HL Jul 23 – Jul 24 — **will be removed**", Remove those days / Keep them; Save ON (the one default, shown). Nothing written before Save; Cancel writes nothing. **Remove (default)** → ATT C 22, HL 20–21 "till 21 Jul"; MED TOT 6 → 3. **Keep them** (the line reads "— kept on file") → ATT C 22, HL 20–21 "till 21 Jul" and HL 23–24 "till 24 Jul"; MED TOT 6 → 5. One Undo each; Redo, reload | **PASS** (both widths) | `01-desktop-A1f-1a-sheet-up`, `-2b-remove-leftover-saved`, `-3a-keep-leftover-chosen`, `-3b-keep-leftover-saved`, `-2c/3c-…-war`; `01-phone-A1f-…` |
| **AB4 door 3** — an UPCHIT dragged into a medical (A2: Piston OML 20–24, ATT C 28, Upchit 31 dragged onto 22) | the upchit's leftovers asked (Keep / Remove, no default) before anything is written | Summary sheet "Upchit — Piston, Jul 22 · OML Jul 20 – Jul 24 → now ends Jul 21 · Fit for full duty from Jul 22 · Still on file after it: ATT C Jul 28 — Keep / Remove", **Save upchit OFF**; same words as the edit window; nothing written; Cancel writes nothing. **Keep** → OML 20–21, Upchit 22, ATT C 28 stays; MED TOT 6 → 3. **Remove** → OML 20–21, Upchit 22, ATT C 28 gone; MED TOT 6 → 2 (war row: 28 empty). One Undo each; Redo, reload | **PASS** (both widths) | `01-desktop-A2-1a-sheet-up`, `-2b-keep-later-saved`, `-3a-remove-later-chosen`, `-3b-remove-later-saved`, `-3c-remove-later-war`; `01-phone-A2-3a-remove-later-chosen` (fits 390 px) |
| **AB4 door 4a** — Edit Schedule's reassign by arm-then-TAP (A3: Cinch ATT C Thu 16–Fri 17 → Drifter, who holds HL Fri 17–Sat 18) | the same question as the edit window's Person change | Sheet "ATT C — Drifter, Jul 16 – Jul 17 · HL Jul 17 – Jul 18 · both cover Jul 17 · ATT C replaces / Keep HL till Jul 18", Save OFF; identical to the edit window's; nothing written; Cancel writes nothing. **Replaces + leftover HL 18 on its default (Remove)** → Drifter ATT C 16–17, his HL gone, Cinch nothing, "Drifter is now unavailable instead of Cinch", MED TOT 2 → 2. **Replaces + Keep them** → Drifter ATT C 16–17 + HL 18 "till 18 Jul", MED TOT 3. **Keep HL till Jul 18** → Drifter HL 17–18 whole, ATT C 16 only, MED TOT 3. One Undo each. On the PHONE the sheet opens over the AIRCREW drawer and is fully usable | **PASS** (both widths) | `01-desktop-A3-1a-sheet-up`, `-2b…4b-*-saved`; `01-phone-A3-1a-sheet-up` (sheet above the drawer), `01-phone-A3-4b-keep-HL-saved` |
| **AB4 door 4b** — the same reassign by DRAGGING Drifter's palette puck onto the row (desktop) | as 4a | Exactly as 4a: sheet, Cancel, the three answers, one Undo each, Redo, reload | **PASS** (desktop; the phone's door is 4a) | `01-desktop-A3d-1a-sheet-up`, `-3a-replaces-keep-leftover-chosen`, `-3b-replaces-keep-leftover-saved` |
| AB4 control (A0) | "a drag or reassign that raises no question still lands at once" | Wisp's ATT C 27 dragged to 29 (no other medical): no sheet, "Moved to 29 Jul", moved | **PASS** (both) | `01-{desktop,phone}-A0-no-clash-lands-at-once` |
| A3s — the same-type refusal on the reassign | refused before anything moves, naming it (unchanged) | "A ATT C is already filed over these days — edit that entry instead…", no sheet, nothing moved | **PASS** (both) | `01-{desktop,phone}-A3s-same-type-reassign-refused` |
| **AB3** — a cut leave says its OWN last day: B6 (ATT C 22–24 over the end of LL 20–24 "…leave till 24 Jul") | "Every cut of a leave — a medical laid over it … — gives each surviving piece its OWN last day in the remark"; the other words kept | LL 20–21 reads **"W2 B6 leave till 21 Jul"** (the first walk saw "till 24 Jul"); the later upchit leaves it so | **PASS** (both) | `02-{desktop,phone}-B6a-after-upchit`, `-B6b-war-after-upchit` |
| AB3a — a medical in the MIDDLE (LL 20–24 "W2 AB3 Bali till 24 Jul", ATT C 22) | each piece its own "till", "Bali" kept | LL 20–21 "W2 AB3 Bali **till 21 Jul**", LL 23–24 "W2 AB3 Bali **till 24 Jul**"; the Inputs table's Remarks column reads it; one Undo → the whole leave, "till 24 Jul" | **PASS** (both) | `02-{desktop,phone}-AB3a-inputs-table-two-pieces` |
| AB3w — the same in the loaded week (Drifter LL Wed 15 – Fri 17 "…till 17 Jul", ATT C Thu 16) | the week's Unavailable rows say it too | Wednesday's row "LL Drifter LATE W2 AB3w Bali **till 15 Jul**", Friday's "…**till 17 Jul**" | **PASS** (both) | `02-{desktop,phone}-AB3w-week-wednesday-unavailable`, `-friday-unavailable` |
| AB3b — a one-day piece left (LL 20–22 "…till 22 Jul", ATT C 21–22) | the piece says its own day | LL 20 "W2 AB3b trip **till 20 Jul**" | **PASS** (both) | `02-{desktop,phone}-AB3b-inputs-table-one-day-left` |
| AB3c — a TIMED medical 08:00–12:00 on the 21st inside LL 20–22 | every piece its own last day | LL 20 "till 20 Jul", LL 21 12:01–23:59 "till 21 Jul", LL 22 "till 22 Jul" (the afternoon piece is the app's own PM half, `HALF_PM` 12:01–23:59 — not a finding) | **PASS** (both) | `02-{desktop,phone}-AB3c-inputs-table-timed-cut` |
| Published day unchanged — P1 extend, P2 upchit, P3 delete, P4 calendar DRAG onto published Friday | D177–D179, D185: 1 pending on every count, the four fall, the face as issued; one Undo → 0 | All four exactly as the first walk: every count 1, the four sign-offs fall, the face keeps what it went out with (P4: face without Piston, the working copy with him), Undo → 0 and the signatures back. P4's drag met no other medical, so it moved at once — the AB4 fix did not add a question where none is owed | **PASS** (both) | `05-{desktop,phone}-00-fri-face-as-published`, `-P1a…P4b` |
| P5 — the member reads published Friday | D211 | "ATT C … Drifter … W2 P2 back strain — PHA review" | **PASS** (both) | `05-{desktop,phone}-P5a-member-reads-published-friday` |
| P6 — the member's own medical; another man's chip | files his own (certificate asked), drags it 20 → 22 ("Moved to 22 Jul"), the war's Undo puts it back; **another man's chip does not lift** (W1-F3) | as expected; Grit's chip: no ghost and no dragging state mid-gesture, nothing said, still 13–17 | **PASS** (both) | `05-{desktop,phone}-P6a-member-moved-own-medical`, `-P6b-member-cannot-move-another` |
| Unchanged ground — B2–B5 (the cascade), H1–H4 (real hours) | as the first walk | identical results (three OML pieces 12/15/18; split once; same-type refused at add / edit / drag with the leave uncut; every Cancel leaves nothing; leave from 14:00 under a 09:00–14:00 medical; 10:30–11:30 beside 08:00–10:00 allowed both orders; six hours = half; the overnight tail refused by name) | **PASS** (both) | `02-…-B2a…B5a`, `03-…-H1a…H4b` |

## New things seen on those screens (none blocks AB4 or AB3)

1. **After "Keep HL till Jul 24" the calendar says "Moved to 24 Jul" — the ATT C now sits on 25 Jul only** (low; words).
   Steps (fresh world, admin): Inputs → file HL 20–24 Jul and ATT C 18–19 Jul for Reaper ("No document" both) → Calendar
   view → July → drag the Reaper ATT C chip from 18 onto 24 → the sheet → "Keep HL till Jul 24" → Save. The toast reads
   "Moved to 24 Jul"; the calendar and the war show the ATT C on Sat 25 only. The words come from the drop day, set before
   the question was answered (`caldrag.ts`: `said: 'Moved to ' + fmtDay(d.start)`). Who it hurts: the admin reads a date
   the entry is not on. Pictures: `rw-w2-01-desktop-A1-3b-keep-HL-saved` (toast + calendar), `-A1-3c-keep-HL-war` (HL 20–24,
   C 25); phone the same (`rw-w2-01-phone-A1-3b-keep-HL-saved`). The reassign's words ("Drifter is now unavailable instead
   of Cinch") stay true under every answer.
2. **One save, two remark shapes by door (low; a question more than a defect).** A medical moved WITHOUT a question keeps
   its remark as typed (A0: "W2 A0 sick" → still "W2 A0 sick" on 29); the same kind of move answered through the sheet
   gains a token (A1: "W2 A1 sick" → "W2 A1 sick till 25 Jul"; A3: "…Cinch sick till 17 Jul"). The sheet's save writes the
   first piece's remark with `withRemarksTail(…, 'till')` whatever was typed; register §12 says a re-date "rewrites the token
   and keeps the other words" and the first walk recorded the adding as O3 (design). Worth one look: the edit window adds it
   too, so the doors that ASK agree with each other — they differ only from the doors that don't. Pictures:
   `rw-w2-01-desktop-A3d-3b-replaces-keep-leftover-saved` (Friday's row "W2 A3 Cinch sick till 17 Jul").
3. **"A ATT C is already filed over these days"** — should read "An ATT C" (low; wording; the same line built for every
   type in `inputedit.tsx`, so "A OML" / "A HL" too). Pictures: `rw-w2-02-desktop-B4c-drag-refused`,
   `rw-w2-01-phone-A3-1a-sheet-up` (the fading toast from A3s). Not filed anywhere I could find.
4. **The desktop week's floating "‹" arrow sits over the start of the Unavailable row's type** on the left-hand day ("ATT C"
   reads "ATT| C") — the same arrow as the filed `[VIEW-ARROW-OVER-LIST]` and `[AMEND-SMALL-SEEN]` item 4, one more place
   it covers. Pictures: `rw-w2-05-desktop-P4a-face`, `-P4b-working-copy`.

**Not findings, recorded:** the reassign carries the remark as typed to the new man (Drifter's ATT C reads "…Cinch sick…")
— the remark belongs to the input; "Sync · slow" in the top bar is the sync toggle's resting label (`Shell.tsx`), not a
fault; every medical change's Undo is still titled "Undo — a batch of inputs" (first walk O7 — D148 / AB8's re-test).

## Driver slips (mine, fixed in the copies, not the app's)

- The first desktop run of `rw-w2-01` failed A3d rounds 2+: the probe `rw-w2-00-probe-a3d.mjs` showed my driver scrolled
  Drifter's palette puck under the sticky top bar (`nearest`), so the press landed on the bar's buttons and no drag began;
  the answer steps then pressed Undo on steps the gesture never made. The copy now centres the puck, checks what is under
  the finger, and never presses Undo when no sheet came up. The rerun (the results above) is clean; the probe's pictures are
  `rw-w2-00-probe-a3d-round{1,2,3}`.
- The first run of `rw-w2-02` picked AB3's cast from men "free Mon–Fri" — there are none in the demo week — and its AB3a
  check counted the medical's save as adding one row where it adds two (the ATT C and the leave's second piece). Both
  corrected; the rerun above is clean at both widths.

## What I did NOT re-walk, and why

- `w2-04-docs.mjs` (documents, the viewer, the Medical view) — not on the re-walk list (no fix touched it).
- The scheduler BOARD's Unavailable panel reassign — the same `reassignInput` as the week's row (walked by tap and by drag);
  W6's R17 covers the panel.
- A finger DRAG of a palette puck on the phone — the phone's reassign door is arm-then-tap (walked).

## Console errors

None — every run's `errors` array was empty (console errors, page errors, 4xx/5xx), both widths, all four scripts.
