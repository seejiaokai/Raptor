<!-- Fable 5.1, blind to Astra: scenario design and plan attack for the amendment batch (25 Sep 26).
     Brief: docs/superpowers/briefs/2026-09-25-amendment-batch-scenario-brief.md. The report as returned; the builder's
     disposition of each finding is in the evidence sheet (docs/handpass/2026-09-25-amendment-batch.md §3). -->

# Fable's report — the amendment batch: scenarios, plan findings, negatives

Read in full: the batch spec, the plan, the register, the rulings (scheduler, OIL, how-we-work), the standing order §4/§6/§7, and the code the plan names (`publish.ts`, `canonical.ts`, `restore.ts`, `drafts.ts`, `slots.ts`, `daytpl.ts`, `editlog.ts`, `html.ts`, `board.ts`, `board-html.ts`, `interactions.ts`, `histbubble.ts`, `HistoryModal.tsx`, `ALPanel.tsx`, `SchedBoard.tsx`, the CSS rules, the three mock-ups and the approved `C_CSS` / variant `s`). No file touched.

## 1. Scenarios — ranked, most specialised surface first

Each: surface + width · setup · action · expected · what would disprove it. "All counts" = day head, ⓘ panel, Amendments panel line, plan-switch toast, "Discard N edits", the pending list head.

**S1 · The one count (item 14) — board + edit week, desktop.** Published Mon (ORIG): Common Programme row MET + NOTAM BRIEF holds Warden and Reaper; duty desk SODB holds Tally, no extras. (a) Drag Warden to SODB's extras line → all counts **1**, list line "Warden: MET + NOTAM BRIEF → SODB". (b) Drag Tally from the SODB holder box to SODB's extras → count rises by **1** (the printed row changed). (c) Reorder Warden/Reaper inside MET's list → **+1**. (d) Take Reaper AND Warden off MET, to nowhere → **+2**. (e) Swap two pilots' seats → **+2**. (f) Replace Tally with Piston in one seat → **+1**. (g) Put everything back → chip gone, four sign-offs back (AM11). *Disproof:* any two of the counts differ; or the day head shows no chip / no Publish AL while the Amendments panel still lists the day with a Publish button (a "0 changes" AL). (b) and (c) are where the plan's rules collapse a real delta to zero units — see F1.

**S2 · A move into a brand-new row — edit week.** Published Mon; add a new programme row SODB, drag Warden from MET into it. *Expected:* 2 lines: "SODB added" and "Warden: MET + NOTAM BRIEF → SODB". *Disproof:* 3 lines, or a line reading "Warden taken off MET + NOTAM BRIEF" with nothing saying where he went.

**S3 · The sign axis (item 7) — edit week, desktop; repeat the wipe on the board.** Published Mon at 2 pending, four signed. (a) As a member (Inputs page), file leave on Mon for a man behind an ALL AVAIL on Mon → the four blank, "Not yet signed", count 3. (b) Delete that leave → four return, count 2. (c) Change a Quals column so a man behind ALL AVAIL stops counting → same wipe/restore. (d) Sign at 3 pending, then Undo the leave → four blank (delta no longer what was signed). (e) Sign Plan B at 2 pending; switch to Plan A → empty; back to B → green. (f) Unpublish AL1 → cleared; sign; Publish AL1 again (D101's way back). *Disproof:* a name surviving a new pending change; a name lost on an exact put-back; Plan A wearing Plan B's names.

**S4 · Two-state marker (item 5) — week head, board strip, view page's working-draft peek; both widths.** Pending + unsigned → "Not yet signed"; all four sign → "Not yet published"; withdraw one signer's Scheduler qual (AM16) → "Not yet signed"; publish → gone; issued face → never. *Disproof:* "Not yet signed" beside four green; anything on the issued face.

**S5 · The pending list's content (item 8) — week desktop, week phone, board phone.** Published Mon with: a moved man, a replaced seat, 07:00→07:30, a programme row cancelled (CX), a ground row from an input removed (filing g→r), a wave reorder, a man taken off a duty desk. *Expected:* one line per unit; callsigns, never ids; "cancelled", never a raw composite; the taken-off line names the ROW he was taken off; keyed items show "Admin · 25/9 02:41", "earlier" after a reload; removal/reorder/filing lines show no who/when and are not buttons; long list scrolls inside; closes on outside tap and Escape; head "Waiting to go out as AL1 · N changes". *Disproof:* `␟` on screen; `nact` instead of Nact; the wrong row named for the taken-off man; "earlier" on a change made seconds ago.

**S6 · The one jump (items 8 + 12) — Edit Schedule desktop AND phone, then the board.** From the pending list and from Edit history, tap the time change: list closes, the week pans to Mon if off-screen (phone: steps to Mon), cell scrolls into view, a 1.4 s ring; **the board never opens**. Tap the taken-off line → lands on the row. In Edit history on the week: an `ar:`/`at:`/`it:`/`wl:` row IS a button (the week draws them); a traffic (`tr:`) row is not. Board: opened from the board's list → today's jump + pinned bubble. *Disproof:* the board opening; a wave-label row dead on the week; "That detail is shown on the scheduler board" for an area change.

**S7 · Jump while the day is under a version preview.** Edit week, Mon previewing AL1 (its cells are not editable, so they carry no `data-txt`). Tap a Mon change in Edit history. *Expected:* the preview clears and the cell is found (or the toast says the day is previewing). *Disproof:* "That detail is no longer on this day" for a live detail.

**S8 · The Signed line (item 3) — the board's preview strip first, then everywhere.** Published Mon at AL2. (a) Board live: "Signed AL2 · CUR CK … APPROVED BY …". (b) Plans selector → Original: the strip names ORIG's signers. (c) → a parked plan: no line (AM30). (d) After Unpublish of AL2: AL1's signers. (e) Type a name into a live sign-off box, then View-only Sched issued face and its working-draft peek: AL2's signers, never that live name. (f) Member login: sees it. (g) Phone: names only. *Disproof:* live box names on the view page or under an older-version preview.

**S9 · The Original's signers are kept.** Sign four, Publish day → View-only Sched "Signed ORIG · …"; reload → still there; Unpublish the Original, re-sign with different names, republish → the line changes. *Disproof:* nothing under ORIG.

**S10 · Template door (item 4) — week "Templates" button and the board's "Templates" control, phone too.** Published Mon, with and without pending edits; also with Plan B live. *Expected:* apply rows disabled with the sentence; "Save this day" still works; nothing arms; a draft day applies as today. *Disproof:* "pick it again to confirm"; a silent tap; the day rebuilt.

**S11 · Load puts filings back (item 6) — the mock's flow, then its edges.** (a) AL1 has Wisp's request on Tuesday; remove the row (request "removed"); Load AL1 → row back AND the request on the programme, "0 pending"; the confirm read "Discard 2 edits". (b) A request filed under Unavailable at AL1 and later un-filed → load → under Unavailable again. (c) A request filed AFTER AL1 (live on the working copy) → load → it comes off and reads fresh. (d) A Mon–Wed leave 'u' in Mon's AL1 but 'r' in Tue's AL1 → Load Mon's AL1 → **Tuesday must not change** (AM1) — disproof: Tue reads "1 pending · 1 input filing". (e) Undo after the load → the row is gone again and the request reads "removed". (f) Load a version after availability behind an ALL AVAIL moved: the day honestly reads "1 pending" and the toast does not say "nothing pending".

**S12 · Tags not rings (item 1) — real size (DPR 1) and phone DPR 3; edit week, board, View-only Sched.** Published Mon at AL3: Tally (amber advisory) changed at AL3; Saber (red clash) waiting AL4; Warden (dotted trace) waiting; a dashed sanctioned man published. *Expected:* no AL-coloured ring anywhere; solid tag / hollow dotted tag; each ring's computed stroke is the warning's; times, areas, remarks keep their marks; the issued face shows solid tags + rings. Then the view page's **working-draft peek**: a pending seat on a man wearing a dotted ring — take the DPR-1 picture (this is design A's geometry, the one the mock measured as blurring) and put it on his card. *Disproof:* an AL-coloured ring; a tag missing on the board's sim seat or Common Programme.

**S13 · Board rings (item 2) — board desktop + phone.** Sanctioned late show → dashed ring with CR chip; the day causing tomorrow's breach → dotted ring **with the CR caption** and the leave-by title; a RUN trace → dotted ring with '7'; under a version preview → none. *Disproof:* a dotted ring with no caption (an unexplained ring, exactly D92's complaint).

**S14 · ORIG seal (item 13) — week head, board strip, View-only Sched; both widths; beside an AL4 (white) day and a DRAFT day.** *Disproof:* reads as AL4; grey survives anywhere; the ⓘ panel changed.

**S15 · Keyboard gap (item 11) — phone board, History on AND off.** Focus a Common Programme name; shrink the visual viewport. *Expected:* nothing of the week behind shows; on blur the board is full height again; the history bubble still places inside the visible area. Real proof only on his phone — look card.

**S16 · The chip on a draft day, and after Unpublish of the Original.** *Expected:* a plain chip, count = draft marks; no "▾", no list ("goes out as AL1" would be false — the day goes out whole). *Disproof:* a button opening an empty or mis-headed list.

**S17 · Counts under a version preview.** Live Mon has 3 units; preview AL1: the chip reads "3 pending" and "Discard 3 edits & load" agree (both now include filing). *Disproof:* the two differ.

**S18 · Item counts after issue.** Publish AL1 carrying a move + a time change: toast "Published AL1 · 2 items"; Amendments history "AL1 Mon · 2 items"; ⓘ "AL1 · 2 items". *Disproof:* "3 items" (the raw diff length leaking).

**S19 · Roles — member on View-only Sched.** No pending button on any face; the Signed line shows; the issued-face ⓘ shows no "unpublished edits"; the working-draft peek shows the marker but no button.

**S20 · Publish + Undo/Redo with the new axis.** Publish AL1; Undo → unpublished, four cleared (AM39c); Redo → published, cleared; sign → valid. *Disproof:* names handed back.

## 2. Plan findings — ranked by consequence, each with the fix

**F1 · HIGH — item 14's unit rules can turn a non-empty delta into zero units; two surfaces then disagree.** Three cases in `dayPendingItems` as designed: (i) "a duty row's desk holder + extras" as ONE place → Tally moved from the holder box to the extras line of the same desk is 0 units (S1b); (ii) "a man shifting inside one row's list is no change" → reordering Warden/Reaper inside MET is 0 units (S1c); (iii) "one item per place" for unpaired events → two men taken off one row count 1, against his "a man only taken off … is one" (S1d). In (i)–(ii) `dayDelta` is non-empty, so `dayHasChanges` is true: the Amendments panel lists the day with a Publish button and "0 changes", while the day head shows no chip and no button. The plan's own assertion ("zero units ⇔ empty delta") would fire — fix the rules, not the assertion.
*Fix — `raptor-port/src/engine/pending.ts`, `dayPendingItems`:* (1) Places: each flying seat, each sim FCP/RCP seat, a duty row's HOLDER (`d:di.wi.ri`) and a ground row's `who` (`g:di.ri`) are single-seat places; each `who[]`, `more[]`, `pax[]` list is its own list place — never fold a holder into its extras. (2) A list place whose off/on multisets are empty but whose order differs from the issued order emits ONE item `{kind:'reorder', addr:<row key>}` ("MET + NOTAM BRIEF: order changed"). (3) Unpaired events: on a LIST place one item per token-event (each man off = 1, each man on = 1); on a SINGLE-SEAT place a replacement (off A, on B) = 1 — that is the GAP as filed, kept. (4) Keep the roll-call test the plan names and add a fixture with S1(b)(c)(d); add the invariant test `dayPendingItems(di).length===0 ⇔ dayDelta(di).length===0` over that fixture.

**F2 · HIGH — the list will name the wrong row and print raw composites.** A hole entry (a man taken off a list) carries the ISSUED day's positional address (`canonical.ts` line 200); `keyLabel` (`editlog.ts`) resolves by position in the LIVE day, so "Reaper taken off SODB" prints for a man taken off MET, and the tap lands on the wrong row. And `DeltaEntry.from/to` are canonical strings: person keys hold ids (`nact`), composites hold `prog␟cx␟cxr␟flag␟info` (also `dr:…role`, `sr:…label`, `ff:…cs`, `fr:`, `gr:…prog`, `wl:`, `dl:`).
*Fix:* (1) `editlog.ts`: refactor `keyLabel(key)` into `keyLabelIn(days, key)` with `keyLabel = k => keyLabelIn(DAYS, k)`. (2) `pending.ts`: for every entry whose row is absent from the live day at that address, translate `ridKey(addr, snapArr)` → `posKey(…, DAYS)`; if the row survives, label and jump through the live address; if it is gone, label with `keyLabelIn(snapArr, addr)` and make the line not tappable. (3) Add `sayDelta(entry)` in `pending.ts`: split on `␟` by prefix, name only the parts that differ ("cancelled", "reason: sick", "flag on", "info on", "shift", "spare"); person tokens through `PEOPLE[id]?.cs`; times through `fmtHM`; an empty side prints "—". The list draws `sayDelta`, never `from`/`to` raw. Unit test on each composite prefix.

**F3 · HIGH — item 6 reaches into other days, and its "nothing pending" over-promises.** `acc` is per INPUT, not per day (`inputs.ts`); restoring Monday's frozen `'u'`/`'r'` onto a Mon–Wed leave changes Tuesday's filing fingerprint too — Tuesday then reads "1 pending · 1 input filing" with nobody having touched Tuesday (D45's test, AM1). Separately, the OIL derived half (who is behind ALL AVAIL) is re-derived, not restored, so the day can honestly still read "1 pending" after the load; and if `dayDiscardCount` becomes the whole unit count, "Discard 1 edit & load" will promise to discard that derived difference and then leave it.
*Fix — `drafts.ts`, `loadVersionToWorkingCopy`, after `reconcileDayFiling(di)` and before the rebase:* for each input covering `DAYS[di].dt` and not `inputProtected`: `want = snap.fil[inpId] ?? ''`; apply it only if the input covers no OTHER loaded day whose current issued fingerprint (`daySnapOf(dj, dayCurVer(dj)).fil[inpId]`) disagrees with `want`; otherwise leave it and return its label so the handler's toast says "· 1 request also covers Tue — left as filed". A `'g'` restore only where its row is on a loaded day (as planned). Toast (`interactions.ts` restore handler): replace the "nothing pending" wording with the real `dayShownPendCount(di)` after the load. `dayDiscardCount`: keep today's exclusion — count the OIL item only when `oilDecisionsKey(live.oild)!==oilDecisionsKey(snap.d.oild)` (tag the OIL item `derivedOnly` in `dayPendingItems`; discard count = items minus derived-only OIL). Test: S11(d)(f).

**F4 · HIGH — item 4's door order arms before it refuses, then goes silent.** `board.ts` `pickDayTpl` returns `'armed'` on a published day with changes BEFORE `applyDayTpl` is asked; with the refusal in the engine the first pick toasts "pick it again to confirm", the second returns `'noop'`, and `dayTplMenu` does nothing for `'noop'`. `window.applyDayTpl` (probe bridge) also refuses silently.
*Fix:* (1) `daytpl.ts`: `export const DAYTPL_PUBLISHED_MSG = (dow) => \`${dow} is published — a template can't be applied to a published day. Edit the working copy, or Unpublish first.\``; in `applyDayTpl` add `if (dayApproved(di)) return false` right after the quarantine check; delete the now-unreachable `wasApproved` branch and its comment (and the stale "Reopen the day first" header comment). (2) `board.ts` `pickDayTpl`: first line `if (dayApproved(di)) return 'refused'`; widen the return type. (3) `dayTplMenu`: when `dayApproved(di)`, render each `data-daytplpick` row with `disabled` and `title=DAYTPL_PUBLISHED_MSG(d.dow)`, plus one `.wm-note` line with the sentence; on `r==='refused'` toast the same constant. (4) `daytpl.test.ts`: the published-day test currently pins the rebase behaviour — replace it (D96 is the ruling), never weaken it; add the `'refused'` and the disabled-row tests.

**F5 · MEDIUM-HIGH — item 2 is half a mark on the board.** The week decides the printed chip through `traceLeads` (`html.ts` `chip`), so the dotted ring comes with its CR / '7' caption; the board keeps `chipOf`, so passing `trace` alone draws a dotted ring with no caption. The week also gates dash/trace on preview (`PV&&!OFW`); the board's gate is its `pv` parameter.
*Fix:* `html.ts`: export one helper `puckMarks(di,id,pv)` returning `{sev, flag, dash, trace}` from the existing `sev/chip/dsh/traceHit` bodies (`pv` ORed with the module's PV rule); use it in `slotCell`'s caller and `lSeat`. `board-html.ts`: use it in `sbSlot` (~line 804), `sbSeat` (~328, incl. `sbMore`) and the Common Programme who-list (~233), passing the board's `pv`. Leave the Personal Inputs / Unavailable panel pucks as they are (the week passes no dash/trace there either). Roll-call test: every board seat builder emits `boxdash`/`boxdot` + the caption on the fixture.

**F6 · MEDIUM — item 12's jump is built for the board in four places.** (i) `histJumpable` uses `NO_BOARD_CELL = ar/at/it/tr/wl`; the week draws `ar:/at:/it:` (`data-area/atime/intimes`) and `wl:` (a `data-txt` cell via `ted`), so on Edit Schedule those rows would be dead and `tr:` is the only undrawable one. (ii) Row title "Go to this detail on the board" (`HistoryModal.tsx` `rowHTML`). (iii) A day under a DPREV preview has no editable cells → `findHistCell` fails with an untrue toast. (iv) Landing the desktop week horizontally through `scrollIntoView` fights the glide/proxy machinery (`pan.ts` `onTrackScroll`/`panHold`).
*Fix:* `histbubble.ts`: `histJumpable(key, surface:'board'|'week')` with `NO_WEEK_CELL=['tr']`; `HistoryModal.tsx` passes `SBDAY!=null?'board':'week'` and titles the row per surface. `interactions.ts` `goToChange(key, di)`: if the board is open, today's path; else `if (view.DPREV.has(di)) view.setDayPreview(di,null)`; `view.setWeekJump(di)`; `notify()`; `setTimeout`: `findHistCell(document.querySelector(\`#eWeek .day[data-day="${di}"]\`), key)`, then scroll VERTICALLY only (`window.scrollBy(0, rect.top - innerHeight/2)`), then the 1.4 s ring; a missing cell: `tr:` → "That detail has no cell on the week"; else "That detail is no longer on this day". `setWeekJump` already lands the phone week on the day (EditWeek's WEEKJUMP consumer).

**F7 · MEDIUM — item 10's desktop hover promise cannot hold with `pointer-events:none`.** Moving the pointer from the cell onto the bubble fires the wrap's `mouseout` with `relatedTarget` = whatever sits UNDER the bubble (the bubble takes no events), so `out` hides it; and the bubble is body-level, outside the wrap, so leaving it never hides. Note `.hb-all{max-height:40vh;overflow-y:auto}` already exists — but on a phone the list takes no touch (pointer-events:none), so the board scrolls instead of the list.
*Fix — `histbubble.ts` + `scheduler.css`:* CSS `@media (hover:hover) and (pointer:fine){.histbub{pointer-events:auto}}` and `.histbub .hb-all{pointer-events:auto}` for every width (the raising tap still lands on the cell — the contract holds). `out`: return early when `e.relatedTarget?.closest('.histbub')`. In `box()`: `bub.addEventListener('mouseleave', e => { if (pinned||HOOKS.isPhone()) return; if (cellOf(e.relatedTarget)===anchor) return; hideHistBub() })`. In `place()`: `b.style.maxHeight = Math.round(vh*0.55)+'px'` and `.histbub{display:flex;flex-direction:column}` so `.hb-all` gets the remaining height. Check `e2e/geometry.spec.ts` for a pointer-events pin and scope it to the phone width.

**F8 · MEDIUM — item 3's "version on screen" differs per surface.** Inside `dayHTMLBody` the version is `PV ? PVV : dayCurVer(di)` (the view page's issued face and the week's preview both run inside `withDaySnap`); the board's preview strip (`boardSignHTML(di, pv=true)`) is built OUTSIDE the swap, so `PV` is false there and the line would name the live version under an ORIG preview (S8b). A `d:` draft version has no signers → nothing (AM30).
*Fix:* `signedLineHTML(di, ver)` takes the version explicitly; `dayHTMLBody` passes `PV?PVV:dayCurVer(di)`; `boardSignHTML` passes `pv ? DPREV.get(di) : dayCurVer(di)`; signers = `verSeq(ver)===0 ? SCHED.orig[di]?.sign?.[di] : als.find(id).sign[di]`; never `SCHED.sign`. Test both surfaces under an ORIG preview and the view page after a live box is filled (S8e).

**F9 · MEDIUM — item 7's cost is ~6 canonical diffs per published day per paint, not one.** `signoffHTML` calls `signShown` and `signMissing` (each `signCur`→`currentBind`), then `dayDelta(di).length`; `dayStatHTML` calls `dayShownPendCount` and `daySigned`; `nysMarkHTML` calls `notYetSigned`. Adding `dayDelta` inside `currentBind` doubles today's ~3 → ~6, × 7 days on the edit week, each a two-walk diff.
*Fix:* `publish.ts`: give `signMissing(di, cur?)`, `signShown(di, cur?)`, `daySigned(di, cur?)` an optional pre-computed bind; `signoffHTML` computes `const cur=signCur(di)` once and `const delta=dayDelta(di)` once and passes/reuses them; `dayStatHTML` and `nysMarkHTML` take an optional `delta`. Then measure before/after per `docs/performance.md` as the plan already promises — with numbers in the evidence sheet, not "fine".

**F10 · MEDIUM — item 8's popover must stack over the board and obey the popup rule.** `.schedboard` is `z-index:400`; a new popover under that is invisible on the phone board. *Fix:* draw the list with `board.ts` `popMenu` (`.wavemenu`, z 480, outside-click close already built) — add Escape there if missing; the chip button only when `ed && !DPREV.has(di) && dayApproved(di)` (S16: a draft day keeps the plain chip).

**F11 · LOW-MEDIUM — item 1 knowingly leaves design A's geometry on the view page's working-draft peek.** `#vWeek .seat[data-alp]` is a dashed outline on the seat, offset 1px; the mock measured exactly that placement as blurring into a dotted ring at real size, and D93 says "View-only Sched included" for the waiting mark. The plan defers it to the walk. *Fix:* walk it at DPR 1 (S12) and put the picture on his card; if it blurs, file `[VIEW-PEEK-WAITING-MARK]` with the recommendation "a hollow neutral tag on `#vWeek .seat[data-alp]::after` (no AL number — the view page keeps no AL colour, AM19)" — his look, not the builder's call.

**F12 · LOW — who/when lookup by exact address will read "earlier" for a change just made.** Log keys are rid-anchored with the who-list index (`a:<rid>.k`); after a removal the list compacts, so the newest row for the move's off-address sits under another `k`. *Fix — `pending.ts`:* match log rows by prefix family + row rid (the key up to the last `.` for `a:/d:/s:/g:` list keys), newest wins.

**F13 · LOW — wording roll-call.** "Go to this detail on the board" (HistoryModal); the week's Templates button title "Save this day, or apply a saved template" on a published day; register §Q3 and §Q4 are now settled by D103/D97 and should say so; `HOOKS.whoami` returns "Squadron member", not "Member" — write what the app records.

## 3. What I checked and found fine

- **D103's binding shape.** `pd` = a sorted stable string of `dayDelta` gives exactly "valid while the pending set is what was signed": a put-back restores (AM11, content-based not mark-based); per-plan sign/signBind stow and reload with the plan (AM12; `restampRev` still correct); Unpublish clears via `signClear` + `signClearPlans`; Undo/Redo of a publish untouched (`timeline.ts` GU5-005). The membership tail excluded by `oilSignKey` (D45) is now caught through the OIL delta entry — that IS D103. An absent `pd` on an old binding forces a re-sign only when something is pending: demo data, not a finding.
- **Item 8's "NET difference" and D109's "the stored diff unchanged"** are consistent: units are a view over `dayDelta`; `alIssue` keeps `diff`.
- **`dayCurVer`/`daySnapOf` under `withDaySnap`** still resolve (SCHED.cur untouched), so the view-page Signed line can be read inside the swap.
- **The Original's signers**: storing `sign:{[di]:signNames(di)}` on `SCHED.orig[di]` before `signClear` rides the stash (`schedFields`), `retireIssued` already copies `rec.sign`, and Unpublish/republish of the Original produces a fresh record. No second writer found.
- **Item 5**: `daySigned` is the right predicate for the two states; the marker is already gated `!PV` on every surface (week head, board strip, working-draft peek).
- **Item 13**: `verTagHTML` is the one drawer on all three surfaces (the "edit-surface only" comment is stale, not the code); `.verchip` is excluded from the generic `[data-alc]` underline rule, and variant `s` sets no `--alc`, so it cannot read as AL4.
- **Item 6's placement** in `loadVersionToWorkingCopy` only, after `reconcileDayFiling`, is the right seam (the general reconcile stays as it is; `draftSelect` and `applyDayTpl` untouched). Undo carries INPUTS (cutover modules) — walk it (S11e), no defect read.
- **Item 4's other doors**: `DayTplModal.tsx` has no apply door; the week's button and the board's control both go through `dayTplMenu` → `pickDayTpl` (one picker).
- **Item 1's CSS**: `alAttr` emits `data-alc` OR `data-alp+aln`, never both; `.seat[data-alp]{position:relative}` positions the new hollow `::after`; removing `.seat[data-alc] .puck{box-shadow}` reaches the view page too (unscoped), as D92 requires; times/areas/remarks rules untouched.
- **Item 11**: `histbubble.ts` `place()` already reads `visualViewport` — the board can follow the same signals; the only real proof is his phone (the plan says so).
- **Roles**: the Edit history button is `page==='editsched'` only, so the jump needs `#eWeek` and the board, never `#vWeek`; the pending button is gated on `ed`.
- **The plan's tier (FULL) and order** match the spec; the stop rule is sound.
