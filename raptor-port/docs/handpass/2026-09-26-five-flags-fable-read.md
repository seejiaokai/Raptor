# Fable 5.1 — the final code read of the five-flags batch (26 Sep 26), saved verbatim by the builder

*Blind to Astra's (`2026-09-26-five-flags-astra-read.md`); brief `docs/superpowers/briefs/2026-09-26-five-flags-read-brief.md`.
Read at commit `9b7c5b94` — before Astra's fixes landed, so its findings 2 and 3 are the same as Astra's 3 and 1. The
builder's dispositions: the evidence sheet §8.*

## Findings, ranked

### 1. MEDIUM — the signed-in man's own puck still glows when it wears the DOTTED flag
- **Where:** `raptor-port/src/ui/scheduler.css:1283` (`.puck.me.boxdot`) declares no `box-shadow`, so the puck keeps `.puck.me`'s blurred purple layer from line 1381 (`0 0 12px 1px rgba(155,47,174,.7)!important`). The solid and dashed rules were fixed (1240, 1262); the dotted one was not.
- **Setup → action → screen:** Edit Schedule, signed in as Saber. Give Saber a late Monday leg and an early Tuesday report so Monday's puck wears the dotted "causes tomorrow's breach" ring; put Reaper in the same state beside him. Saber's puck: dotted red ring over a purple haze. Reaper's: dotted ring, no haze.
- **Should show:** the same as Reaper's — D164, "a flagged puck does not glow"; the batch's own promise says "a flagged puck never glows".
- **New or on main:** same on `main` (`git show main:raptor-port/src/ui/scheduler.css` has the identical `.puck.me.boxdot`). The sheet lists it as a W1 observation and folds it into look-card Q2, but Q2 asks a different question (whether the purple ring should give way to the amber/grey rings). This one is the promise's own words, and it is exactly the "some glow, some don't" he complained of.
- **The test passes vacuously:** `raptor-port/src/ui/flagglow-css.test.ts:37-43` checks only rules that DECLARE a box-shadow (`if (!sh || sh === 'none') continue`). A ring rule that inherits `.puck.me`'s glow is never examined, so the roll-call at line 34 lists `.puck.me.boxdot` and then says nothing about it.
- **Fix:** (1) `scheduler.css:1283` → `.puck.me.boxdot{outline:1.5px dotted var(--hard);outline-offset:2px;box-shadow:0 0 0 2px #F0B6FF!important}` — the purple ring kept, its blur dropped (or `box-shadow:none!important` if he answers Q2 "show the warning ring"). (2) In `flagglow-css.test.ts` add: `it('every "this is you" ring rule declares its own shadow, so none inherits the purple glow', () => { for (const r of ringRules.filter(r => /^\.puck\.me\./.test(r.sel))) expect(shadowOf(r.body), `${r.sel} declares box-shadow`).not.toBeNull() })`. Red first on today's stylesheet.

### 2. LOW — the guard refuses a chat started INSIDE a raptor-port sub-folder, then names a folder that does not exist
- **Where:** `.claude/hooks/bg-cwd-guard.mjs:44-46` — `basename(start).toLowerCase()==='raptor-port'`, and `full = join(start,'raptor-port')`.
- **Setup → action → screen:** a chat whose starting folder is `…/raptor-port/scripts` (or `…/raptor-port/src`) backgrounds a bare `npm run docsize`. Refused — although npm walks up to `package.json` and it would run (the hook's own comment at line 25 says so, and `cd raptor-port/scripts && npm …` is accepted). The refusal then advises `cd "…/raptor-port/scripts/raptor-port"`, a folder that does not exist, so following the advice fails too.
- **Should:** let it through, as a chat started in `raptor-port` itself is.
- **New with this branch** (main had no start-folder logic at all).
- **Fix:** replace the basename test with a segment test: `const segs = start.split(/[\\/]+/); if (segs.some(s => s.toLowerCase() === 'raptor-port')) return null;`. Add to `bg-cwd-guard.test.mjs`: a `SUB` start folder `C:\Users\User\projects\Raptor\raptor-port\scripts` and assert `refusal(bg('npm run docsize'), SUB)` is `null`.

### 3. LOW — a man standing twice on one row (the F6-a/b state the branch still plants) hides a real clash while one copy is dragged away
- **Where:** `raptor-port/src/engine/avail.ts:378` (`selfKeys` takes the whole from-ROW), `:419`, `:562`; `raptor-port/src/engine/validate.ts:1745` (`sole` by row).
- **Setup → action → screen:** Monday, Ranger on FLIGHT SAFETY twice via its "+ add" (the app warns and plants). Add SAFETY WALK 08:45–09:15. Drag one Ranger copy onto SAFETY WALK: caption silent, drop silent — then the warning list raises the clash (the other copy still sits on FLIGHT SAFETY). Picker and list drift, the 11 Aug 26 rule. The cross-day "his only event" test reads the same state as him leaving the day.
- **Should:** caption "already on FLIGHT SAFETY STAND-DOWN 08:30–09:00".
- **New with this branch** in that the widened exclusion now covers every row kind (on main the programme trim was silent for other reasons). It disappears entirely if he answers Q1 "refuse".
- **Fix (if Q1 stays "warn"):** at `avail.ts:378`: exclude the from-row only when he has at most one place on it; at `validate.ts:1745` add the same condition to `sole`. Companion tighten at `validate.ts:1694`: the strip now removes EVERY man's events on the from-row (the day lists are the whole day's); add `e.id!==id||` before each `seatRow(...)` compare. No visible effect today (crew rest reads only his legs) but it is wider than intended.

### 4. LOW — Reset order can read "Arranged by hand" while the grid shows the default
- **Where:** `raptor-port/src/leavewar/state/store.ts:1905-1908` compares the WHOLE roster; `raptor-port/src/leavewar/ui/Matrix.tsx:3251` draws only rows in the visible month window (`rowInWindow` drops posted-out / not-yet-arrived men).
- **Setup → action → screen:** hand-drag a man, then post him out before the visible months (or drag him while his months were visible). ⚙: "Arranged by hand", Reset order lit; the grid looks default; a press changes nothing on screen and leaves an Undo step. One-directional and harmless (the stale order is cleared). New with the branch (the line is new). W2 lists posted-out men as not walked.
- **Fix, if wanted:** give `rosterFollowsDefault` an optional `keep: (p: Person) => boolean` and compare `now.filter(keep)` with `dflt.filter(keep)`; SettingsSheet passes Matrix's window predicate. Leaving it is defensible.

## Explicit negatives

**0. The walk's fixes.** Every ask AFTER a write names the landed place: `drag.ts:360-361` (both cell drops, `lastFilled()`), `view.ts:1105/1102` (`placeArmed`), and the seat/swap branch `drag.ts:319-325` asks each man of his own new seat key. Every ask BEFORE a write keeps `.+`: `hoverWhy` (`drag.ts:111`), `paletteHTML`/`specialRowHTML` (`palette-html.ts:89,195` — `slotRules` strips it, nothing else reads the key), `paintSelRings` (`highlights.ts:241`, raw `data-fill`). No caller does neither. The Inputs page's accept, `iu:` reassign (`rowRef` returns null → empty list), the ALL AVAIL window and undo/redo never call the busy check. `rowPlaces` (`slots.ts:126-136`) lists a crowd's indices, a desk's holder, a ground row's name, a sim's `.p/.w/.pax.N`, and `.xN` extras on all four; a flying seat has no list and needs none (the busy scan compares full seat keys). Free text ("ALL PILOTS") is skipped by `whoId`. The UI's sim add box is `s:di.kind.ri.+` (board-html 517-547), which `seatRow` resolves to the sim's row — the `pax.+` form only lives in `fillSlot`.

**1. Other key-shape compares.** No other trim of a programme key remains: `pendlist.ts:49` reads `.xN`/`.pax.` only to word a line; `validate.ts:1708-1709` compares flying keys only; `refwin.ts:542` is the reference harness. The SC/AVALON walks (`events.ts:165-197`) compare full seat and desk keys and already take `selfKeys` as a list. A swap where the other man lands where he was: both asks name each man's own place → silent (checked by hand through `rowPlaces`). Sim front seat and pax both him: the pax place is another place → named, correct.

**2. Landings.** `scrollWeekToDay` (the carry, WEEKJUMP, the board close via CARRYDAY in `EditWeek.tsx:151/176`, `ViewWeek.tsx:97/122`), `bringIntoView`, and `weekLeftDay` (the palette's follow `pan.ts:351`, `WeekCal.tsx:40`, the carry `view.ts:530`) all read `weekInset`. `panDays` steps by `dayStep` (offsetLeft deltas, padding-blind); `setWeekTail` reads `paddingRight` only; "day a–b of 7" divides by the step. The dots' `scrollIntoView({inline:'start'})` (`pan.ts:478`) and the arm's `inline:'nearest'` (`view.ts:1058`) honour the declared `scroll-padding-left`. `scrollWeekToLanding` lands at the clicked x by design. The arrows and the padding share one boundary (`.week-nav` hidden ≤820px, `scheduler.css:597`; the padding rule inside `min-width:821px`, 3537), so 821px exactly has both. A resize never re-lands (W4 O4, pre-existing). The e2e helpers read no padding constant.

**3. Reset order.** Admin at the door (`Matrix.tsx:4334` renders the sheet only for admin) and at the writer (`store.ts:1868`). One Undo step; none when the order is already empty (`store.ts:1896`). `rosterorder` is written as `[]` (`store.ts:934`) and read back `?? []` (816) → the default after a reload; world-level, one order for every war (state, not per war). Rearrange and Show SANS do not read the order differently — `displayRoster` is the ONE reader (Matrix twice, OilTracker once). `armOrder` is sheet-local, so closing takes the question back; arming either reset disarms the other (both directions wired).

**4. Rings.** Only `scheduler.css` draws puck rings; no `@media` block, print sheet (`printpdf.ts` has no ring rule) or Leave War stylesheet redefines `.box*`. The ghost clones the puck's classes, so it follows the same rules. `.hl`, `.wfoc`, `.oilglow` glows are not flags. The one gap is finding 1.

**5. The guard.** Trailing separator stripped; case-insensitive; Windows and POSIX full paths, quotes, `cd --`, `-LiteralPath`, `Push-Location`, `raptor-port/scripts` accepted; `raptor-port-old`/`raptor-portal` refused. A quoted `--prefix` path containing a space is refused (`[^\s;&|]*` stops at the space) — a false refusal too rare to fix. Exit 0 on garbage and on foreground commands holds.

**6. Tests.** `crowdself.test.ts` pins every route the walk found, including the after-write `lastFilled` shape and the SC from-seat. `weekinset.test.ts` pins the right-side rule. `geometry.spec.ts` gates rest, press, carry, list and warning tap; the calendar pick and board-close landings were walked (W4, 1838 pass) but are not gated — a gap, not a defect. Reset order's reload path has no test; the code is a straight write/read of `[]`. The vacuous pass is finding 1.
