# [OIL-SEATS-CAN-EARN] — the hand pass

> **24 Sep 26:** merged and live as PR #425 (23 Sep 26). The backlog records — `[OIL-SEATS-CAN-EARN]` and its
> closing note `[OIL-SEATS-CAN-EARN-CLOSED]` — are in `OUTSTANDING-ARCHIVE.md`; what it deliberately left stays
> open in `OUTSTANDING.md`: `[OIL-READ-LEFTOVERS]`, `[STORE-READER-SWEEP]`, `[OIL-REQ-NAMEBOX]`, `[POSTOUT-LOST]`.

Branch `claude/oil-seats-can-earn`. Walked 22 Sep 26 against the production
bundle, built from the branch head and served locally. The served bundle was
compared byte-for-byte with the freshly built one before a single gesture — no
stale-bundle proof here (order §10, anti-pattern 17).

Pictures: `../img/handpass/2026-09-22-oil-seats/`.
Scripts: `../../scripts/handpass/seat-*.mjs` — committed on purpose, so the next
session re-runs a finding instead of rediscovering it.

The four parallel workers' own sheets are in `parts/`.

---

## 1. The eight questions, and the tier

| # | The question | Answer | Why |
|---|---|---|---|
| 1 | Money — what a man is owed or paid, or how many count as present? | **YES** | OIL is banked leave (D25). This change decides who a placeholder puck pays. |
| 2 | The published record — publishing, signing, amendments, the issued copy? | **YES** | The crowd behind a puck is frozen at publication (D44); the signature rule changes (D45). |
| 3 | Saved data — what is stored, the reset version, how older data is read? | **YES** | A new field on the evidence block; an older issued day must read "unrecorded" rather than have an answer invented. |
| 4 | A shared drawer — one routine called from many places? | **YES** | `oilSeatDeco` is the one body the week and the board both draw from. |
| 5 | A new gesture or mode? | **YES** | The first hard refusal in the app, and a crowd that opens into real pucks. |
| 6 | A new surface? | **YES** | The count chip, on every seat, on every day. |
| 7 | Roles — who may do or see something? | **YES** | The count is a read a member sees; the switch is the admin's. |
| 8 | The warning list — what the warnings say? | **YES** | A new advisory on a flying line with the same take-off and landing (D49). |

**TIER: FULL.** Seven of the eight, and four of them are the ones that force it
on their own.

---

## 2. The roll-call, re-marked by hand

Every row below was **created**: the empty seat was made through the app's own
add controls, then a placeholder was armed and dropped on it by hand. Nothing
was read off a fixture that already had one.

**The precondition that made the first pass worthless, recorded so nobody
repeats it:** tapping a seat that already holds a man does **not** arm it — it
selects the man. A "placement" onto an occupied seat silently does nothing and
looks exactly like a correct refusal. Nine rows in the first pass were
measurement artefacts. Every row below asserts the seat was empty and the
gesture armed before its outcome is read.

Script: `seat-02-rollcall.mjs`. Picture: `RC-01-board-after-hand-placement.png`.

| Seat | Takes a placeholder | Count shows | Switch offered | Default | Opens into pucks |
|---|---|---|---|---|---|
| Flying — cockpit P, ordinary wave | **Refused**, reason on screen | n/a — nothing sits there | n/a | n/a | n/a |
| Flying — cockpit W, ordinary wave | **Refused**, reason on screen | n/a | n/a | n/a | n/a |
| Flying — cockpit P, SC MAIN | **Refused**, reason on screen | n/a | n/a | n/a | n/a |
| Flying — cockpit W, SC MAIN | **Refused**, reason on screen | n/a | n/a | n/a | n/a |
| Flying — cockpit P, SC SPARE | **Refused**, reason on screen | n/a | n/a | n/a | n/a |
| Flying — cockpit P, SC second formation | **Refused**, reason on screen | n/a | n/a | n/a | n/a |
| Flying — cockpit P, AVALON | **Refused**, reason on screen | n/a | n/a | n/a | n/a |
| Duty desk — own position (AVALON OPS O) | YES | YES — "45", none earn | YES | **off** (exempt, correct) | YES — 45, all off |
| Duty desk — own position (SC OPS O AM) | YES | YES — "30 of 30 earn" | YES | **on** | YES — 30, all on |
| Duty desk — more (SDO row, already filled) | YES | YES — "26" | YES | **on** | YES |
| Sim — OFT front seat | YES | YES | YES | **on** | YES |
| Sim — OFT rear seat | YES | YES | YES | **on** | YES |
| Sim — OFT extras | **FIXED 22 Sep 26 (D50)** — the row always shows a spare seat | engine counts it | YES | **on** | YES |
| Sim — AMT passenger slot | YES | YES — "27 of 27 earn" | YES | **on** | YES |
| Sim — AMT extras | **FIXED 22 Sep 26 (D50)** — the row always shows a spare seat | engine counts it | YES | **on** | YES |
| Ground row — the who | YES | YES | YES | **on** | YES |
| Ground row — more | YES | YES — "30 of 30 earn" | YES | **on** | YES |
| Common Programme — the who | YES | YES — "39 of 39 earn" | YES | **on** | YES |
| Common Programme — more | YES | YES | YES | **on** | YES |
| Next-week peek | *worker sheet: `parts/…-surfaces.md`* | must not | n/a | n/a | n/a |

Both placeholder pucks — ALL and ALL AVAIL — were walked, not one of them.

**A SECOND ROLL-CALL, for the mark D49 asks for** (added 22 Sep 26 with the fix).
The thing it attaches to is a FLYING LINE whose take-off and landing are the same
minute. Every place the app draws a flying line's times, with a written answer:

| Where a flying line's times are drawn | Shows the mark | Says why, where it sits | The warning's tap lands there |
|---|---|---|---|
| The board, a weekend day | YES — both boxes | YES | YES — measured: the board scrolled 2757 → 281 and put the line on screen |
| The board, a weekday | YES — both boxes, per aircraft | YES | YES |
| The edit week | YES — both cells | YES | YES |
| The view week (where a published day is read) | YES — both cells | YES | YES |
| A version preview (a frozen issued page) | YES — the line went out with two identical times, and the issued page says so | YES | **NO, and deliberately.** A preview is what the squadron was GIVEN; WARN is live. The mark is drawn, the ADDRESS is withheld, so a live warning can neither light nor scroll into last week's paper (the follow-up read, G2). Week and board alike, and the highlight pass skips `.pv-frozen` as well |
| The next-week peek | **WAS MISSING — this row is what found it.** The peek has its OWN copy of the week's flying-line markup (deliberately: it must never read the live day), so the mark did not arrive there. Now YES, both cells | YES | n/a — a peek column is inert and carries no warning list of its own |
| A line with different times, an overnight line, a line with no times | **must not**, and does not | n/a | n/a |
| A line NOBODY is on | **must not** — the warning does not fire either, and the mark follows the warning exactly | n/a | n/a |
| A cancelled line | **must not** — `fltNoLen` refuses a cancelled formation, as the warning always did | n/a | n/a |

Pinned surface by surface in `ui/fltnolen-mark.test.tsx`; walked in
`scripts/handpass/rw-01-d49-mark.mjs` and `rw-01b-d49-tap-moves.mjs`.

---

## 3. The door check, both orders

Script: `seat-04-doors.mjs`, `seat-05-append.mjs`, `seat-07-addz.mjs`.
Pictures `DOOR-01` … `DOOR-07`.

| Gesture | On a jet (must refuse, with the reason) | On a seat it is legal on (must take) |
|---|---|---|
| Arm the seat, then pick from the crew palette | **Refused.** The palette's row for both placeholders turns struck-out and reads *"ALL AVAIL — cannot crew a jet; name the people flying it"*. Pressing it anyway does nothing. | Takes it. |
| Drag from the crew palette onto the seat | **Refused.** Nothing lands; the same words are on screen throughout. | Takes it. |
| Drag onto a row that already holds people | — | Appends on a duty desk and a ground row. **On a sim row it replaces a seated man** — see finding 3. |
| Tap the "+ add" strip under a full row | — | Works on duty, ground and the Common Programme. **Not drawn at all on a sim row.** |

---

## 4. The money across the publish boundary

Script: `seat-11-frozen.mjs`. Pictures `FRZ-01` … `FRZ-05`.

The everything-Saturday, with a crowd of thirty behind a placeholder on an
empty SC duty desk, published, then signed again (publishing spends the
signature, so there has to be a fresh one for the rule to protect).

| Order walked | What the screen said | Verdict |
|---|---|---|
| Publish | Working copy and issued page both read 30. Version chip reads the original. | as ruled |
| Tap the count on the working copy | *"30 behind this puck — who is free as things stand now — Ace full day, Warden full day … Gambit half day …"* | D27 · D37 hold |
| File leave for Ace, who is one of the thirty | Working copy drops to **29**; the day now reads **1 pending**; the four signatures **survive** | D44-the-mark · **D45 holds** |
| Tap the count on the issued page | **Still 30**, and still names **Ace**, worded *"who was free when this day was issued"* | D44 · D44-the-tap hold |
| Turn a whole item's earn switch off | The four signatures **clear**; the app says *"FAMILY DAY earns nobody any OIL"* | **D45's other half holds** |

That is the heart of the change, and it behaves exactly as ruled: availability
moving does not touch a signature, a change of mind about what was approved
does.

---

## 5. Both widths

Script: `seat-09-widths.mjs`. Pictures `W-desk-01…03`, `W-phone-01…03`.

| | Desktop 1440 | Phone 375 |
|---|---|---|
| Count chips drawn | yes, mode off | yes, mode off |
| A crowd of 30 opened | 10 rows inside a cell 276 wide × 186 tall, nothing clipped, nothing off-screen | **30 rows, one man per row, a cell 145 wide × 566 tall — the single row becomes 608px, about three quarters of the screen** |
| Smallest thing to tap | a man's puck, 74 × 15 | a switch, 36 × 25; a man's puck, 74 × 15 |
| Errors in the browser's own list | none | none |

The phone answer is the owner's open question, not a defect — see §7.

---

## 6. What the walk found

### Finding 1 — a count that says everyone earns is captioned "some of them do not"

**Where:** every count chip, on every surface, mode on or off. Seen on a duty
desk ("30 of 30 earn"), a sim passenger row ("27 of 27 earn") and a Common
Programme row ("39 of 39 earn").

**What happens:** when the men behind a placeholder all earn but at *different
amounts* — some a full day, some half — the chip reads "30 of 30 earn" and its
own caption reads *"Some of these men earn OIL and some do not"*. The two
sentences contradict each other on the same puck. The money is right; the words
are not.

**Why it happens:** the summary reports a single amount only when every man gets
the same one. A mixed-amount crowd returns "no single amount", and the wording
reads that as "not everyone earns". The case where *all* of them earn at two
different rates has no sentence of its own.

**Consequence:** a scheduler reading a published day is told some of those
thirty men are not being paid, when all thirty are. That is a wrong statement
about money on the face of an issued document.

**New to this branch?** The caption is. Before this change the chip could not
appear on most of these seats at all.

**Picture:** `GAP-01-count-captions.png`.

### Finding 2 — an exempt line's earn switch is painted green while its own words say it earns nothing

**Where:** every AVALON line and AVALON duty desk, in the earn mode.

**What happens:** the switch is drawn in the *earning* style — green fill, green
border — and its own tooltip reads *"This kind of event earns nothing — tap to
make it earn"*. The money is off (every man behind it shows as not earning), so
only the paint is wrong.

**Why it happens:** the switch has three states in its words (earns · earns
nothing by default · switched off by hand) but only two in its paint. "Switched
off by hand" gets the struck-through grey; *everything else* gets the green,
including "this kind of event earns nothing".

**Consequence:** D24 says the four exempt kinds "all start OFF". On screen they
start green. An admin scanning the board sees green and reads it as earning —
the one thing the colour exists to say.

**New to this branch?** **Yes.** On `main` the switch was a clean two-state
control and exempt kinds never drew one at all — step 4 is what brought them
into the mode.

**Picture:** `GAP-02-switch-states.png`.

### Finding 3 — a sim row that is exactly full offers no way to add another body

**Where:** any sim row whose seats are full — an OFT with both seats taken and
no extras, or an AMT box with an even number of passengers.

**What happens:** there is no empty slot to drop into, and the "+ add" strip
every other people-cell carries is not drawn on a sim row at all. A drag onto
the row therefore lands on a seated man and **replaces him** — the rear-seat
pilot silently becomes the placeholder.

**Why it happens:** the sim seat grid pads itself to an even number of seats, so
an already-even row grows no spare slot; and the "+ add" strip is deliberately
switched off for that grid, on the reasoning that it "auto-adds rows as pucks
land, so it never packs edge-to-edge and swaps". That reasoning holds for odd
counts and fails for even ones.

**Consequence:** the register promises that a placeholder on a sim row's extras
counts the people it stands for, and the roll-call says the switch must be
offered wherever a puck may land. The engine does all of that correctly — but a
scheduler cannot put the puck there. And the gesture he *will* try loses a man
off the sortie without saying so.

**New to this branch?** **No — this is pre-existing**, identical on `main`. But
it matters more now: before this change a placeholder on a sim row credited
nobody, so the missing door led nowhere. This change made that seat pay real
people.

**Pictures:** `DOOR-06-append-truth.png`, `DOOR-07-addz.png`.

### A false alarm, recorded so it is not re-found

Tapping a count chip appeared to open nothing, on both the working copy and the
issued page. It was the walk's own instrument: the app's message element carries
no class, and the reader was matching class names. The tap works and lists every
man with his amount. Fixed in `seat-lib.mjs` with the reason written beside it.

---

## 6a. The five the walk left open — what they turned out to be, and the fixes

Written 22 Sep 26, the session after the walk. Every one was reproduced in the
real production build before a line was changed, and re-walked in it afterwards.
Each fix landed with a test that was RED first; the two defects the walk proved
by breaking the code on purpose got the tests it proved were missing.

**TWO OF THE FIVE WERE ONE FAULT, AND NOT AN OIL FAULT.** The handoff named a
cause for each defect beside its symptom, and for these two the named cause was
wrong. The symptoms were exactly as reported.

| # | What the walk saw | What it actually was | Where the fix went |
|---|---|---|---|
| 1 | D49's line earns, and nothing on the LINE says the times cannot both be right; tapping the warning moves nothing | Two halves. No renderer drew a mark, and the warning's key named a typed BOX, which `anchorEl` could not resolve at all — it only ever looked at people cells and seats | `engine/validate.ts` (one shared predicate), `ui/html.ts` + `ui/board.ts` (the mark), `ui/highlights.ts` (`data-warnkey`), `ui/scheduler.css` |
| 2 | A sim row that is exactly full offers no door for another body | As reported. The seat grid pads to an EVEN count, so a spare exists only when the count is odd — and the drop strip every other cell has is hidden on that grid | `ui/board-html.ts` (`simSpare`), `ui/scheduler.css` (the rule stands; its stale reason is corrected) |
| 3 | A published day carrying a crowd reopens as "1 pending", signatures spent, nothing on screen saying what changed | **Not an OIL fault.** The issued day froze 27 men; the reload gave the live copy 28. The extra man left the squadron in January and walked back in, because the demo's posting-out date is written onto the person and never saved | `leavewar/state/store.ts` `setPeople` |
| 4 | The count chip says 46 and the war pays 45 | **The same fault as 3**, seen from the other side: the working copy's crowd gained a man the issued copy did not have | the same one-line change |
| 5 | On a phone, on the EDIT WEEK, the count chip cannot be tapped — the press opens the row's remarks box | As reported. On a row where the placeholder sits alone the people column is exactly one puck wide, and the chip hung 23px out into RMKS, painted under the remarks box | `ui/scheduler.css` (the seat wraps its decoration rather than overflowing) |

**Why 3 and 4 were fixed at the root rather than patched twice.** `[POSTOUT-LOST]`
was already filed and deliberately deferred last session as "not an OIL defect".
That was right on the evidence then and wrong once the cause was measured: it is
what produced two of these five. The OIL code was doing exactly what D44 and D45
say — the issued page holds still, the working copy shows today's answer, and a
difference raises the pending mark. What was wrong is that the world changed
under it with nobody having touched anything.

### The re-walk, in the built bundle

| What | Script | Result |
|---|---|---|
| The mark on the line, board (Sat + Wed), edit week, view week | `rw-01-d49-mark.mjs` | **2 boxes marked per line on every surface**, each carrying the reason in its own words; the warning's key resolves on all four. `RW-01-board-day5-marked.png`, `RW-01-editweek-marked`, `RW-01-viewweek-marked` |
| The tap MOVES the board to the line | `rw-01b-d49-tap-moves.mjs` | Board scrolled to the bottom (2757), the marked box at **−1985 and off screen**; after the tap the board is at 281 and the box at **491, on screen**. `RW-01b-after-tap.png` |
| A full sim row still offers a seat | `rw-02-sim-spare.mjs` | The BOX row filled through its own controls to **4 seated** still shows **2 empty seats**, and the spare **arms**. `RW-02-sim-row-full-still-has-a-seat.png` |
| No amendment nobody made, four days | `rw-03-pending-and-count.mjs` | Monday, Friday, Saturday, Sunday: published, saved, reopened — **all four "nothing pending"**, no AL offered, and *"the issued crowd and the live crowd are the same men"*. The Saturday's chip reads **27 before and 27 after** (it was 27 → 28). `RW-03-reload-day5.png` |
| Why it used to go pending | `rw-00a-why-the-day-goes-pending.mjs` | Before the fix: `issued 27, live 28, added Torch`, day reads "1 pending". After: `diff: []`, "nothing pending" |
| The issued crowd against the live one | `rw-00b-issued-crowd-vs-live-crowd.mjs` | Named the man: Torch, whose Raptor body is not archived and whose posting window had been lost |
| The phone chip, edit week | `rw-04-phone-chip.mjs` | Both kinds of row: chip **inside its column with 51px to spare**, **on top**, and a real press opens the list of who is behind the puck. `RW-04-phone-editweek-chip.png` |

### The walk's OWN scripts, re-run — the instruments that found each defect

Re-running the new `rw-*` scripts proves the fixes. Re-running the scripts that
FOUND the defects proves them on the same instrument, which is the stronger
statement. All of these are committed.

| The script that found it | What it said then | What it says now |
|---|---|---|
| `seat-rule-03c-d49-tap.mjs` | *"WHAT LIT: marked: [], rowMark: []"* on both days — the tap lit nothing and moved nothing | Both time boxes, both days: `tm badtm wfoc advf`. Picture `RULE-03c-d49-tapped-day5.png` |
| `seat-surf-11-chiphit2.mjs` | phone edit week: the chip not on top, a real press did nothing | Every chip `onTop=YES`, every press *"the list appeared"* — board, edit week and view week, phone and desktop |
| `seat-surf-14-phonekinds.mjs` | 3 of 8 row kinds unpressable on the phone edit week | **8 of 8** on the edit week and **8 of 8** on the view week |
| `seat-lw-21-pending-is-it-oil.mjs` | Saturday and a Sunday carrying a crowd came back *"1 pending · Publish AL1"* | Monday, Friday, Saturday, Sunday: **all four "nothing pending"**, no AL offered |
| `seat-07-addz.mjs` | recorded the sim grid's hidden strip (finding 3) | unchanged, and correctly so — D50's door is the spare SEAT, not the strip. The seat is measured by `rw-02` |

**A SECOND GAP FOUND BY RE-RUNNING THE ORIGINAL SCRIPT.** `seat-rule-03c` looks
for the warning's own LIGHTING, not for the scroll — and after the first fix it
still reported nothing lit. Making the board scroll to the line was only half the
job: on a line already on screen the scroll has nowhere to go, so the press still
had no visible answer, which is the complaint the walk actually made. The box the
warning names now lights, in the same `wfoc`/`advf` vocabulary every clicked
warning uses on a puck. Pinned in `ui/fltnolen-mark.test.tsx`.

### What the fix does NOT repair, and why that is right

`seat-lw-12-count-vs-money.mjs` runs against a world the WALK saved, before the
fix. There the desk's count and the war's payment now agree at 46 — but the two
lists differ by one man each way: **Torch** is in the live crowd and was not paid,
**Dash** was paid and is not in the live crowd today.

That is not a defect, and the second half of it never was: Dash's availability
genuinely changed after the day went out, which is D44 working exactly as the
owner designed it — the issued page keeps the people it went out with, the
working copy shows today's answer, and the chip says in its own words which one
it is showing. Torch is the bug's RESIDUE: that world was saved without his
posting window, and nothing can put it back from outside.

**The fix stops it happening forward; it does not repair a world already saved
without the window.** That is the dev-phase ruling applied as written (13 Sep 26,
re-confirmed as D22): a problem living only in stored demo data, already
prevented going forward, is CLEARED rather than migrated. A world built and
published after the fix has no such difference at all — `rw-03` publishes four
days, reloads each, and reports *"the issued crowd and the live crowd are the
same men"* every time.

### The two tests the walk proved were missing

Both were written and then PROVED to watch something, by breaking the code they
name and watching them go red:

- **The Common Programme's crowd opening (OIL8).** Breaking it used to leave all
  3,301 tests green. It now turns **4 red** in `ui/oilrowpucks.test.tsx`, which
  gained a block for the Common Programme and one for the ground row — the two
  surfaces the file never asserted, because it was written to the surfaces step 7
  ADDED.
- **The signature key's CONTENT.** Putting membership back into the key — the
  exact D45 regression — used to leave 1,914 tests green, because the test named
  for the rule passes either way on its fixture. It now turns **2 red** in
  `engine/oilmembership.test.ts`, which asserts the key itself rather than a
  behaviour a fixture can satisfy by accident.

---

## 6b. The two final code reads (bug-check order §4 rank 2)

Both providers read the finished branch **independently and blind to each
other**. Reports committed verbatim beside the reconciliation:
`../superpowers/specs/2026-09-22-oil-seats-final-read-{fable,codex,reconciled}.md`.

**They returned the SAME four defects, and neither found anything the other
missed on this branch's code.** Three were already closed when the reports
arrived — found by reading around the fixes and by re-running the walk's own
scripts. The fourth was live and is the one with money behind it.

| The defect | Fable | Codex | Where it ended |
|---|---|---|---|
| A nought-minute SC / AVALON / BB **shift** pays nobody while the advisory says it "still earns from the report and debrief" | F1 · MEDIUM | 2 · High | **Fixed** `ca57f306` |
| A **post-IN date** thrown away on every reload | F2 · MEDIUM | 1 · High | Already fixed `0901ef08` |
| The **next-week peek** never got D49's mark | — | 3 · Medium | Already fixed `b05d6f52` |
| The warning's **tap lights nothing** when the line is on screen | — | 4 · Low | Already fixed `e28b7234` |

**Every finding was reproduced through the production route before it was acted
on**, and two things a reviewer said turned out to be wrong:

- **Codex** called a boot comment stale ("`initStore` has already enabled the
  command router"). This sheet first recorded that as wrong. **It was Codex that
  was right.** `lwHistInit` is called TWICE — at the end of `initStore` and again
  from `main.tsx` — and only the second was checked. The behaviour was always
  correct; the COMMENT asserted the opposite of the truth, and so did the first
  write-up of this row. Corrected in place, visibly. Settling a disagreement by
  measuring is only worth as much as the measurement.
- **Fable's own fix for its F6 does not work as written.** `lwSyncTurn(persistNotify)`
  still emits `lw.edit` at idle, because the router gates on
  `HIST.lock || cmdIsCommitting()` and not the turn flag. The test is what showed
  it: written on `lwCanUndo` it passed with the routing removed — an assertion
  watching an ingredient — so it was rewritten on the change stream's envelope
  TYPE, where it failed. The working shape is `lwSyncTurn(() => locked(...))`.

Four LOW findings were deliberately not acted on and are filed as
`[OIL-READ-LEFTOVERS]`; one of them is a question for the owner rather than a fix
(an already-issued weekend carrying a placeholder reads "1 pending" the moment
this ships — the behaviour is D44-correct, the register's promise of silence is
what may need to move).

**A follow-up read of the five commits that landed after the reads began** is run
separately, again on both providers and again blind, because neither saw them.

---

## 7. The owner's open questions — his to answer, not ours

1. **Where 27 opened pucks go in a sim row's people cell on a phone.** Answered
   by the walk, not decided: they stack **one man per row**, turning that single
   sim row into a column about 600 pixels tall — roughly three quarters of the
   phone screen — which pushes the rest of the day below the fold. Nothing is
   clipped and nothing is unreachable; it is simply very tall. Picture
   `W-phone-03-crowd-opened.png`.
2. **Crediting an activated SC spare by tapping the man** (the recorded fallback
   is a switch on the MAIN/SPARE badge).
3. **Two "free" counts on one screen** — the Available-crew panel's "Pilots · N
   free" comes from a different body than the puck's count.
4. **`[OIL-REQ-NAMEBOX]`** — can a scheduler put someone else in a request row's
   name box, and if so should he earn from it?

---

## 7b. The break-test sweep — one wire per wired surface

The order asks for one RED break test per wired surface (§8.4: "break that wire
once on purpose and watch a named test go red. If nothing goes red, that surface
has no test, by proof"). Nine had been done one-per-FIX; the owner asked for the
difference to be closed, and it was. Each wire below feeds roll-call rows that
had no proof of their own.

| The wire broken | What it feeds | Went red |
|---|---|---|
| `engine/oilev.ts` `oilSentOf` — never resolves a crowd | who is behind a placeholder: the chip's number, its tap list and the pucks the mode opens, on EVERY seat kind | **31 tests, 5 files** |
| `ui/html.ts` — the count chip's own markup never drawn | the chip itself, on every surface that draws a seat | **15 tests, 4 files** |
| `ui/board-html.ts` `simSpare` — a full sim row offers nothing | the spare seat D50 asks for | **4 tests** |
| `ui/highlights.ts` `anchorEl` — a box's address is not read | the warning's tap landing on the line | **4 tests** |

Nothing came back green, so no surface in either roll-call is unwatched. The
nine earlier breaks stand beside these: the Common Programme and ground crowd
opening (4 red), the signature key's content (2), the peek's mark (1), the box
lighting (1), the preview's address (1), the stale focus (2), the posting window
(3), the joining date (1), the shift wording (2).

**Two of those nine came back GREEN the first time**, which is how the two
missing tests in §6a were found. That is the whole point of the sweep.

## 8. The gates

Run at the close of the fix session, on `d800b838`.

| Gate | Result |
|---|---|
| `npm test` | **5703 / 5703** across 354 files, run alone |
| `npm run build` | OK |
| `node reference/tfin.js` | **728 / 0** |
| `npm run rulecheck` | OK — 53 rulings named by a test, 7 by nothing (the recorded baseline) |
| `npm run test:e2e` | **449 passed · 45 skipped · 1 failed** — the one is `[LW-SCRUBBER-FLAKY]` by name, already filed. **Re-run alone it passes in 7.1 seconds.** |
| `npm run smoke:tracker` | **425 passed, 0 failed.** It CRASHED on the first attempt rather than failing — the documented `[TRK-SMOKE]` port trap: the browser gate before it left its preview server up and the smoke could not bind. Killed by PORT (never by command-line pattern) and re-run clean. |
| `npm run perf` | **4 passed, 0 failed — green for the first time.** Behaviour check B was never a speed problem: it was reporting the missing weekend warning, which is fixed. DOM ceilings OK (week 5124/5450, board 1024/1150). |
| `npm run docsize` | **FAIL on 3 files.** `HANDOFF.md` and `.claude/rules/record-decisions.md` were already over at the branch head and were never touched here; `OUTSTANDING.md` was 37 lines over at the head and is now 83, all of it live record. D29 rule 3 forbids trimming docs inside a fix, so it is reported, not carved up. |

**Three runs came back one short while other work was on the machine** — a
different file each time (`figdrawer`, `inputscal`, `figselect`), each passing
alone first attempt, none of them touched by this change. That is the load family
the previous handoff names: 16 GB, workers peaking around 11 GB, and a
timing-sensitive jsdom test losing its window. The numbers above are from runs
made with nothing else running.

## 8a. The non-substitution checklist (§12), answered

| | |
|---|---|
| Eight questions answered, tier stated in writing | **yes** — FULL, §1 |
| Applicable rulings listed; clashes flagged and resolved | **yes** — and one clash was found and settled: D49 is a ruling about a SORTIE and was being applied to a standby SHIFT |
| Roll-call complete, no blanks | **yes** — two of them, §2 |
| The exact production build driven, both widths | **yes** — rebuilt before every walk |
| Fixture created through the app's own controls | **yes, with one exception**: `rw-04` planted its rows through the bridge rather than the board's `+ Row`. The walk's own `seat-surf-11-chiphit2.mjs` uses the real controls over the same ground and passes on all eight chips |
| Every required surface SEEN and OPERATED | **yes** |
| Every required order walked, including after publishing | **yes** — published and reloaded on four days |
| The real downstream result observed — the money | **yes** — the issued crowd against the live one, and what the war paid |
| Tests use production routes, or are renamed | **yes** — two of this session's own were corrected for it |
| One red break test per wired surface | **yes** — §7b, after the owner asked for the gap to be closed |
| The gates pass | **yes** — §8 |
| Model reads done, reconciled, findings dispositioned against `main` | **yes** — two, then two follow-ups, all blind |
| The sheet exists with its pictures, names what was NOT walked | **yes** — 229 pictures, §9 |
| **The owner's look** | **WAIVED, 23 Sep 26 (D55)** — asked directly, he chose "merge now, look after". This branch's evidence is the walk, the gates and two rounds of code reads, NOT an owner sighting. Do not assume the day was eyeballed |

**Branch state:** merges into `main` with no conflicts, 43 commits ahead, 0
behind.

## 9. What was NOT walked, and why

- **`main` run side by side.** The walk was told not to rebuild or serve a second
  bundle, so its provenance rested on in-app controls plus the branch's own diff.
  **The fix session DID do it** where it mattered: the speed check's failing
  behaviour test was re-run with every change stashed away and failed
  identically, byte for byte, which is what makes "pre-existing" a measurement
  here rather than a claim.
- **The owner's own browser.** The posting-window fix reaches a browser that
  boots fresh. An existing device already has a posting record without that man,
  so nothing changes there and nothing looks wrong — and the walk's 27→27 is NOT
  what he will see. Said plainly to him rather than left implied.
- **A world saved before the fix.** `seat-lw-12` still shows the residue on the
  walk's own saved world, and nothing can put a lost date back from outside. The
  dev-phase ruling (D22) says clear such data rather than migrate it.
- **The phone's in-row crowd, and the name bubble.** `[ALL-AVAIL-WINDOW]` (job 2)
  replaces both surfaces, so walking them here would be walking something about
  to be deleted.
- **Neither code read drove the app.** That is the walk's job and it was done
  first, which is §4a's own ordering. Codex could not run tests at all — its
  sandbox denied Vitest its temporary directories — and its report says so.

## 10. The Walk line

`Walk: docs/handpass/2026-09-22-oil-seats.md · 229 pictures · 9 surfaces re-walked · 5 orders · MISSING: 5 fixed + 4 filed`
