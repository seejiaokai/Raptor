# Fable 5.1 — fix check on `[ACCOUNTS-NEW-PERSON]` (`67832f4c` over `107943cd`), 26 Sep 26

*(Saved verbatim from Fable's report — a read-only subagent, brief
`docs/superpowers/briefs/2026-09-26-accounts-new-person-fixcheck-brief.md`. Blind to Astra's fix check. Its findings are
dispositioned in the evidence sheet §6.)*

Read-only. Opened: the brief, both prior reads, `git diff 107943cd 67832f4c` (code, tests, docs, walk scripts), `UsersPanel.tsx`, `AccessScreen.tsx`, `state/accounts.ts`, `state/roster-add.ts`, `quals-write.ts`, `QualsPage.tsx` (the CAT box), `AdminPage.tsx`, `App.tsx`, `leavewar/sync.ts` (archive/restore), the plan, the register, the evidence sheet, `np-walk.mjs`, the three walk JSONs, the rulings (how-we-work D214–D227, D56, D166; scheduler D149, D218; raptor-executor). Ran read-only: `npx vitest run src/state/roster-add.test.ts src/ui/accounts-newperson.test.tsx` → 2 files, 44/44 pass. Walk results read: `walk3/walk.json` 55/55, 0 browser errors; `acc-rewalk/acc-walk.json` 38 pass, 0 errors; `acc-walk2.json` 42 pass, 0 errors.

## Findings, most serious first

**1. (MEDIUM) The approve note still tells the admin to do something the screen will not let him finish — for a person who is BOTH archived AND holds an account.** Already on `107943cd`; `67832f4c` put the new account branch BELOW the archived line, so it does not reach this case.
- **Where:** `raptor-port/src/ui/UsersPanel.tsx:125-127` (`rosterMatch`): line 125 returns the archived note before line 126 ever asks `accountOfPid(hit)`.
- **Why this state is real, not demo-only:** the app designs for it — an archived person keeps his account (`accounts.ts` header: "A person ARCHIVED under an account keeps it… posting out archives automatically"; the Accounts list draws the "archived callsign" tag, `UsersPanel.tsx:214`). Reachable with NEW data: admin adds an account for Hex; Hex gets a posting-out date with "Archive on PO date"; the date arrives → `runPoArchive` (`sync.ts:1356`) sets `archived`, the account stays; later Hex (new defence mail) or a namesake signs up as "Hex".
- **Scenario:** sign-up "Hex" (or "rocky") → Approve → "On the roster", note: "He typed Hex — Hex is archived; restore them on the Quals page to link them." The picker has no Hex (`linkablePeople` excludes both archived and account-holders). Admin goes to Quals → Archived → Restore → back → Approve again → NOW the note reads "Hex already has an account (hex), so they can't be picked here…" — the first instruction could never lead to linking (`pidProblem` refuses "already has an account" whatever his archived flag). Worse, Restore is not free: `restoreArchivedPerson` (`sync.ts:1389-1409`) runs `setPostOut(id, null)` — it WIPES his posting-out window and puts a posted-out man back on every roster surface (Quals, crew lists, Leave War manning, Tracker) — a real side effect the note walked him into. The observation that disproves correctness: two consecutive notes for the same request give two different instructions, and following the first changes Leave War data.
- **Fix (exact):** (1) In `rosterMatch`, move the `const acct = accountOfPid(hit); if (acct) return {…}` two lines ABOVE the `if (p.archived)` line — an account is the stronger fact: he cannot be picked whether archived or not, and the account editor (not Restore) is where his sign-in is changed. Optionally add "(archived)" after `${p.cs}` when `p.archived`, so the admin knows why the Accounts list tags him. (2) Test in `src/ui/accounts-newperson.test.tsx` NP5: `const was = PEOPLE.rocky.archived; PEOPLE.rocky.archived = true` in try/finally (rocky = "Hex", the seeded `hex` account's person); sign in as `r9@mail`, `ask('Hex')`; sign in `ad`; open Approve; assert `#apvModeRoster` pressed, `#apvNote` matches `/already has an account \(hex\)/`, does NOT match `/restore/i`, and `optTexts('#apvPid')` does not contain "Hex". (3) `np-walk.mjs` S8 (optional but cheap): archive a free person via Quals ✕, sign up as him, assert the same note; re-walk that step. (4) `ui-contracts.md` §Admin → Users: reorder the parenthetical so "one who already has an account" comes before "an archived one", and say the account case wins.

**2. (LOW, wording) The new has-an-account note says what to do if the asker is someone else, but not what to do when it IS him.** NEW in `67832f4c`. The brief asks me to judge the wording: the caution (not claiming "this is someone else") is RIGHT — a man back on a new defence mail is the common case the note was written around. But the note stops one clause short: the path that exists — open his account in Accounts above, change its sign-in to the new defence mail — already answers the request by itself (`accounts.ts:265-268`, `answered`), and nothing on screen says so. The admin's visible options are Decline or New person, both wrong for the "it's him" case.
- **Where:** `UsersPanel.tsx:127`; pinned exact text in `accounts-newperson.test.tsx:162,166`; `np-walk.mjs:348` (`HAS`); `ui-contracts.md` §Admin → Users.
- **Scenario:** Ranger has a new mail, signs up as "Ranger". Note: "…can't be picked here. If this is someone else, choose New person and give them another callsign or name." Admin declines (nothing else offered) — Ranger stays locked out on the new mail.
- **Fix:** append one clause: "If this is them on a new sign-in, change that account's sign-in in Accounts above — the request is answered with it." Update the two test strings, `HAS` in `np-walk.mjs` (re-run S8 steps), and the ui-contracts line. Fold into the same commit as 1.

**3. (LOW, optional, already on `107943cd`) The archived note (person with NO account) gives only the Restore path.** If the asker is a different man who typed an archived callsign, neither the note (`UsersPanel.tsx:125`) nor New person's refusal (`roster-add.ts:81`, the plan's verbatim PID-01 wording) says "give another callsign or name". Not wrong — Restore IS the right door when it is him, and callsign uniqueness is a known rule — but the same "if this is someone else…" clause as finding 2 would close it. Leave `roster-add.ts:81` as the plan wrote it.

## Each fix in the brief, judged for completeness

| Finding | Does the fix do what was asked, completely? |
|---|---|
| Fable #1 (note vs picker) | YES for the case named; NOT for its sibling (finding 1 above). The wording departure is right in its caution, incomplete in its options (finding 2). NP5 tests now use a person with no account and assert he IS in the picker; the Ranger/bane test asserts he is NOT and the note carries no "Pick them". |
| Astra #1 (Add form clears) | YES. `resetAddForm` clears name, pid, np, role, mode after both successes; both tests exist and assert the hidden half (`#accAddPid` '' after a New person add; New person blank after a roster add). Walk `d-A1-form-clears` PASS. |
| Astra #2 (`catsFor`) | YES. FCP / RCP lists, `[]` for GND, '', undefined, made-up; unit-pinned; UI tests on all three forms (sign-up, Add, Approve) go Pilot+C → Personnel → WSO → ''. Walk `d-S9-seatcat` and `d-A2-cat-personnel` PASS. |
| Astra #4 (walk scripts) | YES. Both take "On the roster" after Approve and wait for `#apvPid`; `acc-walk` still exercises the empty-picker refusal; re-run 38 and 42 pass, 0 errors. |
| Fable #2 (three roll-call rows) | YES. `np-walk.mjs` gained `d-S4-board-avail`, `d-S4-inputs` + `d-S4-his-inputs` + `d-S4-his-leavewar`, and the fresh-world `f-S14-first-write-reload`; walk3 55/55, 0 errors. |
| Fable #4 (`mk-new-person.mjs`) | YES — a header note; the script is a record. |

## Explicit negatives — checked and found nothing
- **Every other reader of `catsFor` / a seat value:** `QualsPage.tsx:238` (the CAT box) runs only for non-personnel rows (`if (p.pers)` returns at 220); every seeded non-personnel person carries `seat: 'FCP'|'RCP'` (people.ts 52–139; the three GND rows are all `pers:true`; the two placeholders are FCP); `putNewPerson` writes only FCP/RCP/GND and GND always with `pers:true`; so no Quals row now gets an empty CAT list. `accounts.ts:130` (loader) and `:150` (`requestSummary`), `quals-write.ts:40`, `raptorRoster.ts:52-62`, `peoplewire.ts:25` — none affected. A stored person with no seat would be old demo data (D56), and no writer makes one now.
- **Forms keeping state across a success or a mode switch:** the approve form clears on success (`setOpen(null)`), is keyed by request id, Cancel discards — pinned; the account editor remounts on every open/close (`key={a.id + ':e'}`), so Save/Switch off leave nothing behind; the sign-up card's state lives in `AccessScreen`, which is unmounted the moment anyone else signs in (`App.tsx:33`), and a fresh sign-in mounts it blank; the add form survives a category switch inside Admin (UsersPanel is always mounted, `AdminPage.tsx:230-233`) — no success between, by design.
- **Other notes/messages asking the impossible:** the New person note, the "one account, tied to their callsign" line, the "archived callsign — switch the account off" tag (Switch off exists), the own-account title, `pidProblem`'s archived refusal (unreachable from any picker except `keep`) — all doable on screen.
- **Tests passing for the wrong reason:** none found. NP5's "on the roster" test picks a linkable person whose id differs from his callsign and asserts he is offered; `quals.test.tsx:516` reads the first pilot and first WSO rows (FCP/RCP); `accounts.test.ts:143` pins the old-shape loader; `accounts-ui.test.tsx:170-180` clicks "On the roster" before picking; no test asserts a WSO list for a non-seat.
- **Atomicity, permissions, the one add, the bell, undo:** untouched by `67832f4c` (diff read whole); my earlier negatives stand.
- **D56:** finding 1 is not a stored-data problem — the code produces the wrong note for a state new data reaches.

## Verdict: **FIX FIRST** — finding 1 (a reorder of two lines, one test, one doc line); finding 2 rides the same commit; finding 3 optional.
