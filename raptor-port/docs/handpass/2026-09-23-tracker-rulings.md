# Tracker — every ruling, decision and promise (records sweep, 23 Sep 26)

Read-only sweep for the Tracker `[HUMAN-RETEST]` walk. Sources read: `raptor-port/CLAUDE.md` (Tracker paragraph + Where-things-live row), `docs/tracker/known-gaps.md` (incl. the D120 head note added at HEAD `7286b40b`), the three `docs/tracker/specs/`, `DECISIONS.md` (D4, D54, D56, D58, D59, D62–D64, D66, D85, D86, D120), `OUTSTANDING.md` / `OUTSTANDING-ARCHIVE.md` (every TRK / Tracker item), `HANDOFF.md`, `HANDOFF-NEXT.md`, `HANDOFF-ARCHIVE.md` (frozen 4 Sep 26 — predates the Tracker merge; its "tracker" hits are the OIL and medical trackers, nothing for this tab), `BUG-TESTING.md` rows #376 etc., `data-schema.md` §World 3, `data-model.md`, `undo-contract.md`, `feature-impact.md`, `ui-contracts.md` §The Tracker tab and §[ALL-AVAIL-WINDOW], the TRK-CSID / schema-hardening / stable-ids / storage-seam specs, `OVERNIGHT-BUGCHECK-REPORT.md`, every owner-quoting comment under `src/tracker/**`, `smoke.mjs` comments that cite the owner/"the user", the Tracker commit log (66 commits; owner-citing bodies read in full), and the memory folder.

**Key to "Where":** `CL` = raptor-port/CLAUDE.md, the paragraph "The Tracker tab is a THIRD app…" · `KG` = docs/tracker/known-gaps.md · `UIC` = docs/ui-contracts.md §The Tracker tab · `DS` = docs/data-schema.md §World 3 · `DEC` = DECISIONS.md · `OUT` = OUTSTANDING.md · `SMOKE` = scripts/tracker/smoke.mjs · `TT` = src/tracker/tracker.test.tsx · `core` = src/tracker/app/core.js · commit = short hash.

**Reading the Date column.** A date alone = the owner's ruling that day. "approved spec" = a design document the owner approved. "recorded as" = the record paraphrases him (no exact words kept). "carried contract" = a promise the app makes that the records do NOT attribute to the owner (kept because a walker should check it; not counted as his ruling). "the user" in the vendored standalone suite is the owner.

**Conflicts** are tagged **C1–C11** in the last column; the later-dated side wins (standing rule, owner 17 Sep 26).

| # | Date | The ruling, in the owner's words where short (quote exactly) | What the app must do, plainly — something a tester can check on screen | Where it is recorded | Superseded / narrowed by? |
|---|---|---|---|---|---|
| | | **A. ROLES & THE FILE MENU** | | | |
| R1 | 7 Sep 26 (first answer; recorded as) | Everyone views, only admin edits | — (first cut: a member could only read; menus, Edit, date boxes hidden) | commit ef2f6a56 | **SUPERSEDED same day by R2 (C1 — R2 newer)** |
| R2 | 7 Sep 26 (his "second word") | "make it allowed for both admin and member for all access, except the file portion which is admin only" | Log in as member (`us`/`us`): marking, Edit chart layout, both ✎ pencils (courses, syllabi), + Add / rename / remove / reorder students, dates, pace, lulls, Show All, event details all work exactly as for the admin. ONLY the ⇪ File menu (Import, Export) is missing. Admin (`ad`/`a`) sees ⇪ File. ✓ Save changes is there for everyone. | CL; UIC "Everyone edits…"; `src/tracker/role.js`; core FILE ACCESS note; Header.jsx; data-model §permissions table; commit 41cffe09 | File buttons renamed 9 Sep (Open / Import syllabus / Save a copy → Import / Export) — rule unchanged |
| R3 | 7 Sep 26 | (same ruling — enforcement) | The file lock holds at the doorway, not just the hidden menu: a member / logged-out / view-as-member session cannot start an import or export by any route. | CL ("keep both halves"); core `if (fileLocked) return` on importClick, openCopy, saveCopyClick; TT (guard count) | — |
| R4 | 7 Sep 26 | (same ruling — the lock rides the live session, never saved) | Admin login → File menu; member login or logout → none; the admin's "View as member" flip hides ⇪ File at once and closes an open Export dialog; flipping back restores it. | store.ts resetSession / toggleRole; role.js; TT; commit 74d156b0 | — |
| R5 | 7 Sep 26 (carried contract) | "a work tab for everyone" (Shell comment) | The Tracker tab shows in the nav for every login, after Leave War, before Help; Admin stays last. | Shell.tsx nav; UIC ("the eighth tab, after Leave War, for everyone") | Shell.tsx comment still says "edits are the admin's" — stale vs R2 |
| | | **B. STORAGE — THE STORE IS THE RECORD** | | | |
| R6 | 7 Sep 26 (first answer; recorded as) | Keep its own file — 📁 Open / ✓ Save changes work exactly as before | — | commit ef2f6a56 | **SUPERSEDED 9 Sep by R9 (C2)** |
| R7 | 7 Aug 26 (approved spec) | "one file on your computer holds your work"; saving is manual; automatic saving "Considered and rejected" | — (file as master copy, dot on Save, toolbar names the open file and whether it holds students, a write-permission ask on each fresh open) | docs/tracker/specs/2026-08-07-syllabus-file-design.md (marked superseded in part) | **SUPERSEDED in part 9 Sep by R9 (C2)**. Still live from it: R11, R18, R19, R26, R28, R29, R84 |
| R8 | 7 Sep 26 | "I'll add the database eventually" | No cloud / SharePoint sync, no ☁ Cloud or ⟳ Load latest button, no network calls from the tab (no 404s in the console). | KG preamble; `src/tracker/storage.js` header | — |
| R9 | 9 Sep 26 | "I thought it should be auto synced … isn't it duplicating" | Marks, dates, students, event details and a MOVED ball save themselves the moment they land (the header's saved/saving indicator shows it). ✓ Save changes never opens a save-file dialog. No file name, no "last saved", no Charts/Students tick-boxes on the toolbar or File menu. | CL; UIC "The STORE is the record"; KG; storage.js; commit e5c1a7a7 | Supersedes R6, R7 (C2) |
| R10 | 9 Sep 26 | (same ruling) | ✓ Save changes (far right) appears ONLY while a STRUCTURE edit is unsaved — an added/removed event, a prerequisite, a drawn line, an arrow, a font, the JSON editor, a chart undo — and writes it to the store; a mark, a date, an event-details save or a ball drag never lights it; there is never a second Save button; while it shows, the status is not green. | UIC; Header.jsx; SMOKE "a moved ball saves itself — no Save button for a drag", "Save changes appears the moment a structure edit…" | — |
| R11 | 7 Aug 26 (approved spec) → 9 Sep 26 | "closing the tab with unsaved work warns you first" | Reload or close while ✓ Save changes is showing → the browser asks first; with nothing unsaved it does not. | syllabus-file spec §How saving works; App.jsx unload guard; commit e5c1a7a7 ("The unload guard follows it") | Narrowed 9 Sep to unsaved flow edits only |
| R12 | 8 Sep 26 (brainstorm decisions) + owner approved "everything persists on the built site" | The Tracker: "Same door … The database is where the Tracker lives; the file is a format." | On the built site (local `vite preview`), charts, students, marks, dates, pace and lulls survive a reload (per browser). `?fresh=1` starts that visit clean WITHOUT wiping what the browser holds. A corrupt stored record must not kill the tab. | specs/2026-09-08-storage-seam-design.md §Decisions; DS / data-schema §What persists; commit 5ee4a25f | — |
| R13 | 8 Sep 26 (future, database era) | What persists once a database exists: "Everything"; two people editing: "Live-ish … same-cell clash = latest wins with a notice" | Not walkable today — the data is per browser, and two tabs of one browser can overwrite each other. | storage-seam design §Decisions; BUG-TESTING #377 row | Future |
| R14 | carried contract + 9 Sep 26 | per-browser view choices never go into the shared data | The course you were last on, your own last Crew pick and the hidden-bar choice are remembered by THIS browser only (`ocuLocal:`), never in the store or an export; "the last person anyone graded" and their last-marked chart are shared on purpose. | core "this browser's own preferences"; KG traps ("`kLastStudent`/`kLast` stay shared on purpose"); DS | — |
| | | **C. IMPORT / EXPORT AND THE ROUTE TO THE DATABASE** | | | |
| R15 | 9 Sep 26 | "the file feature is for admin to import newly created flow charts … from external areas", and to "export these data … then wipe … then import" | ⇪ File holds exactly two items: ⇪ Import… and ⤓ Export… — no 📁 Open, no Restore, no Import syllabus, no Save a copy. | core File-menu note; Header.jsx; TT "the File menu is ONE Import and ONE Export" | Supersedes the 7 Aug buttons 📁 Open / ⊕ Import syllabus / ⤓ Save a copy (C3) |
| R16 | 9 Sep 26 | "what's the difference between import syllabus vs restore everything. is it possible to just have 1 button?" → "ok" | ONE Import for both jobs. Charts go in chart by chart — a name already here asks replace or add as new. If the file ALSO holds students & marks it asks ONCE ("also contains students and marks…"); No → charts in, "marks are untouched", the file's course not added; Yes → both, "students & marks restored"; the question comes again on the next such file; a charts-only file never asks about people. | commit fce84581; UIC; CL; SMOKE "ONE Import for both jobs" | Supersedes the same morning's three-move menu (C3) |
| R17 | 7 Aug 26 (approved spec) → 9 Sep 26 | "Every other syllabus, every student, and all their marks stay exactly as they are" | Importing charts (charts-only file, or answering No) changes no mark, date or roster; "add as new" leaves the original chart alone. | syllabus-file spec §Dropping in a new syllabus; SMOKE "applying charts writes no roster, mark or date key" | — |
| R18 | 7 Aug 26 (approved "safety decision") | "on ⤓ Save a copy, the students tick-box is off by default — the handover case starts clean and you have to opt in" | ⤓ Export opens with Charts ticked and Students UNTICKED every time (even after you ticked it last time); it lists the charts (only the current one pre-ticked); the done message says which kind of file was written ("It CONTAINS student names and marks…" vs "charts only"). | syllabus-file spec; core Export note; SMOKE "Export resets students to unticked every time"; commit e5c1a7a7 | — |
| R19 | 7 Aug 26 (approved spec) | "a file containing people gets a name that says so" | Suggested name: charts only → `OCU-syllabus-<date>.json`; with students → `OCU-syllabus-WITH-STUDENTS-<date>.json`. | syllabus-file spec §Making the risky case visible; SMOKE file-name checks | — |
| R20 | 9 Sep 26 | (recorded as) Import must not refuse "Safari/the owner's iPhone with 'Use Chrome or Edge'" | On a phone / Safari, ⇪ Import opens a file picker; ⤓ Export writes in place on Chrome/Edge and downloads elsewhere. | fileStore.js; commit e5c1a7a7; KG | — |
| R21 | 10 Sep 26 (bug-check fix, merged on his word) | Import promises "added to what is here" | Bringing in a file whose students already exist lands on the SAME students (squadron person first, then name) — never the same name twice on a course; a student added since the export stays on the chart with their marks. | KG ("Import matches the file's students…"); commit 1b013ae8; TT | — |
| R22 | 11–12 Sep 26 (merged, PR #386) | refuse a different person under an existing callsign | An import naming a DIFFERENT squadron person under a callsign already on the course is refused whole, with a message, before anything is written; the student already there keeps id and marks. | OUTSTANDING-ARCHIVE [TRK-IMPORT]; OVERNIGHT-BUGCHECK-REPORT; commit 16327d8f; TT | — |
| R23 | 11 Sep 26 | (recorded as) "owner's call, 11 Sep 26 — fix the wording" — no behaviour change | The import question says honestly that a student ALSO in the file has their marks replaced by the file's; anyone the file does not name keeps theirs. | commit a7cb7e87; OVERNIGHT-BUGCHECK-REPORT §smaller things | — |
| R24 | 13 Sep 26 (owner guardrail, recorded as) | add the guardrail — do not keep hardening legacy-marks import | Charts import from any backup version; student marks / dates / rosters import ONLY from a current (v3) file — an older file's students are refused with the plain "These student marks were saved by an older version…" message while its charts still go in. | specs/2026-09-13-trk-csid-syllabus-ids-spec.md §19; CL; TT §19 | **Narrowed by R27 (D120)**: older file formats are no longer walked |
| R25 | 13 Sep 26 (owner decision, recorded as) | Safety backup first: a charts-only backup — "charts ticked, students OFF, ALL syllabuses selected" | Export must let him tick EVERY chart with students off and get one charts-only file. | trk-csid-syllabus-ids-spec §Owner decisions 2 | — |
| R26 | carried (owner's working loop) + 7 Aug 26 (approved spec) | He "Exports charts only, sends the file, a session bakes it … and he Imports the charts-only file he gets back"; "Only one of us holds the chart at a time." | A charts-only file imports with no people question and writes no mark. | KG ("The owner's chart loop still holds"); scripts/tracker/bake-user-charts.mjs; syllabus-file spec §Working together | — |
| R27 | **23 Sep 26 (D120 — the NEWEST Tracker ruling)** | "i will export the syllabus that is already hand drawn by me … Then wipe the app, then upload the data into the tracker. So based on this workflow, i dont want u to chase bugs that is already mitigated by this" | THE ROUTE TO THE DATABASE. Export → wipe → Import (current format) must bring back every ball, position, drawn line, arrowhead, font, prerequisite, chart name, chart order, hidden chart and event detail UNCHANGED — the highest-ranked finding. NOT walked: harm living only in data stored today; the start-up converters for older stored data (course / chart / student ids, the old `ocu:` keys); older file formats. Everything done to NEW data after the wipe (marking, students, dates, pace, chart edits) must still work. | DEC D120; KG head note "The route to the database"; OUT [HUMAN-RETEST] and [DB-STEP]; briefs/2026-09-23-tracker-retest-scenarios-brief.md | Extends R119 (D56). Narrows R24, R41, R49, R76, R122 |
| R28 | 7 Aug 26 (approved spec) | "If you replace a syllabus and an event's code changes … that event's marks are stranded" | Known catch, not a bug: replacing a chart where an event's code changed leaves that event's marks behind; same-code events keep theirs. | syllabus-file spec §Dropping in a new syllabus | — |
| R29 | 7 Aug 26 (approved spec) | Deliberately not doing: a "wipe all syllabi" button; "Merging two people's edits" | No wipe-all-charts button; no merge of two people's chart edits. | syllabus-file spec §Deliberately not doing | The same list's "Automatic saving" line is superseded by R9 |
| | | **D. STUDENTS & THE SQUADRON-ROSTER LINK** | | | |
| R30 | 7 Sep 26 (recorded as) | Standalone first — "Students are the Tracker's own list — no link to Raptor's OCU people yet" | — | commit ef2f6a56 | **SUPERSEDED 9 Sep by R31 (C4)** |
| R31 | 9 Sep 26 | "a person is also linked to the tracker and can be selected to be placed in a course" | + Add (Students card) opens "Add a crew member": heading "From the squadron roster", a search box ("Search by callsign"), one row per person — callsign, then e.g. "Pilot · OCU"; OCU first, pilots before WSOs, then callsign; no archived, placeholder (ALL / ALL AVAIL) or ground-crew people. Typing narrows; Enter picks a sole match; a pick adds them UPPER-CASED and the chip gets a small dot + tooltip "On the squadron roster as <callsign>"; the box below ("Or type a callsign") still adds an unlinked student. | UIC "+ Add in the Students card offers the squadron roster"; specs/2026-09-09-schema-hardening-design.md; peoplewire.ts; commit 6b5aedf6 | Where the link is stored moved 10 Sep from a separate `links` block to the student entry (`pid`) — visible only in the export file |
| R32 | 9 Sep 26 | "make sure the functionality is not affected" | The link is additive: a typed student behaves exactly as before. | schema-hardening design header | — |
| R33 | 9 Sep 26 | "it's just going to be the same old way of typing and creating a student by text" (the Tracker goes back out as a standalone app) | With nobody handed over through the people bridge, + Add is the old "Student callsign:" prompt byte for byte. Every Raptor-fed feature must fall back to the old behaviour; a Raptor-only path never becomes the only path. | CL; HANDOFF §Open ("THE TRACKER GOES BACK OUT…"); core addStudent; commit 731a1860; TT | Not walkable inside Raptor (the bridge is always fed) — a unit pin covers it |
| R34 | 9 Sep 26 | "a shared common id for now is ok for the tracker app" | A picked student is tied to Raptor's person: picking the same person again, or typing a name already on the course, lands on the existing student — never a second copy. | commit 731a1860; DS "The person link"; data-model §Enrolment | — |
| R35 | 9 Sep 26 (the owner-requested schema round) | every mark and date write records who and when | Not on screen yet; the export file's mark and date records carry `by` and `at`; with no known user `by` is left off, never "Unknown"; undo restores the earlier stamp. | schema-hardening design Part B; DS; commit 0ad21b92; TT | — |
| R36 | 10 Sep 26 (recorded as, approved scope) | "scope option 1 — Tracker fully re-keyed … no student rename control" | Students become hidden ids; names are labels. | plans/2026-09-10-stable-ids.md; specs/2026-09-10-stable-ids-design.md | **Rename half SUPERSEDED the same day by R37 (C5)** |
| R37 | 10 Sep 26 (PR #381, merged on his "merge live") | rename a student from a pencil on the chip — the label only | The ✎ on each Students-card chip renames the student (upper-cased): marks, dates, pace, lulls, the squadron link and undo stay attached; the new name shows on every chart of the course; a name another student on the course already has is refused; blank / unchanged / cancel do nothing; a colon is allowed; works for a member too. | CL; DS; commit eac12efb + e2f75f05; TT | — |
| R38 | 10 Sep 26 (carried contract) | same person or same name on a course = the same student | Adding the same person/name to a second chart of the course re-uses that student (their pace and lull periods come with them), even if they sit only on a hidden chart. | CL (findEnrolment); TT | — |
| R39 | carried contract (9 Aug system test) | removing a student takes their name and work with them | × asks "Remove X from <chart>? Their marks, dates, pace and lull periods on this syllabus are deleted."; adding the same callsign back starts clean; pace and lulls survive only if they are still on another chart of the course; their undo steps go too. | core removeStudent; SMOKE "removing a crew member takes their name and their work with them", "adding the same callsign back starts them clean" | — |
| R40 | carried contract | ⇅ Reorder crew sits in the Students card beside + Add | Reordering crew reorders the Crew dropdown AND re-slices every ball's ring; survives a reload. | SMOKE "Reorder crew sits in the Students card…", "reordering crew also re-slices the balls" | — |
| R41 | 10 Sep 26 (carried contract) | a course whose conversion did not finish is HELD | + Add / remove / reorder refuse with a message (known gap: the card shows an empty list and a live-looking + Add until pressed; a one-line notice was the follow-up). | KG ("A course whose id conversion cannot finish is HELD"); HANDOFF | **Narrowed by R27 (D120)**: only older stored data can be held — not walked; the notice follow-up is moot |
| | | **E. MARKS, DATES & FAILURES** | | | |
| R42 | 9 Sep 26 | "Failures will also track the date in which the student fails" | In the grading pop-up, + records a failure on the "Failed on" box's day (today unless changed first); − takes the LATEST failure back; this student's failures on that event are listed under the counter. | KG; core popFail; TT; commit f230e000 | — |
| R43 | 9 Sep 26 | "Indicate each failure individually, so when someone fails twice, it should show ST-01, ST01X" | Failures card: one chip per failure — ST-01, ST-01X, ST-01XX; a single failure shows the plain code; worst event first, its failures oldest first; the total counts failures, not events. | KG; SidePanel.jsx; SMOKE failures block; TT | Refines R44 |
| R44 | 16 Aug 26 (recorded as "the user's notation") | first failure is the plain code, each further one adds an X | (as R43) | SMOKE failures block; core failLabel | Refined by R43 (each failure now its own chip) |
| R45 | 9 Sep 26 | the panel "reflects the dates in which they fail when the mouse hovers over it or on the mobile when the user clicks on it" | Hover a failure chip → bubble with its day; tap it on a phone → same bubble; the next touch anywhere puts it away; an undated failure reads "date not recorded". | SidePanel.jsx FailChip; core showFailBubble; SMOKE | — |
| R46 | 9 Sep 26 | "if the user clicks on the title 'failures' then it will show a full lowdown of all failures with a date" | Tapping the Failures title opens the full list, one row per failure with a date box; changing a box re-dates that one failure and its chip; an emptied box leaves it undated (not deleted); Escape closes it; the list has a ceiling and scrolls. | core openFailLog; SidePanel.jsx FailLog; SMOKE | — |
| R47 | 9 Sep 26 | "the details portion will reflect the date accomplished automatically as the date updated. But the user can also manually change the date after" | Grading pop-up "Done on" box (today by default): DCO / DPCO / Marginal land dated that day; changing the box afterwards re-dates the mark; Not done and N.A. drop the day; a flight's day is its Last Flown. | core popGrade / popDoneChanged; SMOKE "Done on"; TT | — |
| R48 | 9 Sep 26 | "And make sure it shows the right data for each student separately." | Another student's Failures card, pop-up list and bubble show THEIR record; switching back brings the first student's back; the ⓘ details bubble ends with the selected student's own record. | TT (dated failures); SMOKE "another student's Failures card shows their own record" | — |
| R49 | 9 Sep 26 (carried contract) | older failure counts read as undated | A count from before dates existed reads as that many UNDATED failures — never an invented day. | KG; core failDates | **Narrowed by R27** — older data not walked; NEW failures must still be dated |
| R50 | 2 Sep 26 (recorded as "the user's rule") | "the most recent flight always wins" | Marking an OLDER flight after a newer one does not drag Last Flown back; a later flight moves it forward; the two Last Flown boxes still take any date typed by hand. | core flownOn; SMOKE "the most recent flight always wins" | — |
| R51 | 16 Aug 26 (recorded as "the user's choice") | every date the app writes is numeric, day first; the echo line under the date boxes removed "at their request" | App-written dates read dd/mm/yy (a lull chip ending on the 31st proves day-first); no spelled-out month; no extra line under the date boxes (the box's own format belongs to the phone). | SMOKE "every date the app writes is numeric, day first"; SidePanel.jsx | — |
| R52 | carried contract (9 Aug system test) | N.A. cannot be failed | An event marked N.A. shows no red failure ticks; + is refused with "“X” is marked N.A., so it cannot be failed."; "Next event" never offers something that cannot be flown yet. | core popFail; SMOKE fault 8 | — |
| | | **F. PACE & LULL PERIODS** | | | |
| R53 | 7 Aug 26 (approved spec, package 4) | lull periods per student, a pop-up calendar, "Copy to…" | "Set lull period" opens a calendar: 1st click = start, 2nd = end and it saves and closes; the month arrows only change month; tapping a lull chip reopens it to change the dates; Escape or a click outside closes without saving half a period; a lull belongs to ONE student (the others' periods and projected end are unchanged); "Copy to…" ticks other students (with select all) and replaces their periods with a copy; the old side-panel calendar and its mode dropdown are gone. | specs/2026-08-07-interface-rework-design.md §Package 4; SMOKE lull block | — |
| R54 | carried contract (9 Aug system test) | pace and end dates belong to the student, not the course | Each student has their own pace (the box can be cleared to type a new number) and end dates; both survive a reload; a course rename keeps them. | SMOKE pace block and fault 6 | — |
| | | **G. THE FLOW CHART — LANDING & VIEW** | | | |
| R55 | 9 Sep 26 (his screenshot) | ST-01 done for A, pick B, and the chart still lit ACG-01 — a bug | The yellow "can be planned next" rings are the PICKED student's: grade ST-01 for A, pick B → ST-01 ringed for B, not ACG-01; picking closes an open grading pop-up. | core setActive; TT "the rings follow the picked student"; commit c5128bb4 | — |
| R56 | 9 Sep 26 | "the flow chart view should remain the same and not snap to something else" | — (interim: a crew pick kept the view) | core setActive comment; commit 1b4d82d8 | **SUPERSEDED the same day by R57 + R58 (C7)** |
| R57 | 9 Sep 26 | "when u pick a crew it will land on their latest work without having to scroll" | Picking a student from the Crew dropdown scrolls to their last-marked event on this chart, centred; picking a student never changes the syllabus. | KG ("Switching student LANDS…"); core setActive / showLastEdit; SMOKE landing checks; commit e43d18f1 | Supersedes R56 (C7) |
| R58 | 9 Sep 26 | "if nothing is clocked … it will show the view based on the first item if that user is selected on from the drop down" | A student with no mark on this chart lands on the chart's FIRST event, centred — never a bare jump to the top-left. | core setActive (firstEventId); SMOKE; commit c37e3d53 | Supersedes the "no mark keeps the view" half of e43d18f1 (C7) |
| R59 | 9 Sep 26 | "centralise the view if its possible when the crew picker is selected" | Every landing is centred, even the first and last events (the chart carries half a view of slack above and below; sideways only once the chart is wider than the board); zooming keeps the event under the middle. | core padBoard; commit 3853384e; SMOKE ("≤4px off centre", "zooming in holds the event…") | — |
| R60 | 9 Sep 26 | "click exactly at the portion of the pokeball that person exist in" | Outside edit mode, each ball's ring is a second crew picker: tapping ANOTHER student's wedge picks them (the Crew dropdown follows), nothing opens and the view does NOT move; tapping the selected student's own wedge, or the ball's centre, opens the grading pop-up. (Known: with 5–6 students the wedges get thin on a phone — raise if he mis-taps.) | UIC "The ring on every ball…"; core ballTap; KG; SMOKE (real clicks); commit 1bc43350 | — |
| R61 | 9 Sep 26 | "cyan edge only" (his pick from a three-way comp) | The picked student's wedge wears a cyan EDGE on every ball — never a fill, so a DCO / DPCO colour is never hidden. | UIC; core; SMOKE "every ball edges the picked crew member's wedge in cyan" | — |
| R62 | 9 Sep 26 | skip ahead and "put DCO a pokeball down the flow chart. The view jumps back up to the above last empty pokeball" | Grading, or + / − a failure, on a ball well down the chart leaves the view exactly where it is. | KG; core redrawKeepView; SMOKE "grading a ball leaves the view where it is"; commit ca78e9ab | — |
| R63 | 7 Aug 26 (approved spec, package 5) + 9 Sep 26 | "Open on the student's last-edited syllabus, scrolled to the last-updated event" | Opening the app (or switching COURSE) restores that course's last-marked chart and lands on the last event marked, centred; it opens on the course you were last on (this browser), on your own last Crew pick, else the last person anyone graded, else the first on the roster; a remembered chart / student / course that no longer exists falls back quietly. Picking a SYLLABUS never jumps you to another chart (the syllabus stays changeable after marking). | interface-rework spec §Package 5; core loadCourseNow; SMOKE "opening the app lands on the last event marked…", "the syllabus can still be changed…", "the app reopens on the course you were last on" | Landing rules refined by R57–R59 |
| R64 | 3 Sep 26 (recorded as "The user, 3 Sep") | clicking ST-01 under Plannable now snaps the chart to that ball; hovering shows name, type, hours, crew and prerequisites; a phone long press shows the same bubble — "the user asked to read, not to jump" | Side-panel event chips (Next event, Plannable now): click → the chart snaps to that ball and rings it; mouse-over → details bubble; phone long press → bubble without leaving the Info tab; phone tap → switches to the Flow chart and lands on the ball. | SMOKE "the side panel's event chips"; SidePanel.jsx EventChip | — |
| R65 | 9 Aug 26 (recorded as "The user asked for this") | "the list should read ST-01, ST-02 ..." | Show All is grouped by event code (ST, ACG, …), not by phase, the course's first families first, each group in number order (ST-9 before ST-10); typing a code shows that family alone, in order; a plain word still searches names and crew. | SMOKE "Show All is grouped by event code" | — |
| R66 | 7 Sep 26 (his iPhone showed chart + panel stacked) | one half at a time on a phone | Below ~1050px only ONE half shows — Flow chart or Info — never the chart with the whole panel under it, including after leaving the tab and coming back or any Raptor refresh. | UIC "On a phone, ONE half at a time"; App.jsx; SMOKE "phone: a Raptor refresh does not bring the side panel back…"; commit d6d68593 | — |
| R67 | 7 Sep 26 (his "full bug check"; carried contract) | the chart survives a logout → login and tab switches | Log out while on the Tracker, log back in, open it → the chart is there. Switching tabs away and back keeps the chart; nothing of the Tracker shows on other pages; Escape / Delete / Ctrl+Z on other pages never reach the chart. | UIC "A second mount redraws the chart" / "Kept mounted"; commit 74d156b0; TT | — |
| R68 | 7 Sep 26 (carried contract) | a viewport-tall column, never a scrolling page | While on the Tracker the PAGE never scrolls — the chart and the side panel scroll inside their own boxes; leaving the tab restores normal page scrolling. | UIC "A viewport-tall column"; TrackerPage.tsx | — |
| R69 | carried contract (found 15 Aug) + 9 Sep 26 ("change it to i icon") | Details mode says marking is off | With ⓘ on, a hint reads "Details mode — … Marking is off." with a Turn off button; tapping a ball shows its brief and does not open grading. | SMOKE "Details mode says it is on / marking is off / offers a way out"; App.jsx | — |
| | | **H. CHARTS / SYLLABI — RENAME, DUPLICATE, HIDE, DELETE, AND CHART DATA** | | | |
| R70 | 13 Sep 26 | "keep charts, reset marks" (+ the id work split into two passes, courses first) | The id upgrade kept every hand-drawn chart and layout and reset the students/marks per chart. Nothing to walk beyond "his charts are all there". | trk-csid-syllabus-ids-spec §Owner decisions 1; trk-csid-course-ids-spec header; CL | Narrowed by R27 — the converter is not walked |
| R71 | 13 Sep 26 (owner decision, recorded as) | relax the colon refusal on syllabus / chart names | A chart / syllabus name (and a student name) may contain a colon — rename, + Add syllabus, Duplicate, Import "add as new" all accept it; a COURSE name with a colon is still refused ("A name can't contain a colon (:), because the app uses it to file the record."). | trk-csid-syllabus-ids-spec §8; CL; TT "colon relaxation (§8)" | Supersedes the 9 Sep "a colon is refused in every name" (C6) — UIC's + Add bullet still says so |
| R72 | 13 Sep 26 (stable ids) | renaming a syllabus is a label change that moves nothing | ✎ Rename syllabus (built-ins too) changes only the label; marks, layout and students stay; a renamed built-in keeps its name through app updates; the label travels through Export / Import; a name already used is refused. | CL; core renSyl | — |
| R73 | 13 Sep 26 | delete is a real sweep; hidden ≠ deleted | 🗑 Delete syllabus asks ("This removes its flow, its layout and every student's marks on it, in every course."); for a built-in: "you can bring it back later from ⇅ Reorder"; a built-in with saved edits offers "Delete it" or "Revert edits only"; the last syllabus cannot go ("Keep at least one syllabus."); restoring from ⇅ Reorder brings the shipped chart back with no students. | CL; core delSyl / restoreHiddenSyl | — |
| R74 | 13 Sep 26 (part of "keep charts, reset marks") | a duplicate copies the flow + layout under a fresh id with an EMPTY student layer | ⧉ Duplicate syllabus asks for a name ("<name> copy"), makes a copy with the same balls and lines, and starts with NO students and no marks. | CL; core dupSyl; TT | The menu tooltip still promises "including every student's marks" (stale) |
| R75 | 13 Sep 26 (carried contract) | + Add syllabus = a brand-new empty sheet | + Add syllabus makes an empty chart (no events, no marks), switches to it, and hints how to start editing. | core addSyl | The tooltip ("from the current structure") and the hint (hit "✎ Edit") are stale |
| R76 | 14 Sep 26 (recorded as "at the owner's request") | a hidden built-in's edited chart stays a SHOWN custom through the upgrade | Converter behaviour — not walked. | commit 3245ece8 | Narrowed by R27 |
| R77 | 9 Aug 26 (recorded as "at the user's request") | "Tx 2024 was deleted at the user's request" | The chart list offers the user's four charts in the default order 2024 · 2026 · Tx 2026 · A/G - A/A 2026 (2026 opens by default); no "Tx 2024", no "Tx 2026 v2". | SMOKE "the deleted Tx 2024 chart is gone…", "the default order is the user's four charts"; data/syllabi.js | — |
| R78 | 8–10 Aug 26 (recorded) | his hand edits "SUPERSEDE the map/table pins"; the course map is authoritative over its tables ("the owner confirmed"); SA(S)-3 on Tx kept ("User chose keep, twice, 8 Aug"); SAT-1 waits for SA-5 on both Tx courses ("The user's call, 8 Aug"); the DAAR / NAAR refresher chain "added … at the user's request"; the A/G - A/A sim chain SA(S)-2→-3→-4→-5 stays joined (he "confirmed twice"); four A/G - A/A links "approved by the user"; the ST-10 cut from LASDT(S)-1 was "a mistake"; EPE → AAS-04 "is not a missing link" | The shipped charts carry exactly these links (pinned by the smoke's chart-data checks). Never "correct" a chart back to the map; ask him for the rendered page before changing a link. | KG §Things that will bite; SMOKE `USER_EDITS_2026` / `USER_EDITS_AGAA` / Tx blocks; `scripts/tracker/course-map-*.json` | — |
| R79 | carried (his 132 moved boxes, 68 drawn lines) | `gen-agaa-layout.mjs` is history | Never re-run the A/G - A/A layout generator — the layout on screen is his hand-edited one. | KG | — |
| R80 | 23 Sep 26 (D62) | "u can ignore the tracker syllabus file" | Do not flag anything in the Tracker's syllabus data (`src/tracker/data/`) or its two smoke lines in any privacy / "what can a stranger read" sweep. | DEC D62; memory `app-carries-no-unit-designation` | Narrowed by D64 (R82) for the one hint; narrows D58 (R81) for the syllabus data |
| R81 | 23 Sep 26 (D58; his words deliberately not reproduced) | the squadron abbreviation and the service name must never appear; bare "142" is fine | The Tracker's own screens and files (heading "<COURSE> PROGRESS TRACKER", dialogs, export file names) carry no squadron abbreviation and no service name. | DEC D58 | Narrowed by D62 for the syllabus data |
| R82 | 23 Sep 26 (D63 → D64; words not reproduced, in substance) | "only the grey hint text in the format field, change it to F-15" | The event-details box's "Type / format" grey hint reads "e.g. Lecture, OFT/AMT, 2 x F-15". | DEC D64; Modals.jsx | **D64 narrows D62/D63's "the Tracker is left entirely" (C8)** |
| R83 | 9 Sep 26 (recorded as) | the syllabus event names and hours: "owner will remove those himself"; "the owner is replacing the event details himself" | Don't touch the shipped event wording; the event details (name, format, hours, crew, prerequisites) are editable in the app (the pop-up's ✎ Edit details, Show All's inline editor), save themselves, survive a reload, and "Reset to doc" restores the shipped values; an edit on one chart (e.g. Tx) never rewrites another chart's wording. | commit 5d9067cb; data-schema §Loose spots 11; SMOKE Show All editor checks | — |
| R84 | 7 Aug 26 (approved spec) + 2 Sep 26 | Plan 9 — "Nothing may be deleted until the owner has saved a file, reopened it, and said in his own words that his syllabi are inside." | The built-in charts stay shipped in the code; there is no "empty app". | KG §Still open (Plan 9); syllabus-file spec §The changeover | Still blocked (D120 does not lift it) |
| | | **I. COURSES** | | | |
| R85 | 13 Sep 26 (course ids — "a permanent hidden tag") | renaming a course is a label change that moves nothing; the id is never shown | ✎ Rename course changes only the label: students, marks, pace and lulls stay; the heading "<COURSE> PROGRESS TRACKER" and the dropdown show the new name; names are upper-cased; a taken name, a colon, or a reserved name (courses, links, master, lay, SYLLABUS EDIT) is refused. | CL; OUT [TRK-CSID] ("permanent hidden tag"); core renCourse; TT | The Students-card key ball prints the hidden course id, not the name (stale list) |
| R86 | carried contract | + Add course | Prompts "New course name (e.g. 26BBSG)", adds it at the TOP of the list, empty, on the current chart ("add students to begin"). | core addCourse; SMOKE "a new course is added at the TOP" | — |
| R87 | carried contract | Delete course | Asks "Delete course X? (marks remain in storage)"; the last course cannot go ("Keep at least one course."). | core delCourse | KG's "re-create a course under the same name and its old roster comes back" is stale since course ids |
| R88 | 2 Sep 26 review (carried contract) | unsaved flow edits are asked about before the chart is swapped | Switching / adding / renaming / deleting a course, or switching syllabus, while ✓ Save changes shows asks "You have unsaved flow edits on X … Discard them and …?"; No keeps the course and the edits; Yes switches and the status stops claiming unsaved edits. | core leaveFlowEdits; SMOKE "2 Sep review" | — |
| R89 | carried contract | ⇅ Reorder courses / syllabi | Reorder changes the dropdown order and survives a reload; only one reorder list is ever open. | SMOKE course-order checks; KG (#ordModal) | — |
| | | **J. ARRANGE / EDIT MODE** | | | |
| R90 | 7 Aug 26 (approved spec, quick wins) | "Dock below the header as a strip in normal flow"; Fit / Reset layout move into the strip | In edit mode the tool strip sits below the bar and never covers the chart (ST-01 clickable and draggable); turning edit on adds or removes no bar buttons; ⤢ Fit and Reset layout live in the strip. | specs/2026-08-07-interface-quick-wins-design.md; SMOKE edit-toolbar checks | — |
| R91 | 9 Sep 26 | "fuse it into the syllabus edit icon" | "✎ Edit chart layout" is the first item in the Syllabus ✎ pencil; while editing, the pencil lights and the item reads "✓ Done editing chart". | UIC bar bullet; Header.jsx; TT | Supersedes the always-visible ✎ Edit button of the 7 Aug bar (C9) |
| R92 | carried contract (9 Aug system test) | Reset layout says what it deletes | Reset layout warns that the drawn lines go too, does clear them, and ↶ brings them back. | SMOKE fault 3 | — |
| R93 | 2 Sep 26 (carried contract) | the edit hint must not push the chart about, nor sit on the legend | Switching tools never moves the chart; the hint takes exactly one line; at rest (Move) it does not cover the colour legend; on a phone it sits above the Flow / Info / Show All tabs. | SMOKE hint checks; App.jsx | KG's "The edit-mode hint overlays the colour legend" is stale for the resting state |
| R94 | 7 Aug 26 (approved spec) | label the two zoom controls | The chart zoom reads "Chart", the panel zoom reads "Panel". | quick-wins spec; SMOKE | — |
| R95 | 2 Sep 26 review (carried contract) | Escape everywhere | Escape closes the ball editor, the event-details editor, the reorder list, Export, Show All, the grading pop-up, the lull calendar (without half-saving) and the failures list; on a confirm, Escape answers "no" and closes only the confirm — the editor that asked stays open and nothing is deleted. | SMOKE "2 Sep review" | — |
| | | **K. TOOLBAR & PHONE LAYOUT** | | | |
| R96 | 9 Sep 26 | from the mock-up: "Ok proceed"; "change it to i icon" | Bar, left to right: Crew · Course dropdown + ✎ · Syllabus dropdown + ✎ · ⓘ · ↶ ↷ · ☰ Show All · ⇪ File (admin only) · Find event · ⌃ (hide), then a gap, then ✓ Save changes alone in the far-right corner — everything you choose or do stays left of it. Desktop: one row at 1440 wide. | UIC "The bar layout, reordered 9 Sep 26"; Header.jsx; SMOKE bar-order checks; commit e00b0676 | Supersedes the 7 Aug four-menu bar (C9) |
| R97 | 9 Sep 26 ("owner's mobile ask") | Save pinned to the far right on a phone | On a phone, ✓ Save changes (when showing) sits at the far right of its row; the bar is the same height with or without it. | tracker.css; commit e00b0676; SMOKE | — |
| R98 | 9 Sep 26 | "have the option to hide this bar so that the space can be maximised" | ⌃ at the end of the bar hides the whole bar; a slim strip naming the current crew member ("Show bar") brings it back; the chart grows into the space; shown by default; the choice is remembered by THIS browser only; works on desktop too. | KG; core toggleBar; App.jsx; SMOKE; commit 7f93d6cf | — |
| R99 | 9 Sep 26 | "When I am searching for a syllabus event in the search bar, it gives me a drop down menu on the prediction of the syllabus related to the typed text and it changes as there's more text." | Typing in Find event lists every matching event (code, type-colour dot, name) under the box; more text narrows it; ↓ ↑ move ring and highlight together; Enter walks forward; clicking a row rings that ball, keeps the text, shuts the list and puts the phone keyboard away; clicking back in reopens it; Escape / ✕ clear it; a search that finds nothing leaves the chart alone. Phone: 🔍 opens a full-width strip whose list is full width and tappable. | UIC "The Find box lists its predictions"; Header.jsx; SMOKE; commit ccf066c6 | — |
| R100 | 2 Sep 26 (carried) + 9 Sep 26 | Show All is a plain bar button; on a phone the Show All TAB does its job | Desktop: ☰ Show All on the bar, right of the syllabus group; phone: that button is hidden and the Show All tab (title "All events", one short line) is used. | SMOKE; Header.jsx | Supersedes the 7 Aug "View ▾ → ☰ Show All" menu (C9) |
| R101 | 7 Aug 26 (approved spec, package 3) | phone: chart fits the width; room below the last event | On a phone the chart fits the screen width (only scrolls up/down) and the zoom "reset" returns to that fit, not 100%; you can scroll well past the last event, clear of the zoom control; Crew is the leftmost control; every control is on screen (no sideways-scrolling bar); every menu is tappable. | interface-rework spec §Package 3; SMOKE phone block | — |
| R102 | (recorded as "The user asked for two rows instead, accepting the height") | phone bar in two rows | The phone bar shows every control in two rows. | SMOKE "the phone bar shows every control, in two rows"; KG ("The phone bar is two rows"); UIC ("the phone's two-row bar") | Pinned only as ≤3 rows — the suite says it "lands at three" (stale list) |
| R103 | (recorded as "The user's requirement, in their words") | "everything must be visible in one look so it can be photographed — except Lull periods" | Phone, Info tab: every side-panel statistic except Lull periods fits one screen without pinching (at two students; a fifth may start it scrolling — known). | SMOKE "every statistic but lull periods fits one phone screen"; KG | — |
| R104 | 16 Aug 26 (approved) | Lull periods alone on the last row "at the user's request"; Failures beside it | The side panel's last row is Lull periods + Failures, paired; the Students card is the same size as the Overall card beside it; the key ball's names are big and never run off it. | SMOKE "the last row is Lull periods and Failures, paired", "the Students card is the same size…" | — |
| R105 | (recorded as "the user asked for that layout back after a stacked version shipped") | Set pace, End date A and End date B share ONE row | Pace card: Set pace, End date A and End date B on one row; no date box runs past its box or the card; each date box has room on an iPhone. | SMOKE "set pace and both end dates sit on one row" | **Supersedes the 7 Aug quick-wins layout (Set pace alone on row 1, end dates on row 2) (C10)** |
| R106 | (recorded as "the user preferred it") | the info panel opens at what used to be 80% | The Info panel opens smaller (the old 80%) while its zoom reads 100%; the desktop chart shows full size, not shrunk to fit. | SMOKE "the info panel opens at 100% on its new baseline", "the desktop chart is still shown full size" | — |
| R107 | 7 Sep 26 — OPEN QUESTION, not a ruling | "Known: the tab keeps its own dark colours (say if you want Raptor's)" | The Tracker keeps its own dark look; whether it should take Raptor's has never been answered — ask, don't change. | UIC "Its own look, inside the section"; HANDOFF §OWNER'S DEVICE PASS; BUG-TESTING #376 | Open |
| | | **L. UNDO** | | | |
| R108 | 9 Sep 26 | "Create an undo redo button for tracker even for all users … moved to the main top bar for all users to access and not only isolated to under edit. Make sure u check carefully weird scenario when u undo and redo." | ↶ ↷ sit on the bar for everyone (a member too), right after ⓘ; greyed when there is nothing to take back; the tooltip names the step ("Undo the mark on ST-05 for STUDENT A (Ctrl+Z)"); ONE history holds grades, failure counts, date boxes (Last Flown ×2, down days, upchit) and chart edits, in order. | UIC "↶ ↷ are on the bar for EVERYONE"; core undo section; Header.jsx; TT; SMOKE; commit ccf066c6 | The 13 Sep global-undo ruling (R112) has not reached the Tracker yet |
| R109 | 9 Sep 26 (the "weird scenario" list) | (same ruling) | Undoing a mark of a student you have moved away from switches the Crew picker to that student and closes the grading pop-up; keystrokes into one date box within 2 s are one step; a removed student's steps vanish (undo never marks nobody); Ctrl/⌘+Z, Ctrl+Y and Ctrl/⌘+Shift+Z work only while the tab is up, never inside a text box or under a question. | same; TT (history cases) | — |
| R110 | 9 Sep 26 (same ruling — its boundaries) | (same ruling) | Both stacks clear on a course or syllabus switch, an Import, and ✓ Save changes (↷ is greyed after a syllabus switch); NOT undoable: adding / removing / renaming students, courses and syllabi, event details, Import; undoing a chart step lights ✓ Save changes. | same; SMOKE "Redo after changing syllabus … the button is greyed" | — |
| R111 | 13 Sep 26 (recorded as — the end-state, with "build near-term undo session-scoped and per-user") | undo is scoped to the current LOGIN SESSION and never affects another user | After a logout and a login as someone else, the Tracker's ↶ must not offer the previous user's steps. | memory `future-undo-semantics-multiuser`; OUT [GLOBAL-UNDO] and [GU-MAYREV] ("Clearing the timeline on logout is the near-term direction") | **Likely NOT met today** — the Tracker's stacks are cleared only on a chart change or Save (stale list) |
| R112 | 13 Sep 26 (owner decision) | [GLOBAL-UNDO] — "One undo for the whole app, before the database step" | Not built for the Tracker (its phase 4 is unbuilt; "a whole Import as one undo step" is deferred into it). Walk only that the two undos don't cross: a Ctrl+Z on another page never undoes a Tracker mark, and the Tracker's ↶ never undoes a schedule edit. | OUT [SYNC-INTEG] decision, [GLOBAL-UNDO], [CMDL-FINISH]; specs/2026-09-17-arch-stack-3-global-undo-design.md (Phase 4) | Open |
| | | **M. ANYTHING ELSE — SEAMS, APP-WIDE RULES, PROCESS** | | | |
| R113 | 23 Sep 26 (D66) | "close it" | Opening the Tracker tab closes the schedule's ALL AVAIL window. | DEC D66; ui-contracts §[ALL-AVAIL-WINDOW] | — |
| R114 | 4 Sep 26 (app-wide) | a click-open popup closes on a click outside it | Every Tracker pop-up a tap opens — the three menus, the grading pop-up, the lull calendar, Copy to…, the failures list, the Find list, the event bubbles, the dialogs — closes on a tap outside it. | CL §Standing UI rules; SMOKE "a menu opens, and clicking away closes it again" | — |
| R115 | 2 Sep 26 (app-wide) | a control the user taps repeatedly must not move under them | The lull calendar's ‹ › arrows, ↶ ↷, the zoom − / + and the Save slot hold still as the content changes (the Save slot keeps its width). | CL §Standing UI rules; Header.jsx save-slot comment | — |
| R116 | 25 Aug 26 (app-wide) | "Is it possible to create a production ready interface." | No prototype / demo / "session-only" caveats in the Tracker's words; every label and tooltip says what the control really does. | CL §Product bar | The Duplicate / Add syllabus tooltips and the "Empty sheet ready" hint break it (stale list) |
| R117 | 12 Aug 26 (app-wide) | "see how I am also concerned about user interface… remember this when making decisions" | Walk phone AND desktop and report UI faults beyond the item being checked. | CL §Product bar | — |
| R118 | 21 Sep 26 (D4) + 23 Sep 26 (D85, D86) | "This also means that all the previous bug tests we did there will be bugs not captured. Because I didnt test them when i told u that u would test like a human since." · "actually i would like to execute the tracker first instead" · "yes parallel" | Re-test by driving the running app — every surface, both orders of every gesture. This chat serves on port 4180, takes ruling numbers from D120, and never starts a full gate run while the amendment chat (port 4173) is mid-run. | OUT [HUMAN-RETEST]; DEC D85, D86; HANDOFF-NEXT | — |
| R119 | 23 Sep 26 (D56, D54) | "Make sure the bug checks dont waste time catching these bugs in the future. It will not happen because ill clear all the demo data anyway before shiping this app into a real database." | A problem living only in data already stored, while the code is right going forward, is not a finding; the whole store is wiped before the database. | DEC D56, D54; docs/bug-check-order.md §2b | Extended for the Tracker by R27 (D120) |
| R120 | 17 Sep 26 | "most probably the latest instructions I gave is usually the most correct one to follow" | Where two rows here disagree, follow the later one (C1–C11). | CL §How to work here | — |
| R121 | carried (the standalone public-repo rule) | "No student name, mark or date may enter it" (the repository) | The shipped starting data and the demo rosters name nobody (placeholders only); an Export with students unticked names nobody. | KG §Things that will bite; SMOKE "shipped starting data names nobody", "demo rosters use placeholder names only" | **Its reason — "The repository is public" (KG; data-model §13 "stays that way") — is stale since D59 made the repo private (C11)**; the rule itself was never withdrawn |
| R122 | 11–12 Sep 26 (his two overnight decisions) | Decision A: "accept it as a documented limitation until the DB step?" · Decision B: build the safe resumable legacy import | [TRK-DISK] (a failed disk write during an upgrade or rename) is deferred to the database step; B was built and merged (PR #387). Nothing to walk. | OUT [TRK-DISK]; OUTSTANDING-ARCHIVE [TRK-LEDGER]; KG; OVERNIGHT-BUGCHECK-REPORT | The rename half of A dissolved when renames became label-only (13 Sep); the upgrade half is not walked (R27) |
| R123 | open backlog, not a ruling | [TRK-ATTEMPTS] "Remember a student's earlier attempts" | Not built: only the latest grade plus the dated failures are kept. Nothing to walk. | OUT [TRK-ATTEMPTS]; data-model §Attempt | Open |

## Conflicts found (the later-dated side wins)

- **C1** R1 (7 Sep, everyone views / admin edits) vs **R2** (7 Sep, his second word) — R2.
- **C2** R6 (7 Sep, keep its own file) and R7 (7 Aug approved: one file, manual saving, auto-save rejected) vs **R9** (9 Sep, the store is the record) — R9.
- **C3** the 7 Aug approved buttons (📁 Open / ⊕ Import syllabus / ⤓ Save a copy) and the 9 Sep morning three-move menu vs **R15/R16** (9 Sep, "just have 1 button") — R15/R16.
- **C4** R30 (7 Sep, standalone first) vs **R31** (9 Sep, linked to the squadron roster) — R31.
- **C5** R36 (10 Sep scope: no student rename control) vs **R37** (10 Sep, rename pencil merged) — R37.
- **C6** the 9 Sep schema round "a colon is refused in every name" (still in UIC's + Add bullet) vs **R71** (13 Sep: charts/syllabi and students may carry a colon; only course names refuse) — R71.
- **C7** R56 (9 Sep, "remain the same and not snap") vs **R57/R58** (9 Sep, later: land on latest work / on the first event) — R57/R58.
- **C8** D62/D63 ("the Tracker is left entirely") vs **D64 / R82** (the hint reads the bare type) — D64.
- **C9** the 7 Aug approved top bar (Course ▾ / Syllabus ▾ / File ▾ / View ▾ menus, a separate ✎ Edit button) vs the 2 Sep plain Show All button and **R91/R96/R100** (9 Sep reorder) — 9 Sep.
- **C10** the 7 Aug approved pace card (Set pace on its own row) vs **R105** (all three on one row, his later ask) — R105.
- **C11** "The repository is public (and stays that way)" (KG, data-model) vs **D59** (23 Sep, private) — D59.

Narrowings, not counted as conflicts: D62 narrows D58 for the syllabus data (R80/R81); D120 narrows R24, R41, R49, R70, R76, R122.

## Promises with no test that names them (only where clearly unpinned)

| Ruling | What is not pinned | Evidence |
|---|---|---|
| R85 (course ids are hidden; a rename is a label) | Nothing reads the course label in the centre of the Students-card key ball — the smoke's key-ball check drops that text on purpose ("[0] is the course"). The code prints the hidden course id there (`core.js` renderKeyBall uses `course`, the id), while the heading uses the name. | SMOKE key-ball block; core renderKeyBall |
| R111 (undo per login session) | No test logs out, logs in as someone else and checks the Tracker's ↶ is empty; the logout → login test only checks that the chart is redrawn. The stacks are cleared only on a chart change or Save, and the Tracker is not in Raptor's session-reset list. | TT "a second mount redraws the chart"; core loadCourseNow / clearDirty; state/view.ts VIEW_RESET comment |
| R27 (D120 — export → wipe → import) | No check drives the real ⤓ Export dialog with EVERY chart ticked, then a wipe, then the ⇪ Import button; no check covers a hidden or deleted built-in, or a hand-made (duplicated / added) chart, through that round trip. The smoke's round trips call the internal collect / apply functions on the visible charts only (Export and `collectCharts` both skip hidden charts). | SMOKE "a chart survives a full save-and-reopen unchanged", "the chart order you saved is the order you get back"; core orderedSylIds / collectCharts / openCopy |
| R114 (a popup closes on a click outside it) | Only the menus' click-away is pinned. The grading pop-up, lull calendar, Copy to…, failures list and the dialogs are pinned for Escape only. | SMOKE "a menu opens, and clicking away closes it again"; no test clicks `.lullback`, `#dlgOverlay` or the pop-up's outside |
| R102 (phone bar in two rows) | The check holds it to "within three rows", not two. | SMOKE "the phone bar stays within three rows" |
| R74, R75, R116 (production copy) | No test reads the Syllabus-pencil tooltips or the "Empty sheet ready — hit “✎ Edit”…" hint, which is how the stale words survived. | Header.jsx; core addSyl |
| R82 (D64) | No test pins the event-details format hint text. | Modals.jsx; tests grep clean |
