# D260, D261, D262 — his answers to the absence re-test's questions, built and FULL-checked (27 Sep 26)

Branch `claude/absence-record-d147-af6a50` (PR seejiaokai/Raptor#444), built in the worktree
`five-flags-batch-build-ef7d85` from `9f7814d9`. The order this follows: `raptor-port/docs/bug-check-order.md`. The rulings:
`.claude/rules/decisions/oil.md` D260, D261; `.claude/rules/decisions/leave-war.md` D262. The backlog: `OUTSTANDING.md`
`[ABSENCE-ASK]` items 1 and 3, `[LW-MOVE-ONE-CHIP]`. Fable's scenario design (rank 1, before the build):
`docs/superpowers/briefs/2026-09-27-d260-d262-scenarios-fable.md`. Walk scripts `scripts/handpass/mv/`; results
`docs/handpass/parts/2026-09-27-d260-d262-{mv10,mv20}-{desktop,phone}.txt`; pictures `docs/img/handpass/2026-09-27-d260-d262/`.

## 0. Where this stands (kept current)

- **Built**, each red first: D260 `6ad8491f`, D261 `2d7e92b3`, D262 `e7b70068` (+ `04f4cc5a`, found writing the walk).
- **Walked** at desktop 1440 and a 390 touch phone (real touch events over CDP): awards 21 / 21 each width; Move 20 / 20
  desktop, 18 / 18 phone. No console errors.
- **Break tests:** 19 wires, each turns a named test red (§6).
- **Gates:** §9. **The two code reads:** §8. **His look:** §10.

## 1. The eight questions → FULL

| # | question | answer | why |
|---|---|---|---|
| 1 | Money (earned leave, D25) | **YES** | Delete / Clear remove OIL awards — a man's OIL balance drops (and comes back on Undo) |
| 2 | The published record | NO | the landing rules, the dotted mark and the published war's finished paperwork are unchanged; only the door into a move and the words of a clear changed (a published war's Delete was walked, A6) |
| 3 | Saved data | **YES** | the awards removed are stored records |
| 4 | A shared drawer | **YES** | `wireMove` serves the day sheet's Move, the drag-selection's Move… and the event move |
| 5 | A new gesture or mode | **YES** | the one-chip Move; press-and-drag; the mouse's edge scroll; the outside-tap cancel |
| 6 | A new surface | **YES** | the member's read-only award sheet |
| 7 | Roles | **YES** | a member opens his own award; a member's clear takes none |
| 8 | The warning list | NO | no warning or rule text is touched |

**Tier: FULL.** Told to him at the start of the chat with the steps.

## 2. The rulings, and the agent's readings he can correct

- **D260** (his "B"): a dragged block's Delete removes everything in it, awards included, and names each first; the
  one-day Clear the same; awards never move. **Readings:** (a) the bid sheet's RANGE Clear is a third door that removes
  awards, so it names them too; (b) an award beneath any leave goes with the Delete (it stayed under a leave before);
  (c) a block holding ONLY awards now offers Delete (not Move) — it is not an empty box; (d) a member's clear never
  takes an award (an award is the admin's, N11), so it names none; (e) on a published war the approved leave stays and
  the note SAYS it was skipped even when the award beside it went (Fable S18).
- **D261** ("3 yes"): **Readings:** where the bid sheet opens for him (inside the window) it stays, its foot reading the
  award back; everywhere else a read-only "Your OIL award" sheet — reason, given by, days, "Given by an admin — only an
  admin can change it", ✕ only; a day with more on it opens the tap list as before; another man's award stays shut.
- **D262**: **Readings** (a)–(e) in the ruling's row, as built, plus: (f) desktop — the chip follows the mouse, so
  carrying the mouse to the grid's left / right edge IS the drag and scrolls the days (sideways only: a move lands by
  date, on any row); a press-and-drag (mouse or a held finger) scrolls both ways and lands on the release; (g) "an
  empty area outside the grid" = not the grid's card (its counts, month buttons, header, days, drawer, the floating date
  header, the foot scrollbar) and not a control, the banner, a sheet or a popup's shade; (h) the event move shares the
  machine and gets the same behaviour; (i) the unmounted `DecisionSheet` and its date box are removed; (j) the tap
  list's per-record Move… (a day with several records) keeps its date box — not in D262's reading — **a question for
  him**; (k) leaving the Leave War ends a move.

## 3. Every finding

| # | finding | found by | status |
|---|---|---|---|
| S1 | A double-click on Move: the second click landed the chip on whatever day sat under the button | Fable (scenarios) | **FIXED** `e7b70068`, red first — a move ignores clicks for its first 400 ms (`select.ts MOVE_SETTLE`); walked C8. **Refined after the gate run:** the e2e "a loose box moves the inputs present" clicks a landing day straight after Move… and the time-only guard swallowed it — the guard is now a place as well as a time (only a click within 12 px of where Move was pressed is ignored), red first (`movewire.test.ts`) |
| S2 | The sheet closes under a still mouse that may rest in an edge band — the grid would run by itself | Fable | **FIXED** `e7b70068` — a band the mouse was already in must be left first; `movewire.test.ts` |
| S6 | Landing on its own day read "Nothing to move." and the phone offered a Confirm that then failed | Fable | **FIXED** `e7b70068` — "It is already on that day — pick another day.", no Confirm; walked C2 |
| S9 | A move left on followed the admin onto the next page (its ghost and its listeners) | Fable | **FIXED** `e7b70068` — leaving the Leave War ends it (`subLwScreen`); walked C10 |
| S11 | A finger's long press (Android's right-click) cancelled the move silently | Fable | **FIXED** `e7b70068` — only a mouse right-click cancels; walked C12 |
| S17 | Clear asked about one day, then a range picked: would the range go unnamed? | Fable | **PINNED** — the span change resets the ask (built with D260); `awardclear.test.tsx` |
| S18 | A published war's Delete took the award and closed as done though the approved leave stayed | Fable | **FIXED** `e7b70068` — the leave is counted skipped; walked A6 |
| S3 | A bid sharing its day with an award cannot be moved by ANY door (the award is the day's top record; the tap list's bid line has no Move) | Fable | **FILED** `[LW-MOVE-BENEATH]` (the same root — a move reads the day's top record); pre-existing |
| NEW-1 | A tap on the Legend's or the under-manned list's shade (to close it) ended the move | the host, writing the walk | **FIXED** `04f4cc5a`, red first (`movewire.test.ts`); walked C5b |
| Q-1 | The tap list's per-record Move… (war-approved leave on a day with several records) still moves by a date box | the host | **A question for him** (§10) — D262's reading (a) names the two one-day sheets |

## 4. The roll-call

Widths D desktop 1440, P phone 390 (touch); roles A admin, M member (Ranger, `bane`).

| # | place | shows it / acts on it | must not, because… / painted over | walked | result |
|---|---|---|---|---|---|
| R1 | Bid sheet — Clear, one day | names the award, "Clear — sure?", second tap takes it | the note sits in the sheet above the +OIL row | A1 D·P A | PASS; OIL −1 then back on Undo |
| R2 | Bid sheet — Clear over a picked range | names each award (with its day when one man has two) | — | A2 D·P A | PASS |
| R3 | Drag-selection — Delete | names every award, takes everything; one Undo back | on a phone the 3-award confirm wraps to two lines; the grid above stays reachable | A3 D·P A | PASS |
| R4 | Drag-selection — a block of awards only | Delete offered, Move not | Move must not: an award never moves | A4 D·P A | PASS |
| R5 | Drag-selection — Delete on a published war | names and takes the award, says the leave was skipped | the approved leave must stay (finished paperwork) | A6 D·P A | PASS |
| R6 | Drag-selection — Move… over leave + award | moves the leave only ("1 entry") | the award must not move | A5 D·P A | PASS |
| R7 | Drag-selection — Fill over an award | must not remove it: a leave code replaces requests only (`setCell`; N13, N16) — unchanged | — | not walked (door unchanged) | unit: `oil-award-add.test.ts` |
| R8 | Tap list — the award's own Clear / Edit… | the award's own door, names itself — unchanged | — | not walked (unchanged) | `daylist.test.tsx` |
| R9 | +OIL — Remove; the OIL tracker | the award's own doors — unchanged | — | not walked (unchanged) | `creditdoor.test.tsx`, `oiltracker.test.tsx` |
| R10 | A member's Clear | must not name or take an award (the admin's, N11) | — | B1 D·P M | PASS |
| R11 | A member's drag Delete | must not (same gate, `awardsIn` empty) | — | unit only | `awardclear.test.tsx` |
| R12 | His own award — OPEN, inside the window | the bid sheet, the award read back at its foot, no +OIL | — | B1 D·P M | PASS |
| R13 | His own award — OPEN, outside the window | "Your OIL award", read only | nothing to press but ✕ | B1 D·P M | PASS |
| R14 | His own award — CLOSED / PUBLISHED | the same read-only sheet | — | B2, B3 D·P M | PASS |
| R15 | His own award — DRAFT | the same branch (`ownAwardOnly`, no stage term) | — | not walked (no draft war holds an award in the demo) | unit: `ownaward.test.tsx` "DRAFT" |
| R16 | Another man's award (member) | must not open | — | B1 D·P M | PASS |
| R17 | A day with more than one record (+n) | the tap list reads the award (unchanged) | — | not walked (unchanged) | `daylist.test.tsx` |
| R18 | Bid sheet — Move | no date box, never greyed; picks the chip up | the sheet closes; the banner "Tap a day to move 1 entry"; desktop: landing painted under the mouse | C1 D·P A | PASS |
| R19 | Landing: a free day / a refused day / its own day / after CLOSED | lands undecided / refused with its reason, stays on / "already on that day" / the dotted mark + "moved from" | a refused tap stages no Confirm | C1–C3, C9 D·P A | PASS; LVE unchanged by a move |
| R20 | Month buttons while moving | the move stays on | — | C4, C11 D·P A | PASS |
| R21 | The grid's edges | desktop: the mouse at the right / left band scrolls the days, the middle stops; a press-and-drag (both widths) scrolls and lands on the release | the banner is not grid: the mouse over it never scrolls | C6 D; C7 D·P | PASS |
| R22 | An empty tap outside the grid | ends the move, nothing moved | a control (Manning, Legend), its shade, the banner, a sheet must not | C5, C5b, C11 D·P A | PASS (after NEW-1) |
| R23 | A quick swipe / a long press (phone) | the swipe scrolls and stages nothing; the long press stages the day, never cancels | — | C12 P | PASS (the step's aim re-run, see §5) |
| R24 | A double-click on Move (desktop) | lands nothing | — | C8 D | PASS |
| R25 | Leaving the Leave War mid-move | ends it; no ghost on the next page | — | C10 D·P A | PASS |
| R26 | Undo / a stage change / a war change mid-move | ends it (the existing guard) | — | unit (`moveone.test.tsx`); walked Undo after landing (C1) | PASS |
| R27 | The drag-selection's Move… | the same edges, months, outside tap | — | C11 D·P A | PASS |
| R28 | The event move (a PH / event band's Move…) | shares `wireMove` — the same edges and outside tap | — | not walked (see §7) | unit (`movewire.test.ts` drives the shared machine) |
| R29 | The tap list's per-record Move… | keeps its date box — Q-1 | — | not walked | a question for him |

## 5. The orders walked

- **D260:** Clear asked → Clear taken → Undo (A1); a range asked → taken → Undo (A2); Delete asked → taken → ONE Undo
  brings three awards and the OL back (A3); a block of awards only (A4); Move over leave + award → Undo (A5); OPEN →
  CLOSED → PUBLISHED, then a Delete on the published war, then Undo, then back to OPEN (A6, Z). Both widths.
- **D261:** the admin gives Ranger two awards through +OIL; Ranger signs in (a reload, as he would) at OPEN; the admin
  closes the war; Ranger again; the admin publishes; Ranger again. Both widths.
- **D262:** pick up → land → Undo → Redo (C1); its own day (C2); a refusal then a landing (C3); pick up → MAR → land →
  Undo (C4); pick up → an empty tap outside (C5); pick up → Manning → Legend → its shade (C5b); desktop hover edges
  right → left → middle → click (C6); press-and-drag to the right edge → release (C7, finger at P); double-click on Move
  (C8); CLOSED → pick up → land → its sheet → Undo → OPEN (C9); pick up → Inputs → back (C10); drag-select → Move… →
  FEB → land → Undo, then → an outside tap (C11); phone: a swipe, then a 900 ms press (C12). **C12's first run held the
  finger on a point that was not a day** (the script's aim): probe `mv-21-longpress-probe.mjs` shows 300 / 600 / 900 ms
  presses each stage the day; the step now aims at a free day in view and passes.

## 6. The break tests (bug-check order §8.4)

Each wire broken once in a SCRATCH worktree (`mv-break-scratch`), its tests run, restored. Script
`scripts/handpass/mv/mv-breaks.py`; the raw record `docs/handpass/parts/2026-09-27-d260-d262-breaks.txt`.

| wire | red |
|---|---|
| W1 D260 the store names no award (`awardsIn` empty) | 8 (`awardclear`, `inputgate`) |
| W2 D260 the Delete confirm drops its award clause | 2 |
| W3 D260 the Clear never asks | 3 |
| W4 D260 Delete leaves the award beside leave | 2 (`inputgate`) |
| W5 D260 a block of awards alone offers no Delete | 2 |
| W6 S18 a published leave that stays is not counted | 1 |
| W7 D261 his own award is not tappable | 4 (`ownaward`) |
| W8 D261 the read-only award sheet never mounts | 4 |
| W9 D262 the sheet's Move picks nothing up | 20 (`moveone`, `deciding`) |
| W10 D262 no hover edge scroll | 3 (`movewire`) |
| W11 D262 a press-and-drag lands nothing | 2 |
| W12 D262 an empty tap outside does not cancel | 2 |
| W13 D262 no double-click guard | 2 |
| W14 D262 a long press cancels | 1 |
| W15 D262 leaving the war keeps the move | 1 |
| W16 D262 its own day reads "Nothing to move" | 1 |
| W17 D262 a band the mouse starts in scrolls at once | 1 |
| W18 D262 the whole page counts as the grid | 1 |
| W19 D262 a popup's shade cancels | 1 |

## 7. What was NOT walked, and why — and errors seen

- **The event move** (R28) — it shares the one `wireMove`, whose rules are unit-pinned; the walk's time went to the two
  doors D262 names. Walked in the re-walk if time allows (§11).
- **A member's drag Delete over his award** (R11), **a draft war** (R15) — unit only; the demo has no draft war holding
  an award.
- **The unchanged doors** R7, R8, R9, R17 — not touched by this build.
- **iOS Safari** — Chromium only; the phone's hold-drag, long press and quick swipe are on his look card.
- **Errors seen:** none in any run (each result file's last line).

## 8. The two code reads (Fable 5.1 and Astra, blind to each other, with this sheet in hand)

*(filled when both reports are in)*

## 9. The gates

**First run** (27 Sep 26, one run under the PC lock, `E2E_PORT=4192`, on `28e388b1`): unit **6320 / 6320** (392 files) ·
build clean · tfin **728 / 0** · e2e **473 passed, 1 failed**, 48 skipped · smoke **443 / 0** · rulecheck OK · docsize OK.
The one failure — `leavewar.spec.ts` "a loose box moves the inputs present, first input landing on the clicked day" —
was this build's: the double-click guard ignored every click in a move's first 400 ms, and that older test clicks a
landing day straight after Move…. **Fixed** `01f466ca` (the guard is a place as well as a time), red first; the two Leave
War browser files re-run under the lock: **320 passed**, 48 skipped.
**On GitHub** (the PR's last run, on `9f7814d9`, before this build): the desktop geometry job failed four drag-select tests
(a second drag in one test) — the same code passed there an hour earlier on `494e0f10` (only documents changed between),
the known slow-machine family (D84: one re-run, never an investigation); the next push is its fresh run.
**Final run:** *(after the reads' fixes)*

## 10. His five-minute look (the card handed to him)

On the branch's Vercel link, signed in as admin (ad / a) unless it says otherwise. About five minutes; a real phone for
step 5. The names are the demo's; on a browser that already holds your own world, any man's award or bid does the same. The absence re-test's own card (`docs/handpass/2026-09-26-absence.md` §12) still stands beside this one.

1. **Delete names the awards.** Leave War, January: drag from Drifter's 1 Jan down to Hunter's 4 Jan → Delete. Expect
   "… including 3 OIL awards (Drifter 3 Jan 1 day, Hunter 1 Jan 1 day, Hunter 4 Jan 1 day)? Tap Delete again." Tap it:
   the awards and Drifter's OL go and their OIL drops. Press Undo once: everything is back.
2. **Clear names the award.** Tap Hunter's FO on 4 Jan → Clear. Expect the button to read "Clear — sure?" and the line
   "Clear also takes Hunter's OIL award (1 day)"; Clear again takes it (Undo brings it back).
3. **A member's own award.** Tap Ranger's 20 Apr → +OIL → Give FO. Sign out, sign in as us / us (Ranger): tap that FO.
   Expect "Your OIL award" — reason, given by, days, "Given by an admin — only an admin can change it", nothing to press
   but ✕. Another man's FO opens nothing for him.
4. **One chip, one Move.** Tap Gambit's <LL on 23 Jan → **Move** (no date box beside it). The sheet closes and "Move 1
   entry" follows your mouse. Carry it to the right edge of the grid: the days scroll on. Press FEB: still moving. Click
   a February day: it lands there, undecided. Do it again and click the empty page beside the grid: the move ends,
   nothing moved.
5. **Your phone.** Tap the chip → Move → swipe the grid (it scrolls, nothing lands) → tap a day → "Move 1 entry here?" →
   Confirm. Then hold a finger on a day, drag to the screen's right edge (the grid scrolls), lift → Confirm.

**One question rides with the card** (`OUTSTANDING.md` `[LW-MOVE-TAPLIST-ASK]` — nothing waits on it): a day holding
several records opens the tap list, where an approved leave's own "Move…" still has a date box (it has to say WHICH
record moves). Should it pick the one record up and land it on the grid like the chip does, or keep its date box?

**The readings he can correct** (§2): a block holding only awards now offers Delete (never Move); an award beneath a leave
goes with the Delete; the range Clear asks too; the event move gets the same edge scroll and outside-tap; a phone's
hold-and-drag carries a picked-up chip; leaving the Leave War ends a move.
