# [HUMAN-RETEST] the absence record, walked with [S4-HUNT-REST] — the FULL-tier evidence sheet (26 Sep 26)

Branch `claude/absence-record-d147-af6a50` (cut from `main` at `e27e15fe`). The order this follows:
`raptor-port/docs/bug-check-order.md`. The plan: `raptor-port/docs/superpowers/plans/2026-09-26-absence-retest-plan.md`
(§9 carries what the two plan reviews changed). The rules: `…/specs/2026-09-20-CURRENT-STATE.md` and
`…/specs/2026-09-20-one-absence-behaviour-register.md` (§11 — the rulings since the hunt). Pictures:
`raptor-port/docs/img/handpass/2026-09-26-absence/` (`host/…`, `w1`–`w5`). Walk scripts: `raptor-port/scripts/handpass/ab/`.
Walkers' sheets: `parts/2026-09-26-absence-w{1..5}.md`; the host's result logs: `parts/2026-09-26-absence-h{1..6}-*.txt`.

## 0. Where this stands (kept current — read this first after a compaction or in a new chat)

- **Done:** the rules sweep (register §11); the plan; Fable's and Astra's blind plan reviews and scenario lists
  (`2026-09-26-absence-{fable,astra}-scenarios.md`), folded in (plan §9); the host walks H1 (a late absence on a
  published day, four doors), H2 (Fable's findings reproduced), H3 (storage faults), H4 (roles and the guest), H5 (Edit
  history), H6 (the Inputs export).
- **Walkers back:** W1 (calendar), W2 (medical), W4 (phone and money) — every finding reproduced by the host and
  fixed red first (§3). **Still walking:** W3 (the war's gestures), W5 (orders and lifecycle). The PC lock is held.
- **Fixed and committed, NOT yet rebuilt** (the walkers are on the old bundle — bug-check order §4): AB3, AB4, AB5,
  AB6, AB8a, AB9, AB10, W4-1, W1-F1–F4.
- **Backlog chores owed at the close (one docs pass):** file `[LW-ISO-DATES]` (W4-2); add W4's "a late joiner's row
  vanishes" to `[AMEND-SMALL-SEEN]` item 6; archive `[PUB-UNAVAIL]` (closed — H1); file his questions (AB1/AB2 Clear
  and bulk Delete taking an award; AB8b what History should record); rewrite `[S4-HUNT-REST]` and `[HUMAN-RETEST]`
  (the absence record done); `docs/file-map.md` rows for the new files (`MedMoveConfirm.tsx`, the new tests,
  `scripts/handpass/ab/`); `docs/ui-contracts.md` / `engine-rules.md` lines for what changed.
- **Next:** W3 + W5's findings → fix → rebuild → re-walk the fixes (host + the walkers' own scripts) → the gates under
  the lock → Fable and Astra read the final code with this sheet → fix → re-walk → his look card (§12).

## 1. The eight questions → FULL

| # | question | answer | why |
|---|---|---|---|
| 1 | Money (earned leave counts, D25) | **YES** | every leave balance, the OIL balance, the medical total, the 15-day run, manning |
| 2 | The published record | **YES** | a late input reads pending and the face stays as issued (D177–D179); publishing flags a clashing bid (N9) |
| 3 | Saved data | **YES** | Inputs, the war's records, notices, postings, documents; storage faults |
| 4 | A shared drawer | **YES** | one day view draws every war cell; one Unavailable reader draws the schedule's rows |
| 5 | A new gesture or mode | **YES** (re-test) | calendar drag and hold-to-add, bulk drag, two-step move, the one window, Post in, awards |
| 6 | A new surface | NO — a re-test | every existing surface is walked |
| 7 | Roles | **YES** | admin / member on his own row (D166) / guest (D213, D215) |
| 8 | The warning list | **YES** | leave over work, clash notes, the amber `!`, "1 pending" |

**Tier: FULL.** Told to him at the start of the chat, with the order of steps.

## 2. Scope rulings this session

- **None new from him yet.** This chat runs under D228 (the PC-wide lock; ports 4175 / 4192; rulings D260–D269 —
  recorded on `claude/accounts-new-person`).
- **D177's medical sentence** marked set aside by D179 / D185 in its own row (D201) — the code already froze a medical.
- **N5 / B8 (an award's hours)** not live — gone with N13 / N19 (register §11; Astra's plan read).
- **D148 (undo reverses only your own changes)** is decided and not built — the change-recording re-test's. Recorded, not judged.
- **What is NOT a finding (D56)** — in every brief.

## 3. Every finding

| # | finding | found by | rule | status |
|---|---|---|---|---|
| AB1 | The bid sheet's **Clear**, on a day holding an OIL award, removes the award — no word; the man's OIL drops | Fable F1/S1 | N11 (an award is the admin's; it has its own Remove) | **REPRODUCED** H2 (`host/h2-desktop/h2-F1-*`: FO → blank, OIL 1 → 0) |
| AB2 | The drag-selection's **Delete** removes every award in the rectangle | Fable F2/S2 | as AB1 | to reproduce — W3 |
| AB3 | A leave cut by a medical keeps its old **"till <date>"** on the piece that now ends earlier | Fable F3/S5 | D189 (the words a day printed must match); plain wording | **FIXED** `643b616e`, red first (`inputgate.test.ts` "a cut leave says its own last day") — `sync.ts sliceInput`, the one body every cut uses; REPRODUCED H2 first; W2 met it again |
| AB4 | The calendar's **drag of a medical / upchit**, and the schedule's **reassign** of one, skip the "who holds the shared days" / upchit questions | Fable F4/S4; Astra A | owner 27 Aug 26 ("never resolves silently … no default") | **REPRODUCED by W2 at both widths through four doors** (a drag onto a different medical — HL 20–24 cut to 20–23 silently; a drag inside the old one — the tail KEPT where the sheet's default removes it; an upchit drag — no Keep/Remove; the reassign by arm-then-tap and by palette drag); **FIXED** `eecacad5`, red first (`medmove.test.tsx`) — both doors hand the move to the same sheets (`ui/MedMoveConfirm.tsx`); the Inputs table's edit runs the same bodies |
| AB5 | A **posting that closes before it opens** is refused silently — the sheet closes, nothing saved, nothing said | Fable F5/S3 | a refused value is put back and SAID (robustness doctrine) | **FIXED** `643b616e`, red first (`pi.test.tsx`, `selectsheet.test.tsx`) at every door — the bid sheet's PI / PO, the two posting sheets, the drag-selection's PO (`store.ts postingProblem`); REPRODUCED H2 first |
| AB6 | An overnight leave's tail ambers the next day, whose tap list does not show it | Fable F6 (predicted) | N10 (the sheet behind the amber says what clashes) | **CONFIRMED by W4** (desktop admin, phone admin, phone member — `w4/w4-sat-AB6-*`); **FIXED** `699fff5e`, red first (`dayview.test.ts`, `daylist.test.tsx`) — `DayView.clashTails`, a read-only line naming the leave and when it runs till |
| AB7 | Bulk Approve's "N decided" counts an already-approved leave | Fable F7 (predicted) | the note must tell the truth | to walk — W3 |
| AB8 | **Edit history** records only an input REMOVED (H5: after an Inputs-page add, an edit, a war approval, an un-approve and a medical cutting leave, the list held ONE line — "Input removed … Saber") | Astra B; H5 | engine-rules §The edit log (a record of the SCHEDULE; inputs log add / remove only) | **(a) a defect by the design's own words:** the Inputs page's own "Add input" writes no line, while the same add through the edit window writes "Input added — …" (`inputedit.tsx commitNewInput` vs `InputsPage.tsx` commit) — two doors, two answers; **(b) a question for him**, with the one changes window (`[DRAFT-PENDING]`, D169's transparency): should the war's approvals / moves and an input's edits and cuts join the history? |
| AB9 | Tapping a **medical** (or a course / OD) on the war opens the read-only sheet that says it is "APPROVED … already approved" — leave's words | host H4 | plain wording | **FIXED** `643b616e`, red first (`raptorsheet.test.tsx`) |
| AB10 | The Inputs page's **Export to Excel drops every multi-day input's END date** — LL 20–24 Jul exports as "Jul 20"; the file's columns are Name, Date, Start (time), End (time), Type, Remarks | host H6 | the export is the data leaving the app — it must say what is stored | **FIXED** `643b616e`, red first (`export.test.ts`) — From / To columns (`export.ts inputRows`); pre-existing since the page was built |

| W4-1 | On a phone, a finger HELD on one day and lifted opens the selection sheet — and the finger's own tap (20–30 ms later) closes it; on a day that already holds a mark a phone user had no one-day door to the sheet | W4; REPRODUCED by the host (its `w4-08-hold-why.mjs`) | a door drawn and dead (bug-check §2a) | **FIXED** `699fff5e`, red first (`selecttap.test.ts`) — a touch gesture's trailing tap is awaited 400 ms; a mouse keeps the 0 ms sweep |
| W4-2 | Three war places print raw ISO dates ("2026-07-17"): the bid sheet header and its PI / PO button, the one-day selection header, the move-refusal banner | W4 | the day-first date voice (ui-contracts) | **FILED** `[LW-ISO-DATES]` (low; a wording batch — every war sheet together, with their tests) |
| W1-F1 | On a phone the Inputs calendar's month SWIPE never pages — the browser takes a sideways finger as its own pan | W1; REPRODUCED by the host (`w1-06-probe-swipe-tap.mjs`: pointercancel after one move) | owner 22 Aug 26 (swipe to see other months) | **FIXED** `699fff5e`, red first (`caltouch.test.tsx`) — `.ic-grid` `touch-action:pan-y` |
| W1-F2 | On a phone a TAP on a chip opens its edit and the same tap's click shuts it (10 of 33 July chips) | W1; REPRODUCED by the host (same probe: `click:touch` on `#inpEditPop` → closed) | R3's promise (a tap opens the input) | **FIXED** `699fff5e`, red first (`caltouch.test.tsx`) — the tap's trailing click eaten, as the drag's already was |
| W1-F3 | A MEMBER's calendar lifts another man's chip, and a tap opens that man's input with Delete and Save (refused only when pressed) | W1 | D166 (his own row); a control is never offered to someone who cannot use it | **FIXED** `699fff5e`, red first (`memberinput.test.tsx`) — the chip does not lift; the edit window on an input its reader may not change is read only, whichever door opened it |
| W1-F4 | The war's tap list prints a leave filed 12:00–14:00 as "12:01–14:00" | W1 | N1 moves the HALF, not the time | **FIXED** `699fff5e`, red first (`daylist.test.tsx`) |

**Recorded by the walkers, not findings:** W2 — a reload before the first Leave War write drops the demo's opening
balances (D56 — demo data; the §7.7 trap); a medical trim adds "till" to a medical's remark (design); a leave and a
medical in the same half at hours that do not meet count 0.5 leave AND 0.5 medical (the halves model — N6 and §7 —
each record charged its own half); the schedule has no door to a medical's document (the viewer opens from the Inputs
table and the Medical view). W1 — leave onto a WEEKDAY flying day draws nothing on the war (`[LW-WEEKDAY-WORK]`); the
Inputs page has no Undo of its own (the top bar's); a two-date custom window 12:00 → 12:00 is refused by the form
("not the same time" — Fable S21's case: the form reads a custom window's times as each day's, so a noon-to-noon span
is not a thing it can be typed as; not a finding). W4 — the phone figure sheet jumping the grid to January is
`[AMEND-SMALL-SEEN]` item 6 (a late joiner's row vanishes when it happens — added there); the war draws no DUTY line.

**Walked and CORRECT (host):** H1 — a late leave, a late medical, a war approval and a medical cutting issued leave on
published Friday each read pending on every count, drop the four sign-offs, and leave the published face as issued;
taking the late leave out again → 0 and the four sign-offs back (D98, AM11) — `[PUB-UNAVAIL]` is closed. H3 — a refused
save says "Not saved — Retry"; the retry lands the WHOLE group (the leave, the bid removal, the notice); closing on a
failed save keeps none of it (never half); a half-written save is finished at the next boot and never doubled. H4 — a
member and a guest read a medical's type and remarks on the published face (D211, D213, D215); a member sees another
man's medical on the Inputs page (filter "Everyone") with no edit door; the guest has no Inputs page, no war, no ⓘ;
the waiting screen offers "View the schedule" (D221); the notice names who filed it by callsign (D166). Both widths.
