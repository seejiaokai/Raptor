# [HUMAN-RETEST] the absence record, walked with [S4-HUNT-REST] — the FULL-tier evidence sheet (26 Sep 26)

Branch `claude/absence-record-d147-af6a50` (cut from `main` at `e27e15fe`). The order this follows:
`raptor-port/docs/bug-check-order.md`. The plan: `raptor-port/docs/superpowers/plans/2026-09-26-absence-retest-plan.md`
(§9 carries what the two plan reviews changed). The rules: `…/specs/2026-09-20-CURRENT-STATE.md` and
`…/specs/2026-09-20-one-absence-behaviour-register.md` (§11 — the rulings since the hunt). Pictures:
`raptor-port/docs/img/handpass/2026-09-26-absence/` (`host/…`, `w1`–`w5`; the re-walks `rewalk/` and `rewalk2/`). Walk scripts: `raptor-port/scripts/handpass/ab/`.
Walkers' sheets: `parts/2026-09-26-absence-w{1..5}.md`; the host's result logs: `parts/2026-09-26-absence-h{1..6}-*.txt`.

## 0. Where this stands (kept current — read this first after a compaction or in a new chat)

- **Walked:** the host (H1–H6) and five walkers (W1–W5) at both widths, admin, member and guest; every finding
  reproduced and fixed red first, or filed, or put to him (§3). The roll-call has no blank cell (§4); a sixth walker (W6)
  walked the rows nobody had reached.
- **Checked:** the break tests (§6 — two thin wires pinned); Fable's and Astra's blind final code reads — six findings,
  all fixed red first (§8); the re-walk of every fix on the rebuilt app (§11), and a second re-walk of the fixes it
  found (the phone hold, the vanishing row, the chip swipe, the board's crew column, and smaller ones) — all pass.
- **The gates** on the final code, under the PC lock: all green (§9).
- **Filed:** `[LW-ISO-DATES]`, `[LW-MOVE-BENEATH]`, `[LW-OFFER-ONLY-TAKEABLE]`, `[PO-RESTORE-POSTING]`,
  `[ABSENCE-SMALL-SEEN]`, a note in `[DRAFT-PENDING]` (R30) and in `[AMEND-SMALL-SEEN]` items 4 and 6. **His questions:**
  `[ABSENCE-ASK]`. `[S4-HUNT-REST]` and `[PUB-UNAVAIL]` archived.
- **Next:** his five-minute look (§12) → his "merge live".

## 1. The eight questions → FULL

| # | question | answer | why |
|---|---|---|---|
| 1 | Money (earned leave counts, D25) | **YES** | every leave balance, the OIL balance, the medical total, the 15-day run, manning |
| 2 | The published record | **YES** | a late input reads pending and the face stays as issued (D177–D179); publishing flags a clashing bid (N9) |
| 3 | Saved data | **YES** | Inputs, the war's records, notices, postings, documents; storage faults |
| 4 | A shared drawer | **YES** | one day view draws every war cell; one Unavailable reader draws the schedule's rows |
| 5 | A new gesture or mode | **YES** (re-test) | calendar drag and hold-to-add, bulk drag, two-step move, the one window, Post in, awards |
| 6 | A new surface | NO — a re-test | every existing surface is walked |
| 7 | Roles | **YES** | admin / member on his own row (D166) / guest (D213, D215) |
| 8 | The warning list | **YES** | leave over work, clash notes, the amber `!`, "1 pending" |

**Tier: FULL.** Told to him at the start of the chat, with the order of steps.

## 2. Scope rulings this session

- **None new from him yet.** This chat runs under D228 (the PC-wide lock; ports 4175 / 4192; rulings D260–D269 —
  recorded on `claude/accounts-new-person`).
- **D177's medical sentence** marked set aside by D179 / D185 in its own row (D201) — the code already froze a medical.
- **N5 / B8 (an award's hours)** not live — gone with N13 / N19 (register §11; Astra's plan read).
- **D148 (undo reverses only your own changes)** is decided and not built — the change-recording re-test's. Recorded, not judged.
- **What is NOT a finding (D56)** — in every brief.

## 3. Every finding

| # | finding | found by | rule | status |
|---|---|---|---|---|
| AB1 | The bid sheet's **Clear**, on a day holding an OIL award, removes the award — no word; the man's OIL drops | Fable F1/S1 | N11 (an award is the admin's; it has its own Remove) | **REPRODUCED** H2 (`host/h2-desktop/h2-F1-*`: FO → blank, OIL 1 → 0) |
| AB2 | The drag-selection's **Delete** removes every award in the rectangle | Fable F2/S2 | as AB1 | **REPRODUCED by W3** (W3-F1: two awards gone, the confirm "Delete 3 days for 4 people?" never names them) — **a question for him, with AB1** (an existing test pins Clear taking the award as meant; which is right is his) |
| AB3 | A leave cut by a medical keeps its old **"till <date>"** on the piece that now ends earlier | Fable F3/S5 | D189 (the words a day printed must match); plain wording | **FIXED** `643b616e`, red first (`inputgate.test.ts` "a cut leave says its own last day") — `sync.ts sliceInput`, the one body every cut uses; REPRODUCED H2 first; W2 met it again |
| AB4 | The calendar's **drag of a medical / upchit**, and the schedule's **reassign** of one, skip the "who holds the shared days" / upchit questions | Fable F4/S4; Astra A | owner 27 Aug 26 ("never resolves silently … no default") | **REPRODUCED by W2 at both widths through four doors** (a drag onto a different medical — HL 20–24 cut to 20–23 silently; a drag inside the old one — the tail KEPT where the sheet's default removes it; an upchit drag — no Keep/Remove; the reassign by arm-then-tap and by palette drag); **FIXED** `eecacad5`, red first (`medmove.test.tsx`) — both doors hand the move to the same sheets (`ui/MedMoveConfirm.tsx`); the Inputs table's edit runs the same bodies |
| AB5 | A **posting that closes before it opens** is refused silently — the sheet closes, nothing saved, nothing said | Fable F5/S3 | a refused value is put back and SAID (robustness doctrine) | **FIXED** `643b616e`, red first (`pi.test.tsx`, `selectsheet.test.tsx`) at every door — the bid sheet's PI / PO, the two posting sheets, the drag-selection's PO (`store.ts postingProblem`); REPRODUCED H2 first |
| AB6 | An overnight leave's tail ambers the next day, whose tap list does not show it | Fable F6 (predicted) | N10 (the sheet behind the amber says what clashes) | **CONFIRMED by W4** (desktop admin, phone admin, phone member — `w4/w4-sat-AB6-*`); **FIXED** `699fff5e`, red first (`dayview.test.ts`, `daylist.test.tsx`) — `DayView.clashTails`, a read-only line naming the leave and when it runs till |
| AB7 | Bulk Approve's "N decided" counts an already-approved leave | Fable F7 (predicted) | the note must tell the truth | **REPRODUCED by W3** (W3-F2: "3 decided", 2 changed); **FIXED** `3531d20c`, red first (`inputgate.test.ts`) — an already-approved leave is said apart ("1 already approved"), never counted as decided |
| AB8 | **Edit history** records only an input REMOVED (H5: after an Inputs-page add, an edit, a war approval, an un-approve and a medical cutting leave, the list held ONE line — "Input removed … Saber") | Astra B; H5 | engine-rules §The edit log (a record of the SCHEDULE; inputs log add / remove only) | **(a) a defect by the design's own words:** the Inputs page's own "Add input" writes no line, while the same add through the edit window writes "Input added — …" (`inputedit.tsx commitNewInput` vs `InputsPage.tsx` commit) — two doors, two answers; **(b) a question for him**, with the one changes window (`[DRAFT-PENDING]`, D169's transparency): should the war's approvals / moves and an input's edits and cuts join the history? |
| AB9 | Tapping a **medical** (or a course / OD) on the war opens the read-only sheet that says it is "APPROVED … already approved" — leave's words | host H4 | plain wording | **FIXED** `643b616e`, red first (`raptorsheet.test.tsx`) |
| AB10 | The Inputs page's **Export to Excel drops every multi-day input's END date** — LL 20–24 Jul exports as "Jul 20"; the file's columns are Name, Date, Start (time), End (time), Type, Remarks | host H6 | the export is the data leaving the app — it must say what is stored | **FIXED** `643b616e`, red first (`export.test.ts`) — From / To columns (`export.ts inputRows`); pre-existing since the page was built |

| W4-1 | On a phone, a finger HELD on one day and lifted opens the selection sheet — and the finger's own tap (20–30 ms later) closes it; on a day that already holds a mark a phone user had no one-day door to the sheet | W4; REPRODUCED by the host (its `w4-08-hold-why.mjs`) | a door drawn and dead (bug-check §2a) | **FIXED** `699fff5e`, red first (`selecttap.test.ts`) — a touch gesture's trailing tap is awaited 400 ms; a mouse keeps the 0 ms sweep |
| W4-2 | Three war places print raw ISO dates ("2026-07-17"): the bid sheet header and its PI / PO button, the one-day selection header, the move-refusal banner | W4 | the day-first date voice (ui-contracts) | **FILED** `[LW-ISO-DATES]` (low; a wording batch — every war sheet together, with their tests) |
| W1-F1 | On a phone the Inputs calendar's month SWIPE never pages — the browser takes a sideways finger as its own pan | W1; REPRODUCED by the host (`w1-06-probe-swipe-tap.mjs`: pointercancel after one move) | owner 22 Aug 26 (swipe to see other months) | **FIXED** `699fff5e`, red first (`caltouch.test.tsx`) — `.ic-grid` `touch-action:pan-y` |
| W1-F2 | On a phone a TAP on a chip opens its edit and the same tap's click shuts it (10 of 33 July chips) | W1; REPRODUCED by the host (same probe: `click:touch` on `#inpEditPop` → closed) | R3's promise (a tap opens the input) | **FIXED** `699fff5e`, red first (`caltouch.test.tsx`) — the tap's trailing click eaten, as the drag's already was |
| W1-F3 | A MEMBER's calendar lifts another man's chip, and a tap opens that man's input with Delete and Save (refused only when pressed) | W1 | D166 (his own row); a control is never offered to someone who cannot use it | **FIXED** `699fff5e`, red first (`memberinput.test.tsx`) — the chip does not lift; the edit window on an input its reader may not change is read only, whichever door opened it |
| W1-F4 | The war's tap list prints a leave filed 12:00–14:00 as "12:01–14:00" | W1 | N1 moves the HALF, not the time | **FIXED** `699fff5e`, red first (`daylist.test.tsx`) |

| W5-F1 | The posting sheet's **"Undo post out (PO)"** cleared the date but NOT the archive the Post out made ("Archive on PO date" is on by default and a past date archives at once) — an archived man with no posting dates is dropped from the war, so his row, bids and leave vanished from every month, a reload included | W5; REPRODUCED by the host (`w5-05b-postout-row-probe.mjs` check 4) | "undo" = as before; answer C / N8 (his leave shown and charged) | **FIXED** `5b58041a`, red first (`poarchive.test.ts`) — `sync.ts undoPostOut`: the Quals Restore when the archive is the Post out's own |
| W5-F2 | After that undo, every LATER Post out made that man's row vanish until a reload | W5 (`w5-05d`, V2 vs controls V0 / V1 / V3) | as W5-F1 | **FIXED** `1113f085` at its REAL cause, found by W5's re-walk, red first in a real browser (`e2e/leavewar.spec.ts`, both widths): no undo is needed — the grid's note of which months are on screen went stale on a month jump that kept the same people (SEP → JUL), so a Post out dated in the month on screen hid that man's row. (This row first said the trigger was gone with W5-F1's fix — wrong, corrected.) |
| W5-F3 | With a bid sheet open, **Tab** left the sheet and reached the Period picker behind the scrim; the war switched, the sheet stayed live, and its LL wrote a bid into the 26 war while 27 was on screen (Tab also reached Stage advance) | W5; REPRODUCED by the host (`w5-06b-stale-sheet-probe.mjs`) | Fable S14 / Astra 11 (a sheet closes or goes inert on a war switch) | **FIXED** `5b58041a`, red first (`sheetfocus.test.tsx`) — every Leave War sheet holds Tab / Shift+Tab; a war switch closes the open sheet |
| W5-F4 | Beside an ATT C recorded 09:00–14:00 the bid sheet OFFERED the afternoon and then refused it: "That day is already ATT C" — no hours | W5 (`w5-02b`) | §7 (real hours decide every clash); item D (a half is offered only when it can be taken) | **FIXED** `5b58041a`, red first (`freehalf.test.tsx`) — the free half read off the real hours (the tap reads the day), and the refusal names a timed medical's hours |
| W5-F5 | The bid sheet's heading read "NOW *LL" while the box read "<LL" | W5 | one notation on screen (`displayCell`) | **FIXED** `5b58041a`, red first (`freehalf.test.tsx`) |

| W3-F1 | = AB2 (above) | W3 | | a question for him, with AB1 |
| W3-F2 | = AB7 (above) | W3 | | **FIXED** `3531d20c` |
| W3-F3 | Bulk **Delete** and bulk **Move** skipped an undecided bid sharing its day with leave filed on the Inputs page (a filed morning, an afternoon bid): Delete counted it "skipped — owned by Raptor", Move left it behind. Approve / Refuse / Ack already reached it | W3 | old plan D3 / D4 (a bulk action reaches the war's own requests) | **Delete FIXED** `3531d20c`, red first (`inputgate.test.ts`) — the bid beneath goes, the filed leave stays (`store.ts clearRequestsAt`). **Move FILED** `[LW-MOVE-BENEATH]` (low: a move of a block whose day carries TWO owners needs a design — where the bid lands beside a leave that stays) |
| W3-F4 | = AB5 at the Post out / Post in sheets' date boxes, the drag-selection's Post out and the bid sheet's PO — each refused silently | W3 (both widths) | as AB5 | walked on the OLD bundle — AB5's fix (`643b616e`) covers all four doors; **re-walked on the rebuilt app** (§11) |
| W3-F5 | Moving the Post out sheet's date PAST the tapped day swapped the sheet, under the admin's hands, for a bid sheet on that day | W3 (`w3-03-desktop-C3-*`) | the sheet's promise: it "stays up so the admin can see the grid move behind it" | **FIXED** (this change), red first (`po.test.tsx`) — the tap pins the posting sheet it opened for as long as it is up (`Matrix.tsx`, the one door that opens a cell); a PO placed from the bid sheet closes that sheet, as before (corrected — Fable's read, N6: this row first said it turned into the Post out sheet) |
| W3-F6 | An award's CODE did not follow a change of its days on the OIL tracker or the tap list: a half-day HO made 2 days still read HO | W3 (both widths) | N19 (the number is the fact; under a day HO, a day or more FO) | **FIXED** `3531d20c`, red first (`oil-award-add.test.ts`) — `store.ts editManualCredit` sets the code from the days |
| W3-F7 | A sheet left open across a **keyboard** war switch was re-read against the war now on screen: on the PUBLISHED 26 war a note sheet became a live bid sheet, and its Clear **deleted published, war-approved leave** while JAN–DEC 27 was on screen | W3 (desktop; a finger or a mouse closes the sheet first) | Fable S14 / Astra 11; owner 21 Sep 26 ("approved and published is finished paperwork") | **FIXED** `5b58041a` with W5-F3, red first (`sheetfocus.test.tsx`) — every Leave War sheet holds Tab / Shift+Tab, and a war switch closes the open cell (the tap list, the read-only sheet, the posting sheets and the note editor all hang on it); **re-walked on the rebuilt app** (§11, W3's `w3-05b`) |
| W3-F8 | **Redo** (and Undo) put an undone bid back onto a day a medical had been filed on since — the bid stood on the sick day | W3 (both widths) | B7 (the same rules at every door, undo and redo included) | **FIXED** `3531d20c`, red first (`inputgate.test.ts`) — the war answers the timeline before a restore moves anything (`restoreBlocker`), and names what holds the day; an absence the same step changes is no blocker (undoing a filing that replaced a bid gives the bid back) |
| W3-F9 | = AB3 on the war side: after a medical cut, the war's Refuse, its Delete and its drag-Move of part of a leave, every piece read "till 31 Jul" | W3 | as AB3 | walked on the OLD bundle — AB3's fix is the one body every one of those cuts uses (`sync.ts sliceInput`); **re-walked on the rebuilt app** (§11) |
| W3-F10 | A MEMBER cannot open his own OIL award outside the bidding window (inside it, his award reads reason / given by / days) | W3 (both widths) | the one-tap read-back (owner, 21 Sep 26) | **a question for him** — whether his own FO / HO should open read-only outside the window; the OIL tracker already shows the same facts |

**Recorded by the walkers, not findings:** W2 — a reload before the first Leave War write drops the demo's opening
balances (D56 — demo data; the §7.7 trap); a medical trim adds "till" to a medical's remark (design); a leave and a
medical in the same half at hours that do not meet count 0.5 leave AND 0.5 medical (the halves model — N6 and §7 —
each record charged its own half); the schedule has no door to a medical's document (the viewer opens from the Inputs
table and the Medical view). W1 — leave onto a WEEKDAY flying day draws nothing on the war (`[LW-WEEKDAY-WORK]`); the
Inputs page has no Undo of its own (the top bar's); a two-date custom window 12:00 → 12:00 is refused by the form
("not the same time" — Fable S21's case: the form reads a custom window's times as each day's, so a noon-to-noon span
is not a thing it can be typed as; not a finding). W5 — after a sign-out Undo reaches only the person's own steps; a posting is not on the one timeline; undoing a publish takes back the earned credit; a reload asks to sign in again (all recorded — D148 is the next re-test's). W4 — the phone figure sheet jumping the grid to January is
`[AMEND-SMALL-SEEN]` item 6 (a late joiner's row vanishes when it happens — added there); the war draws no DUTY line.

**Walked and CORRECT (host):** H1 — a late leave, a late medical, a war approval and a medical cutting issued leave on
published Friday each read pending on every count, drop the four sign-offs, and leave the published face as issued;
taking the late leave out again → 0 and the four sign-offs back (D98, AM11) — `[PUB-UNAVAIL]` is closed. H3 — a refused
save says "Not saved — Retry"; the retry lands the WHOLE group (the leave, the bid removal, the notice); closing on a
failed save keeps none of it (never half); a half-written save is finished at the next boot and never doubled. H4 — a
member and a guest read a medical's type and remarks on the published face (D211, D213, D215); a member sees another
man's medical on the Inputs page (filter "Everyone") with no edit door; the guest has no Inputs page, no war, no ⓘ;
the waiting screen offers "View the schedule" (D221); the notice names who filed it by callsign (D166). Both widths.

## 4. The roll-call — every place the app draws an absence (plan §4, rows R25–R36 from its §9)

Assembled from every walker's sheet and the host's logs; widths D desktop 1440, P phone 390; roles A admin, M member on
his own row, G guest. No blank cell: a row nobody walked says why.

| # | place | shows it? | can act on it there? | anything painted over it? | walked by | result |
|---|---|---|---|---|---|---|
| R1 | Inputs page: the table | YES. Each input with its type, dates, times and remarks. The paperclip opens documents (W2). Under "Everyone" a member sees other men's medicals with remarks (H4). No source read the blue "filed on the Inputs page" edge | YES. The admin can edit in place (✎), delete (✕) and export. A member acts on his own rows only: another man's row shows no edit or delete (H4; W1's note). There is no Undo on this page; the top bar's Undo covers it (W1, recorded) | While editing, the clash sheet or the upchit summary sits over the row (W2) | H1 D·P A; H4 D·P M; H6 D A; W2 D·P A; W3 D A (read the rows after cuts) | AB10 FIXED `643b616e` (the export dropped end dates). AB3 FIXED `643b616e` (the old "till" on cut pieces, read here). AB4: the table's edit now runs the same bodies (`eecacad5`). Otherwise PASS |
| R2 | Inputs page: the add / edit dialog | YES. Person (admin only), Type, When (all day / AM / PM / custom), Remarks, document upload (W1, W2) | YES. Save and Cancel work. Refusals are named (the same type twice; the real hours). The cut note shows amber (W5). A member was offered Delete and Save on another man's input (W1-F3). The OIL ask on a weekend duty: no source walked it | The document ask, then the clash sheet or the upchit summary, stacked over the form in that order (W2) | W1 D·P A, D M (from the calendar); W2 D·P A·M; H1 D·P A | W1-F3 FIXED `699fff5e`. N1d (the form refuses a two-date 12:00 → 12:00 window) recorded, not a finding. Otherwise PASS |
| R3 | Inputs page: the calendar | YES. One chip per input, coloured by kind. On the phone a chip is a bar with no words (design). "+N more" shows on crowded days (W1, W2) | YES. Drag (admin any chip, member his own), hold to add, tap to open, month swipe. A refused drag is named and leaves no ghost behind. Four doors failed: the phone swipe did nothing (W1-F1); on the phone a tap closed the edit it had just opened (W1-F2); a member could lift another man's chip (W1-F3); a medical or upchit drag skipped the questions (AB4) | Above the chips: the day number, the day title and plan sections. At the foot: "+N more". During a drag: the outline and the landing flash. The toast shows over the month's foot | W1 D·P A, D M; W2 D·P A·M | W1-F1, W1-F2 and W1-F3 FIXED `699fff5e`. AB4 FIXED `eecacad5` |
| R4 | Inputs page: the Medical view | YES. Medically Down, Pending Upchit and Upchit Complete, as of today. Cards read "N documents" (W2) | YES. A card opens the viewer. A Pending card offers Upchit. On another man's card a member gets Close only (W2) | The Medical button's red and amber badges, which agree with the lists (W2) | W2 D·P A·M | PASS. The "as of" date picker was not walked (W2) |
| R5 | Leave War: a day cell | YES. One code by the ladder, a grey +n, an amber !, FO* for the schedule's credit, the dotted moved mark, the PI hatch and the PO tag | YES. A tap opens the right sheet for the stage and the role (W3, W4, W5) | The +n and ! sit on the chip's corner and are readable at both widths. The PO tag and a +1 share that corner, both readable (W4). The move's landing paint shows over the cell (W3) | W1 D·P A, D M; W2 D·P A; W3 D·P A·M; W4 D·P A·M; W5 D·P A·M; H1 D·P A | PASS. W3-F6 FIXED `3531d20c`: an award's code did not follow its days, so the box read HO for 2 days. Leave on a weekday flying day draws nothing: `[LW-WEEKDAY-WORK]`, filed before, not re-reported |
| R6 | Leave War: the tap list | YES. Every record in ladder order (days with 4 and 8 were walked), the notices, the award's OIL block and the clash sentence. MISSING before the fixes: an overnight leave's tail on the next day (AB6). The list also printed a noon start as 12:01 (W1-F4) | YES. Each record has its own buttons: Approve / Ack / Refuse / Clear on a bid, Edit… / Clear on an award, Note on published leave. OK, seen and ✕ work | On the phone the amber toast covers "OK, seen" for about 4 s; taps pass through it (W5 O1). Otherwise nothing covers the list, and 8 records fit on a 390 px screen (W4) | W1 D·P A, D M; W2 D·P A; W3 D·P A (member: not on a busy day of his own); W4 D·P A·M; W5 D·P A·M; H3 D A | AB6 and W1-F4 FIXED `699fff5e`. W3-F6 FIXED `3531d20c` (Edit… left the code wrong). W3-F7 FIXED `5b58041a` (the list became a bid sheet after a keyboard war switch) |
| R7 | Leave War: the bid sheet | YES. It offers the free half beside filed leave and names what holds the other half (W5). An award's details show at its foot (W3). The header printed the raw machine date (W4-2), and the heading used a different half-day notation from the box (W5-F5) | YES. Pick a code, one half or both, or a range; Clear. The decision row shows for an admin outside a draft war. +OIL, PO and PI are admin only. Refusals are named. Three faults: it offered an afternoon beside a 09:00–14:00 medical and then refused it (W5-F4); Clear removed an award (AB1); it stayed live across a keyboard war switch (W5-F3) | The award details at its foot (W3). Nothing else | W1 D A; W3 D·P A·M; W4 D·P A·M; W5 D·P A·M; H1 D·P A; H2 D A; H3 D A | AB1: a question for him (with AB2). W5-F3, W5-F4 and W5-F5 FIXED `5b58041a`. W4-2 FILED `[LW-ISO-DATES]` |
| R8 | Leave War: bulk (drag-select and its sheet) | YES. The shaded block while held, the sheet naming the block, the landing painted before Confirm, a refusal in the banner (W3, W4) | YES. Fill, Ack, Approve, Refuse, Delete, Move, and PO for one man. A member can only fill, on his own row. Four faults: Delete took the awards (AB2); Approve counted an already-approved leave (AB7); Delete and Move skipped a bid under filed leave (W3-F3); on the phone a one-day hold lost its sheet (W4-1) | The move banner covers about 70 px at the foot of the grid; nothing needed is hidden. The orange landing boxes paint over empty days only (W4) | W3 D A·M (on the phone only by mouse: the draft war and the member cases); W4 P A·M by finger, D A by mouse; W5 D·P A (a range bid's note, O2) | AB2 = W3-F1: a question for him (with AB1). AB7 = W3-F2 FIXED `3531d20c`. W3-F3: Delete FIXED `3531d20c`; Move FILED `[LW-MOVE-BENEATH]`. W4-1 FIXED `699fff5e`. W4-2 (the one-day header, the move-refusal banner) FILED `[LW-ISO-DATES]`. W5 O2 recorded: the "skipped" note does not name the day |
| R9 | (as first written: a person's sheet, PersonSheet) | NO. It must not: plan §9 says this sheet is seat / band / SXO and holds no absence | It must not, for the same reason | Nothing is painted; it is not an absence surface (plan §9) | NOT WALKED. Plan §9 struck it as the wrong sheet | Not a finding. Replaced by R9a–R9c |
| R9a | Bid sheet: the PI / PO / +OIL row | YES for the admin: PO (with "Archive on PO date", on by default), PI, +OIL. It must not show for a member, and it does not (W3) | YES for the admin. A posting takes effect at once: days before PI are hatched, days after PO are greyed. A PO dated before the PI closed the sheet silently (AB5) | The PI / PO fold replaces the sheet's lower rows. The button label printed the raw date (W4) | W3 D·P A·M; W4 D·P A; W5 D·P A; H2 D A | AB5 FIXED `643b616e` (reproduced in H2; also W3-F4). W4-2 FILED `[LW-ISO-DATES]` |
| R9b | The Post in / Post out sheets (an admin's tap on a hatched day) | YES. The date, archive, Undo post in / out, and "Place leave or OIL here instead…" (W3, W5) | YES. The date takes effect at once. "Place…" hands the day to the bid sheet (N12). Four faults: a date that would close the posting before it opens was refused silently (W3-F4); moving the date past the tapped day swapped the sheet for a bid sheet (W3-F5); Undo post out left him archived, so his row vanished (W5-F1); after that, every later Post out vanished too (W5-F2) | Nothing (W3, W5) | W3 D·P A; W5 D·P A | W3-F4 = AB5 FIXED `643b616e`, to re-walk on the rebuilt app. W3-F5 FIXED `370c2d4c`, red first (`po.test.tsx`). W5-F1 FIXED `5b58041a` (the archive is the Post out's own since `0eb0d754`). W5-F2 FIXED `1113f085` at its real cause (a stale visible-months note) |
| R9c | The drag-selection's Post out | YES, for one man. There is no bulk Post in; recorded, not a defect (plan §9) | YES. It posts one man out (W3 E1). A PO dated before his PI closed silently (W3-F4 D3). Posting out from a past date with Archive on keeps his row on the war (W3 S37) | Nothing recorded (W3) | W3 D·P A | W3-F4 = AB5 FIXED `643b616e` |
| R10 | Bid sheet: the inline award | YES. One number in halves, a reason and a giver; no hours (plan §9) (W3, W5). On the tap list it shows as a three-line block (W4) | YES for the admin. Bad values are refused with words and nothing is written: 0.25, 0.75, 1.25, 0, −1, "abc" (W3). An award can be given on a day the schedule already credits (W5). A member gets no +OIL (must not). A member could read his own award only inside the bidding window (W3-F10). Clear on the bid sheet removed an award (AB1) | The day's other records sit behind a +1 (W3) | W3 D·P A·M; W4 D·P A; W5 D·P A; H2 D A | PASS (N19 refusals). AB1: a question for him. W3-F10: a question for him |
| R11 | Leave War: the balance column and the figures drawer | YES. The balance, with the days used under it. The drawer shows eight figures per man. Both move at once after every file, cut, move, delete, undo, redo and public holiday (W1, W2, W4, W5) | A tap opens the breakdown (W4). These are read-outs; there is nothing to change here | While open, the drawer covers the left of the days (W4). Otherwise only the figure box | W1 D A·M; W2 D·P A; W4 D·P A·M (drawer: A only); W5 D·P A·M | PASS: W4's five figure readers agreed at every step. W2 O1 (a reload before the war's first write drops the demo's opening balances) is D56, not a finding |
| R12 | Leave War: the manning rows, the manning sheet, the under-manned list | YES. A man with 4 or 8 records on a day falls by 1, once; a posted-out or not-yet-posted-in man never counts; an award counts nobody (W4, W5). On 29 Jul with six of seven IWSOs away the row reads 1, red, and a man with LL morning + ATT C afternoon is away ONCE (W6) | YES. A row's name opens its sheet; the under-manned list and its jump work, admin and member (W6) | Nothing | W4 D·P A; W5 D·P A; W6 D·P A·M | PASS. The DUTY line is not drawn (recorded, plan R12) |
| R13 | Leave War: the OIL tracker | YES. Each credit's reason, giver and days ("+1 18 Jul AUTO Weekend/PH Duty", "+3 … award · 3 days"), and the balance (W3, W5) | YES for the admin. Edit an award's days, reason and giver; bad values are refused with words; one Undo takes an edit back. The code did not follow the days (W3-F6) | Nothing recorded (W3, W5) | W3 D·P A; W5 D·P A | W3-F6 FIXED `3531d20c`. Otherwise PASS |
| R14 | Leave War: the remarks sheet (published, approved leave) | YES. On a published war, approved leave opens the note sheet only: Remarks and Save (W3) | YES. Save works for the admin on any leave and for a member on his own. Another man's leave is read only (W3) | Nothing recorded (W3) | W3 D·P A·M | PASS. W3-F7 FIXED `5b58041a`: after a keyboard war switch the note sheet became a live bid sheet, and its Clear deleted published leave |
| R15 | Leave War: the war picker and the stage control | YES. The period picker, + New (admin), and stage forward and back (admin; the member has none) (W3) | YES. Switch wars. A draft war made by + New shows no decision row. An undo made in another war goes back to that war (W3 E2, W5 6b). A sheet left open across a KEYBOARD switch stayed live (W3-F7, W5-F3); with a finger or a mouse the sheet closes first (PASS) | The open sheet's scrim covers the picker for the mouse (W5) | W3 D·P A·M; W5 D A; W1 D A (moved the stage to closed) | W3-F7 and W5-F3 FIXED `5b58041a`. Pressing Enter on "Stage advance" behind a sheet was not tried (W5) |
| R16 | Edit Schedule week: the Unavailable rows and Personal Inputs | YES. The absence on its day with type, times, the man's puck, remarks and the LATE mark (W1, W2, H1). A leave across the week boundary shows on both weeks (W5). "1 pending" shows on the week head (H1, W1, W2). No source names the Personal Inputs half | YES. The type button opens the edit window, which asks on a change of person. Reassigning by arm-then-tap or by palette drag skipped the questions (AB4) | On the phone, arming opens the AIRCREW drawer over the top bar (W2). The LATE mark sits on the remark (W1) | H1 D·P A; W1 D A; W2 D·P A (palette drag at D only); W5 D A | AB4 FIXED `eecacad5`. Otherwise PASS |
| R17 | The scheduler board: the Unavailable and Personal Inputs panels | YES. Every absence incl. the late one on a published day, not the course (W6) | YES. The late leave's edit window opens; + Add opens; Personal Inputs folds and opens. A member has no Edit Schedule, as intended (W6) | Nothing | W6 D·P A (count also H1, W1, W2) | PASS |
| R18 | View-only Sched: the ISSUED face and the working-draft peek | YES. The face kept what it was issued with through every late change (H1, W1, W2, W5); a medical row shows its type and remarks to admin, member and guest (H4, W2). "Working draft — not issued" shows the late input and says so (W6) | Nothing to press on a medical row; no door to its document (recorded, D211 "as today") | Nothing | H1, H4, W1, W2, W5 D·P; W6 D·P A·M | PASS |
| R19 | The crew picker and the ALL AVAIL window / count | YES — a man on leave is struck and out of "N free" when the column reads the right day; on the DESKTOP board with nothing armed the column read the WEEK's first day, so Thursday's board showed a man on leave Thursday as free (W6) | YES — dragging him on raised "On leave but tasked" only after (W6) | Nothing | W6 D·P A (the member has no board) | **FAIL → FIXED** `02721580`, red first (`boardcrew.test.tsx`): the board's column answers for the board's own day; re-walked (§11) |
| R20 | The warning list and the ⓘ day panel | YES. "Basher — On leave but planned to fly…" (W1). Both ⓘ panels read "1 unpublished edit" (H1, W1, W2). "Became a holiday after publishing — republish for its OIL" (W5) | These are read-outs. The guest has no ⓘ; it must not (H4) | Nothing recorded | W1 D A; H1 D·P A; W2 D·P A (ⓘ counts); W5 D A; H4 D·P G (absent, as it must be) | PASS. A medical on a worked day (Q6) was not walked (W2). No source names N14 (a worked weekend that earns nobody says so) |
| R21 | History (the edit log) | PARTLY. It showed only "Input removed … Saber". After an Inputs-page add, an edit, a war approval, an un-approve and a medical cut, the list held one line (H5) | Read only | Nothing recorded | H5 D A | AB8 (a): the Inputs page's own Add wrote no line. FIXED `643b616e` (§0 lists AB8a as fixed). AB8 (b): should the war's approvals and moves, and an input's edits and cuts, appear in History? A question for him. H5's "edit" step did not run (the script timed out on Save) |
| R22 | Export: the Inputs file and the schedule export | Inputs file YES (AB10 fixed — From / To); the schedule CSV carries the day "Published — Original" as issued; no Unavailable list in it (already `[FLAG-EXPORT]`) (H6, W6) | YES — Export (admin); the member has no export buttons (W6) | A downloaded file | H6 D A; W6 D·P A·M | PASS (AB10 FIXED `643b616e`) |
| R23 | Week Insights | not walked | not walked | not walked | NOT WALKED — on purpose: what it should count of a working copy is his open question (`[INSIGHTS-WORKING-COPY]`) | no result, by his question |
| R24 | The guest view | YES. The published medical row, as a member reads it (H4) | Nothing to act on, as it must be: no Inputs page, no war, no ⓘ (D213, D215). The waiting screen offers "View the schedule" (D221) (H4) | Nothing recorded | H4 D·P G | PASS |
| R25 (= W3's R38) | The read-only sheet ("Leave from Raptor" / "OIL the app credited") | YES. Each source gets its own sentence: filed on the Inputs page; earned off the published schedule; earned off a duty input (W3, W5, H4) | Nothing: ✕ only, by design (W3, W5). On the admin's credit days the bid sheet opens with that sentence at its foot, so he can add an award (W3) | Its source sentence (W3) | W3 D·P A·M; W5 D·P A·M; H4 D·P M | PASS. AB9 FIXED `643b616e` (a medical read "APPROVED"). W3-F7 FIXED `5b58041a` (the sheet became a bid sheet after a keyboard switch) |
| R26 | The header's save status (the only sign of a storage fault) | YES. "Not saved — Retry" while storage refuses (H3) | YES. Retry lands the whole group. Closing on a failed save keeps none of it. A half-written save is finished at the next start, never doubled (H3) | Nothing recorded | H3 D A | PASS (FS1–FS3) |
| R27 | The war's clash strip (admin only) | YES — "Fable: weekend/PH work earns FO but 18 Jul 26 holds LL — resolve on the sheet"; the member sees none (W6) | Read-out; its "resolve on the sheet" names no control on the sheet — filed `[ABSENCE-SMALL-SEEN]` 1 | Nothing | W6 D·P A·M | PASS (words filed) |
| R28 | Undo / Redo (one shared timeline) | YES. The war's pair for everyone, and Edit Schedule's top bar for the admin. The labels name the step (W1, W2, W5) | YES. One press is one step: a calendar move, a medical cascade, a whole bulk block, a filing with its notice. Both are off after a reload (W3, W4). Recorded for later: a posting is not on the timeline (W3, W5); after a sign-out, only the person's own steps (D148, W5). Redo put a bid back over a medical filed since (W3-F8) | Nothing recorded | W1 D·P A, D M; W2 D·P A·M; W3 D·P A·M; W4 D A; W5 D·P A·M | W3-F8 FIXED `3531d20c`. The rest is recorded for the change-recording re-test (D148) |
| R29 | A notice's actor | YES. "…bid was replaced by LL (Saber)": the callsign (D166 (5)) | YES. OK, seen, for the admin and the member | On the phone the amber toast sits over "OK, seen" (W5 O1) | W1 D·P A; W4 D·P A; W5 D·P A·M; H3 D A; H4 D·P (evidence "Walked and CORRECT") | PASS |
| R30 | The pending list | YES — "1 pending" lists the late input ("Drifter · LL filed") on the week and the board (W6) | **NO** — its line cannot be tapped: "no place of its own on the schedule to go to", though his Unavailable row stands (D99) | Nothing | W6 D·P A | **FAIL → FILED** to `[DRAFT-PENDING]` (the list becomes that window; its jump must reach an input's row) |
| R31 | The Amendments panel | YES. "Fri · 1 change · 1 input change", agreeing with every other count (H1, W1, W2) | Only read in the walks. W5 published AL1, but no source says through which control | Nothing recorded. It is not drawn at phone width (H1: "no panel at this width") | H1 D A (P: not drawn); W1 D A; W2 D A | PASS |
| R32 | The four confirm sheets (medical clash, upchit, OIL ask, document ask) | YES — all four; the OIL question comes before anything is written, Save off until answered; a leave over a named weekend duty files with an amber note instead (W6) | YES — no default where the rule says none; Cancel / Escape write nothing; each fits the phone (W2, W5, W6, H1) | Nothing | W2, W5, H1 D·P A; W6 D·P A | PASS |
| R33 | The document viewer | YES. The title, the type's words and remarks, a "1 of 2" pager, a picture or PDF (W2) | YES. ‹ ›, Close and Escape. Edit input for the admin or the man himself. Upchit on a Pending card. On another man's document a member gets Close only (W2) | Nothing recorded | W2 D·P A·M | PASS. There is no door from the schedule: recorded, not a finding (W2 O5; D211 "as today") |
| R34 | The figure readers (the cell, the breakdown, a person's every-figure sheet) | YES. The breakdown shows the opening balance, granted, and LL / OL taken, with a half charged once. It also shows the every-figure sheet's eight figures and MED TOT (W4, W2, W5) | YES. Tap a row to see the breakdown; ✕ closes it. Set (admin balances) was not walked by W4 (W2 used it in a probe) | Nothing recorded. On the phone, opening the figure sheet jumps the grid back to January (W4) | W4 D·P A·M; W2 D·P A; W5 D·P A | PASS: the five readers agreed at every step. The jump to January is `[AMEND-SMALL-SEEN]` item 6 (filed; W4's sharper form added) |
| R35 | The toast | YES. Cut notes in amber, "This replaces your LL bid…", "Moved to …", refusals in amber (W1, W5, W2) | A read-out | At 390 px it covers the tap list's "OK, seen" for about 4 s; taps pass through (W5 O1). On the calendar it covers the month's foot (W1) | W1 D·P A, D M; W2 D·P A; W5 D·P A·M; H2 D A | PASS. W5 O1 recorded, not a finding |
| R36 | Print | YES — the printed day "Published — Original" as issued; no Unavailable list (already `[FLAG-EXPORT]`) (W6) | Print | A print view | W6 D A | PASS |

## 5. The orders walked (both orders, after publishing, after undo, reload and a war switch)

- **W1:** Drag a leave onto a bid (C1a), then undo, redo and reload. Drag a multi-day leave over bids (C2), then undo. Drag leave onto a published weekend duty (N4W), then undo. On published Friday: a late drag on, then undo, redo and reload, then dragged back to 0 (PUB-ON / UNDO / BACK). An issued input dragged off and back (PUB-OFF), with a reload. A calendar move, then the top bar's Undo and Redo (TOPBAR). Member: his bid, then his own leave dragged onto it, then his Undo (M-B6). Stage set to CLOSED, then the member files and drags (M-CLOSED). Member reload (M-RELOAD). **D A·M; the finger drags at P A.**
- **W2:** A medical drag, then one Undo, Redo and reload (A1). The three-piece cascade, then one Undo, Redo and reload (B2). Split and upchit, then one undo (B3, B6). A document removed and saved, then reload (D4). **After publish:** a medical extended, an upchit, a delete, a calendar drag onto the day, then Undo back to 0 (P1–P4). Member files, drags, then the war's Undo (P6). **D·P A·M.**
- **W3:** Each of the seven bulk actions, then one Undo, Redo and reload (D A). The one window: Ack and Move, then Undo and Redo; the stage open → closed → published, then reload (D·P). A posting, then the top bar's Undo; reload; each posting's own Undo (D·P). Three award fields edited, then one Undo (D·P). **War switch:** by finger it closes the sheet (P PASS); by keyboard the sheet stayed live (D, W3-F7). An undo made in the other war (E2). Bid, Undo, a medical filed, then Redo (E3, W3-F8). Reload, after which Undo and Redo are off (E4). Approve, then a medical, refuse, delete or move over the approved leave (W3-06, D). Member at CLOSED and PUBLISHED (D·P M).
- **W4:** Four records filed one after another, the figures read after each (F1). OD then LL, and LL then OD (Astra 19). Course then OL (S28). Post out, then file (F2). Post in, then file before it (F3; P partly, the script timed out). Bid, then filed over, deleted, and OK, seen (F4). Eight records in sequence, then OK, seen (G2). An overnight leave, then the Saturday desk, publish, and read (AB6, D·P A·M). A fill, then Undo, Redo and reload (D A). Member files four records, then reload (D·P M). A refused move, then a staged move, then Confirm (G1b, P).
- **W5:** **Order 1**, file ↔ bid: undo, redo, reload, OK seen, and the member's Undo after signing in (D·P A·M). **Order 2**, approve ↔ medical, unpublished and published: undo, redo, reload (D·P). **Order 3**, publish ↔ file: undo (the four sign-offs come back), redo, reload, AL1. N9: publish over a bid, then undo and redo of the publish, then an AL (D·P). **Order 4**, award ↔ work: undo, redo, reload, unpublish, an AL that takes him off, reload (D·P). **Order 5**, Post out ↔ file: the top bar's undo, the posting sheet's Undo (W5-F1), reload, the published pair (W5-F2). **Lifecycle:** a war switch with the sheet open (mouse PASS, keyboard W5-F3); undo across wars; a leave across the new year; a week change and back, with undo, redo and reload; sign out and in as the other person (recorded, D148); resize 390 ↔ 1440 with the tap list, the bid sheet and the clash window open. **Public holiday:** declared after the leave, and after publishing, then republished as AL1 (D·P).
- **Host:** **H1.** Filed before publishing, then publish. Then, on published Friday: a late leave from the Inputs page, then deleted back to 0 and signed again; a late war approval, then cleared back to 0; a late medical; a medical cutting issued leave (D·P A). **H3:** a failed save, then retry; a close on a failed save; a half-written save, then the next start (D A). **H4:** publish, then read as admin, member and guest (D·P). **H5:** add, edit (did not run), approve, un-approve, medical cut, delete, then History (D A).

## 6. The break tests (bug-check order §8.4)

The seven wires plan §7 names, each broken once in a SCRATCH copy of the branch (never the working one — a reader was
reading the code), its tests run, the red tests named, the wire restored. Script: `scripts/handpass/ab/ab-breaks.py`.

| wire | broken how | what went red |
|---|---|---|
| the absence door — a filing over a bid replaces it | `inputgate.ts replaceBids` returns nothing | 4 — `inputgate.test.ts` "a clashing input replaces an undecided bid" ×3; `scenarios-doors.test.ts` "an EDIT of leave onto a pending bid replaces the bid" |
| the absence door — the clash refusals | `inputgate.ts vet` returns nothing | 5 — `inputgate.test.ts` "refuses a second leave on the same time", "refuses leave over a medical", "a redo that would put two leaves on the same time is refused"; `scenarios-doors.test.ts` "REASSIGN to a person who already has leave", "an EDIT that moves leave onto another leave" |
| the merged read — the ladder | `dayview.ts compareContrib` returns 0 | 6 — `dayview.test.ts` "the main code (agreed ladder)" ×6 |
| the tap list | its first record dropped (`DayList.tsx`) | 5 — `daylist.test.tsx` (the OIL blocks, a replaced-bid notice, the published note, back to a bid, …) |
| the bid sheet's refusals | no refusal past the stage / row / medical-code checks (`store.ts cellProblem`) | **1 only** (`freehalf.test.tsx` "a bid refused by the medical names its hours") — thin; **pinned**: `inputgate.test.ts` "the bid sheet refuses by name" (the two sentences a bidder meets most) — now 2 red |
| the calendar drag's refusal | a member may move another man's input (`caldrag.ts`) | 1 — `caldrag.test.tsx` "refuses to move someone else's input" |
| the issued face | reads the live inputs, not the ones it went out with (`engine/inputs.ts inputsOn`) | 6 — `latepub.test.tsx` (D177, D178, D179) |
| the war grid's repaint on an Inputs change | the input gate's repaint route off (`sync.ts refreshAbsencesAndRepaint`) | **0 of 60 — no test**, by proof; **written**: `repaint.test.tsx` mounts the real grid with the real wiring and watches the box as a leave is filed, re-typed and deleted on the Inputs page — red with that route off (a re-type never reached the grid), and red with the repaint off at its root |

## 7. What was NOT walked, and why — and errors seen

- **W1:** The member on the phone (time; the admin's phone walk covers the gestures). A finger drag onto or off a published day (the desktop walk covers the rule; the same machine). Redo and reload after the multi-day C2 drag (walked on C1a and the published drag instead). Medical chips (W2's ground). iOS Safari (Chromium only; W1-F1 and F2 should be seen on his phone). A drag into a stashed week other than the demo week. A keyboard move (none exists).
- **W2:** The board's Unavailable panel (the same function as the week's row; not driven separately, for time). A finger drag of a palette puck on the phone (the phone was walked by arm-then-tap). The warning list for a medical (Q6, the ⓘ) and History (the host's ground). The Medical view's "as of" date picker. A calendar drag of a medical that carries a "till" (AB3's family, W1 / W3). The guest view and storage faults (the host's ground).
- **W3:** Touch drag-select and the phone's two-step move (W4's ground). The tap list's Back to bid / Refuse / Delete / Move on approved leave at OPEN and CLOSED (walked only at PUBLISHED). R27, R29 and R26 (the host's ground). D148 (decided, not built). A member's drag on a published war (walked at CLOSED only). Fable S17 (seen in passing; left as Fable's question). A member on a busy day of his own in the tap list.
- **W4:** A SANS man's published weekend credit while hidden (the board will not put a SANS man on a duty desk, and the demo Saturday has no flying line; beyond time). The DUTY tally (it has no reader on screen). The every-order pairs (W5's ground). Undo and redo by finger, and after a move (desktop, after a fill only). Bulk Approve / Refuse / Delete (W3's ground). The manning sheet. The member's desktop drag (finger only). Set on the every-figure sheet.
- **W5:** The manning DUTY tally (in the manning sheet, which was not opened). A war switch with the tap list, drag selection, posting sheets or read-only sheet open (bid sheet only; W3 walked the others). Enter on "Stage advance" reached by Tab (not tried: it would change the stage). The phone for 02b, 05b, 05c, 05d, 06 and 06b (desktop only), and the manning rows at 390 px (folded). A public holiday inside a pilot's 15-day run (not set up). History, the guest and storage faults (not its ground). "Medical then approve" through the approve door (unreachable by design).
- **Host:** H1 left the war's own Move to W3. H2 did not reproduce the war-written "till" (the war's approve did not take; left to W3, which found W3-F9). H5's "edit" step did not run (the script timed out on Save). H2, H3, H5 and H6 ran at desktop width only.
- **Filled since (W6, a first walk of the rows nobody reached, 26 Sep 26):** R12's manning sheet and under-manned list,
  R17's panels, R18's working-draft peek, R19, R22's schedule export, R27, R30, R32's OIL question, R36 — all walked at
  both widths (§4). **Still not walked, on purpose:** R23 Week Insights (his question, `[INSIGHTS-WORKING-COPY]`).
- **Everywhere:** iOS Safari — every walk ran Chromium; the phone gestures (W4-1, W1-F1, W1-F2, the chip swipe) are
  on his look card for his own phone.

**Errors seen:** no console error, page error or failed request in any run of the first walk, the re-walk or the
second re-walk (each walker's sheet, "Console errors").

## 8. The two final code reads (Fable 5.1 and Astra, blind to each other, with this sheet in hand)

Brief: `docs/superpowers/briefs/2026-09-26-absence-final-read-brief.md` (bug-check order §4's finder wording and D56,
verbatim). Reports: `2026-09-26-absence-final-fable.md`, `2026-09-26-absence-final-astra.md`. Every finding was
reproduced by a test that failed first, and compared against `main`.

| # | finding | found by | on `main`? | disposition |
|---|---|---|---|---|
| FR1 | Undo / Redo made a bid LIVE again over a medical filed since, when the bid was already on the day in another state — a refused bid turned back into an acknowledged one (Redo), or a refusal undone (Undo) | Astra 1, Fable F2 | yes — `main` had no restore check at all; this branch's first check (W3-F8) asked only about a bid that was missing | **FIXED** `bb8bf8bc`, red first (two tests in `inputgate.test.ts`, both red on the code before it) — `store.ts restoreBlocker` asks about every bid a restore makes live, skips only one already standing as the same live bid, and judges the day as the restore will leave it (`sync.ts restoreAbsencesOf`); undoing a medical that replaced a bid still gives the bid back in one step (its own test). Fable's Undo-of-a-refusal is reached once Undo reverses only your own changes (D148, decided, not built) |
| FR2 | A Post out that had archived him, moved to a later date or with "Archive on PO date" turned off, left him archived — off the Quals roster while the sheet said he stays | Astra 2 | yes | **FIXED** `0eb0d754`, red first (`poarchive.test.ts`) — the Post out's pass marks its own archive (`archivedBy: 'po'`); every posting door goes through `sync.ts postOut`, which puts him back when the new posting no longer archives him today |
| FR3 | A calendar DRAG (and the upchit's date box) kept the remark's old "till <date>" / "on <date>" — the AB3 defect at a door the walk did not reach | Astra 3, Fable F1 | yes | **FIXED** `e29aa9f2`, red first (`caldrag.test.tsx`) — the one save every door uses rewrites the app's token when the dates change, keeping the typist's other words; a remarks-only edit is left alone |
| FR4 | "Undo post out" also brought back a man the admin had archived by hand on Quals | Astra 4 | NEW — W5-F1's fix read any archive with the switch on as the Post out's | **FIXED** `0eb0d754`, red first — only the Post out's own archive (`archivedBy: 'po'`) is taken back |
| FR5 | A dragged block's Delete over a leave the WAR approved took the leave and left the bid beside it | Fable F3 | yes | **FIXED** `27e367bd`, red first (`inputgate.test.ts`) — the war's own requests are taken first, whoever owns the leave on top |
| FR6 | The event sheet (an event, PH or blocked band) survived a war switch | Fable F4 | the new guard's gap; reached only by a switch made in code (the shade covers the picker, Tab is held) | **FIXED** `27e367bd`, red first (`sheetfocus.test.tsx`) |

**Nits (Fable):** N1 the cell id read from the left — **fixed** `bb8bf8bc`. N2 the dead days-writer without N19's code
rule — **fixed** `27e367bd`. N6 this sheet's W3-F5 row and a code comment said a PO from the bid sheet turns it into
the Post out sheet (it closes it) — **corrected**. N3 the edit window keeps its own copy of "what to ask" beside
`medAskFor` — recorded, not a defect (the walk shows it asks; W2 A1, A2). N4 the read-only edit window's inert body
"kills the document control" — **not a finding**: that field only ever offered upload and remove, never a viewer; the
names still show, and a member opens documents from the table and the Medical view (D211 holds). N5 one error line under
both the PI and PO panels when both are open — recorded, cosmetic. N7 the Quals Restore clears a posting it did not
make (unchanged `main` code) — **filed** `[PO-RESTORE-POSTING]`.

**Their explicit negatives agree:** "till" on every cut (only the drag was missing — FR3); a medical moved asks at all
four doors; every posting door shows the refusal; every restore path goes through the one check; every Leave War sheet
holds Tab; Approve / Refuse / Ack / Fill reach beneath; both award editors set the code; every member door into another
man's input is read only; the shared functions' callers are unbroken. Neither found a stored-data-only problem (D56).

## 9. The gates, on the final code (27 Sep 26, one run under the PC lock, a quiet PC)

| gate | result |
|---|---|
| unit (`npx vitest run`) | **6274 / 6274** (388 files) |
| build | clean |
| tfin (`node reference/tfin.js`) | **728 / 0** |
| e2e (`E2E_PORT=4192`) | **474 passed**, 48 skipped |
| Tracker smoke | **443 / 0** |
| rulecheck | OK — Q14 now named by a test (`raptorsheet.test.tsx`), so it left the baseline in the same change |
| docsize | OK — every record accounted for; `OUTSTANDING.md` over its tripwire (deferred, D29 — this branch carries code) |

## 11. The re-walk of the fixes (on the rebuilt app, `index-KbvILaPW.js`, under the PC lock)

Each walker re-ran the scripts that showed its findings into `docs/img/handpass/2026-09-26-absence/rewalk/<who>/`
(the first walk's pictures kept as the defects' evidence), read every result against the NEW flow and looked at every
picture. Sheets: `docs/handpass/parts/2026-09-26-absence-rewalk-{w1..w6}.md`; the host's logs
`…-rewalk-h{1,2,5,6}*.txt`. Brief: `docs/superpowers/briefs/2026-09-26-absence-rewalk-brief.md`.

| who | re-walked | result | new on the way, and what was done |
|---|---|---|---|
| host | H1 (both widths), Fable's first findings (H2), Edit history (H5), the export (H6) | H1 16 / 16 each width; H2 — AB3 (the Inputs-filed "till") and AB5 (the posting refusal said) now PASS, AB1 unchanged on purpose (his question); H5 — AB8a PASS; H6 — AB10 PASS. H2's war-side "till" check and H5's edit step never ran their setup (the same in the first walk — the script, not the app; W3 walked the war side) | the posting refusal printed the machine date ("Posted in on 2026-06-01") — **FIXED** `e0833c83`, red first (day-first, "1 Jun 26") |
| W1 | W1-F1, F2, F3, F4, FR3 | **all PASS** at both widths (120 checks; the member on a phone walked for the first time) | **NEW-1** (caused by W1-F1): a sideways swipe starting ON a chip opened its edit — **FIXED** `e7729577`, red first. **NEW-2** (on `main`): the calendar's "previous month" arrow, pressed five times, switched on the hidden drag readout — **FIXED** `390100c4`, red first. The read-only window drawing its locked fields as live: a look question, recorded |
| W2 | AB4 at all four doors (both answers of every question), AB3, the published day | **284 / 0** at both widths | the toast after "Keep HL" named the drop day, not where it landed — **FIXED** `660672b7`, red first; "A ATT C" → "An ATT C" — **FIXED** `660672b7`; a medical moved through the question sheet gains a "till" in its remark (the edit window's own behaviour — not a defect); the desktop week's arrow over the left day's type — added to `[AMEND-SMALL-SEEN]` item 4 |
| W3 | AB7, W3-F3 (Delete), F4, F5, F6, F7 (every road to a war switch found), F8, F9, FR1, FR5, FR6 | **21 / 21 rows, 178 checks, 0 FAIL** | N1, N2 (offered then refused — no harm, said) — **filed** `[LW-OFFER-ONLY-TAKEABLE]`; N4 an empty band on the posting sheets — **FIXED** `83c6e9be`; W3-F3's Move and AB2 unchanged, as recorded |
| W4 | AB6, W4-1, the eight-record day | AB6 **5 / 5**; the money unchanged at both widths; **W4-1 FAILED 9 / 9** — the fix did not work in the running app | **W4-1 FIXED again** `8210a585`, red first with the REAL sheet mounted on a coarse pointer (the first test had neither — observation 294): the open sheet's own tap shield closed it, and the swallow did not stop a listener on the same node. A one-off paint-over of a member's leave on the frozen balance column, not reproduced by a dedicated probe (4 / 4) — recorded |
| W5 | W5-F1, F2, F3, F4, F5, FR2, FR4 | **6 PASS / 1 FAIL** at both widths — F1, F3 (every Leave War sheet, not only the bid sheet), F4, F5, FR2 (all five posting doors), FR4 pass; **W5-F2 FAILED** — his row still vanished, with no undo at all | **W5-F2 FIXED** `1113f085` at its real cause, red first in a real browser (§3). NF2 the read-only / decide / note sheets' headings in the raw code ("*ATTC") — **FIXED** `e8a6f68e`, red first. Recorded: the read-only sheet for a timed medical gives no hours; a Post out dated the 1st leaves no hatched day to tap in that month on a phone; the posting sheet of a man archived by hand still says "Moves to the Quals archive"; a posting is not on the Undo timeline (R28) |
| W6 | R12, R17, R18, R19, R22, R27, R30, R32, R36 — a FIRST walk of the rows nobody reached | **7 PASS / 2 FAIL** at both widths, admin and member, after a reload (§4) | **R19** (on `main`): the desktop board's crew column read the week's day, so a man on leave showed free — **FIXED** `02721580`, red first. **R30** (on `main`): the pending list's late-input line cannot be tapped (D99) — **filed** to `[DRAFT-PENDING]`. Small things — `[ABSENCE-SMALL-SEEN]`, `[AMEND-SMALL-SEEN]` item 4 |

**The second re-walk** (the fixes made after the first re-walk's build, on `index-fBnh4WXe.js`, the walkers' own
scripts as `rw2-*.mjs`, pictures `rewalk2/`): the phone hold keeps its sheet at every hold length, admin and member
(`rw2-w4-07`, `-08`); a swipe starting on a calendar chip pages the month and opens nothing (`rw2-w1-15`); the corner
switch stays off through 18 presses of the calendar arrow (`rw2-w1-16`); a post-out in the month on screen keeps the row
at both widths (`rw2-w5-05h`); the board's crew column is its own day at both widths (`rw2-w6-03`); the posting refusals
read day-first and the blank band is gone (`rw2-w3-03`); the heading reads the box's notation (`rw2-w5-02b`); the
kept-medical toast names where it landed (`rw2-w2-01` — its two "fails" are the premise the fixes consumed: the old
bundle's name and the old "Moved to 24 Jul"). No console errors.

## 12. His five-minute look (the card handed to him)

On the branch's Vercel link, signed in as admin. About five minutes; a real phone for step 5.

1. **A late leave on a published day.** Publish Friday, then on the Inputs page file a leave for someone on Friday.
   Expect "1 pending" and the four sign-offs cleared; View-only Sched's Friday still shows what went out. Delete that
   leave again: back to 0 and the sign-offs back.
2. **A medical cuts a leave, and the words follow.** File a leave Monday to Friday with the remark "Bali", then ATT C
   on Wednesday. Expect two pieces, each saying its own last day ("Bali till <Tue>", "Bali till <Fri>"). Now drag the
   second piece a week on in the Inputs calendar: its "till" moves with it.
3. **Postings say why.** On the Leave War, tap a man's day → PO, and pick a date BEFORE his post-in: the sheet stays
   open and says why, in plain dates. Post someone out from today with "Archive on PO date" on: he moves to Quals'
   archive; tap his greyed day and move the date a week later: he is back on the Quals roster until then.
4. **Undo keeps the rules.** On the Leave War, bid LL for someone, press Undo, file ATT C for him that day on the
   Inputs page, then press Redo: it refuses, naming the ATT C, and the bid stays off the sick day.
5. **Your phone.** Leave War: hold a finger on one day and lift — the selection sheet stays up. Inputs → Calendar:
   swipe sideways to change month; tap a chip — its edit opens once and stays.

**Three questions ride with the card** (`OUTSTANDING.md` `[ABSENCE-ASK]` — nothing waits on them): should the bid
sheet's Clear and a dragged block's Delete leave an OIL award alone (it has its own Remove), or take it and say so?
Should the Edit history also record the Leave War's approvals and moves and an input's edits and cuts? Should a member
be able to open his own OIL award read-only at every stage, not only while bidding is open?
