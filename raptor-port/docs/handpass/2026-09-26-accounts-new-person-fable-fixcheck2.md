# Fable 5.1 — second fix check on `[ACCOUNTS-NEW-PERSON]` (`20e3d4f4` over `c0c1e213`), 26 Sep 26

*(Saved verbatim from Fable's report — a read-only subagent, brief
`docs/superpowers/briefs/2026-09-26-accounts-new-person-fixcheck2-brief.md`, blind to Astra's. Dispositioned in the
evidence sheet §6, round 3.)*

Read-only. Opened: the brief, both prior fix-check reports, the full `git diff c0c1e213 20e3d4f4` (code, tests, docs, walk script), `raptor-port/src/state/accounts.ts` (whole), `raptor-port/src/ui/UsersPanel.tsx` (whole), `raptor-port/src/ui/accounts-newperson.test.tsx` (setup + the diff), `engine/people.ts` (`nameToId` / `ID_BY_CS`), `state/roster-add.ts` (the PID-01 refusal), `leavewar/sync.ts` (archive/restore, grep), `accounts.test.ts:188` (archive keeps the account — still pinned), the walk file `walk3/walk.json` (57/57, `d-FC1-archived-account` and `d-FC2-archived-editor` both `ok:true`) and LOOKED at pictures 46 and 47 (the note renders word for word; the editor shows "Hex" selected under the ARCHIVED CALLSIGN tag). Ran read-only: `cd raptor-port && npx vitest run src/ui/accounts-newperson.test.tsx` → 25/25 pass. No edits, no server, nothing else run.

## Findings, most serious first

**1. (LOW, wording, optional) The account-plus-archived note's "it is him" path answers the request but leaves the man off the roster, and the note no longer says so.** NEW in `20e3d4f4` (this branch of the note is new text).
- **Where:** `raptor-port/src/ui/UsersPanel.tsx:131` (the `acct` branch, when `p.archived`).
- **Scenario:** Hex posted out → `runPoArchive` archives him, account kept. He is posted back in with a new defence mail, signs up as "Hex". Note: "Hex (archived) already has an account (hex) … change that account's sign-in under Accounts — that answers this request." Admin renames the sign-in → the request is answered (true), Hex signs in fine — but he is still archived: not on Quals' live list, no crew list, Leave War row frozen. The old note's "restore" was the wrong door for LINKING (that fix is right) but it was also the one step a returning man still needs, and the new text dropped it entirely. The "(archived)" hint and the row's ARCHIVED CALLSIGN tag show the state, not the step. (If the account was also switched off — the tag's own advice for someone who left — the editor's "Switch on" sits beside Save, visible; no words needed there.)
- **Why it is only LOW:** nothing harmful happens; the admin sees "(archived)" twice and Quals will show it; Restore is a real side effect (wipes the posting-out window) so the note must not push it unconditionally.
- **Fix (exact):** in the `acct` branch, when `p.archived`, append one clause after "that answers this request": " — and restore them on the Quals page once they are back." Then: (a) the archived+account test (`accounts-newperson.test.tsx`, "ARCHIVED and holding an account") narrows `not.toMatch(/restore|Pick them/i)` to `/restore them .* to link|Pick them/i`; (b) `np-walk.mjs` `d-FC1` narrows the same regex; (c) `ui-contracts.md` §Admin → Users adds the clause; (d) re-run only that walk step. Do not touch the non-archived account note.

**2. (LOW, wording, optional) When the matched account is the signed-in admin's OWN, the door the note names is locked for him.** Older (the own-account guard is `[ACCOUNTS]`, D166); the clause that now points at it is new in `20e3d4f4`.
- **Where:** `UsersPanel.tsx:131` (the note) vs `:200`/`:214` (the row is `disabled` for `own`) and `accounts.ts:257` (`updateAccount` refuses "You can't change your own account — ask another admin").
- **Scenario:** Saber (`ad`, the only admin in the demo world) gets a new mail, signs up on his phone as "Saber"; on the desktop as `ad` he reads "change that account's sign-in under Accounts" — the `ad` row will not open. The row's tooltip does say "another admin can change it", and a second admin can do it, so it is not a dead end — just a note that does not know it is talking to him.
- **Fix (exact, if wanted):** in `rosterMatch`, compute `own` the way `AccountRow` does (`SESSION.user === acct.id || me() === hit`) and, when true, replace "change that account's sign-in under Accounts" with "another admin changes that account's sign-in (it is your own)". One test: sign up as "Saber" from a fresh name, approve as `ad`, assert the note. Skip if you judge the tooltip enough.

## Each fix in the brief, judged for completeness

| Finding | Complete? |
|---|---|
| Both #1 (archived + account → "restore to link") | **YES.** `rosterMatch` now asks `accountOfPid` before `p.archived`; "(archived)" shown; picker omits him; the test covers the callsign AND the bare id; walk step and picture 46 confirm. Restore is no longer suggested where it could not link. |
| Fable #2 (only the someone-else way out) | **YES.** Both ways named; the "him" way is real: `updateAccount` rename → `nameProblem` cannot refuse a listed request's name (no listed request's name ever has an account — `requestAccess` refuses, the loader drops, every account-creating write and the rename remove it) → `answered` fires → the request row and the Admin count go. Test strings, `HAS` in the walk, ui-contracts and register NP5 all carry the new text verbatim. |
| Fable #3 (archived, no account) | **YES.** `SOMEONE_ELSE` appended; pinned by its own test. |
| Astra #2 (editor drops its archived person) | **YES.** `keep` now precedes the archived/has-account filter; the editor test asserts the option, the selected value, a sign-in-only save keeping `pid: 'rocky'` durably, and re-open; picture 47 shows it rendered. |

## Explicit negatives — checked and found nothing
- **`linkablePeople(keep)` cannot offer a forbidden person.** The only caller passing `keep` is `AccountRow` → `PuckSelect keep={a.pid}` (`UsersPanel.tsx:229`); the add and approve pickers pass none, so they are byte-for-byte the old list. `!p.special` sits OUTSIDE the `||`, so ALL / ALL AVAIL are never offered even as `keep`. Another account's person as `keep` is impossible: one person one account is held at the loader (`seenPid`, `accounts.ts:100`), at `pidProblem` for add/approve/update, the two new-person writes mint a fresh pid, and the lock-out fallback filters the seed admin's pid before pushing (`:112`). An archived person who is not this account's own still fails `!p.archived`. A `keep` whose person is gone from `PEOPLE` drops out (`p` undefined), as before.
- **`updateAccount` still refuses what it should.** Leaving the archived person in place sends no `pid` (`save()` diffs against `a.pid`, `:207`); even if it did, `pidProblem(a.pid, a.id)` passes both the own-account and the own-archived checks (`:233-234`). Picking "Pick…" → refused "Pick the callsign or name…". Moving the account to a live free person is allowed, after which the archived one leaves the picker and can only come back through Quals Restore — consistent with `pidProblem`'s archived refusal. Role/on-only patches never touch `pid`; a rename never touches the admin lock.
- **Every sentence names a door the screen has:** "under Accounts" (the `Accounts` block on the same panel, desktop and phone drill-in), "New person" (the segmented pair on the approve form), "restore them on the Quals page" (`restoreArchivedPerson`, the Archived section). The one older pointer left as the plan wrote it: `roster-add.ts:81` still says "restore … instead" for an archived callsign typed into New person — reachable only if the admin ignores "another callsign or name"; my first fix check said leave it, and I stand by that.
- **Nothing nearby broke.** `rosterMatch`'s live-no-account branch is unchanged; `who` carries "(archived)" only in the account branch; `startApprove`'s mode choice unchanged. `grep linkablePeople|pidProblem|accountOfPid` over `src` shows no other reader. The register NP5, ui-contracts and the walk script's `HAS` string match the code text exactly.
- **Tests pass for the right reason:** the archived+account test asserts `#apvModeRoster` pressed, Hex absent from the picker, the note's exact opening for both spellings, `(hex)` named, and neither "restore" nor "Pick them"; the editor test reads `accountByName('hex2@mail').pid` after a save, not just the DOM; both restore the seed state in `finally`.
- **D56:** neither finding is about stored data; both are code text.

## Verdict: **CLEAN** — the two items are optional wording; nothing here should hold "merge live".
