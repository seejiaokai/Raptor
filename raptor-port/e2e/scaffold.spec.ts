import { expect, test } from '@playwright/test'

// Placeholder so `playwright test` has a suite to run before phase 4
// adapts the reference probes. No dev server needed yet.
test('playwright harness runs', () => {
  expect(true).toBe(true)
})
