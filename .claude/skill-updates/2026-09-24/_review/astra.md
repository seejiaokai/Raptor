# Astra (Codex gpt-5.6-sol, high) — independent read of the 24 Sep 26 drafts (D70), verbatim

1. [bug-check-order.md:261](</C:/Users/User/projects/Raptor/.claude/skill-updates/2026-09-24/_docs/raptor-port/docs/bug-check-order.md:261>) contradicts D16 by deleting its own-port requirement without a new owner ruling. Observation #213 can freeze artifacts, but cannot narrow D16. Replace lines 261–266 with: “Each walker keeps D16’s separate world: its own port, fresh browser context/tab, and demo-data copy. Nobody rebuilds a preview while it is being walked; the host may typecheck without emitting and run unit tests; rebuilds, gates and re-walks wait.” Keep the shared-preview refinement OPEN pending an owner ruling.

2. [systematic-debugging/SKILL.md:81](</C:/Users/User/projects/Raptor/.claude/skill-updates/2026-09-24/systematic-debugging/SKILL.md:81>) incorrectly groups linked worktrees with fresh clones. Git normally shares repository-local config across worktrees; only worktree-specific config differs when enabled. [Git’s worktree documentation](https://git-scm.com/docs/git-worktree) confirms this. Replace with: “A fresh clone or CI checkout lacks another clone’s local config and falls back to machine defaults. A linked worktree normally shares repository config; when `extensions.worktreeConfig` is enabled, compare its worktree-specific config too.”

3. [systematic-debugging/SKILL.md:87](</C:/Users/User/projects/Raptor/.claude/skill-updates/2026-09-24/systematic-debugging/SKILL.md:87>) loses half of observation #210’s actual safeguard: `GIT_TERMINAL_PROMPT=0` alone does not explicitly disable Git Credential Manager interaction. The project fix uses both variables. Replace the parenthesis with: “for Git on Windows, set both `GIT_TERMINAL_PROMPT=0` and `GCM_INTERACTIVE=never`.” GCM documents its separate interactive control for unattended services. [GCM configuration](https://github.com/git-ecosystem/git-credential-manager/blob/main/docs/configuration.md?plain=1)

4. [bug-check-order.md:393](</C:/Users/User/projects/Raptor/.claude/skill-updates/2026-09-24/_docs/raptor-port/docs/bug-check-order.md:393>) requires `elementFromPoint` to return the surface itself. A legitimate child at the centre makes that fail even when layering is correct. Replace the assertion with: “let `hit = document.elementFromPoint(x, y)` and assert `hit === surface || surface.contains(hit)`.”

5. [writing-plans/SKILL.md:55](</C:/Users/User/projects/Raptor/.claude/skill-updates/2026-09-24/writing-plans/SKILL.md:55>) changes observation #231’s “control against the old checker” into “run against the old structure.” Those are different controls. Replace the parenthesis with: “replay every known failure mode in a self-test, including the corresponding control run against the previous checker, and record the expected fail/pass result for each.”

6. [gates-and-deploy.md:19](</C:/Users/User/projects/Raptor/.claude/skill-updates/2026-09-24/_docs/raptor-port/docs/gates-and-deploy.md:19>) weakens #222’s regression threshold from “two stops on the change and none on base” to the ambiguous “Stops.” Replace line 22 with: “Two isolated stops on the change’s build and none on the base’s is a regression: stop there.”

7. [bug-check-order.md:467](</C:/Users/User/projects/Raptor/.claude/skill-updates/2026-09-24/_docs/raptor-port/docs/bug-check-order.md:467>) says a multi-finger gesture is “one CDP call.” The cited worked example uses a sequence. Replace with: “Drive multi-finger gestures through an `Input.dispatchTouchEvent` sequence—`touchStart`, one or more `touchMove` calls, then `touchEnd`—with two touch points.”

8. [systematic-debugging/SKILL.md:70](</C:/Users/User/projects/Raptor/.claude/skill-updates/2026-09-24/systematic-debugging/SKILL.md:70>) and [test-driven-development/SKILL.md:136](</C:/Users/User/projects/Raptor/.claude/skill-updates/2026-09-24/test-driven-development/SKILL.md:136>) incorrectly call a `MutationObserver` callback “the same task.” It runs as a microtask after the mutation-producing code returns and before the browser’s next rendering opportunity. [MDN microtask guide](https://developer.mozilla.org/en-US/docs/Web/API/HTML_DOM_API/Microtask_guide) Replace both formulations with: “Arm a `MutationObserver` before the action and measure in its callback, during the microtask checkpoint before the next rendering opportunity.”

9. [test-driven-development/SKILL.md:132](</C:/Users/User/projects/Raptor/.claude/skill-updates/2026-09-24/test-driven-development/SKILL.md:132>) leaks Tracker-specific absolutes into a generic upstream skill (“the tool,” “nothing else selected,” “never by a count”). Replace with: “Before the action, assert every premise and identify state created by this case with a unique handle rather than an aggregate the fixture may already satisfy. For rendered geometry, use a measurement that includes the relevant transforms.”

10. [test-driven-development/SKILL.md:138](</C:/Users/User/projects/Raptor/.claude/skill-updates/2026-09-24/test-driven-development/SKILL.md:138>) says never to use platform semantics for failure tests. That is wrong when permissions or filesystem behavior are the contract. Replace with: “For deterministic recovery tests, inject failure at the intended call. When platform behavior itself is the requirement, exercise it on every supported operating system.”

11. [bug-check-order.md:429](</C:/Users/User/projects/Raptor/.claude/skill-updates/2026-09-24/_docs/raptor-port/docs/bug-check-order.md:429>) presents OIL-specific `lib.mjs` as the generic shared helper, while Tracker uses `trk-lib.mjs`; [line 461](</C:/Users/User/projects/Raptor/.claude/skill-updates/2026-09-24/_docs/raptor-port/docs/bug-check-order.md:461>) names TypeScript e2e helpers as though hand-pass `.mjs` scripts can call them directly. Replace with: “Reusable drivers live under `scripts/handpass/`; use the surface-specific helper (`lib.mjs` for OIL, `trk-lib.mjs` for Tracker). In e2e tests use `lwRole`/`raptorRole` from `e2e/app.ts`; in `.mjs` walks call the corresponding localhost bridge methods through `page.evaluate`.”

Explicit negatives: #198, #204 and #230 are already stated clearly enough to close; #120/#122 correctly remain OPEN under D73. I found no deleted safeguard. The condition-waiting addition does not contradict its flaky-test trigger or the external-condition handling. The gate text preserves D84’s exception and D151; the handoff additions preserve D69, D29 and D145. D17 is faithful. The remaining named commands, effort values, APIs and paths exist and are correctly spelled; I found no further material bloat or generic-skill leakage.

REVISE

