# Fable 5.1 (high) — independent read of the 24 Sep 26 drafts (D70), verbatim


## Review of the 24 Sep 26 skill drafts — findings, most severe first

1. **`_docs/raptor-port/docs/gates-and-deploy.md` lines 19–24 (the intermittent-stop rule) costs two runs and can collide with D151.** "File the residual as a backlog item … then ONE re-run" — but filing the item is a commit, and any push to a PR that carries code restarts every gate (D151, #204). So the push that files the item *is* a full run, and a `gh run rerun` after it is a second one. Fix: replace "— then ONE re-run, citing the item." with "— then the push that files it IS the one re-run (any push to the PR restarts every gate, D151); never `gh run rerun` on top of it, and never push while a run is still going."

2. **`session-handoff/SKILL.md` draft lines 203–207 (the D69 condense step) drops D138's guard.** D138 says it "extends … D69 — neither licenses a change of meaning", and requires a condensed version to keep every rule/date/reason and be meaning-checked by a model that did not write it. The draft says "cut down to state, decisions and pointers" with no such limit — a future chat will condense a working log into a paraphrase. Fix: after "pointers when the work closes," insert "keeping every rule, condition, date and reason (D138 — when in doubt, move the block whole to `raptor-port/docs/archive/` with `backlog-archive.mjs --move` instead of rewording it),".

3. **`_docs/…/bug-check-order.md` §7.7 draft lines 316–320 contradicts the sentence it is attached to.** §7.7 says "Create the fixture through the app's own controls … Do not inject it"; the new text then says "Switch roles with `lwRole` / `raptorRole` (… the probe bridge)" — a test-only door, not the app's. The two are reconcilable only if the order of preference is stated. Fix: reorder the last sentence to "Make one write before any reload (the app's own controls, still 7.7's rule), or — when the fixture must stay unwritten — switch roles through the probe bridge (`window.lwSetRole` / `window.raptorRole`; the Playwright wrappers are `lwRole` / `raptorRole` in `raptor-port/e2e/app.ts`), which changes only the role a person would reach by signing in, not the world." (Walk scripts are plain `.mjs` and call the bridge directly — `oil-credit-tags.mjs:174` — so name the bridge calls, not only the e2e helpers.)

4. **`_memory/additions.md` line 13 narrows #234.** The observation says hand over "the host's path-scoped rules files whose paths match the changed files"; the draft says only `.claude/rules/decisions/<area>.md`. `.claude/rules/raptor-executor.md` is also path-scoped (`raptor-port/src/**`, `e2e/**`) and carries the never-bypass-the-reviewer rule — the one Codex most needs. Fix: replace "(`.claude/rules/decisions/<area>.md`)" with "(every `.claude/rules/**/*.md` whose `paths:` match the changed files — the area rulings under `decisions/` and `raptor-executor.md`)".

5. **`_docs/…/bug-check-order.md` §7.2 draft line 293 widens D17.** D17's ruling begins "For a long hand pass"; the draft's bullet governs every walk, LOOK tier included. D138: keep the condition. Fix: begin the bullet "**For a long hand pass, drive it with a SCRIPTED real browser from the start** (owner, D17, 21 Sep 26)". (§4's D16 paragraph is faithful; the #213 refinement narrows only the agent's recipe — "own port" was the mechanism, "no two share a fixture" the ruling, and a fresh context serves it — so it does not narrow the owner's ruling.)

6. **`condition-based-waiting.md` draft line 70 heading "Before You Pace a Test" misses the case that actually happened.** In #199 *settling* (a condition wait — this skill's own remedy) also hid the bug. A reader converting a sleep to `waitFor` will read the section as not for them. Fix: heading "## Before You Pace or Settle a Test"; first bullet "Replay the scenario at human pace first, before adding a pause, a settle or a condition wait"; and change the `SKILL.md` line 58 pointer to "§Before You Pace or Settle a Test".

7. **#198 closure is only half right** (`dispositions.md` line 31). The live row and "Long check runs" pattern cover the pipe-eats-the-verdict mechanism, but not the observation's lesson: the same chain that ran the gate *committed*. Fix: add to the "Long check runs" block in `verification-before-completion/SKILL.md` (after line 107): "❌ `gate | tail -4 && git add -A && git commit …`   (the chain commits on `tail`'s 0 — read the exit code in its own command before you write the commit)". Then the closure holds.

8. **`_docs/…/gates-and-deploy.md` line 21 changes #222's threshold.** Observation: "Two stops on the change's build and none on the base's *in the isolated probe*". Draft: "Stops on the change's build and none on the base's". Fix: "Two or more stops on the change's build and none on the base's, in that isolated probe, is a regression: stop there."

9. **`writing-plans/SKILL.md` draft line 58 swaps #231's control case.** Observation: prove each check can fail with "a control case against the OLD checker"; draft: "run it once against the old structure" (a different, weaker proof — it only fires if the old structure happens to break the new rule). Fix: "(replay the known failure modes, and run each new case against the checker WITHOUT that rule to prove it can fail)".

10. **`_notes/SUPERPOWERS-VENDORED.md` — §Updating still says "then change 3 … diff each skill in change 3's table"** (live lines 53–55). Change 4's table would be skipped on a refresh. Fix: "then changes 3 and 4 … diff each skill in the tables of changes 3 and 4".

11. **`systematic-debugging/SKILL.md` draft line 30 loses #210's reason for creation times.** Fix: "the process tree's creation times (another account's command lines are hidden) show what started when the step began."

**Checked and found nothing:** every command and name — `git config --show-origin --get-all` (valid), `GIT_TERMINAL_PROMPT=0` (in `deploy.yml:146`), `Emulation.setCPUThrottlingRate` (`e2e/app.ts:18`), `Input.dispatchTouchEvent` with two `touchPoints` (`trk-pinch.mjs:29`), `getClientRects` tops, MutationObserver-before-paint (correct: microtask checkpoint precedes the rendering opportunity), runner `--effort` choices `low|medium|high|xhigh|max` (`runner.py:397`, maps to `model_reasoning_effort` / `--effort`), `lwRole`/`raptorRole` (`app.ts:221,232`), `handpass/lib.mjs`, `MSYS_NO_PATHCONV`; #204 and #230 closures (live `session-handoff` Step 1 and `doc-structure.md` §When it leaves state them); #120/#122 kept open per D73; D16 §4 meaning (every clause of the ruling row present, "own browser tab" correctly tightened to "fresh context"); the "product bug" bullets vs the upstream retry/timeout paragraph and the "flaky is a hypothesis" block (no contradiction — the handling is scoped to proven-external product conditions); D84 carve-out (consistent with the unit-gate family's own re-run rule); Rule 9 and the #232 line vs D145's approved skill and Rule 5; the memory files and `plain-language-in-reports` all exist; #210's fix in `deploy.yml`; the `[BG-CWD-GUARD]` priority-list line matches live `OUTSTANDING.md:70–71`; no project detail in the generic skills that would mislead outside this repo.

**Verdict: REVISE**
