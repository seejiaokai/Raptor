# Rulings — How we work (loaded in every session)

The general rulings: how his rulings are recorded, models and reviews, bug checks, merging and pushing, the
PC that runs the checks, the repo and privacy, the docs. Every session carries these. Each other area loads
by itself when a file in that area is read — the map, and how to add or retire a ruling: `DECISIONS.md`.
Newest first; each row keeps the date it was recorded.
**Also read** — filed under OIL, but it governs every report: **D25** (OIL is earned leave, time off banked —
never pay or money).

| # | Date | His ruling, in his words where short enough | What it means | Where it lives now |
|---|---|---|---|---|
| D106 | 25 Sep 26 | *"Im making my repo public now to enjoy the faster checks i can do on github. just for a short period"* · *"Private repo machines is very slow, public machines i think have more cores"* — after the agent recommended staying private and moving the checks to GitHub's machines instead | **THE REPO GOES PUBLIC FOR A SHORT PERIOD, FOR GITHUB'S FASTER FREE MACHINES (public repos get 4 cores, private 2).** Sets D89 aside for that period, his call made knowing the two risks put to him: (1) **his PC's runner must be stopped while public** — GitHub's advice: never a self-hosted runner on a public repo (a fork's pull request can rewrite the checks); the workflow already sends a public repo's runs to GitHub's machines (`.github/workflows/deploy.yml`); (2) **the old history becomes readable** — D58 cleaned the current files, not the history. **Back to private afterwards** (D59, D89): make it private, start the runner again | `.claude/rules/shipping.md` §The checks; this row |
| D90 | 24 Sep 26 | *"there might be conflicting rules and multiple changes. i guess compare what rules we have adopted too and i think latest rule is the most correct?"* — said while starting the amendment re-test, whose first job gathers every amendment rule into one register | **RE-CONFIRMS HIS 17 SEP 26 RULE: WHERE TWO OF HIS RULINGS CONFLICT, THE LATER ONE WINS.** The original, 17 Sep 26: *"most probably the latest instructions I gave is usually the most correct one to follow"* — until now it had no D-number and lived only in `raptor-port/CLAUDE.md` §How to work here (which loads only once a `raptor-port/` file is opened) and a memory entry, so it becomes a row here, where every session reads it. **How it is applied:** every clash is NAMED — the ruling followed, the ruling set aside, both dates; a rule with no date loses to a dated one; the stale text is fixed or marked in the same change. **Its one limit, kept from the original:** where the newer ruling does not clearly cover the case (it only partly overlaps, or answers a different question), that is a real gap and it goes to him as a question — during this re-test filed in `OUTSTANDING.md` and the handoff block rather than stopping, since he is away. **Numbered D90**: the amendment chat's range is D90–D119 (D86) | this file (loaded in every session); `raptor-port/CLAUDE.md` §How to work here (the original text); memory `newest-instruction-wins`; applied in `raptor-port/docs/superpowers/specs/2026-09-24-amendment-behaviour-register.md` |
| D162 | 24 Sep 26 | **"11 yes"** — asked whether a setting may be added that stops a background command from starting in the wrong folder (it runs in every chat) | **ADD THE GUARD.** `[BG-CWD-GUARD]` has his go: a hook under `.claude/` that refuses a backgrounded `npm` command without `cd raptor-port` — standing configuration, approved by him | `OUTSTANDING.md` `[BG-CWD-GUARD]`; on build: `.claude/settings.json` and a hook under `.claude/hooks/` |
| D148 | 24 Sep 26 | **"5 agree"** — to the recommendation after he asked *"what if … admin 1 already made a change, then admin 2 comes in and make another change, if u grey out the undo button, then admin 1 cant undo his own change correct?"* | **UNDO REVERSES ONLY YOUR OWN CHANGES.** The signed-in person's Undo steps back through their own changes only, and the list clears when they sign out; it never greys out because someone else changed something since, and never undoes another person's change. If someone else has since changed the very same thing, Undo refuses and says who, rather than overwrite their newer work. Settles `[GU-MAYREV]` (the agent's first suggestion — grey it out — was wrong for his two-admin case) | `raptor-port/docs/undo-contract.md` (§4); `OUTSTANDING.md` `[GLOBAL-UNDO]` |
| D147 | 24 Sep 26 | **"1-4 yes"** — to the agent's four proposals for the order of work, put to him after he asked for the whole backlog and a recommended order | **THE ORDER AFTER THE AMENDMENT RE-TEST IS SET.** (1) Re-test the absence record TOGETHER with `[S4-HUNT-REST]` — one pass, since the hunt's untouched ground IS the absence record; (2) then re-test change-recording (the one undo and the command layer); (3) then the Leave War links LAST, with the 7 Sep phone check folded in. This settles the order of `[HUMAN-RETEST]`'s other three (D86 left it open) and `[S4-HUNT-REST]`'s place (never ruled). (4) **`[OIL-AWARD-IS-A-GRANT]` has his go:** after his after-the-hunt items (`[PUB-UNAVAIL]` → `[LW-LOCKMARK]` → `[LW-WEEKDAY-WORK]`, unchanged) and BEFORE `[DB-STEP]`, fix how an award is stored — one kind, not two, so the database stores one form (architecture first, D144) — with `[OIL-EARNED-VS-GRANTED]` folded in (its label is still his figure: ask when it comes); then the small OIL follow-ups as ONE batch | `OUTSTANDING.md` (the priority list, `[HUMAN-RETEST]`, `[S4-HUNT-REST]`, `[OIL-AWARD-IS-A-GRANT]`); `HANDOFF.md` §Next, in order |
| D145 | 24 Sep 26 | **"approve the skill and handoff"** — after both Fable and Astra had read the session-handoff skill change (D70) and it was put to him in plain words | **THE SESSION-HANDOFF SKILL CHANGE IS APPROVED.** At every handoff it writes this chat's own block under `## Now` in `HANDOFF.md` (the one handoff, D140) — never another chat's; a merged chat's block goes only once its open items are in the backlog — files open work in `OUTSTANDING.md`, keeps the file map in `raptor-port/docs/file-map.md`, and ends with his ready-to-paste opening line. Every earlier safeguard stays. Spends D70's review step for this change; D70 itself stands for every later skill change | `.claude/skills/session-handoff/SKILL.md`; `raptor-port/docs/file-map.md` (its row) |
| D144 | 24 Sep 26 | His rule of 13 Sep 26, given with the whole-app architecture review (both providers): *"fix the architecture first, then individual bugs"* · and, the same day, *"STOP: interim two-system undo patches + further quarantine rounds"* — recorded 24 Sep 26: it had no D-number and lived only in the backlog’s old priority list (archived in the spring clean) and a memory entry; found by Fable’s red team | **FIX THE ARCHITECTURE FIRST, THEN THE INDIVIDUAL BUGS.** Decide by what is good for the WHOLE app, not by least effort (the easy path is fine when it is genuinely good); before fixing a batch of bugs, look for the bigger root cause behind them, and for a real batch run ONE whole-app review by BOTH Fable and Astra. **The stop order that came with it:** no interim patches that keep two systems alive side by side, and no further rounds of the quarantine machinery — the stack (`[ARCH-STACK]`) replaces them. Its undo half is now moot (one global undo shipped 18 Sep 26); the quarantine half holds until the stack’s step 6 removes that machinery | this file (loaded every session); memory `architectural-root-cause-before-minute-fixes`; `OUTSTANDING.md` `[ARCH-STACK]` |
| D143 | 24 Sep 26 | **"Live in vercel"** — asked, during the spring clean, when a change counts as DONE after his "merge live" (both reviewers had flagged that the agent was about to invent the answer: the old chain ended at GitHub Pages, gone since D59) · he then offered *"would u like me to give u access to my vercel link as well"* · *"Account"* | **"DONE" AFTER "MERGE LIVE" MEANS LIVE ON VERCEL.** The chain: merge → the checks on his PC go green → Vercel reports the live app (its production deployment for that `main` commit) READY → ONE notification with the link. His own look on his phone replaces the live-page look the agent used to do on Pages. The agent reads Vercel’s status from GitHub (the commit’s Production deployment status) — it cannot open the app, which sits behind his sign-in. **His offer of access to his Vercel link or account — the agent’s answer, stated to him:** not needed, and the agent must not be given an account login or password; opening the protected app would mean handing the agent a secret key, which it must not hold or type; if it is ever truly needed, the route is Vercel’s own automation-bypass setting, set up by him. **Replaces** the Pages endpoint of the 12 Aug 26 "done MEANS LIVE" chain (in `raptor-port/CLAUDE.md` §How to work here until the spring clean) | `.claude/rules/shipping.md` (the "done" line); `raptor-port/docs/gates-and-deploy.md` (§Now) |
| D141 | 24 Sep 26 | *"Don’t need to hit the target line, I don’t think it’s realistic because this app increasing as always in size. So no hard line, more of like is this info needed etc and optimised"* · *"Is this also organised and written clearly … Etc"* — said while the spring clean (`[DOC-TRIM]`) was being planned | **NO LINE TARGETS. THE TEST IS WHETHER EACH PIECE OF INFORMATION IS NEEDED WHERE IT SITS — AND WHETHER IT IS ORGANISED AND WRITTEN CLEARLY.** The app keeps growing, so a fixed line count for a document is not realistic and is not the goal. The spring clean, and every later edit, asks of each block: is it needed by the sessions that read this file? If not, it moves to where it is read less often (an area file, a reference doc, an archive — D138, never reworded, never lost). A file is also judged on whether a reader can FIND things in it (clear headings, one topic per section, a pointer instead of a repeat) and whether it reads plainly; a clarity edit that rewords anything goes through D138’s meaning check like any other summary. **Changes in part D14** (its "budget that can only go DOWN" and the target figures in `doc-budget.md` — 500 / 400 / 600 lines) **and refines D140’s point (4). The agent’s reading, stated to him so he can correct it:** the document gate keeps a CEILING per read-every-session file only as a TRIPWIRE — crossing it means "look at what is in this file that does not belong here", never "cut to a number"; a ceiling RISES, with its reason, when what crossed it genuinely belongs there (as D136 already does for the rulings); the fixed targets are withdrawn | `raptor-port/docs/doc-budget.md` (§1 and §4); `raptor-port/scripts/docsize.mjs` (the ceilings note); `OUTSTANDING-ARCHIVE.md` `[DOC-TRIM]` (closed 24 Sep 26) |
| D140 | 24 Sep 26 | *"My intent is to make this structured and the ai read only what’s applicable for the job automatically and knows how to navigate … u can still have a general one, but when the job moves to like another section of the app then it should know what to read and find out the context … And it also knows how to archive or delete info that is no longer needed. Teach it such that new info going into the repo will follow this structure automatically and won’t bloat up the context again. So that I don’t need to do another optimisation in the future or spring cleaning"* · *"Yes also know my workflow and see how u can weave this in"* · his workflow, in his words: *"I usually work until the context bloats and I’ll ask for a hand off between chats. Until I’m happy with the work then I’ll merge. I seldom compact."* · *"I may sometime work parallel on a worktree and merge both together"* · *"I also always use Claudex review for my workflow to red team , make plans, see how it affects the rest of the app structure, bugs, reviews, with fable and Astra and opus 5.5 now being a smart work horse"* — said while the spring clean (`[DOC-TRIM]`) was being planned | **THE WHOLE REPO READS BY RELEVANCE AND KEEPS ITSELF TIDY — NOT ONLY THE RULINGS.** Extends D137 (the rulings split by area and loaded by relevance) and D14 (tiers and a budget) to every document a session reads. (1) **A small general layer** is read in every session; **each area’s context** (its settled decisions, its architecture, its rulings) loads BY ITSELF when a session touches that area, and a map says where everything else lives, so a session reads what the job needs and finds the rest. (2) **Every kind of new information has ONE home by rule** — a new decision, a finished story, an open job, a trap, a file — so what a session writes lands in the right tier without anyone deciding it again. (3) **What is finished or replaced LEAVES** the files sessions read, moved whole to an archive (D136, D29, D138 — never lost, never reworded). (4) **It is enforced, not remembered** — the document gate (in CI and at the end of every turn) fails a change that bloats a read-every-session file past its ceiling or leaves clutter where it does not belong, and says where the text belongs. **The goal in his words: no future spring clean.** (5) **It is WOVEN INTO HIS WORKFLOW, not bolted beside it** — the session start (what a fresh chat reads), the work (where each new fact is written), the handoff notes (his standing instruction that they go into the repo, `HANDOFF.md`, so the next device just pulls and reads), compaction (chat-only notes persisted to the repo first), parallel chats merging one at a time (D78, D86) and his "merge live" — each step names the file it reads or writes, and the handoff skill writes to the structure. **Because the HANDOFF between chats is his main way of carrying work on (he seldom compacts), the handoff is the step the structure must get right:** the new chat reads one file and knows where things stand; finished stories leave it at each handoff; open work goes to the backlog. **Because parallel worktrees are merged together,** each chat writes only its own part of the shared files, and the later merge reconciles them (D78). **Because the Claudex loop is how he plans, red-teams, checks the ripple across the app and reviews** (Opus 5.5 builds; Fable and Astra review — D67): a plan or review record is a tier-3 doc linked from its backlog item, the ripple walk reads `feature-impact.md`, and a brief for ASTRA names the area files the change touches — Codex does not load `.claude/rules` by itself, so what loads automatically for Claude must be handed to it. **The agent’s reading, stated to him so he can correct it:** the always-read layer is the project guide, the rules files and the How-we-work rulings; the area layer is one file per area under `.claude/rules/decisions/`, loaded by its `paths:`; the routing and the lifecycle are written in ONE always-loaded rule file and enforced by `raptor-port/scripts/docsize.mjs` | `raptor-port/docs/doc-budget.md` (§2, the routing and the lifecycle); `OUTSTANDING-ARCHIVE.md` `[DOC-TRIM]` (closed 24 Sep 26) |
| D138 | 24 Sep 26 | *"I hope if u summarise things in the repo, it shouldn't change its meaning"* — said while asking how to spring-clean the repo so every session reads less | **A SUMMARY MUST NEVER CHANGE WHAT THE ORIGINAL MEANT.** Any trim, split or condensing of the repo's documents keeps every rule, condition, exception, date and reason that could change a future decision; when in doubt, MOVE the text whole to where it is read less often (an area file, an archive) instead of rewording it. A condensed version is checked against the original for meaning by a model that did not write it before it replaces it. Extends D14 (state the decision, not the transcript) and D69 (condense what the work wrote) — neither licenses a change of meaning | `raptor-port/docs/doc-budget.md` §2 |
| D137 | 24 Sep 26 | *"The idea is that general rules should be read each session or rules that are applicable should be read, once we venture into things concerning other areas then that should be automatically read too to not loose context. Basically optimise the structure so that the ai doesn't need to read everything everytime. And the new rules will follow the structure automatically and replaced rules will move into archive automatically etc. think it as I don't want to bloat the ai to context that doesn't matter and get confused and waste tokens too."* — said while D136's pass was being prepared (the same message thread asked Fable and Astra to help with it) | **THE RULINGS ARE READ BY RELEVANCE, NOT ALL AT ONCE — AND THE STRUCTURE KEEPS ITSELF.** Extends D136 (never shrunk by moving live rulings out; sorted by area; only replaced rulings and spent one-off permissions leave). (1) The GENERAL rulings (How we work) are read in every session. (2) Each other area's rulings (Tracker, Leave War, Scheduler & amendments, OIL) load AUTOMATICALLY the moment a session touches that area — so work that strays into a second area picks up that area's rulings too, without anyone having to remember. (3) A new ruling goes into its area by the same route, and a ruling replaced by a later one (or a one-off permission once spent) moves to the archive — enforced by the gate, not left to memory. (4) The point is fewer tokens and less confusion: a session does not carry rulings for areas it never touches. **The agent's reading, stated to him so he can correct it:** one file per area under `.claude/rules/` (the app's own mechanism for loading a rules file when files that match it are touched — How we work loads always); `DECISIONS.md` stays the front door, holding the map of which file carries which D-number, checked by the gate so an old pointer like "DECISIONS.md D38" still lands; the archive is `DECISIONS-ARCHIVE.md`, searched, never loaded. This changes D136's FORM (one file in sections → one file per area) and keeps every one of its rules | `.claude/rules/record-decisions.md` (§Keeping the list whole); `.claude/hooks/record-decisions.sh`; `raptor-port/scripts/docsize.mjs` (the map and the archive checks) |
| D136 | 24 Sep 26 | **"Yes raise and sort"** — after asking *"If you archive an old ruling would it be lost and not read even tho it's applicable? Shouldn't the rulings be categorised and dated so that if there is an applicable item is being asked the ai will search for it? … just increase the budget to be safe so that anything I change you will have the context of the ruling"* | **THE RULINGS LIST IS NEVER SHRUNK BY MOVING LIVE RULINGS OUT.** (1) Its size budget is RAISED (`docsize.mjs`), so every ruling that still applies stays in the one list sessions read. (2) It is SORTED BY AREA — Tracker, Leave War, Scheduler & amendments, OIL, How we work — every row keeping its date, so a chat working on an area reads that whole section instead of relying on a search. (3) The ONLY rows that may leave it: rulings REPLACED by a later one, and one-off permissions spent on the day (e.g. D133's "merge live for this session only") — to a dated archive, marked, never deleted, still searched before asking him anything. Replaces the agent's first recommendation (archive the oldest rows) — his point: an archived ruling a chat does not know about is a ruling it will not look for | `.claude/rules/record-decisions.md` (§Keeping the list whole); `raptor-port/scripts/docsize.mjs` (the DECISIONS.md ceiling note); `OUTSTANDING-ARCHIVE.md` `[DOC-TRIM]` (closed 24 Sep 26) |
| D135 | 24 Sep 26 | *"I'm done with the demo already"* | **THE PRESENTATION / DEMO-VIDEO WORK (D154) IS FINISHED — THE PC IS NO LONGER SHARED WITH IT.** D125's "the demo has first call on the PC" ends: the next chat may run its heavy work (the gates, the walkers) without yielding to a demo chat. D86's own conditions stand — never two full gate runs at once, and never while his PC's check runner is mid-run | this row (its old homes — `HANDOFF-NEXT.md` item 1 and the `[HUMAN-RETEST]` line — were archived 24 Sep 26: `raptor-port/docs/archive/handoff-2026-09-24.md`, `OUTSTANDING-ARCHIVE.md`); D86's conditions: `.claude/rules/shipping.md` §The checks |
| D151 | 23 Sep 26 | *"Will u remember to not push while the runner is running?"* — after a notes-only push cancelled a check run on his PC mid-way | **NEVER PUSH TO A BRANCH WHILE ITS PULL REQUEST'S CHECKS ARE RUNNING.** Any push to a pull request that carries code starts the whole check run again and cancels the one in progress — even a notes-only push, because GitHub judges the whole pull request, not the last push. Batch the notes and push once the run has finished (`gh run list` shows it). It costs a full run of his PC's time, or ~60 minutes of GitHub's allowance, each time it is broken | `.claude/rules/shipping.md` (loaded in every chat; the D60 line moved there 24 Sep 26); `raptor-port/docs/gates-and-deploy.md` §Now (the old `HANDOFF.md` §Deploy moved there) |
| D89 | 23 Sep 26 | *"1a teach me how to do it"* — choosing, from the options put to him, "stay private and use your PC for the checks" | **THE REPO STAYS PRIVATE, AND GITHUB'S CHECKS MOVE TO HIS OWN PC (a self-hosted runner). REVERSES D88 the same day, before it was acted on.** Why: free (no monthly minutes), faster, and the squadron's name stays hidden — and GitHub's own advice is never to use your own machine with a public repo, so the two go together. **Costs he was told and accepted:** the PC must be on and awake for checks to run; the checks share the PC with the chats (a check at the same moment as a chat's test run can cause false failures — D86's reason); setup ~1–2h. **Who does what:** he installs and registers the runner himself (its registration token is his credential and never goes to the agent); the agent changes the workflow, with a way back to GitHub's machines | `OUTSTANDING.md` `[REPO-PRIVATE]` and `OUTSTANDING-ARCHIVE.md` `[CI-TWO-CORES]`; `.claude/rules/shipping.md` §The checks (moved from `raptor-port/CLAUDE.md` §Pipeline & repo invariants, 24 Sep 26) |
| D87 | 23 Sep 26 | *"Make those three Leave War browser tests robust on a slow machine: wait on what each test needs, not a fixed time."* (the `[LW-MONTHJUMP-PHONE]` task) | **A BROWSER TEST THAT FAILS ON A SLOW MACHINE IS FIXED BY MAKING IT WAIT ON WHAT IT NEEDS, NOT ON A FIXED TIME.** A fixed pause is too short on a slow runner and dead time on a fast one. Of the two remedies the backlog offered (wait on what they need / a longer time limit on GitHub), this is the one he chose | `raptor-port/e2e/app.ts` (the header, beside the slow-machine switch); `OUTSTANDING-ARCHIVE.md` `[LW-MONTHJUMP-PHONE]` |
| D86 | 23 Sep 26 | *"Or i can run the tracker in parallel with the amendment system right? Since the tracker is standalone"* → told yes, with three conditions → **"yes parallel"** | **`[HUMAN-RETEST]` RUNS TWO CHATS AT ONCE: THE TRACKER AND THE AMENDMENT SYSTEM, IN PARALLEL.** Extends D85 (the Tracker still starts; the amendment system now starts beside it). Safe only because the Tracker has its own code and its own store, so the two chats' fixes do not meet. **The three conditions, which are part of the ruling:** (1) separate ports: the amendment chat serves on 4173, the Tracker chat on 4180; (2) never two FULL gate runs at the same moment — tonight one run held the processor at 86–97%, and two at once cause false failures (the load-sensitive `[LW-MONTHJUMP-PHONE]` test is the known one); a chat checks the other is not mid-run first; (3) separate ruling numbers: the amendment chat from D90, the Tracker chat from D120. Whichever merges second brings `main` in first, as with tonight's window. **Still not settled:** the order of the remaining three features after these two | `OUTSTANDING.md` [HUMAN-RETEST] (the ORDER paragraph); `HANDOFF.md` ## Next; `.claude/rules/shipping.md` §The checks (never two full gate runs at once) |
| D85 | 23 Sep 26 | **EXTENDED BY D86 (same evening): the amendment system runs in parallel with the Tracker.** *"actually i would like to execute the tracker first instead"* — after the agent recommended running `[HUMAN-RETEST]` one feature per session, the amendment system first, and moving the Tracker to an end-of-development pass | **`[HUMAN-RETEST]` STARTS WITH THE TRACKER.** The agent's recommendation it sets aside: the amendment system first, because it decides what the database will store. Told to him, and his call stands: the Tracker is a separate app with its own code and store, and all stored data is demo data wiped before the database (D54), so going first costs nothing but time. **What it does NOT settle:** the order of the rest, and whether the Tracker and the Leave War links wait for an end-of-development pass. He was not asked to rule on the agent's split (the amendment system, the absence record and the change-recording before `[DB-STEP]`); the next session proposes the order after the Tracker and asks. **Numbered D85** so it clears `claude/docs-guard`'s rulings, which take D78–D84 when that branch renumbers its D70–D76 on merge | `OUTSTANDING.md` [HUMAN-RETEST] |
| D84 | 23 Sep 26 | *"If a Leave War desktop test times out on GitHub, that is the known slow-runner issue in [LW-MONTHJUMP-PHONE] in OUTSTANDING.md: re-run the failed group once rather than investigating."* — given with the "merge live" for `[DOCS-GUARD]` | **A LEAVE WAR DESKTOP E2E TIMEOUT ON GITHUB GETS ONE RE-RUN OF THE FAILED GROUP, NOT AN INVESTIGATION.** It is the known slow-runner family already measured as not a defect. If the SAME group fails again after its one re-run, that is new evidence — stop and report it, do not re-run again. Making those tests robust stays `[LW-MONTHJUMP-PHONE]`'s job | `OUTSTANDING-ARCHIVE.md` `[LW-MONTHJUMP-PHONE]` |
| D78 | 23 Sep 26 | *"Two other chats are running in parallel … Each reaches main only on my "merge live", one at a time; whichever goes later merges main in first, and if a D-number clashes, renumber yours."* | **PARALLEL CHATS MERGE ONE AT A TIME, AND THE LATER ONE ADAPTS.** Each branch still needs his own "merge live" (D60 unchanged). The branch that merges second first merges `main` into itself, so it is checked against what is already live; a clashing D-number is fixed by renumbering the LATER branch's rows, so a number already on `main` never changes meaning. Given while starting `[DOCS-GUARD]` beside `claude/all-avail-window` and `claude/skill-review` | this file's header (the rule, where D-numbers are assigned) |
| D73 | 23 Sep 26 | **"B later"** — asked whether to write a guide for bringing a whole separate app in as a new tab (done twice: Leave War, Tracker) | **NOT NOW — WRITE IT ONLY IF A THIRD APP IS BROUGHT IN AS A TAB.** The two notes that hold the recipe (#120, #122) stay OPEN in the notebook, their status carrying this condition, so the job that brings a third app in finds them. The standing rule that new modules go onto the one command layer (16 Sep 26) already makes a third app in the old shape less likely | `.claude/skill-observations/log.md` #120 and #122 |
| D72 | 23 Sep 26 | **"A retire"** — asked whether to keep the bug-testing list (`BUG-TESTING.md`, not kept up since 10 Sep 26, 40+ merged changes unlisted) or retire it | **THE BUG-TESTING LIST IS RETIRED.** The bug-check standing order (D10, one method) keeps its own record per change — the evidence sheets under `raptor-port/docs/handpass/` — and re-testing older features is `[HUMAN-RETEST]` in `OUTSTANDING.md`. Two records drift apart, and this one already had. The file stays as history, marked retired: nothing adds rows to it and no handoff checks it | `raptor-port/docs/archive/BUG-TESTING.md` (retired header; moved to the archive folder 24 Sep 26); `raptor-port/docs/file-map.md` (moved out of `HANDOFF.md` 24 Sep 26); note #75 closed |
| D70 | 23 Sep 26 | *"Do u recommend for fable and Astra to also review the skills?"* — told yes, one round each on the finished drafts, before he approves anything — then **"Ok"** | **CHANGES TO THE WORKING GUIDES (the skills) ARE READ BY BOTH FABLE AND ASTRA BEFORE HE APPROVES THEM — ONE ROUND EACH, NEVER BY THE MODEL THAT WROTE THEM.** Why: the guides shape every later build, so a wrong or contradictory line repeats silently across sessions; D67's independence rule already bars the writer from checking its own work, and both providers are available. **Scope, so it is not over-read:** one capped round each (the ~3-round cap still governs design loops), on the drafts, with a narrow brief — does each change say what its note meant, does it contradict a guide or a ruling, does it drop a safeguard or add bulk. It is a READ of documents; it never replaces walking the app for code changes. **Numbered D70 on `claude/skill-review`** (first written as D68, renumbered 23 Sep 26 once `claude/all-avail-window` reached D69) — renumber at merge if that branch has gone further | this file; `.claude/skills/TASK-OBSERVER-VENDORED.md` §Reviews in this repo |
| D69 | 23 Sep 26 | *"But at the end of this work just summarise or clean up or optimise the saved context in the repo. Don't wanna be bloating it"* — the same minute as D68 | **THE OTHER HALF OF D68: WRITE AS YOU GO, THEN CONDENSE AT THE END.** The progress notes a long piece of work writes into the repo to survive a compaction (the handoff, the evidence sheet's working notes) are scaffolding: when the work closes, they are summarised down to what the next session actually needs — the state, the decisions, where things live — and the rest goes. **Done as its OWN docs-only commit at the end, never inside a fix** (D29 rule 3 still stands, and this does not license trimming anyone else's entries — it is about what THIS work added). Records keep the decision, not the transcript (D14) **ONE RULING, TWO APPLICATIONS (merged 23 Sep 26 — the same words were recorded as D69 on both `claude/all-avail-window` and `claude/skill-review`).** The skill review's application: **WORK THAT SAVES CONTEXT INTO THE REPO CLEANS UP AFTER ITSELF WHEN IT ENDS.** For the skill review: once the approved drafts are installed, the holding folder `.claude/skill-updates/<date>/` is deleted (git history keeps every draft); the resolved notes move out of the active notebook into its archive the SAME day, not after the skill's one-day grace period (the notebook every session reads drops from ~2,900 lines to the few still open — the grace period protects same-day parallel writers of one file, and the other session writes on its own branch); and the one handoff note is deleted once the branch merges. It extends D14 (the always-read docs only get lighter) to the scratch context a job leaves behind. **A third application (the docs guard, same words, clarified: *"I mean if your context is bloating and u decide to save the context in the repo"*):** a session that saves its own working context into the repo saves a SUMMARY — decisions, state, next step — never the conversation | `raptor-port/docs/doc-budget.md` §2; memory `read-fully-over-context-economy` (the same entry, both halves); this file; this file; `.claude/skills/TASK-OBSERVER-VENDORED.md` §Reviews in this repo |
| D68 | 23 Sep 26 | *"Don't hold back in terms of reading as I'm afraid u may loose things"* — said after he warned the context was at ~47% and the agent answered that it would keep its reads small | **CORRECTNESS BEATS CONTEXT ECONOMY: NEVER SKIP OR SHRINK A READ TO SAVE CONTEXT.** Read what the work needs, in full where it matters — the file, the ruling, the test — and protect against a compaction the other way: by WRITING the state into the repo (the handoff, the evidence sheet, commit messages) at every clean point, so nothing lives only in the chat. **Corrects the agent's own stated plan the same minute** ("I'll keep my reads small from here"). **What it does NOT change:** the token-discipline rule against dumping raw tool output (logs piped through `tail`/`grep`) still stands — that is noise, not reading; and a file over ~300 lines is still read in parts, just all the parts that matter. **Numbered while two other sessions run in parallel** (`claude/skill-review`, `claude/docs-guard`); if either also used D68, the later merge renumbers per HANDOFF-NEXT.md | memory `read-fully-over-context-economy` (loaded every session); this file |
| D67 | 23 Sep 26 | *"just to confirm when a new plan is being made to implement something, opus 5.5 will be use to plan then let fable and astra review correct?"* — answering the open "adopt it?" on the model workflow, after the agent researched Opus 5.5 (released 22 Sep 26) against Fable 5.1 | **ADOPTED: OPUS 5.5 PLANS AND BUILDS; FABLE 5.1 AND ASTRA REVIEW.** Recorded as adoption, and said to him so he can correct it if he only meant to ask. **The whole of it:** (1) **Opus 5.5 writes the plan and builds** — on Anthropic's published benchmarks it leads Fable 5.1 on every one, at $4/$20 against $10/$50, and Anthropic's own guidance is "start with Opus 5.5; use Fable 5.1 when Opus 5.5 at higher effort still falls short". (2) **Fable 5.1 and Astra review the plan AND the code** — never the model that wrote it, because a reviewer shares its own blind spots; that is how two strong reviewers passed the OIL build. Both on money, published records, permissions or persistence (bug-check order §4); the design red team stays capped at ~3 rounds. (3) **When ASTRA builds or plans, Opus 5.5 is the Claude-side reviewer** — stronger and 60% cheaper than Fable there, and independent of Astra. (4) **A bug Opus 5.5 cannot crack escalates to Fable 5.1** (or Astra) — Anthropic's own advice, and the standing escalate-to-the-fixer rule. **Supersedes** the 7 Sep 26 CLAUDE.md §MODELS text (Opus 4.8 builds, Fable budget-limited — the budget half was already lifted 16 Sep 26). **Caveat he was told:** the benchmarks are Anthropic's own, a day old, and Anthropic says the real-world gap is narrower than the scores | `raptor-port/CLAUDE.md` §How to work here → the MODELS bullet (superseded in place); memory `prefer-codex-for-bug-checks-fable-scarce` |
| D63 | 23 Sep 26 | His instruction on where the aircraft type's remaining mentions go, answering the list he was shown. **His exact words are not reproduced, because they contain the string this ruling removes** — in substance: the project guide's line becomes "a flying-schedule planner for a fighter squadron", the briefs' description of him becomes "a non-technical fighter squadron scheduler", the two-seat comment becomes "an F-15 has two seats", the Logic page swaps the type for the bare "F-15", and the Tracker is ignored | **THE COUNTRY-SPECIFIC AIRCRAFT TYPE GOES; TWO REPLACEMENTS, BY WHAT THE SENTENCE IS ABOUT.** Where a sentence DESCRIBES the squadron or him, it becomes **"fighter squadron"** (the project guide's first line; the design briefs that describe him and the app). Where it names the AIRCRAFT or its documents, it becomes **"F-15"** — the bare type, flown by several air forces, so it no longer points at one country (the Logic page's two rule rows, the engine-rules doc, the code comments, the uploaded original). **The Tracker is left entirely** — its syllabus data (D62), its event-box hint text and the smoke test that asserts the syllabus. **NARROWED BY D64 (same day): the event-box hint now reads the bare type; the syllabus data and its smoke-test lines stay left.** **Applied to the lines he did not list one by one, by his own two patterns,** and said so to him so he can correct any. The Logic page keeps its document reference ("F-15 Table 1.5-2") — his wording, his call. Same intent as D58: nothing in the app or the repo should point at a real unit | `raptor-port/CLAUDE.md` line 3; `raptor-port/src/ui/logic-html.ts`; `raptor-port/docs/engine-rules.md`; the three design briefs under `docs/superpowers/specs/`; code comments in `src/engine/slots.ts`, `validate.ts`, `validate.test.ts`, `src/testing/refwin.ts`; `raptor-port/reference/scheduler.html`; memory `app-carries-no-unit-designation` |
| D62 | 23 Sep 26 | *"u can ignore the tracker syllabus file"* — said while the agent was sizing the aircraft type's mentions, after finding the Tracker's syllabus data carries it 52 times alongside real course content (222 events: formats, aircraft and adversary counts, the ejection-seat and simulator events) | **THE TRACKER'S SYLLABUS DATA IS OUT OF SCOPE FOR EVERY PRIVACY OR "WHAT CAN A STRANGER READ" SWEEP. LEAVE IT; DO NOT FLAG IT AGAIN.** It covers the syllabus data files under `raptor-port/src/tracker/data/` and the Tracker smoke test's two lines that assert that data. **(D64 later changed one Tracker item outside this — the event box's grey hint — and left everything here as it is.)** **Recorded so a later sweep does not re-find it and put the same question to him a second time.** **What it does NOT settle:** the aircraft type's OTHER mentions — two rule rows on the Logic page, the Tracker's event-box hint text, the project guide's first line, three older design briefs that describe HIM by it, the engine-rules doc, four code comments and the uploaded original. Those were put to him and are still open. **For the record, honestly:** the 23 Sep "no restricted material was ever uploaded" check looked for classification MARKINGS and at the scheduler's demo data; it never opened the Tracker's syllabus content. This ruling makes that gap moot, but the earlier check was narrower than its wording, and a later reader should know it. The source repo it was vendored from (`seejiaokai/Tracker`) is PRIVATE with no forks | this file; `OUTSTANDING.md` `[REPO-PRIVATE]`; memory `app-carries-no-unit-designation` |
| D60 | 23 Sep 26 | **"2"** — choosing, from three standing options, *push freely to a branch, always ask before `main`*, after being told that a private repo meters its Actions minutes so every push now costs him | **PUSHING A BRANCH NEEDS NO PERMISSION; `main` ALWAYS DOES.** A branch push is how he SEES a thing — it produces a Vercel link of its own and changes nothing official — so the agent does it without asking. `main` stays gated behind his **"merge live"**, exactly as before. **This EXTENDS the standing merge rule rather than replacing it:** that rule only ever covered merging, because while the repo was PUBLIC pushes were free and unlimited and there was nothing to ration. D59 made them metered, which is what created the question. **The two words are not synonyms and the agent must not blur them:** "push" puts work on GitHub on its own branch; "merge live" makes it the app. **The cost he accepted by choosing this:** the careful route (branch push, look, then merge) is TWO billed runs against one for a direct merge — he chose the look. **He rejected** "ask every time" (too many interruptions) and "ask only when it is more than one push" (cheapest, least oversight) | this file; `.claude/rules/shipping.md` (loaded in every chat; moved there from `raptor-port/CLAUDE.md` §Stable decisions → Pipeline & repo invariants, 24 Sep 26); memory `push-branch-freely-ask-before-main` |
| D59 | 23 Sep 26 | *"ok my visibility is private"* · *"in the future i would like to share my private repo so that developers can work on it"* · *"in order to see my app in the future i just need to generate a vercel link? ... As in see my app on my phone"* | **THE REPO IS PRIVATE AS OF 23 Sep 26 — DONE, BY HIM. `[REPO-PRIVATE]` IS NO LONGER PARKED; IT IS TAKEN.** This reverses the 23 Sep *"nvm disregard this first"*. **Three consequences, all confirmed rather than predicted:** (1) the GitHub Pages site is GONE — the API returns 404 — so the old public URL is dead, which is the point; (2) the publish job in the deploy workflow would now FAIL on every push and still bill its minutes, so it is **switched off in the same change**, with the gates left running so a red merge is still blocked; (3) Actions minutes are METERED on a private repo at a measured ~37 billed minutes a push. **The app is now viewed on VERCEL**, which is his alone behind Vercel's own sign-in — that is how he reaches it on his phone, and it is unaffected by the repo going private so long as Vercel keeps its GitHub access. **SHARING WITH DEVELOPERS IS HIS STATED INTENTION, NOT YET DONE:** the route is Settings → Collaborators → add by GitHub username with Write, and they run it locally (`cd raptor-port && npm install && npm run dev`) — they do not need Vercel, whose free tier is single-person. **The open question that comes with it:** a collaborator on THIS repo sees everything — the uploaded original, the full history, every agent-facing document. If that matters, the fresh-single-commit-repo route is the answer; his call, unmade | this file; `.github/workflows/deploy.yml` (the publish job, disabled with its reason); `OUTSTANDING.md` `[REPO-PRIVATE]`; `raptor-port/README.md` (the live-site line, now stale); `.claude/rules/shipping.md` and `raptor-port/docs/gates-and-deploy.md` (the Pages-era text archived 24 Sep 26) |
| D58 | 23 Sep 26 | Ruling on what the app may be branded with, after a check of the public repo found the unit named in the browser title, the printed schedule header and the sign-in page. **His exact words are deliberately not reproduced here, because they contain the two strings this ruling removes** | **THE BARE NUMBER STAYS; THE SQUADRON ABBREVIATION AND THE SERVICE ABBREVIATION GO, EVERYWHERE.** `142` on its own is fine anywhere — it is already the export filenames (`142-schedule.csv`, `142-inputs.csv`, `142-LoX-*.csv`) and those are untouched. What must not appear is the number PAIRED with the squadron abbreviation (that pairing is what names a real unit), or the service abbreviation in any form — including code comments, docs, the archive and the uploaded original. **Why it is worth the churn:** the 63 people in the app are invented callsigns with no real names, ID numbers, emails or phone numbers, so the demo data is harmless on its own. The unit designation was the single thing turning it into a named squadron's internal tool — and the repo is PUBLIC with a live public site. **DONE the same hour, 11 files:** the browser tab title, the printed schedule's own tab title and its heading, the test that pinned that heading, the React app's guide file, three code comments, an e2e comment, a sample export and the uploaded original. **The gate risk was checked and is nil:** `npm run test:reference` RUNS the original app and asserts its behaviour — it does not compare text, and none of its 728 assertions touches the branding | `raptor-port/index.html`; `raptor-port/src/ui/printpdf.ts` and `printpdf.test.ts`; `raptor-port/CLAUDE.md`; `src/leavewar/ui/Chrome.tsx`; `src/ui/Login.tsx`; `raptor-port/e2e/leavewar.spec.ts`; `raptor-port/docs/img/flag-export-sample-new.html`; `raptor-port/reference/scheduler.html`; `HANDOFF-ARCHIVE.md`; `OUTSTANDING.md` `[REPO-PRIVATE]` |
| D57 | 23 Sep 26 | *"the RAPTOR-Command-Brief.pptx is ok"* — asked, while reviewing what a stranger can read in the public repo, whether the commander briefing deck was a concern | **THE DECK IS HIS AND STAYS. NOT A CONCERN, NOT TO BE WIPED.** He made it himself; it is not material anyone else uploaded and nothing in it worries him. **Recorded so a later clean-up does not "find" it in the history and propose removing it again** — it was raised here only because it is the one presentation file the history carries (added `d53ce954`, edited twice, already gone from the current files). **What this does NOT settle:** the two things the same check did find — the repo is PUBLIC with a live public site, and the app was branded with the SQUADRON ABBREVIATION AND THE SERVICE NAME in its browser title, its printed schedule header and its export filename, which is the one thing tying invented callsign demo data to a real unit. The branding half is now settled by D58 above and DONE; the public-repo half remains open and is his call | this file; `OUTSTANDING.md` `[REPO-PRIVATE]` (the measured findings block) |
| D56 | 23 Sep 26 | *"Make sure the bug checks dont waste time catching these bugs in the future. It will not happen because ill clear all the demo data anyway before shiping this app into a real database. Remember this"* | **A PROBLEM THAT LIVES ONLY IN DATA ALREADY STORED IS NOT A FINDING. DO NOT SPEND A REVIEWER, A WALK STEP OR A FIX ON IT.** D54 said such a thing is not worth designing around; this says the CHECKING must not go looking for it either. The reviewers are to be TOLD so up front, in the brief, so the finding is never produced — that is where the time actually goes. **The measured cost that produced this ruling: ~25 minutes of one session building a sentence that can never fire again once the data is cleared** (`OIL_OLD_BLOCK`), on a finding a reviewer raised and the owner asked to be fixed before he gave the reason that made it moot. **THE GUARD, so this can never be used to wave a real defect away: BOTH must be true — (i) the harm exists only in data already stored, AND (ii) the code is already correct going forward.** If the app would do it again to NEW data, it is a real finding and the rule does not apply. A "pre-existing" label alone is NOT this rule — the standing order's own warning stands, that the dangerous category is old code a new feature has just made reachable | `raptor-port/docs/bug-check-order.md` (new §2b, the brief wording in §4, and a named anti-pattern in §10); `.claude/rules/bug-check.md` (the always-loaded copy, so it is in force before the order is opened); memory `dev-phase-reset-demo-data-not-migrate` |
| D54 | 23 Sep 26 | *"leave it as it is. any way this app is going to get wiped of data before its being brought into a database as these are demo data anyway"* — asked whether an already-issued weekend carrying a placeholder on a duty desk, a sim seat or an extras line should raise the pending mark the moment `[OIL-SEATS-CAN-EARN]` ships, or stay silent | **THE DAY RAISES THE MARK. LEAVE IT.** Such a day reads "1 pending", he republishes it once, and the men behind that puck get the OIL the issued day never paid them — D48's correction-and-republish path, working as he wrote it. The alternative was silence, which would have left those men unpaid for that seat unless the day were republished for some other reason, and quietly withholding earned leave is the thing this whole job exists to stop. **No code change; the behaviour stands.** What WAS built before he answered is the day's explanation of itself (`OIL_OLD_BLOCK`), because the day used to say "1 pending" with no cell marked and nothing in History while the chip beside the puck said "?" — he asked for that separately ("ok fix this first") and it is right under either answer. **THE REASON IS THE BIGGER HALF AND IT REACHES PAST THIS JOB:** the whole store is demo data and **will be wiped before the database step**, so a one-off amendment on days that already exist costs nothing and is not worth designing around. That is the 13 Sep 26 dev-phase rule and D22 **strengthened** — those said clearing is APPROVED when it is the simpler path; this says the wipe is PLANNED and certain, so "it only affects data that already exists" is now a reason to stop worrying rather than a reason to weigh. Apply it as a test to any future finding: if the harm lives only in stored demo data and is prevented going forward, it is not a defect worth building around | this file; `raptor-port/docs/superpowers/specs/2026-09-22-oil-seats-behaviour-register.md` (the row now states the settled answer); `OUTSTANDING.md` `[OIL-READ-LEFTOVERS]` item 3 (closed as ruled) and `[DB-STEP]` (which now carries the wipe as a stated intention); memory `dev-phase-reset-demo-data-not-migrate` |
| D53 | 22 Sep 26 | *“God dam why u need me to tell u 3 times to find the mock up? Is it because u didn’t save this to outstanding?”* | **NO — THE RECORD WAS FINE; THE AGENT NEVER READ IT.** The mock-up was saved (`docs/mock/allavail-window.html`), ruled on four times (D38–D41), and filed in `OUTSTANDING.md` as job 2. All three places were correct and current. The agent answered “there is no design” from a CODE COMMENT that had gone stale within the same afternoon, put an already-settled choice to him as an open question, and only searched the record after being pushed three times. **So the writing half of the rule worked and the READING half was missing.** Three specific errors, named so they can be checked for: (1) the session was handed “read DECISIONS.md D48–D49” and grepped for exactly those two instead of reading the file; (2) `OUTSTANDING.md`’s read-trigger was scoped to “resuming parked work”, and this was classified as “continuing a build”, so it was skipped; (3) when he asked about the mock-up the agent searched for the phrase the stale comment used — a search that could only CONFIRM, never disconfirm. **The standing fix, added to the rule file:** before telling him anything is undecided, before putting any choice to him, and before calling anything missing, search `DECISIONS.md` and `OUTSTANDING.md` FIRST. A code comment is dated the moment it is written and is never evidence that something is unruled. **— AMENDED 24 Sep 26 BY D137:** the rulings to search are now `.claude/rules/decisions/` (EVERY area — one loads only when its files are read) and `DECISIONS-ARCHIVE.md`, with `OUTSTANDING.md`. | `.claude/rules/record-decisions.md` (new §“Read it before you ask him anything”); the memory entry for `OUTSTANDING.md` (read-trigger widened) |
| D30 | 22 Sep 26 | *"Nvm I'll do what fable recommend"* — on how much of the backlog-process work to do, after the agent had offered three different recommendations in three messages | **FABLE'S ORDER IS THE PLAN OF RECORD FOR `[DOCS-GUARD]`. Do not re-decide the scope or the sequence.** All of it, in its order: **(1) F1 + F3** — the inventory check and the gate that can never demand a trim inside a code change, one docs/scripts pass, ~1 h; these make everything after them enforceable. **(2) F2 + F6** — the docs-only correction pass, ~45 min, done UNDER the new gate: the four live texts that still order trim-on-touch, and the rulings with no D-number. **(3) F4 items 1/3/4 and F5 items 1–2**, ~30 min. **(4) F5's archive mover and F7** when the next archive pass is actually due. Total ~2–2½ h, none of it touching `raptor-port/src`. **Why his call and not the agent's:** the agent recommended F1+F3 first, then reversed to F2-only as the cheap win, then reversed back to F1+F3 — three positions on the same question, which is precisely the condition the bug-check order §4 rank 4 describes (the builder defending, and re-arguing, the process that missed the defect). The owner ended it by taking the outsider's ranking whole. **The lesson for the agent is not the ordering, it is that a settled outside ranking beats a third re-derivation of its own.** | `OUTSTANDING-ARCHIVE.md` `[DOCS-GUARD]` (all four steps done and the item archived, 23 Sep 26); the findings and exact steps are `raptor-port/docs/superpowers/specs/2026-09-22-backlog-process-attack.md`; `raptor-port/docs/archive/HANDOFF-OIL-WALK.md` §3 (moved to the archive folder 24 Sep 26) |
| D29 | 22 Sep 26 | *"Seems like compressing is causing more harm?"* — then, given three options, **"1"** (do the archive split now) | **THREE STANDING CHANGES TO HOW THE BACKLOG IS KEPT.** (1) **FINISHED WORK LEAVES `OUTSTANDING.md`** for `OUTSTANDING-ARCHIVE.md` — searched, never read whole, never appended to by hand — the same shape `HANDOFF-ARCHIVE.md` already has. Measured before the split: 1,186 lines, of which **345 across 20 items were finished work** — 29% of a file every session reads. (2) **A RULING NEVER LIVES ONLY IN THE BACKLOG.** Every one gets its home in `DECISIONS.md`. The trigger was a near-miss the same hour: the agent was one step from retiring `[OIL-AWARD-ADD-RULING]` — *"an award and a worked day add up. So it's 4"* — which has no D-number to this day. **CORRECTED 22 Sep 26 by Fable's attack on this very entry:** the agent wrote that the ruling "existed in no other file", and that was WRONG — it is N16 in the one-absence behaviour register. The real point survives and is narrower: its only homes are a tier-3 spec and the backlog, so no session reads it, and the backlog is the file being trimmed. (3) **NEVER TRIM DOCS IN THE SAME CHANGE AS A FIX.** A blocked docsize gate means the file needs its own pass, NOT that old entries get carved up to make room. **Why this is structural and not a promise to be careful:** the agent read the bounded-edit rule the same day and still destroyed two filed items with an unbounded span replace, because the gate forced the trim to happen under pressure inside a money change. The coupling was the fault, not the carelessness **— AMENDED 24 Sep 26 BY D137:** "`DECISIONS.md`" in rule (2) now means the rulings files — one per area under `.claude/rules/decisions/`, with `DECISIONS.md` as their map. | `OUTSTANDING-ARCHIVE.md` (**DONE** — 8 items, 94 lines, every item verified into exactly one file). **THE OTHER TWO HOMES THIS ROW ORIGINALLY CLAIMED WERE NOT TRUE WHEN IT WAS WRITTEN, and Fable's attack caught it — anti-pattern #4, the comment that vouches, committed by the entry that exists to stop exactly this.** `OUTSTANDING.md` §Maintaining this file still says "move the item to Done", and `raptor-port/scripts/docsize.mjs` is unmodified. **The ceiling must NOT be ratcheted down** — Fable measured seven of eight gated files at ZERO headroom, so ratcheting the eighth to zero would recreate the pressure this ruling exists to remove; that clause is withdrawn. **DONE 23 Sep 26 under `[DOCS-GUARD]` (D30):** the three rules are in `OUTSTANDING.md` §Maintaining; the four texts that ordered trim-on-touch are corrected (memory `outstanding-tasks-file`, `.claude/rules/record-decisions.md`, `raptor-port/docs/doc-budget.md` §3, and the gate's failure message); and it is STRUCTURAL — `raptor-port/scripts/docsize.mjs` now fails a lost, duplicated or truncated record, never demands a trim inside a code change, and checks every row's home. Fable's report: `raptor-port/docs/superpowers/specs/2026-09-22-backlog-process-attack.md` |
| D23 | 22 Sep 26 | *"Work Codex's open findings before the remaining jobs — start with its Rank 4"* · *"Each one red first"* · *"I'll be sleep and u can try to finish all the bug jobs"* | **A REVIEWER'S OPEN FINDINGS OUTRANK THE PLANNED JOB LIST.** The settled plan's §2 order is set aside until every open finding from Codex's read is closed — its ranks 4, 2, 3, 5 and 6, in that order, Rank 4 first because it is the unfinished half of a repair already reported as done. Only then do the remaining numbered jobs (3, 4, 5, 6, 9) resume in their own order. Each fix is proved RED first through its own production function, and nothing merges without his "merge live" | `raptor-port/docs/superpowers/specs/2026-09-22-oil-fixplan-settled.md` §2 (the job list now opens with the findings queue); `…/2026-09-22-oil-fix-handoff.md` |
| D22 | 22 Sep 26 | *"if it's easier to reset why not"* (on Fable's advice to clear stored data rather than reconstruct a missing field) | **Clearing the demo data is APPROVED whenever it is the simpler path** — a re-confirmation of the dev-phase stance that this app is pre-promulgation and a defect living only in existing data is cleared, not migrated. It was NOT taken this time, because the reconstruction was already built and proved by test, so resetting would have been more churn and lost his data for no gain. The fallback stands if the reconstruction is ever shown wrong | `raptor-port/docs/superpowers/specs/2026-09-22-oil-fix-handoff.md`; memory `dev-phase-reset-demo-data-not-migrate` |
| D17 | 21 Sep 26 | *"Isint playwright the same as browser test? Why didn't u choose that in the first place?"* | **For a long hand pass, drive the app with a SCRIPTED real browser from the start**, not the in-app browser panel. Both run the real bundle, but the panel is one click per message, resizes under you and times out on screenshots, so the pictures the standing order requires come out unusable. The scripted driver replays a whole day in about a minute, saves full-size pictures to disk, and is what makes handing the work to parallel agents possible. Looking at the pictures is still the agent's job — the tool changed, the looking did not | `raptor-port/docs/bug-check-order.md` §7.2; the reusable driver `raptor-port/scripts/handpass/` |
| D16 | 21 Sep 26 | *"Should we run agents to expedite this?"* → *"But if u spawn agents that are opus would that be better?"* | **A long hand-test pass is FANNED OUT across parallel Opus agents, not walked serially.** Partition by WORLD (its own port, its own browser tab, its own copy of the demo data) so no two agents share a fixture; hand each the fixture recipe rather than letting each rediscover it. The standing caution is unchanged and is the price of the speed: an agent's "found nothing" is weaker evidence than the host's own look, so each returns PICTURES and a filled table, and the host REPRODUCES every finding before it enters the evidence sheet | `raptor-port/docs/bug-check-order.md` §4 (a new paragraph under "where to spend the other models"); this session's evidence sheet `raptor-port/docs/handpass/2026-09-21-oil.md` |
| D14 | 21 Sep 26 | *"theres going to be alot of context for the AI to read ... you record down word for word on what i mentioned. Not sure if u can summarise it ... reading so much context as an AI it starts to hallucinate"* | Records state the DECISION, not the transcript. Quote him only where the exact words are load-bearing; otherwise one line + date + a pointer. Docs are tiered by how often they must be read, and the always-read tier has a budget that can only go DOWN **— AMENDED 24 Sep 26 BY D141:** no line targets and no budget that "only goes down"; the test is whether each block is needed where it sits, and a ceiling is a tripwire that can rise with its reason. | `raptor-port/docs/doc-budget.md`; gate `npm run docsize`; `OUTSTANDING-ARCHIVE.md` → `[DOC-TRIM]` (closed 24 Sep 26) |
| D13 | 21 Sep 26 | *"How do we fix your behavior lapse?"* (on rulings not being written down) | Record a ruling the moment it is made, in `DECISIONS.md`, before doing the work it implies — enforced by a hook on every message and a `Rulings:` line in every report **— AMENDED 24 Sep 26 BY D137:** recorded in its AREA's file under `.claude/rules/decisions/`; `DECISIONS.md` is now their map. | `.claude/rules/record-decisions.md`; `.claude/hooks/record-decisions.sh`; this file |
| D12 | 21 Sep 26 | *"Just to confirm the bug check is not designed only for this OIL check scenario right? ... it can be interface, rules, cross platform and more"* | The bug-check order must adapt to any kind of work, not just this feature | `raptor-port/docs/bug-check-order.md` §2a |
| D11 | 21 Sep 26 | *"how does it integrate with the Claudex review workflow. Make sure that's covered"* | Claudex owns the plan and the code read; the order owns the running app. The walk goes BEFORE Claudex's final inspection, and money work needs both providers, not one | `…/bug-check-order.md` §4a; `.claude/rules/bug-check.md` |
| D10 | 21 Sep 26 | *"The previous methods I told I to bug check. I think this is the most comprehensive one. So just follow this instead"* | The bug-check order SUPERSEDES the 16 Sep scenario rule, the 20 Sep rules sweep and the 21 Sep test-like-a-human rule. One method, not four | `…/bug-check-order.md` §0a; the three superseded memories now point at it |
| D9 | 21 Sep 26 | *"the bug check order auto automatically kick in based on the right scenario and will recommend what checks to do ... So that i dont need to figure out what kind of bug checks to execute"* | The order fires itself; the agent states the tier and the checks; the owner never picks them | `…/bug-check-order.md` §0a; `.claude/rules/bug-check.md` |
| D8 | 21 Sep 26 | *"Can u ask fable and codex to come up with a robust way to bug check, I want to save that as a standing order"* | Both providers proposed independently; the merged result is the adopted order | `…/bug-check-order.md`, plus both proposals verbatim in `…/superpowers/briefs/` |
| D7 | 21 Sep 26 | *"there are also different way to execute bug tests ... Do we think we should execute all 3 to be robust?"* — answered yes, proportionately | Code read, automated tests and a hand pass each catch what the others cannot; run all three, scaled by what the change touches | `…/bug-check-order.md` §3 and §5 |
| D6 | 21 Sep 26 | *"U can ask fable and codex to think through all the possible scenarios to test for bugs and opus be the executor"* | The other providers DESIGN the scenarios; Opus executes them in the running app | `…/bug-check-order.md` §4 rank 1; the two lists in `…/specs/2026-09-21-oil-scenarios-{fable,codex}.md` |
| D5 | 21 Sep 26 | *"Remember this way of testing henceforth when a bug test is made"* | Driving the running app IS the bug test; a code review plus green tests is not one | `…/bug-check-order.md` §0; memory `bug-tests-drive-the-app-like-a-human` |
| D4 | 21 Sep 26 | *"all the previous bug tests we did there will be bugs not captured ... perhaps the next session we can do that after this task is truely completed"* | Every earlier feature checked the review-plus-tests way is carrying the same class of defect; sweep them after OIL closes | `OUTSTANDING.md` → `[HUMAN-RETEST]`, priority 1a |
