---
paths:
  - raptor-port/src/engine/**
  - raptor-port/src/ui/**
  - raptor-port/src/state/**
  - raptor-port/src/undo/**
  - raptor-port/src/command/**
  - raptor-port/src/storage/**
  - raptor-port/src/*.ts*
  - raptor-port/src/testing/**
  - raptor-port/e2e/**
  - raptor-port/probes/**
  - raptor-port/docs/engine-rules.md
  - raptor-port/docs/ui-contracts.md
  - raptor-port/docs/feature-impact.md
  - raptor-port/docs/performance.md
  - raptor-port/docs/undo-contract.md
  - raptor-port/docs/data-schema.md
  - raptor-port/docs/mock/**
  - raptor-port/docs/**/*allavail*
  - raptor-port/docs/**/*amend*
  - raptor-port/docs/**/*crewrest*
---

# Rulings — the scheduler and amendments

**Loads by itself** whenever a session reads a scheduler, board, engine, amendment or storage file (the
`paths:` at the top of this file). The general rulings are in `.claude/rules/decisions/how-we-work.md`, loaded in every session; the map of every ruling and how to add or retire one: `DECISIONS.md`. Newest first; each row keeps the date it was recorded.
**Also read** — in `oil.md` (it loads by itself with the placeholder, publishing and OIL files): **D28, D31,
D32, D43, D46, D52** (what the ALL / ALL AVAIL pucks earn wherever they may land — the earning list follows
the landing list), **D48, D49** (a published day and a rule change), **D19** (a weekend no Leave War period
covers).

**Where the detail lives:** the rules engine — `raptor-port/docs/engine-rules.md`; how each surface renders,
drags and edits — `raptor-port/docs/ui-contracts.md`; what a change touches — `raptor-port/docs/feature-impact.md`;
speed — `raptor-port/docs/performance.md`; what is stored — `raptor-port/docs/data-schema.md`; undo —
`raptor-port/docs/undo-contract.md`; the files — `raptor-port/docs/file-map.md`. The rules every area shares
(the store, the funnels, what persists, the robustness doctrine) stay in `raptor-port/CLAUDE.md`.

| # | Date | His ruling, in his words where short enough | What it means | Where it lives now |
|---|---|---|---|---|
| D188 | 26 Sep 26 | **"Q6yes"** — to the `[LEAVE-LATE-PUBLISHED]` look card's Q6: *"My reading of D185: 'a lapsed qualification' is live — SC currency, AAR currency, AAR instructor clearance and an illegal seat; the crew-pairing warnings (an OCU with no instructor, no IR examiner, an unauthorised pairing) stay frozen and read pending. And the 'tight turning — crew rest' note is live with the crew-rest break (the same check). Right?"* | **CONFIRMED: THE AGENT'S READING OF D185, AS BUILT.** Live on a published face: the warnings the app marks with its Qualification flag (an illegal seat, SC currency, AAR currency, an AAR instructor not cleared) and the crew-rest check's tight-turn note beside its breach; the crew-pairing warnings stay frozen and read pending when they change. Closes the reading D185's row left open for him | `raptor-port/src/engine/validate.ts` (`LIVE_ON_FACE`); `raptor-port/docs/engine-rules.md` §Publishing ("What stays LIVE on the face"); the look card `raptor-port/docs/handpass/2026-09-26-late-pub.md` §10 |
| D187 | 26 Sep 26 | **"Q5 it should"** — to the look card's Q5: *"The board's 👁 look at a published version shows no warnings at all (as it always has); View-only Sched shows them. Show them there too?"* | **THE BOARD'S (AND THE EDIT WEEK'S) 👁 LOOK AT A PUBLISHED VERSION SHOWS ITS WARNINGS, AS VIEW-ONLY SCHED DOES** — the rings, flags and warning list of that document. Sets aside the long-standing "a version preview reads and does not check" (PV: no flags — `html.ts`, the board). **The agent's reading, stated to him so he can correct it:** looking at the day's CURRENT published version shows exactly what View-only Sched shows (the warnings it went out with, with the live ones — D183–D185 — on top); looking at an OLDER, superseded version shows the warnings THAT version went out with (it is a record of what was issued then; today's live warnings belong to the current document) | BUILT 26 Sep 26 on the branch claude/leave-late-published: `raptor-port/src/engine/validate.ts` (`versionFaceWarn`, `withVersionWarn`), `raptor-port/src/ui/html.ts` (`withVersionFlags`, `dayPreviewHTML`, `puckMarks`, `withChipWorld`), `raptor-port/src/ui/SchedBoard.tsx` and `raptor-port/src/ui/board.ts` (`boardWarnHTML(di, true)`), `raptor-port/src/state/view.ts` (`displayedBundle`); `raptor-port/docs/ui-contracts.md` §Version preview; pinned by `raptor-port/src/ui/latepub.test.tsx` "D187 …"; `OUTSTANDING.md` `[PV-NO-FLAGS]` (answered and built; to the archive with the merge) |
| D186 | 26 Sep 26 | **"Q1 yes"** — to the `[LEAVE-LATE-PUBLISHED]` look card's Q1: *"A man's CAT letter on his puck and a blank line's suggested brief time now stay as they were published, like everything else; a change to either reads pending. Keep it?"* — after asking, the same morning, *"only new published schedules will show the new cat? And if there is a new change in callsign will it update the published schedule?"* and being told yes (the CAT waits for the next AL; a callsign updates at once, a label) | **KEEP IT: A MAN'S CAT / SEAT / POSTING ON HIS PUCK, AND A BLANK LINE'S SUGGESTED BRIEF TIME, STAY AS PUBLISHED UNTIL THE NEXT AL** (or Unpublish and publish again), on View-only Sched, the CSV and the print; a change to either reads pending and takes the four down; days published later, and unpublished days, show the new value at once. A callsign rename still updates the published schedule at once (a label — 14 Sep 26), and a warning the new CAT raises that stays live (D185) still shows at once. **Narrows the 7 Aug 26 "no rule versioning" invariant** (`raptor-port/CLAUDE.md` §Product invariants) for ONE printed value: a published day keeps the brief lead it printed with it; the rulebook itself stays unversioned (the validator and every working surface read today's rules). Settles `[LATE-PUB-FACE-LIVE]` items 1–2 as built (Astra's code read #2) | `raptor-port/docs/engine-rules.md` §Publishing (`snap.pa` / `snap.rv`); `raptor-port/src/engine/faceattrs.ts`; `raptor-port/CLAUDE.md` §Product invariants (the pointer on "no rule versioning"); `OUTSTANDING.md` `[LATE-PUB-FACE-LIVE]`; the look card `raptor-port/docs/handpass/2026-09-26-late-pub.md` §10 |
| D185 | 26 Sep 26 | **"2 & 3 make it live and handoff"** — answering the agent's three candidates for staying live on a published day (1 a medical downchit, 2 a crew-rest or 7-day break on the day itself, 3 a lapsed qualification; recommended: all three, live and pending) | **A CREW-REST OR 7-DAY BREAK ON A PUBLISHED DAY, AND A LAPSED QUALIFICATION, STAY LIVE ON ITS FACE; A MEDICAL DOWNCHIT STAYS FROZEN (D179 unchanged for it).** Confirms D184's reading (2) and adds the qualification. **The agent's reading, stated to him so he can correct it:** (a) "2 & 3" are items 2 and 3 of that list — so medical is NOT made live (he chose the same for it in D179, against the same recommendation); **CONFIRMED the same minute — "1 frozen still": the medical downchit stays frozen**; (b) live the way D183/D184 are: the `CREW_REST`, `DAYS_RUN` and `QUAL` warnings (with the rings and flags they draw) come from today's judgement on the published face, are not stored and not compared, so they alone never make the day pending; (c) a man's CAT, seat or posting change still reads pending through the roster comparison (`snap.pa`) and still draws as issued on the puck (Astra #2, built) — only the warning it raises is live. **The agent's reading WIDENED at the build, 26 Sep 26 (overnight), stated to him on the look card so he can correct it:** (d) "a lapsed qualification" = the warnings the app marks with its Qualification flag — an illegal seat (`QUAL`), SC currency (`SC_QUAL`), AAR currency (`AAR_QUAL`) and an AAR instructor not cleared (`AAR_INSTR`); the first reading named `QUAL` alone, which would have left a lapsed SC or AAR currency — the most literal "lapse" — frozen; the crew-pairing warnings (an OCU without an IP, no IR examiner, an unauthorised pairing) stay frozen; (e) the crew-rest check's other answer, "Tight turning — crew rest" (`CREW_TIGHT`), is live with the breach — one check answers breach OR tight turn, so a neighbour's change turning one into the other must not leave a frozen tight turn beside a live breach, nor read pending for one answer and not the other; (f) a currency tick is not in the roster comparison, so a lapsed currency shows on the face at once and reads nothing pending | `OUTSTANDING.md` `[LATE-PUB-FACE-LIVE]` (item 4); `HANDOFF.md`; on build: `raptor-port/src/engine/validate.ts` (`LIVE_ON_FACE` gains `CREW_REST`, `DAYS_RUN`, `QUAL`; `faceWarn` rebuilds each published day's rings and flags from the frozen warnings plus the live ones), `raptor-port/src/engine/publish.ts` (`warnSliceKey`), `raptor-port/docs/engine-rules.md` §Publishing |
| D184 | 26 Sep 26 | **"The 7 day warning as well"** — straight after D183, answering the agent's question which other safety warnings should stay live on a published day | **THE 7-DAY RUN WARNING STAYS LIVE ON A PUBLISHED DAY'S FACE TOO** — another exception to D179's "freeze everything for now", beside D183's dotted crew-rest mark. **The agent's reading, stated to him so he can correct it:** (1) it covers the consecutive-days warning (`DAYS_RUN`) on the published day itself and its "7" mark (the RUN trace, already live under D183); (2) with D183's purpose — "to see the break of crew rest" — a CREW-REST breach warning on the published day itself (`CREW_REST`, e.g. caused by a late duty on the day before) is live too, so the dotted mark and the break it points to never disagree; (3) live the way D183 is: drawn from today's judgement, not stored and not compared, so these alone never make the day pending — the change that caused them is pending where it was made. **Still open, put to him:** medical downchits and a lapsed qualification (the agent recommends live AND pending) | `OUTSTANDING.md` `[LATE-PUB-FACE-LIVE]`; `HANDOFF.md`; on build: `raptor-port/src/engine/validate.ts` (`LIVE_ON_FACE`, `faceWarn` — the per-person rings and flags rebuilt from the frozen warnings plus the live CREW_REST / DAYS_RUN ones), `raptor-port/src/engine/publish.ts` (`warnSliceKey`), `raptor-port/docs/engine-rules.md` §Publishing |
| D183 | 26 Sep 26 | *"The dotted 'breaks tomorrow's crew rest' mark. Should be live to see the break of crew rest"* — answering the look card's Q2 on `[LEAVE-LATE-PUBLISHED]` | **THE DOTTED "BREAKS TOMORROW'S CREW REST" MARK (and its RUN twin) STAYS LIVE ON A PUBLISHED DAY'S FACE — an exception to D179's "freeze everything for now".** The mark shows a crew-rest break as it stands today, so a change on the next day that creates or clears the break shows on the published day at once. **Sets aside** Astra's code-read #1 (freeze it with the day's warnings) and the build of 26 Sep 26 that followed it; **agrees with** Fable's plan read F4. **The agent's reading, stated to him so he can correct it:** the mark is not stored with the published version and is not compared, so a change to the next day that moves it does NOT make the published day pending or take its four down — the break itself is the NEXT day's warning, pending there if that day is published; everything else D179 froze stays frozen | `OUTSTANDING.md` `[LATE-PUB-FACE-LIVE]` (item 3); `HANDOFF.md`; on build: `raptor-port/src/engine/validate.ts` (`warnSliceOf`, `faceWarn`), `raptor-port/src/engine/publish.ts` (`warnSliceKey`), `raptor-port/docs/engine-rules.md` §Publishing |
| D179 | 25 Sep 26 | **"freeze everything for now"** — answering D178's open question, what should still be live on the published schedule, after the agent recommended keeping medical downchits and a lapsed qualification live (and pending) | **NOTHING STAYS LIVE ON A PUBLISHED DAY FOR NOW: EVERY CHANGE THAT WOULD MOVE WHAT IT SHOWS FREEZES, AND SHOWS AS PENDING FOR THE ADMIN** — medical downchits and a lapsed qualification included. **Sets aside the agent's recommendation** (those two live), and the 15 Sep 26 crew-rest flagging design (`raptor-port/docs/superpowers/specs/2026-09-15-crewrest-flagging-plan-v2.md` §4, Codex FW-002 / CRPF-001: "current safety facts are never versioned" — a design decision, never a ruling of his; the newer word wins). **The consequence, said to him:** a man who goes medically unfit after the day is published still reads fit on the published schedule until the admin re-issues it; the admin sees the change pending at once. **"For now":** provisional — put it to him again when the build shows it, not re-decided without him. **The agent's reading, stated to him so he can correct it:** "everything" also takes in a rule setting the admin changes (its effect on a published day's warnings waits for a re-issue, as D48 already holds the day's OIL); left as they are only a reader's own view choices — the "Working draft" view on View-only Sched, one's own hidden LATE mark — and the Leave War's reminders that a leave period is missing (an admin to-do, not a change to what was published) | `OUTSTANDING.md` `[LEAVE-LATE-PUBLISHED]`; the sweep `raptor-port/docs/superpowers/specs/2026-09-25-published-face-live-inputs.md` (B1, B2 now freeze); on build: `raptor-port/src/engine/events.ts` (the medical exception in `inpShow`), `raptor-port/src/engine/validate.ts`, `raptor-port/docs/engine-rules.md`, the crew-rest plan §4 marked set aside |
| D178 | 25 Sep 26 | *"actually based on what we discussed, whenever there is a change, in terms of input from a member, they will inform the admin, so the admin should see the pending after the schedule is published and either publish an AL or just unpublish and republish if it does not affect anyone. I understand the downside if someone takes and reads the version where its silently retracted. But it doesnt affect operations. So with that in mind, what do u think should still be live on the schedule?"* — after the sweep of what changes a published day's face when an input goes in | **EVERY MEMBER INPUT CHANGE AFTER A DAY IS PUBLISHED IS A PENDING CHANGE FOR THE ADMIN — filed, edited, deleted or moved — and the published face keeps what it was issued with.** The member tells the admin; the admin sees it pending and either publishes an AL, or — if it affects no one — Unpublishes and publishes the day again. **The cost he accepted, knowingly:** someone may have read the version that is then silently withdrawn; it does not affect operations. **Widens D177** (a late leave) from one kind of input to every input change, and is the rule `[LEAVE-LATE-PUBLISHED]` builds (its scope: the sweep `raptor-port/docs/superpowers/specs/2026-09-25-published-face-live-inputs.md`, A0–A7). **ANSWERED BY D179 ("freeze everything for now") — the recommendation below set aside.** His question back, what should still be live on the published schedule: answered by the agent with a recommendation (medical downchits and a lapsed qualification stay live AND show pending — they change who may fly; the working-draft view, one's own hidden LATE marks and the Leave War period advisories stay as they are; everything else input-driven freezes, incl. a neighbour day's input moving a published day's warnings), awaiting his word | `OUTSTANDING.md` `[LEAVE-LATE-PUBLISHED]` (its scope widened); the sweep document; on build: `raptor-port/src/engine/publish.ts`, `raptor-port/src/ui/html.ts`, `raptor-port/src/ui/board.ts`, `raptor-port/docs/engine-rules.md`, the register AM20 |
| D177 | 25 Sep 26 | **"Question 2 yes"** — to *"A leave filed after a day is published shows on View-only Sched at once, while the day says nothing is pending and the sign-offs stay. Your D44/D45 say a change like this should show as pending, so the scheduler amends or publishes the EOD version. Should a late leave read '1 pending' and clear the sign-offs (the published face then keeping what it was issued with until the next AL)? (Recommended: yes — its own small branch; the published face part is the bigger half.)"* (D175's look card, question 2) — and, the same message: *"Is there anything else that does this too? Like change the published schedule when an input goes in"* | **— WIDENED 25 Sep 26 BY D178: every member input change after publishing (filed, edited, deleted, moved) is pending, not only a late leave.** **A LEAVE FILED AFTER A DAY IS PUBLISHED IS A PENDING CHANGE: THE DAY READS "1 PENDING", THE FOUR SIGN-OFFS FALL, AND THE PUBLISHED FACE KEEPS WHAT IT WAS ISSUED WITH UNTIL THE NEXT AL (or the EOD version).** D44/D45 applied to a late leave: nothing on a published schedule changes without the scheduler acknowledging it. Built on ITS OWN branch (the agent's recommendation, taken with his yes). **The agent's reading, stated to him so he can correct it:** it covers every Unavailable-type input filed after publishing (leave, OD, courses) — NOT a medical downchit, which stays live on the published face the moment it is filed (a safety fact, never versioned — the standing exception in `raptor-port/src/engine/events.ts`); taking such a late input back out again reads 0 (D98). **His follow-up question** — what else changes the published schedule when an input goes in — is answered from a sweep of the code and scopes that branch (`OUTSTANDING.md` `[LEAVE-LATE-PUBLISHED]`) | `OUTSTANDING.md` `[LEAVE-LATE-PUBLISHED]` (its own branch); on build: `raptor-port/src/engine/publish.ts` (the filing comparison and the signature), `raptor-port/src/ui/html.ts` (the published face's Unavailable block and every other reader of live inputs), `raptor-port/docs/engine-rules.md`, the register AM20 |
| D176 | 25 Sep 26 | **"Question 1 make it 0"** — to *"A request taken off before the day was published, then deleted on the Inputs page, still says '1 pending' and clears the sign-offs, though nothing on either schedule changed. Make it 0? (Recommended: yes — the mirror of your D174; built on this branch before 'merge live' if you say yes.)"* (D175's look card, question 1) | **A REQUEST THAT WAS "TAKEN OFF" WHEN THE DAY WAS PUBLISHED, AND IS SINCE DELETED (OR RE-DATED OFF THE DAY), IS NO PENDING CHANGE — THE DAY READS 0 AND THE FOUR SIGN-OFFS HOLD.** The mirror of D174: the published face showed it dormant, and neither face shows it now (D98). Built on `claude/request-one-row` before "merge live". **The agent's reading, stated to him so he can correct it:** a dormant request still covering the day and woken back to a live one (retyped, so it flags again) stays a change — only its going away is not | **BUILT on the branch claude/request-one-row:** `raptor-port/src/engine/publish.ts` (`filingSame`, read by `filingDelta` and `filingKey`), `raptor-port/docs/engine-rules.md` §Publishing, the register AM20; `OUTSTANDING.md` `[REQ-DECLINED-DELETED]` |
| D175 | 25 Sep 26 | **"2. Ok"** — to *"A two-day request moved from Monday to Tuesday, then Monday's older version loaded (or an older Monday plan switched in), ends up on BOTH days. Recommended fix: the load leaves that row out and says so. When? (Recommended: its own small branch right after this one merges, before accounts.)"* (D114's look card, question 2) | **A LOAD OR A PLAN SWITCH NEVER PUTS A REQUEST ON A SECOND DAY: IT LEAVES THAT ROW OUT AND SAYS SO — built on ITS OWN small branch right after PR #435 merges, BEFORE `[ACCOUNTS]`.** One request, one row (the rule `acceptInput` already keeps); a whole-day replacement (loading a version, switching to a plan) that would bring back a row whose request now stands on another day leaves that row out and names it in its message, the way the load already leaves a request's filing it cannot set (LOADLEFT) — one day's load never moves another (AM1). **Changes D173's order in part:** a step between its 1 and 2 — this fix, with D174 riding the same branch (both are about a request's row on a published day); D173's steps 2–4 follow unchanged | **BUILT 25 Sep 26 on the branch claude/request-one-row:** `raptor-port/src/engine/publish.ts` (`rowsLeftOut`, `leaveRowsOut`), `raptor-port/src/engine/drafts.ts` (the load, the switch, `rowsLeftSaid`), `raptor-port/docs/engine-rules.md` §Publishing ("A REQUEST'S ROW AND ITS FILING…"), the register AM1; evidence `raptor-port/docs/handpass/2026-09-25-req-one-row.md`; `OUTSTANDING-ARCHIVE.md` `[REQ-TWO-ROWS]`; `.claude/rules/decisions/how-we-work.md` D173 (the order, amended) |
| D174 | 25 Sep 26 | **"1. Yes"** — to *"A request filed on a published day, then taken off, still says '1 pending', although the schedule looks exactly as published. Make it 0? (Recommended: yes — your D98 rule: back to what was published shows nothing pending.)"* (D114's look card, question 1) | **A REQUEST THAT WAS NOT THERE WHEN THE DAY WAS PUBLISHED, FILED SINCE AND THEN TAKEN OFF, IS NO PENDING CHANGE — THE DAY READS 0.** D98 applied to a request's filing: the schedule is back to what was published, so nothing is pending and no AL carries it. **The agent's reading, stated to him so he can correct it:** the request stays silenced as today (taken off, flags nothing — 26 Aug 26); a request that WAS there when the day was published and is now taken off still counts one (the issued day showed it); the four sign-offs hold on that day as they do for any change put back (D103, AM11). Built with D175 on its own branch | **BUILT 25 Sep 26 on the branch claude/request-one-row:** `raptor-port/src/engine/publish.ts` (`filingSame`, read by `filingDelta`, `filingRestorePlan` and `filingKey`), `raptor-port/docs/engine-rules.md` §Publishing, the register AM20; evidence `raptor-port/docs/handpass/2026-09-25-req-one-row.md`; `OUTSTANDING-ARCHIVE.md` `[REQ-DECLINED-PENDING]` |
| D172 | 25 Sep 26 | *"The ORIG tag seems abit too long, what other tag can u suggest"* — on the corner tag proposed for a changed puck on a day not yet published (D118: a hollow ORIG tag) | **THE CORNER TAG ON AN UNPUBLISHED DAY'S CHANGED PUCK SHOULD BE SHORTER THAN "ORIG".** Amends D118's choice of the word (the hollow, dotted form stands). **OPEN — the agent's suggestions, put to him:** **"NEW"** (recommended — three letters like "AL1", and it says what the tag means under D170: new to you; the same word as the day's "N new" count and the window's NEW badge), in the same plain white dotted outline; or a small hollow dot with no word; or "OG". Never gold or amber — amber is a warning colour (D110's rule for this tag). **Then his own proposal, the same hour:** *"Show me a pic, including AL0, and all AL souls adopt a tick on the of each day as well, just like how the ORIG is shown currently. But before we accept the decision show me a mock up"* — (a) "AL0" as a further option for the tag (the Original is amendment nought, so a change going out with it reads AL0, as one going out with the first amendment reads AL1); (b) every AL tag in a day's heading carries a tick like the ORIG seal (D111). **Both OPEN until he has seen the mock-up** (`raptor-port/docs/mock/tags-ticks.html`). **SETTLED after it — *"OG makes sense, maintain the header"*:** the corner tag on a changed puck of a day not yet published reads **"OG"**, in the plain white dotted outline; and the day headings **stay as they are** — ORIG keeps its ticked seal (D111), the AL tags stay solid with no tick (his own tick proposal, (b) above, not taken) | `OUTSTANDING.md` `[DRAFT-PENDING]`; on build: `raptor-port/src/ui/scheduler.css` (the corner tags), `raptor-port/docs/ui-contracts.md` |
| D171 | 25 Sep 26 | *"I think members don't need a changes at the top bar. When they are curious they can click on the orange changes or pending button of the applicable day. For admin, instead of changes on a desktop, just show the icon at the top. Just to confirm the icon at the top, will it show the number beside the icon for the changes of the week? Or just the first day in view of the left side of the screen"* — on the mock-up of the ways into the changes window (`raptor-port/docs/mock/changes-doors.html`) | **THE WAYS INTO THE CHANGES WINDOW.** (1) **Members get NO top-bar button** — they open the window from the day's own count ("3 changes" / "N pending") when curious. (2) **Admins get the top-bar door as an ICON ONLY on desktop too** (the clock icon the phone already has), not the word "Changes". (3) Every day's heading count stays the per-day way in, for everyone. **SETTLED the same hour — "Ok week":** the icon's number counts what is new to you across the WHOLE WEEK that is loaded — not the first day in view (which changes as the week scrolls sideways, so the number would jump without anything changing); the per-day counts in the day headings, and the day picker's dots inside the window, say which days | `OUTSTANDING.md` `[DRAFT-PENDING]`; the mock-up `raptor-port/docs/mock/changes-doors.html`; on build: `raptor-port/src/ui/Shell.tsx` (`#histBtn`), `raptor-port/docs/ui-contracts.md` |
| D170 | 25 Sep 26 | **"A"** — choosing, on the one-changes-window mock-up (`raptor-port/docs/mock/changes-window.html`), option A (no Hand over button) over B (keep it), after asking *"Do u think a hand over button is needed?"* | **NO HAND OVER BUTTON. CHANGES ARE "NEW TO YOU" UNTIL YOU MARK THEM SEEN — LIKE UNREAD EMAIL.** With accounts (D166) the app knows who has seen what: a change made by someone else shows as new to you (a gold dot, NEW, the "N new" count) until you press **"Mark all as seen"** in the changes window, which affects only your own view; the app groups changes by person and sitting on its own ("Hex · 25/9 15:20–15:38 · 3 changes"); every line says who and when. **Replaces** D118's hand-over mechanism (the "Hand over" word, the chained hand overs) and D167's list of hand overs (the window lists people and sittings instead) — both stay live for everything else they say: D118's ask that a day not yet published counts changes like a published one and opens a list, and D167's window. **The agent's reading, stated to him so he can correct it:** on a PUBLISHED day the window also still says what will go out as the next ALn (the pending count, unchanged); a member's "Mark all as seen" is his own too | `OUTSTANDING.md` `[DRAFT-PENDING]`; the mock-up `raptor-port/docs/mock/changes-window.html` (option A); on build: `raptor-port/src/ui/pendlist.ts`, `raptor-port/docs/ui-contracts.md` |
| D169 | 25 Sep 26 | *"I recommend to show it to the members as well from view only schedule and clicking on live working copy. This is because I want to shame admins who abuse their authority to try to plan themselves with easier lives and I want full transparency."* — answering the agent's recommendation that the full change history be for admins only | **MEMBERS SEE THE FULL CHANGE HISTORY TOO — WHO CHANGED WHAT, ON EVERY DAY — FROM VIEW-ONLY SCHED, BY OPENING THE LIVE WORKING COPY.** His reason is the ruling's purpose: full transparency, so an admin who plans himself an easier life is seen doing it. **Sets aside the agent's recommendation** (admins only). **The agent's reading, stated to him so he can correct it:** members READ it (no "Mark all as seen" affects anyone but themselves; nothing in it edits); one limit stays, because it is already a non-negotiable of the database brief — a MEDICAL input's details are readable by that person and admins only, so a member's history names the change ("a medical input") without its remarks | `OUTSTANDING.md` `[DRAFT-PENDING]`; the mock-up `raptor-port/docs/mock/changes-window.html`; `raptor-port/docs/handover-dataverse.md` (medical stays restricted); on build: `raptor-port/docs/ui-contracts.md`, the view page's working-copy picker |
| D168 | 25 Sep 26 | *"One changes window makes sense"* · *"Ok mock up please"* (to grouping the changes by Who or by Where) — answering the agent's suggestion to merge the three places that list changes | **ONE CHANGES WINDOW FOR THE WHOLE APP.** It merges the three lists that show changes today: the published day's pending list (D99, D100), the hand-over list of an unpublished day (D118, D167) and the Edit history list (D116, D117). Its shape is D167's (movable, resizable, the schedule behind stays usable; a tap takes the schedule to the change and the window stays; the phone's bottom panel shrinks to a bar), with **a day picker** (D117), **a "Group by: Who / Where" switch** (Who = by the person; Where = by the day's own sections) and a tap-to-go-there. **The agent's reading, stated to him so he can correct it:** two views inside it — what is new to you, and every change — and on a published day the list also says what will go out as the next ALn; D116's History toggle keeps its bubbles on the changed details, and its LIST is this window. **Still open, put to him with pictures:** whether the Hand over button stays at all, or gives way to an "unread" model (new to you until you mark it seen) | `OUTSTANDING.md` `[DRAFT-PENDING]`; the mock-up `raptor-port/docs/mock/changes-window.html`; on build: `raptor-port/src/ui/pendlist.ts`, `raptor-port/src/ui/HistoryModal.tsx`, `raptor-port/docs/ui-contracts.md` |
| D167 | 25 Sep 26 | *"should we design it such that all the handovers can be seen by all users? Should we design it such that instead of a fixed bubble, can it be an adjustable window that can be resized. and moved around and the admin can still work on the schedule while refering to the the changes. Like when the admin clicks on the item, it brings the background view to that area of change but keeps the window open. But for the phone i think due to the window size this is limited, see what u can forgo or keep in terms of function. Inaddition should we have an option to hide the dropdown of each hand over and the handover should have day and month. in a form of 25/9 for e.g. let me know if u have better suggestions"* — after the hand-over-with-accounts mock-up (`raptor-port/docs/mock/handoff-accounts.html`) | **THE CHANGES LIST BECOMES A WINDOW, AND EVERY HAND OVER IS VISIBLE.** (1) **A movable, resizable window that does not block the schedule** — the ALL AVAIL window's own pattern (D38–D41: the six-dot grip, resizable, editing behind it goes on); a tap on a change takes the schedule BEHIND to it and marks it, and the window STAYS OPEN. (2) **On a phone** the window takes the ALL AVAIL window's phone form (D77: a full-width bottom panel that moves but does not resize); the agent's call on what to keep: after a tap it shrinks to a slim bar at the bottom so the change can be seen, and a tap on the bar brings it back. (3) **Every hand over of the day is listed** **— AMENDED 25 Sep 26 BY D170: the window lists people and sittings, not hand overs** — with who and the date and time ("25/9 16:00"); what is new to the signed-in person is the emphasis, not a filter. (4) **Each hand over folds and unfolds**; the agent's default: the ones new to you open, the rest folded. **The agent's reading, stated to him so he can correct it:** a scheduler's tool — members, who do not edit the schedule, do not get it (put to him); and the SAME window should serve a published day's pending list, so the app has one changes window — which would REPLACE D100's approved pop-up (a list that closes on a tap outside and on a tap of a change) with this window — also put to him. The design is his to approve from a mock-up | `OUTSTANDING.md` `[DRAFT-PENDING]`; the mock-up `raptor-port/docs/mock/handoff-window.html`; on build: `raptor-port/src/ui/pendlist.ts` (the list becomes the window), `raptor-port/src/ui/AvailWindow.tsx` (the pattern), `raptor-port/docs/ui-contracts.md` §The pending list |
| D119 | 25 Sep 26 | *"Shouldn't the latest change for pending be at the top? Just like history list"* — during his look at PR #435 | **THE PENDING LIST SHOWS THE NEWEST CHANGE FIRST, AS EDIT HISTORY DOES.** Replaces the list's order of 25 Sep 26 (the day's own sections top to bottom). **The agent's reading, stated to him so he can correct it:** ordered by when each change was last made (the edit record's time), newest at the top; a change the record has no time for ("earlier", what the day earns, a filing) goes below the timed ones, in the day's order | on build: `raptor-port/src/ui/pendlist.ts` (the order), `raptor-port/docs/ui-contracts.md` §The pending list; the register AM53 |
| D118 | 25 Sep 26 | *"I currently see pending when I build a schedule that hasn't been published. But it can't be clicked. And it functions weirdly, like when I move a puck to another area it still registers as 2 pending, I move it back it's still 2 pending. I add a new puck it doesn't register anything as pending. Can this be done similar to what u developed currently once a published schedule has been made and an edit is made after to show pending."* — during his look at PR #435 | **A DAY NOT YET PUBLISHED SHOULD NOT SHOW A "PENDING" THAT MISCOUNTS AND CANNOT BE TAPPED.** Today it counts every cell touched (a move reads 2, moving it back still 2, a new puck in a crowd 0) and is not a button. His ask: make it behave like the published day's (a move 1, put back 0, a tap opens the list). **HIS INTENT, the same minute:** *"the intent of seeing pending is so that when another scheduler logs into the app, they can see what changed from the last draft. how is it possible to do this? I understand the current pending after published schedule works correctly"* — so it is a HANDOVER between schedulers: the next one sees what changed since the last draft. That rules out the agent's first recommendation (drop "N pending" on an unpublished day). **OPEN — the how, put to him:** a point the pending compares against, in `OUTSTANDING.md` `[DRAFT-PENDING]`. **His correction to the agent's name for it, the same hour:** *"instead of save draft i think its misleading because the app saves and shows changes live to every user, is there another word to replace it, to tell the user that once u click on this is like a checkpoint in which pending is tracked after this is saved, and when a scheduler clicks this button, it will register who last set that checkpoint"* — so NOT "Save draft" (the app already saves live); it is a CHECKPOINT: pending counts from it, and it records WHO set it last (and when). The button's word is still to be picked. **Then:** *"can u show me a mock up of 1 and 3. yup a hollow orig tag makes sense … can u show me a mock up of orig tag hollow and another one thats hollow and dotted exterior just like the rest of the tags when they are pending changes?"* — a changed puck on an unpublished day wears a HOLLOW ORIG tag (the form — a plain outline or a dotted one like a waiting AL tag — picked from the mock-up); the word is "Set checkpoint" or "Hand over", picked from the mock-up; and (his question, answered yes) setting a checkpoint clears the marks and the count, as the day becomes the new point they count from. **Then, after the mock-up (`raptor-port/docs/mock/checkpoint.html`):** *"Handover looks better but I am wondering how does a scheduler for example, after the schedule is made and handover is clicked, any subsequent changes made by another scheduler will also click another handover and the first scheduler who sees the schedule they will just see everything that changed but not highlighted changes"* — the WORD is **"Hand over"**; and he expects EVERY scheduler to press it after their own changes, which a single reset point cannot serve (the next person would see nothing highlighted). **— THE HAND-OVER MECHANISM BELOW IS REPLACED 25 Sep 26 BY D170 (no Hand over button; "new to you" until marked seen); the rest of this row stands.** **OPEN — the model, put to him:** each hand over is kept, and the highlights show what changed since the hand over BEFORE the latest one — the last person's work — so pressing it after your own changes is the right act (`OUTSTANDING.md` `[DRAFT-PENDING]`). The tag's form (plain or dotted edge) is still his to pick. **Then, after the storyboard (`raptor-port/docs/mock/handover.html`):** *"What if there are multiple schedulers. When u say only you will see the change"* — the highlights are SHARED (everyone who opens the day sees the same ones — the agent's "only you" was wrong wording), and with three or more schedulers one who has not looked for a while would miss the changes from before the last hand over. **OPEN — the fix, put to him:** a "Since" choice on the list (the last hand over by default, any earlier hand over to catch up) in `OUTSTANDING.md` `[DRAFT-PENDING]`. The published day's pending is right as it is (his words). Nothing built until he answers | `OUTSTANDING.md` `[DRAFT-PENDING]`; on build: `raptor-port/src/engine/publish.ts` (`dayShownPendCount`), `raptor-port/src/ui/html.ts` (`dayStatHTML`), `raptor-port/docs/ui-contracts.md` |
| D117 | 25 Sep 26 | *"Can u also allow the history list to instead just show all days, i can select which day to view in that week"* — during his look at PR #435 | **THE EDIT HISTORY LIST SHOWS THE WHOLE WEEK, WITH A DAY PICKER TO NARROW IT TO ONE DAY.** **The agent's reading, stated to him so he can correct it:** it opens on all days of the loaded week, newest first as now; a row of day chips (All · Mon … Sun) narrows it to one day and back; the same list on the board and on Edit Schedule | on build: `raptor-port/src/ui/HistoryModal.tsx`, `raptor-port/docs/ui-contracts.md` §History on the board |
| D116 | 25 Sep 26 | *"Edit history works differently on scheduler board and edit schedule. The scheduler board opens up a list to tap and also it's more of a toggle button when u tap or hover over the item u see a bubble. But edit schedule when u tap on the history button it just shows the list of changes. Can u make the edit schedule the same as the way schedule board functions?"* — during his look at PR #435 | **EDIT SCHEDULE'S HISTORY BUTTON WORKS LIKE THE BOARD'S: A TOGGLE.** Tapping it turns History mode on for the edit week — a changed detail shows its bubble on hover (desktop) or tap (phone), as on the board (D105), and the list is there to tap (D107: a tap stays on Edit Schedule) — and tapping it again turns it off. Replaces the 23 Aug 26 behaviour (`#histBtn` opened the list only, `ui-contracts.md` §the sticky bar). **The agent's reading, stated to him so he can correct it:** one History mode shared by the board and the edit week, so turning it on in one place is on in the other; everything else the board's mode does (the bubble's scroll, its grace) comes with it | on build: `raptor-port/src/ui/Shell.tsx` (`#histBtn`), `raptor-port/src/ui/histbubble.ts`, `raptor-port/docs/ui-contracts.md` §History on the board and §the sticky bar |
| D114 | 25 Sep 26 | **"6 yes"** — to *"taking an accepted request off a published day (✕ on its row) counts 2 — the row and the request's filing, two lines in the list. Make it 1? (Recommended: yes — one act, one line.)"* (his look at the batch, question 6) | **TAKING AN ACCEPTED REQUEST OFF A PUBLISHED DAY IS ONE PENDING CHANGE, NOT TWO.** One tap (✕ on the request's row) is one change in every count — the day head, the board, the ⓘ panel, the Amendments panel, "Discard N edits", the pending list (ONE line: whose request, taken off the programme) and the published AL's item count. What goes out (the stored diff: the row removed and the filing) is unchanged. **The agent's reading, stated to him so he can correct it:** the mirror act — accepting a request onto a published day (its row added + its filing) — is also ONE, by the same reason; a filing change with no row of its own on that day (a request filed under Unavailable, or one whose row stands on another day) stays one on its own. Refines D109's unit (AM23) for a request's row and its filing | `OUTSTANDING-ARCHIVE.md` `[REQUEST-OFF-ONE]`; the register AM23; `raptor-port/docs/engine-rules.md` (the pending count's unit); `raptor-port/docs/ui-contracts.md` (the counting unit); on build: `raptor-port/src/engine/publish.ts` (`dayPendingItemsIn`), `raptor-port/src/ui/pendlist.ts` |
| D113 | 25 Sep 26 | **"5 yes"** — to *"replacing Rune with Tally in the SAME seat counts as 1 change. Keep it one? (Recommended: yes — it reads as one change, and it is what the day showed before.)"* (his look at the batch, question 5) | **A REPLACEMENT IN ONE SEAT IS ONE PENDING CHANGE.** Rune taken off VIPER 1's front seat and Tally put in it — neither moved anywhere else — counts one, and the pending list reads one line ("VIPER 1 · FCP: Rune → Tally"). Settles the gap D109 left (`[MOVE-REPLACE-ONE]`) as built: on a SEAT (a flying seat, a sim seat, a desk's holder, a ground row's name). On a CROWD (a programme row's list, a desk's extras) each man taken off or added is still one, as D109 says. Nothing to build | `OUTSTANDING-ARCHIVE.md` `[MOVE-REPLACE-ONE]`; the register AM23; `raptor-port/docs/engine-rules.md` and `raptor-port/docs/ui-contracts.md` (the pending count's unit); pinned by `raptor-port/src/engine/pendunits.test.ts` |
| D111 | 25 Sep 26 | **"A1"** — picking from the four drawings of option A on `raptor-port/docs/mock/orig-tag.html` (A the first draft, A1 seal, A2 check-cap, A3 round seal); the agent had recommended A2 | **THE ORIG TAG IS "A1, SEAL": a faint white wash, a thin light outline, and a drawn tick in a white disc before "ORIG"** — no colour, so it never reads as an AL colour (D110), and the tick says the day is published. Settles D108's form. **Sets aside the agent's recommendation (A2, the check-cap)** — his pick. **The agent's reading, stated to him so he can correct it:** the ORIG tag only — the AL tags and the dashed DRAFT stay as they are; the same tag on every surface it is drawn (the edit week, the scheduler board, View-only Sched; desktop and phone), built from the mock-up's CSS (`mk-orig-tag-refine.mjs`, variant `s`) | the mock-up `raptor-port/docs/mock/orig-tag.html`; `OUTSTANDING.md` `[ORIG-TAG-STANDOUT]`; the batch spec item 13; on build: the register AM22, `raptor-port/docs/ui-contracts.md`, `raptor-port/src/ui/html.ts` (`verTagHTML`) and `raptor-port/src/ui/scheduler.css` (`.verchip.orig`) |
| D110 | 25 Sep 26 | **"A but is there a nicer design"** — choosing among the ORIG mock-up's three options (`raptor-port/docs/mock/orig-tag.html`: A a ticked outline, B a ticked green, C the word "Published"), after asking *"wouldn't people confuse the AL green with this ORIG green?"* and the agent, having pictured B beside a real AL3 day, changing its recommendation from B to A | **THE ORIG TAG TAKES OPTION A'S DIRECTION — NO COLOUR, A TICK — AND GETS A NICER DESIGN WITHIN IT.** Settles D108's form in part: **B is out** (its green reads as AL3's green at a glance, and AL3 marks its changes in that green — his point), **C is out** (the word costs a row on every published day). **The agent's reading, stated to him so he can correct it:** the finished look is still his pick — refined variants of A are drawn on the same page, each beside real AL3 (green) and AL4 (white) days, and none may take an AL colour or look like a button | the mock-up `raptor-port/docs/mock/orig-tag.html`; `OUTSTANDING.md` `[ORIG-TAG-STANDOUT]`; the batch spec item 13 |
| D109 | 25 Sep 26 | **"A move counts as one"** — after asking *"what do u think is more intuitive?"* about a puck moved from MET + NOTAM BRIEF to an empty SODB on a published Monday reading "2 pending" (and Reaper then put into MET + NOTAM leaving it at 2), and being given the agent's recommendation to count a move as one | **A MAN MOVED FROM ONE PLACE TO ANOTHER ON THE SAME DAY IS ONE PENDING CHANGE, NOT TWO.** Moving Warden from MET + NOTAM BRIEF to SODB reads "1 pending"; putting Reaper into the row Warden left then reads "2". Why it matters: the row he left carries no mark (D91), so today one marked puck sat beside "2 pending" — the mismatch he hit in his look at PR #434. **The agent's reading, stated to him so he can correct it:** a man (or a placeholder puck) taken off one place and put on another place of the SAME day pairs into one move, and the pending list's line reads "Warden: MET + NOTAM BRIEF → SODB"; a swap of two men is two moves (2); a man only taken off, or only added, is one; times, areas and remarks still count one per box; a move put back is nothing (AM20); a man moved to ANOTHER day is one on each day (each day is its own amendment). Every count uses the same counting — the day head's "N pending", the Amendments panel, the ⓘ panel, the plan-switch message, "Discard N edits" and the pending list — or two of them disagree. **Unchanged:** what goes out in the amendment, the marks on screen, and the sign-offs (D103 binds them to the content, not to the count). **Refines AM23** ("N pending counts real differences") — the unit a difference is counted in, not what counts as one — and the people lines of the approved pending list (D99, D100) | `OUTSTANDING.md` `[MOVE-COUNTS-ONE]`; the batch spec `raptor-port/docs/superpowers/specs/2026-09-25-amendment-batch.md` item 14; on build: the register AM23, `raptor-port/docs/engine-rules.md` (the pending count), `raptor-port/docs/ui-contracts.md` |
| D108 | 25 Sep 26 | *"I think ORG indicated at the top that is currently grey could stand out more, so that people know that the scheduled is published"* — during his five-minute look at PR #434 | **THE ORIG TAG STANDS OUT, SO A READER SEES AT A GLANCE THAT THE DAY IS PUBLISHED.** Today the Original's tag is grey — quieter than every coloured AL tag and close to the dashed DRAFT — so a published Original can read as not published. **Changes the register's AM22 "grey ORIG"** (15 Sep 26 — `raptor-port/docs/superpowers/specs/plans-selector-followups.md` §3, "ORIG stays grey"), set aside as the older. **The form is NOT settled — the agent's reading, stated to him so he can correct it:** a mock-up of the real app first (the house rule for a visual change), with options drawn on every surface the tag appears — the edit week, the scheduler board, View-only Sched, at desktop and phone; whatever it becomes must not read as an AL colour (AL4 is white) or as a warning (amber, red). **He answered "ok show me" the same night** — the mock-up is next | `OUTSTANDING.md` `[ORIG-TAG-STANDOUT]`; the batch spec `raptor-port/docs/superpowers/specs/2026-09-25-amendment-batch.md` item 13; on build: the register AM22, `raptor-port/docs/ui-contracts.md`, `raptor-port/src/ui/scheduler.css` (the ORIG tag) |
| D107 | 25 Sep 26 | *"when i click on history on edit schedule and i click on the specific change, it jumps me to schedule board. Shouldnt it keep me on edit schedule page and show me the specific area? and not jump to scheduler board."* — during his five-minute look at PR #434 | **A TAP ON A CHANGE IN EDIT HISTORY KEEPS HIM ON THE PAGE HE IS ON.** Opened from Edit Schedule, it takes the view to that change on the week and marks it there; it does NOT open the scheduler board. Opened on the board, today's jump stays (the board's own bubble, pinned). **Reproduced 25 Sep 26, and not new with the re-test:** the list's jump was built for the board (11 Aug 26) and the Edit history button added to Edit Schedule's top bar (23 Aug 26) reused it unchanged — `main` does the same. **The agent's reading, stated to him so he can correct it:** the pending list's tap (D99, D100) is the same gesture, so ONE "take me to this change" serves both and lands on the page you are on, with the same brief mark; on a phone the edit week steps to the day and scrolls to the change; a change the week does not draw says so on screen rather than opening the board. **He answered "ok" to that reading the same night** | `OUTSTANDING.md` `[HIST-JUMP-STAYS]`; the batch spec `raptor-port/docs/superpowers/specs/2026-09-25-amendment-batch.md` item 12; on build: `raptor-port/docs/ui-contracts.md` §History on the board (the jump) |
| D105 | 25 Sep 26 | *"keep the hover / tap bubble? … yes, and that bubble once past a certain size the user can scroll within that bubble to see the list"* — his answer to the list put to him, pasted back with his notes (25 Sep 26) | **THE CHANGE BUBBLE STAYS — HOVER ON A DESKTOP, TAP ON A PHONE — AND A LONG ONE SCROLLS INSIDE ITSELF.** Settles part (4) of `[PENDING-SUMMARY]`: History mode's bubble over a changed part of the day is kept beside the new pending list (D99, D100); past a set height its list scrolls within the bubble instead of growing off the screen | `OUTSTANDING.md` `[PENDING-SUMMARY]`; on build: `raptor-port/src/ui/histbubble.ts`, `raptor-port/docs/ui-contracts.md` |
| D104 | 25 Sep 26 | *"callsigns only once the database arrives? I recommend yes."* — after asking whether tracking by "View as" was worth the effort — his answer to the list put to him, pasted back with his notes (25 Sep 26) | **WHO MADE A CHANGE IS SHOWN BY CALLSIGN ONLY ONCE THE DATABASE BRINGS PERSONAL ACCOUNTS; UNTIL THEN THE SHARED ACCOUNT ("Admin" / "Member").** Settles part (3) of `[PENDING-SUMMARY]`. Not the "View as" person: that is a lens an admin looks through, so it would name the wrong man, and the edit record lasts only while the page is open anyway. **The agent's reading, stated to him:** he took the recommendation as written | `OUTSTANDING.md` `[PENDING-SUMMARY]` and `[DB-STEP]` (the per-person account) |
| D103 | 25 Sep 26 | *"any change on a published day wipes the sign-offs? I recommend yes."* — his own idea of the same day ("why dont we just wipe the sign offs for any changes to the schedule?") — his answer to the list put to him, pasted back with his notes (25 Sep 26) | **ANY CHANGE THAT SHOWS AS PENDING ON A PUBLISHED DAY WIPES THE SIGN-OFFS — ONE RULE: SOMETHING WAITING MEANS SIGN AGAIN.** Replaces D45's SIGNATURE half: a change in who is behind ALL / ALL AVAIL, an edited request's times, or a person's Quals/posting change — today pending but still signed — now clears the four too. **D45's other half stands:** who was behind a placeholder is frozen at publication and a change shows as pending. Putting the change back restores the signatures, as ever (AM11). Settles part (1) of `[PENDING-SUMMARY]` and closes `[AMEND-D45-FILING]` (a leave landing on a published day keeps wiping them). **The agent's reading, stated to him:** he took the recommendation as written; build it by binding the signature to the same comparison the pending count uses | `OUTSTANDING.md` `[PENDING-SUMMARY]`; `OUTSTANDING-ARCHIVE.md` `[AMEND-D45-FILING]`; on build: the register AM13, `raptor-port/docs/engine-rules.md` (what a signature binds to) |
| D102 | 25 Sep 26 | *"A looks nice, just apply A concept to the scheduler board too. and edit schedule"* — choosing between A (one slim "Signed ALn" line under the day head) and B (the ⓘ panel listing every version's signers) — his answer to the list put to him, pasted back with his notes (25 Sep 26) | **THE SLIM "SIGNED ALn" LINE UNDER THE DAY HEAD — ON VIEW-ONLY SCHED, THE SCHEDULER BOARD AND THE EDIT WEEK.** It names the four who signed the published version (roles on a desktop, names only on a phone). Settles the form of D95. **The agent's reading, stated to him so he can correct it:** A only — B (the ⓘ panel's list of every version) is not taken; on the edit surfaces the line names who signed the published version the working copy sits on, beside the sign-off boxes for the next one, and while an older version is being looked at, that version's signers. The Original's signers must start being kept (today they are cleared unkept) | `OUTSTANDING.md` `[AMEND-PHONE-APPROVER]`; the mock-up `raptor-port/docs/mock/amend-answers.html`; on build: `raptor-port/docs/ui-contracts.md`, `raptor-port/src/engine/publish.ts` (keep the Original's signers) |
| D101 | 25 Sep 26 | *"drop the 'Reissue AL1' idea? I recommend yes, drop it."* — his answer to the list put to him, pasted back with his notes (25 Sep 26) | **NO "REISSUE AL1" BUTTON.** After an Unpublish the withdrawn amendment's changes stay on the working copy as pending and "Publish AL1" brings the day back (walked 25 Sep 26 — the finding that prompted the idea did not hold). Closes `[AMEND-REISSUE-DOOR]`. **The agent's reading, stated to him:** he took the recommendation as written | `OUTSTANDING-ARCHIVE.md` `[AMEND-REISSUE-DOOR]`; the mock-up `raptor-port/docs/mock/amend-answers.html` §Question 6 |
| D100 | 25 Sep 26 | *"For the Tap pending window, i can scroll it as well if the list gets huge"* · *"the tap pending window looks good"* · *"and function"* — after the mock-up `raptor-port/docs/mock/pending-list.html` | **THE PENDING-LIST MOCK-UP IS APPROVED, LOOK AND FUNCTION — THE DESIGN OF RECORD FOR D99.** "N pending ▾" opens the short list of what will go out (where, before → after, who and when), and a tap on an item takes the view to it and marks it for a moment. **Added by him:** a long list SCROLLS inside the window rather than growing past the screen. Recorded because "he saw it" and "he approved it" are different facts (as D41, D93) | the mock-up; `OUTSTANDING.md` `[PENDING-SUMMARY]`; on build: `raptor-port/docs/ui-contracts.md` |
| D99 | 25 Sep 26 | *"So can u work on having a clickable pending button to show what is currently pending? so that the scheduler dont need to search everywhere. And if they click on that area, it brings the view to that pending area"* — after being told the difference between pending (what will go out) and Edit History (every edit) | **"N PENDING" BECOMES A BUTTON THAT LISTS WHAT IS WAITING TO GO OUT ON THAT DAY, AND TAPPING AN ITEM TAKES THE VIEW TO IT.** The list is the NET difference from the published version — a change made and put back is not on it (AM20, D98) — so the scheduler never has to hunt for what changed. Settles part (2) of `[PENDING-SUMMARY]`. **The agent's reading, stated to him so he can correct it:** each item says what changed and where, and — from his idea of the same day — who changed it and when, where the app knows (see the item's limits); parts (1) wipe-on-any-change, (3) callsign and (4) the bubble are still his to answer; a mock-up comes first | `OUTSTANDING.md` `[PENDING-SUMMARY]`; on build: `raptor-port/docs/ui-contracts.md` |
| D98 | 25 Sep 26 | *"if the change results in going back to the same as the published schedule like for e.g AL1, u delete a puck and then put it back, it shouldnt show as pending, before no change happened, and it shouldnt be counted as an amendment. Anyway is that what u are asking?"* — about question 7 (`[AMEND-LOAD-FILING]`) | **A DAY THAT IS BACK TO WHAT WAS PUBLISHED SHOWS NOTHING PENDING AND IS NO AMENDMENT — AND "LOAD ONTO WORKING COPY" MUST GET IT THERE.** Re-confirms the register's AM20 (16 Aug 26: a mark means "differs from what was issued", not "was touched") and applies it to the load: loading a version also puts back a request the scheduler had taken off, so the day reads exactly as that version, nothing pending. **The agent's reading, stated to him so he can correct it:** his principle answers question 7 "yes"; build it in the load alone, from the version's own filing record (the item's caution — a general reconcile turned a deliberate removal into a fresh input on a plan switch) | `OUTSTANDING.md` `[AMEND-LOAD-FILING]` (decided; build with the D92 batch); the register AM20; on build: `raptor-port/docs/engine-rules.md` (the load) |
| D97 | 25 Sep 26 | **"5 ok"** — to *"two wordings: 'Not yet signed' while any of the four is missing, and 'Not yet published' once all four are signed?"* (`[AMEND-NYS-WORDING]`) | **THE WORKING-COPY MARKER HAS TWO STATES.** On a published day with changes waiting: **"Not yet signed"** while any of the four sign-offs is missing or no longer valid; **"Not yet published"** once all four are valid and the change has not gone out. Today it reads "Not yet signed" in both (it never looks at the sign-offs — found 25 Sep 26), which contradicts four green sign-offs. Changes the register's AM24 wording (16 Sep 26), not its rule: never on the issued face | `OUTSTANDING.md` `[AMEND-NYS-WORDING]` (decided; build with the D92 batch); on build: the register AM24, `raptor-port/docs/ui-contracts.md` |
| D96 | 25 Sep 26 | **"4 refuse"** — to `[AMEND-TEMPLATE-PUBLISHED]`: should applying a day template to a published day be (a) refused, (b) kept to what matches the issued day, or (c) left as it is? | **A DAY TEMPLATE CANNOT BE APPLIED TO A PUBLISHED DAY.** Today it rebuilds the day, reads as every row removed and re-added, and quietly drops the members' accepted requests; refusing it removes all of that. **The agent's reading, stated to him so he can correct it:** refused with its reason on screen at every door that applies a template (the house rule for a refusal); a day not yet published takes a template exactly as today. Sets aside the agent's recommendation (b) — his option (a) is the one the agent offered for templates on a published day being rare | `OUTSTANDING.md` `[AMEND-TEMPLATE-PUBLISHED]` (decided; to build with the D92 batch); on build: `raptor-port/docs/ui-contracts.md` and the register (AM20, AM23) |
| D95 | 25 Sep 26 | *"Actually view only schedule should also see who signed off each publish/amendment. Can u generate a nice mockup of how it can look on a view only schedule and doesnt take much space"* — answering `[AMEND-PHONE-APPROVER]` (should a phone show who approved each amendment?) | **VIEW-ONLY SCHED SHOWS WHO SIGNED OFF EACH PUBLISHED VERSION — THE ORIGINAL AND EVERY AMENDMENT — COMPACTLY.** Wider than the question put to him and than the agent's recommendation (the approver alone, in the ⓘ day panel): everyone who reads View-only Sched, members included, sees it, at every width. **The agent's reading, stated to him so he can correct it:** all four sign-offs of each version (CUR CK, SKED CK, PLANNED BY, APPROVED BY) — "who signed off"; the form waits for the mock-up he asked for, which must not take much space | `OUTSTANDING.md` `[AMEND-PHONE-APPROVER]` (answered; the mock-up next); on build: `raptor-port/docs/ui-contracts.md` |
| D94 | 25 Sep 26 | **"1. yes"** — to *"make the board draw the dashed and dotted rings, like the edit week?"* | **THE BOARD DRAWS ALL THREE WARNING RINGS AS THE EDIT WEEK DOES** — the dashed ring for a sanctioned late show and the dotted ring on the day that causes tomorrow's crew-rest breach, where today it draws a sanctioned breach solid and the cause day not at all. Settles `[BOARD-RING-STROKES]`; built with the D92 tag-only marks, which gives the rings the puck's edge to themselves | `OUTSTANDING.md` `[BOARD-RING-STROKES]`; on build: `raptor-port/docs/ui-contracts.md` §Three crew-rest rings |
| D93 | 24 Sep 26 | *"the fix looks good"* — after the redrawn mock-up (version 4 of `raptor-port/docs/mock/amend-seat-marks.html`: a changed puck gets a tag, never a ring) | **THE MOCK-UP IS APPROVED AS THE DESIGN OF RECORD FOR `[AMEND-MARK-RING-CLASH]`, D92's open half with it:** a change still waiting to go out is a hollow ALn tag and no dotted line round the puck, as a published one is its solid tag and no ring — a puck's edge carries only its warnings; times, areas and remarks keep their marks; View-only Sched included. Recorded because "he saw it" and "he approved it" are different facts (as D41). **The agent's reading, stated to him:** it answers the page's first question only — whether the board should draw the dashed and dotted rings (`[BOARD-RING-STROKES]`) is still put to him | the mock-up; `OUTSTANDING.md` `[AMEND-MARK-RING-CLASH]`; `raptor-port/docs/file-map.md` (the mock's row); on build: `raptor-port/docs/ui-contracts.md` §Amendment marks on screen |
| D92 | 24 Sep 26 | *"this doesnt make sense to me, like why dont u just a do solid AL3 chip and not box up the whole puck? people might think that behind the AL3 green ring there is a hidden warning that is not shown"* — looking at a published change on the busy-day mock-up (Rune: a green AL3 ring round the puck and the AL3 tag) | **A PUBLISHED CHANGE ON A PUCK IS MARKED BY ITS SOLID ALn TAG ONLY — NO RING ROUND THE PUCK.** His reason: a ring in the amendment's colour sits where the warning rings sit, so a reader can take it for a warning, or suspect it hides one. **Checked the same hour in the running app — he is right in fact, not only in look:** Tally, published at AL3 with an advisory, had AL3's green ring as her puck's edge and no amber ring (the edge's colour, measured); by the style rules the published ring out-ranks the thin amber, grey and thin red rings, while the thick red ring and the dashed ring win over it and the dotted ring sits outside it. **The agent's recommendation, put to him with pictures and not yet his word:** the same for a change still waiting to go out — a hollow ALn tag and no dotted line round the puck — so a puck's edge carries only its warnings and the tag alone says it changed (solid = out, hollow = waiting). **Changes the August look** (`ui-contracts.md` §Amendment marks on screen: pucks "get outlines and an ALn tag"; a waiting change "DOTTED in the upcoming AL's colour") for PUCKS only — the time, area and remark cells keep their marks — and supersedes the fix first drawn for `[AMEND-MARK-RING-CLASH]` (design B: the ring kept only where a warning ring competes). Reaches every surface that draws a changed puck, View-only Sched included | the mock-up `raptor-port/docs/mock/amend-seat-marks.html`; `OUTSTANDING.md` `[AMEND-MARK-RING-CLASH]` (now builds this); on build: `raptor-port/docs/ui-contracts.md` §Amendment marks on screen |
| D91 | 24 Sep 26 | *"Dont do the man taken off seat"* — answering the first pick on the seat-marks mock-up (`raptor-port/docs/mock/amend-seat-marks.html`): where a man is taken off a duty desk, a Common Programme row or a sim seat on a published day, show his name faded and crossed out, or an empty marked box (Option B) | **DON'T BUILD IT. TAKING A MAN OFF A SEAT LEAVES NO MARK ON THE ROW** — neither the crossed-out name nor the empty box. The change is still an amendment like any other: the day's pending count, the Amendments panel, History and the AL's comparison all carry it; only the row shows nothing — the seat simply reads empty on the edit week, the board and the issued schedule. **Closes `[AMEND-EMPTY-SEAT-MARK]`** (the re-test's walk finding W1-2) as ruled: a later walker who finds an emptied seat with no mark has found this ruling, not a defect — do not re-file it. **The agent's reading, stated to him so he can correct it:** it covers the whole item, so an emptied COCKPIT seat also stays as it is (the week shows the "ALn" badge once issued; nothing while pending, nothing on the board). **What it does NOT settle:** the same mock-up's second half, the warning ring hidden under a pending mark (`[AMEND-MARK-RING-CLASH]`) — he asked to see examples of that fix first | `raptor-port/docs/ui-contracts.md` §Amendment marks on screen; the behaviour register `raptor-port/docs/superpowers/specs/2026-09-24-amendment-behaviour-register.md` AM19; the evidence sheet `raptor-port/docs/handpass/2026-09-24-amendment.md` W1-2; `OUTSTANDING-ARCHIVE.md` `[AMEND-EMPTY-SEAT-MARK]` |
| D164 | 24 Sep 26 | *"some of the flagging on the puck is glowing, some are not can u not make it glow. like the 2nd puck picture"* — with two pictures: a red-flagged puck that glows (Ranger) and one that does not (Saber) | **A FLAGGED PUCK DOES NOT GLOW.** The glow belongs to the "View as" highlight: the person being viewed as wears a purple fill and ring, and when that puck also carries a red flag, two rules add a red glow (`.puck.me.boxred`, `.puck.me.boxdash`); Saber was not the View-as person. **The agent's reading, stated to him so he can correct it:** remove that red glow so a flagged View-as puck shows the plain red ring every other flagged puck has; the purple "this is you" fill and ring stay | `OUTSTANDING.md` `[PUCK-FLAG-GLOW]`; on build: `raptor-port/src/ui/scheduler.css` (the two rules) |
| D161 | 24 Sep 26 | **"10 next time we revisit this again"** — asked whether he still wants the Admin "Display" area of section fold defaults (his 26 Aug 26 ask) | **NOT NOW — REVISIT IT LATER.** `[ADMIN-DISPLAY]` stays open and unbuilt; put it to him again when Admin or the section folds are next touched, not before | `OUTSTANDING.md` `[ADMIN-DISPLAY]`; this row |
| D149 | 24 Sep 26 | *"actually i want members to automatically update their qualifications, so member can only edit their own row and SXO and scheduler also does no harm. so they can edit anything in their own row."* | **ON THE QUALS PAGE A MEMBER EDITS HIS OWN ROW ONLY — AND EVERYTHING IN IT.** Every column of his own row — callsign, CAT, SXO, SCHEDULER, SANS and every qualification — so members keep their own qualifications up to date themselves; never another person's row. **Narrows** the 5 Aug 26 rule, which let a member edit any row with editing on. **Sets aside the agent's recommendation** that SCHEDULER and SXO be admin-only: his reason, they do no harm on one's own row (a member still cannot sign — only an admin records signatures, on Edit Schedule). **To build:** the own-row gate at the page AND the write path, like the 5 Aug authority sweep, with a test per column; permissions, so FULL tier | `raptor-port/docs/engine-rules.md` (§Auth / roles, the Quals row); `OUTSTANDING.md` `[QUALS-MEMBER-SCOPE]` |
| D77 | 23 Sep 26 | *"yes leave the phone drag corner"* — asked whether the window, which on a phone MOVES but cannot be RESIZED (a browser's own resize grip needs a mouse; D38 asked for "resizable"), should get a touch drag-corner before it goes live, or be left | **LEAVE IT: NO PHONE RESIZE CORNER.** On a phone the window keeps the approved full-width bottom panel (62% tall) and can be moved; it cannot be resized. Desktop resizing is unchanged. D38's "resizable" is met on desktop and knowingly NOT on a phone. **Do not build a touch resize corner, and do not put the question to him again,** unless he raises it himself. **Numbered D77, not D74,** because `claude/docs-guard` already holds D70–D76 on its own branch and `main` holds D69–D73 — skipping past both keeps the later merges from clashing again | `raptor-port/docs/ui-contracts.md` §[ALL-AVAIL-WINDOW] (the sizing paragraph); `OUTSTANDING-ARCHIVE.md` [ALL-AVAIL-WINDOW] status |
| D66 | 23 Sep 26 | **"close it"** — asked whether the window should close when he goes to a different page (Leave War, Tracker, Inputs), with the recommendation "close it when you leave the schedule" | **THE WINDOW CLOSES WHEN HE CHANGES PAGE.** It is a schedule tool; floating it over the Leave War grid or the Tracker is clutter. **The outside-click rule still does NOT apply** — a click on the schedule behind it must never close it, because editing behind it is the whole point (D38). **The agent's own calls, made the same hour and stated to him so he can correct them:** it also closes on a switch between Edit Schedule and View-only Sched (the two show different versions of the day, so a window opened on one would list the wrong crowd on the other — D44), on a WEEK change (the event it was opened from is no longer on screen; every other per-week transient already resets there), and on logout (the next person must not inherit it — every other session transient resets there). Those three are consistency with the app's existing rules, not new product direction | `raptor-port/docs/ui-contracts.md` §[ALL-AVAIL-WINDOW] (the dismissal table, now corrected); `raptor-port/src/state/view.ts` (the resets) |
| D65 | 23 Sep 26 | *"For oil earn only Id say no. What about like normal all avail puck when not in oil earn mode? I would like a tap on a man to highlight him everywhere as well."* — asked whether tapping a man in the window should also highlight him across the schedule behind it | **IT DEPENDS ON THE MODE.** **With OIL Earn OFF:** a tap on a man highlights him everywhere on the schedule — the ordinary puck selection — AND the window still shows his reason. That is how a puck tap behaves everywhere else in the app, and it is what makes the list useful for scheduling: tap a man, see every row he is already on. **With OIL Earn ON:** a tap only switches him on or off earning, and selects nothing — matching how every other tap behaves inside the mode, where the board is a "who earns" editor. **The agent's reading of one case he did not name, stated to him:** with the mode ON but the window on its "who's available" tab, the tap does NOT highlight either — his words key it to the mode ("when not in oil earn mode"), and inside the mode no tap selects | `raptor-port/src/ui/AvailWindow.tsx`; `raptor-port/docs/ui-contracts.md` §[ALL-AVAIL-WINDOW] |
| D51 | 22 Sep 26 | *“Yes 2 pucks a row, pilot left wso right”* — shown a comp of the real app at phone width with a crowd of 27 opened behind one placeholder, one man per row, running about 600 pixels (three quarters of the screen) for a single row | **INSIDE THE EARN MODE, A CROWD OPENS TWO ACROSS ON A PHONE, AND THE TWO COLUMNS CARRY SEAT IDENTITY — PILOTS LEFT, WSOs RIGHT.** Not a wrapped list that fills left-to-right: the LEFT column is the pilots and the RIGHT column is the WSOs, so the split says something rather than just saving space. That is the same pairing the sim seat grid already uses (front-seat left / rear-seat right, owner 13 Aug 26) and the same seat colours the app already paints (pilot olive, WSO green). **Consequence he accepted by choosing it:** the two columns are independent lists, so they are usually UNEQUAL in length and the row is as tall as the longer one — taller than a naive wrap would be, and worth it because a glance now shows how many of each. Desktop is unchanged (there is room there). **Open consequence, tied to his ground-crew answer:** a man who is neither pilot nor WSO has no column yet; if ground crew are ever swept into a crowd, where they sit has to be decided. | **CORRECTED THE SAME HOUR — this is not new, it is D38–D41.** The agent put the two-column question to him as an open choice without having read `DECISIONS.md` or `OUTSTANDING.md`, where the answer was already settled and APPROVED: D38 replaces the name bubble with a movable, resizable, non-blocking window of real pucks, pilots left and WSOs right, and **the SAME window serves both counters — who is available, and who is credited OIL, where he switches individual pucks off.** D39/D40 set one puck per row and a skinny default; D41 approved the mock-up as the design of record. So this ruling ADDS NOTHING NEW and is **not built here**: the surface it describes is `[ALL-AVAIL-WINDOW]`, already filed as job 2 in `OUTSTANDING.md`, straight after this one. Patching two-a-row into the current in-row crowd would be thrown away by that job. | `OUTSTANDING-ARCHIVE.md` `[ALL-AVAIL-WINDOW]` (job 2, where it is actually built); the approved mock-up `raptor-port/docs/mock/allavail-window.html` (D41); `raptor-port/docs/handpass/2026-09-22-oil-seats.md` §7 |
| D50 | 22 Sep 26 | *“Keep one spare seat showing”* — asked, during the walk, what to do about a sim row that is already full: it has nowhere to drop an extra person, so the drag a scheduler would naturally make lands on a seated man and replaces him with no message. Every other kind of row carries a thin drop strip under its people; the sim seat grid deliberately does not, to save a row of screen height | **A SIM ROW ALWAYS SHOWS ONE SPARE SEAT, EVEN WHEN IT IS FULL.** That empty seat IS the door for an instructor, an observer or a placeholder — exactly as a sim row with an odd number of people already shows one today. It costs one row of height on sim rows that are full, and he accepted that cost knowingly against the alternative of filing it. **The gap is PRE-EXISTING** (identical on `main`); what changed is that [OIL-SEATS-CAN-EARN] step 5 made that seat pay real people, so the promise that a placeholder on a sim row counts the men it stands for could not be exercised at all through the screen. He was offered “leave it and file it” and “only show the strip while dragging” and chose neither — the second because the tap-a-seat-then-pick-a-name order would still have had no door. **This NARROWS the 26 Aug 26 reasoning behind `.schedboard .ppl.fcprcp .addz{display:none}`** (“the seat grid auto-adds rows as pucks land, so it never packs edge-to-edge and swaps”), which is true for odd counts and false for even ones — fix the stale comment in the same change. | `raptor-port/src/ui/board-html.ts` (the sim seat cell); `raptor-port/src/ui/scheduler.css` (the `.fcprcp .addz` rule and its comment); `raptor-port/docs/ui-contracts.md`; `raptor-port/docs/handpass/2026-09-22-oil-seats.md` §6 finding 3 |
| D47 | 22 Sep 26 | *“Keep the refusal”* — asked whether a placeholder puck on a JET seat should be refused outright or merely warned about, after being shown that the two options differ in where the bugs land | **D33 STANDS WHOLE, AND IT BEATS THE 13 AUG 26 “PLACEHOLDER PUCK IS A SHORTCUT” RULE ON COCKPIT SEATS ONLY.** ALL and ALL AVAIL are refused on flying-line cockpit seats, with the reason on screen at every door. **The 13 Aug shortcut is NOT retired** — a placeholder puck still arms its seat everywhere a placeholder is legal (duty desks, sim seats and passengers, ground rows, the Common Programme, an accepted request row), and tapping an EMPTY cockpit seat still narrows the crew list as it always did. Only the cockpit instance of the shortcut is gone, because the puck can no longer get there. **What settled it:** the two lists are not the same question. Tapping an empty seat asks “who can fly THIS seat” — seat, qualification, SC currency, crew rest, busy, absences. The puck asks only “who has nothing else on at that time”, so on a front seat it would include WSOs, men not SC current and men who would break crew rest — and would then pay every one of them for a sortie they could not have flown. The two also use different windows (availability step→dekit; the flying rule report→debrief, D36). **The risk argument he asked for:** a refusal removes the state, so roughly ten downstream readers never have to handle it, and its failure mode is LOUD (a placeholder blocked where it should be allowed). A warning keeps the state, needs a cockpit branch in every one of those readers, and its failure mode is SILENT and about money. **Not covered, and belted separately:** a placeholder arriving from a copied day template, a parked plan or older saved data bypasses both doors — a jet seat credits nobody whatever sits in it. | `raptor-port/docs/ui-contracts.md` §Arm-and-plant; `raptor-port/docs/superpowers/specs/2026-09-22-oil-seats-can-earn-plan.md` §2 (clash list); pinned by `raptor-port/src/ui/interact.test.tsx` and `src/ui/oilseat-refusal.test.tsx` |
| D45 | 22 Sep 26 | *"It should be like any other weekday, it shouldn’t. I sign my published schedule and just show pending"* — asked whether an availability change should invalidate a signature · and, stated as the principle underneath it: *"Things shouldn’t change silently without the scheduler acknowledging it."* | **SIGNATURE HALF REPLACED 25 Sep 26 BY D103 (any pending change wipes the sign-offs); the freeze-and-pending half below stands.** **A CHANGE IN WHO WAS AVAILABLE NEVER INVALIDATES A SIGNATURE. THE PENDING MARK IS THE WHOLE MECHANISM, AND EVERY DAY BEHAVES THE SAME.** He signs the published schedule; a later change in availability shows as pending and nothing more; he clears it by amending or by issuing the EOD version. **Rejects the signature-binding half of Codex OSE-05** while keeping the freeze whole (D44) — the freeze is about the document not changing under the reader, the signature is about authorising the schedule, and he has separated them. **It also rejects the agent’s own narrower proposal** (bind the signature only on a day that earns): he wants ONE behaviour for every day, not a money/no-money split, and the simpler rule is his to choose. **THE PRINCIPLE IS THE BIGGER HALF AND IT REACHES BEYOND THIS FEATURE:** nothing on a published schedule may change without the scheduler acknowledging it. That is WHY the freeze exists and WHY the pending mark exists — together they are the acknowledgement mechanism, and neither works alone. **Apply it as a test to any future change that touches an issued day:** if a reader could see a different answer tomorrow with nobody having acknowledged it, the design is wrong | `raptor-port/docs/superpowers/specs/2026-09-22-oil-seats-can-earn-plan.md` §4; `OUTSTANDING.md` `[EOD]` and `[OIL-SEATS-CAN-EARN]`; on build: `raptor-port/docs/ui-contracts.md` §Amendment marks, and this file (its settled decisions, since 24 Sep 26 the scheduler's part of the old `raptor-port/CLAUDE.md` §Stable decisions) as a standing test |
| D44 | 22 Sep 26 | *"I think what we discussed just now is better to put it frozen. Because the scheduler will then see the change if someone takes a leave and they will then publish a latest amendment of the schedule or publish an EOD schedule version to show all that’s happened on the day itself as the truth. Which is in our process. Someone will review all the changes end of day and publishes a EOD version of the schedule"* | **WHO WAS BEHIND A PLACEHOLDER PUCK IS FROZEN AT PUBLICATION ON EVERY DAY, EARNING OR NOT.** The issued version keeps the people it was issued with; the working copy shows the live answer; the difference raises the ordinary pending mark, and the scheduler amends or publishes the EOD version. **OVERTURNS THE AGENT’S ARBITRATION** — it had chosen live-on-a-non-earning-day and argued that a pending mark for a number owing nobody anything would devalue a mark that does carry money. **His reasoning is better and the agent could not have reached it from the code:** that pending mark is not noise, it is the SIGNAL their end-of-day process runs on. Someone reviews every change at the close of the day and issues an EOD version as the record of what actually happened — and a change in who was available IS something that happened. **This makes Codex right and the agent wrong** (Codex OSE-05 wanted membership frozen on every publication and bound into the signature and the amendment comparison; Fable M6 wanted the split the agent took). **It also supersedes Fable’s R2-3 fix 1**, which would have recorded the membership only where it is money-bearing. **SECOND TIME his knowledge of the PROCESS corrected the agent’s reasoning about the code** — D36 was the first (the debrief window). The pattern is now named in the plan §5a: a disagreement between two bodies of code is a QUESTION, not a verdict, and only someone who runs the squadron can settle it | `raptor-port/docs/superpowers/specs/2026-09-22-oil-seats-can-earn-plan.md` §4 (the arbitration, now reversed) and §5 step 9; the review log; `OUTSTANDING.md` `[EOD]` and `OUTSTANDING-ARCHIVE.md` `[OIL-SEATS-CAN-EARN]`, which this leans on; on build: `raptor-port/docs/ui-contracts.md` |
| D41 | 22 Sep 26 | *"that mock up looks good"* — after three rounds of corrections | **THE MOCK-UP IS APPROVED AND IS NOW THE DESIGN OF RECORD FOR `[ALL-AVAIL-WINDOW]`.** `raptor-port/docs/mock/allavail-window.html` (also an Artifact) is no longer a sketch to argue with — it is what gets built, and a change to it now needs his word the same as any other settled design. **Everything it demonstrates is settled:** movable by the six-dot grip, resizable, does not block the schedule behind it; one puck per row, pilots left and WSOs right; 212px default and a 186px floor; flag reasons under their pucks, moving beside them when widened; the counter with no "free"; and the two-tab shape where the OIL half exists only while OIL Earn is on. **Recorded because “he saw it” and “he approved it” are different facts** and nothing else in the repo would tell them apart later. **What is NOT settled by this** — the mock is a comp, not the build: it uses invented people and one invented event, it does not read the real availability rule, and it has not been through a walk. The build still owes its own FULL-tier checks | `OUTSTANDING-ARCHIVE.md` `[ALL-AVAIL-WINDOW]` (now marked design-approved); the mock itself; on build: `raptor-port/docs/ui-contracts.md` |
| D40 | 22 Sep 26 | *"Why it still shows the window so wide by default. I should be skinny like my previous screen shot. And I prefer the 2nd picture 6 nicely placed neat dots to indicate that this window is movable. Not the dashed on the first picture top left"* | **THE WINDOW OPENS SKINNY, AND ITS DRAG HANDLE IS THE APP’S OWN SIX-DOT GRIP.** (1) **SKINNY BY DEFAULT** — tall and narrow, just wide enough for the two puck columns, not a wide panel with dead space beside each list. He can drag it wider; the DEFAULT is the narrow one. **Follows from D39**: once each man has his own row, the window only ever needs two puck-widths, and a puck is a fixed 74px. **He asked for skinnier twice** — 332px was still too wide — so the settled default is **212px**, which is the two 74px pucks plus their gap, the body padding and about 16px of slack per column. That is close to the floor: below ~186px a puck starts to clip, so 186 is the minimum the user can drag it to. **Build it to those numbers rather than re-deriving them.** **What this costs and how it is handled:** a flagged man’s reason will not fit beside his puck at that width, so it WRAPS onto its own line under him, and sits beside him only when the user drags the window wider. The reason never disappears. (2) **THE GRIP IS `⠿`** — the six-dot braille grip the app already uses on every draggable row and section header, not a dashed or hamburger glyph. **This is not only taste: it is the app’s existing vocabulary for “drag me”**, so the window is recognisable as movable without being explained. The mock had drifted off it while the file was being made encoding-proof | `OUTSTANDING-ARCHIVE.md` `[ALL-AVAIL-WINDOW]`; the mock-up (Artifact, rebuilt to this); on build: `raptor-port/docs/ui-contracts.md` |
| D39 | 22 Sep 26 | On the mock-up: *"it should show like this by default. Dont need to put the word free there."* — with a picture of the window drawn NARROW, each puck on its own row | **TWO FIXES TO THE WINDOW’S DEFAULT, BOTH FROM HIS OWN PICTURE.** (1) **ONE PUCK PER ROW.** Each column is a LIST running down, not a wrapping grid — pilots down the left, WSOs down the right. The mock-up let them wrap two and three to a row on a wide window, which is what the crew palette does; he saw the narrow version, where they stack, and chose that. So the stacking is the DEFAULT at every width, not a phone fallback. **Why it matters beyond taste:** a puck per row gives each man a full row for his flag and his figure, which is what makes them scannable — the whole point of replacing the name bubble. (2) **THE COUNTER CHIP DROPS THE WORD "free".** It reads `ALL AVAIL · 27`, not `ALL AVAIL · 27 free`. **Consistent with D37**, which said the count must read as what it is rather than as a promise — "free" is exactly the kind of claim D37 warns about, since the window’s own job is to show the men who are flagged rather than assert they are clear | `OUTSTANDING-ARCHIVE.md` `[ALL-AVAIL-WINDOW]`; the mock-up (Artifact, rebuilt to this); on build: `raptor-port/docs/ui-contracts.md` |
| D38 | 22 Sep 26 | *"the current interface to show just names on a bubble … is not intuitive. I think we should have a window opened that is movable and … resizable and a user can still click and edit/scroll the schedule behind while that window is still opened … show the pucks just like how the placeholder shows the personnel and I can click on the flagging as well … intuitive for the user to click on his puck and know that the ops brief he’s scheduled for as all avail is inbetween the standard debrief time … similar to the oil earned when I click on the counter … I can individually select the pucks on who doesn’t earn oil in that interface too … pilot then wso, left right column … Perhaps make a mock up before we execute this."* | **THE NAME BUBBLE IS REPLACED BY A MOVABLE, RESIZABLE, NON-BLOCKING WINDOW OF REAL PUCKS — ONE WINDOW, TWO JOBS.** Not a popup and not a sheet: it stays open while the scheduler scrolls and EDITS the schedule behind it, he can move it and resize it, and it draws the people as PUCKS in the placeholder panel’s own layout — pilots in the left column, WSOs in the right — carrying the same warning flags the rest of the app draws, clickable. **The SAME window serves both counters:** who is available behind an ALL AVAIL / ALL puck, and who is credited OIL — and in the OIL one he switches individual pucks off. **The reason it must SHOW rather than decide, in his own example:** a man whose ops brief sits inside his standard debrief should appear, flagged, so the scheduler sees the overlap and judges it. **This is the other half of D36 and explains it** — the availability window stays narrow (the man IS available) precisely because the app’s job is to surface the clash, not to remove him from the list. **MOCK-UP BEFORE BUILD, his ask** — and the house rule already says a visual direction gets a picture in the app’s own stylesheet at phone and desktop before any product code | `OUTSTANDING-ARCHIVE.md` `[ALL-AVAIL-WINDOW]` (new item, carries the design and the mock-up); on build: `raptor-port/docs/ui-contracts.md`. Sits AFTER `[OIL-SEATS-CAN-EARN]`, which builds the counters this window opens from |
| D37 | 22 Sep 26 | Having asked *"should I not have this all avail counter? It just makes things more in accurate and confusing? … maybe we don't need this function?"*, and been given the case both ways, he chose **"Keep it, worded honestly"** | **THE COUNT STAYS, ON EVERY SEAT THE PUCK CAN LAND ON — BUT IT MUST READ AS WHAT IT IS, NOT AS A PROMISE.** It counts who has nothing else on the programme at that time. It is a starting point a scheduler can correct, and the wording on screen has to say so; a bare number reads as a fact and this one is not. It taps through to the names, as it does today. **CONFIRMS D27's display half rather than withdrawing it**, and adds the wording requirement on top. **The argument that decided it, and the one to re-use if this is ever reopened:** because D28/D32 let the puck EARN, the app must resolve it to named men whether or not a number is shown — earned leave lands on people, not on a placeholder. So removing the count would not remove the guess, only hide it, and an invisible guess that banks leave is worse than a visible one. The missing count is also how the silent-credit defect was found at all: his Sunday desk showed no number, and underneath it the puck was crediting nobody. **He was right about the risk** — the fix is the wording, not the removal | `OUTSTANDING-ARCHIVE.md` `[OIL-SEATS-CAN-EARN]` half three; `raptor-port/docs/superpowers/specs/2026-09-22-oil-seats-can-earn-plan.md` §4–§5; on build: `raptor-port/docs/ui-contracts.md` (the exact wording) |
| D36 | 22 Sep 26 | *"sometimes we will schedule a brief much earlier to cater for a brief for all available to join after the brief. So like 1400 is the brief, a brief ends around 1520 and then ops brief commences at 1520 till step time of 1620. So technically the person is available to join the ops brief. There’s also scenarios in which when someone lands, instead of debriefing immediately within the 2 hours the management wants them to attend a ops brief then debrief after."* | **THE AVAILABILITY WINDOW STAYS NARROW — STEP BEFORE TAKE-OFF TO DEKIT AFTER LANDING. DO NOT WIDEN IT TO THE REPORT OR THE DEBRIEF.** A flying man counts as free up to his step and again from dekit; the 3-hour nominal report and the 2-hour flight debrief do NOT block him from an event. **His two scenarios are the proof, and they are why this is recorded rather than left to be re-derived:** an ops brief deliberately slotted between the flight brief and step (1520–1620 against a 1620 step) is a man who IS available, and a debrief that management moves to AFTER an ops brief is a man who is available straight after dekit. Widening the window breaks both — it would mark men unavailable for the very events the squadron schedules around them. **This settles a real disagreement rather than hiding it:** the warning list DOES treat land+2h as an obligation (it raises "Not enough time to attend the debrief"), and the OIL earning rules DO use report→debrief as the working day. Those two are correct for what they each decide; availability is a different question and keeps its own, narrower answer. **The agent recommended widening it and was wrong** — it read the disagreement as a defect without knowing how the squadron actually schedules. **Nothing to build; this exists so a later session does not "find" the gap and close it.** Money is unaffected either way: a man in debrief earns from his own sortie regardless, and OIL is measured across the whole day, not per event | `raptor-port/docs/engine-rules.md` (the availability window, on build); `raptor-port/docs/superpowers/specs/2026-09-22-oil-seats-can-earn-plan.md` §6; `OUTSTANDING-ARCHIVE.md` `[ALL-AVAIL-REDEF]`, which settled WHO counts but never how wide a flying man's tasking is |
| D33 | 22 Sep 26 | Given three placement rules for the ALL / ALL AVAIL pucks, he chose **"Only flying lines refuse it"** — over the agent's recommendation that every ONE-PERSON seat refuse it | **THE TWO PLACEHOLDER PUCKS ARE REFUSED ON FLYING LINES ONLY — EVERYWHERE ELSE THEY LAND.** Refused: both cockpit seats on every flying line, whatever the wave (Standard, SC MAIN, SC SPARE, AVALON, BB), with the reason on screen. Allowed: the sim's own two seats, a duty desk's own position, sim passenger slots, the extra-people line beside any row, ground rows and common programme rows. **With D32 this is also the EARNING list** — those same places offer the OIL switch, off by default. **What the agent recommended and he set aside:** that every seat holding ONE NAMED BODY refuse it (cockpit seats, sim seats AND a duty desk's own position), because qualifications, crew pairing and the manning count all read such a seat. **The consequence he accepted, flagged to him before building:** a duty desk can now answer "who holds this desk?" with a crowd rather than a name, so its manning count reads a placeholder. That is defensible for a weekend duty anyone available covers, which is why it is his call. **Corrects his own premise:** he said "obviously flying lines can't have all avail" — checked, and the app does NOT stop it today; the puck drops into a cockpit seat with no refusal and no warning, the aircraft looks crewed and nobody is on it **— AMENDED 22 Sep 26 BY D43:** the placeholder pucks are ON by default wherever they can land, like named people; only the four exempt KINDS (D24/D35) default off. Read this row with that correction. | `OUTSTANDING-ARCHIVE.md` `[OIL-SEATS-CAN-EARN]`; on build: `raptor-port/docs/ui-contracts.md` (the refusal) and the OIL behaviour register |
| D27 | 22 Sep 26 | *"my intent for all avail should also display who is avail even when the oil earn is not selected. So by default when that puck lands on an area in the schedule. It will auto calculate who would be available to attend. And the counter would show."* · and, as his current LEAN not yet a ruling: *"it doesn't make sense for all avail and all to be on duty… but I want the flexibility to do so in the event that happens"* | **ALL AVAIL / ALL ARE A SCHEDULING FEATURE, NOT AN OIL FEATURE.** Dropping one on any area of the schedule works out who would be available to attend and SHOWS THE COUNT — by default, with OIL Earn switched OFF. Today the count exists only inside the OIL mode's decoration, so it appears only where OIL is being decided; this makes it an ordinary property of the puck. **It EXTENDS `[ALL-AVAIL-REDEF]` (21 Sep) rather than reversing it:** that ruling settled WHO counts as available (no ground crew, a SANS man only when planned with us, ATT B in, a time clash out) and this one settles WHERE the answer is shown. Same membership rule, one body. **SEPARATES DISPLAY FROM EARNING**, which had been conflated: showing who is available is a read and owes nobody anything; earning is money. His lean is that neither puck should EARN on a duty desk, and he asked whether allowing them everywhere would cost bugs — the agent's answer and recommendation are in `[OIL-SEATS-CAN-EARN]`; the earning list is NOT settled until he says so **— AMENDED 22 Sep 26 BY D43:** the placeholder pucks are ON by default wherever they can land, like named people; only the four exempt KINDS (D24/D35) default off. Read this row with that correction. | `OUTSTANDING-ARCHIVE.md` `[OIL-SEATS-CAN-EARN]` (the two halves, and the seat list awaiting his word) and `[ALL-AVAIL-REDEF]`; on build: `raptor-port/docs/ui-contracts.md` and the OIL behaviour register |

## Settled before this list — moved from `raptor-port/CLAUDE.md` §Stable decisions (24 Sep 26)

**CLOSED: a new decision is a row in the table above, with a D-number — never a line added here.** These are
the scheduler's (the board, the week, the waves and duties, time, inputs and Admin) settled decisions from before the rulings list began (21 Sep 26), MOVED WHOLE, word for word
(D138), so they load with this area instead of in every chat (D140). Where a row above changes an entry
here, the row wins (newest instruction wins). The preamble they carried in `raptor-port/CLAUDE.md`, unchanged:

Each entry is a tripwire: the decision is SETTLED — don't rebuild, re-propose or
re-litigate it. Where a reference doc holds the full story, the line keeps the
decision + a pointer. Owner + date establish authority; keep them.

### The late-input mark (owner, 9 Aug 26 unless noted)
Rules: `docs/engine-rules.md` §The late-input mark. Placement: `docs/ui-contracts.md`
§The late-input mark on screen, §The LATE marks can be dropped per input.
- **It is a MARK, not a warning** — never in the checks list, never closes a slot,
  invisible to `validate()`. Measures the input's **last change**, not first
  submission (an early input amended after the deadline reads late).
- **Downchits are EXEMPT** (going DNIF isn't planned); leave and overseas duty stay
  in scope (they're applied for). Don't re-propose either.
- **It reads in the REMARKS cell**, not beside name/type (moved there the day after
  it shipped) — except the board's promoted ground row (bare `<input>`, no room to
  nest a chip) which keeps its amber row edge.
- **The word stays a word — a compact dot was OFFERED and DECLINED.** Measured on an
  11-input day: desktop costs nothing (666px w/ and w/o); phone costs 33px over 11
  rows (757 vs 724) and splits long remarks mid-word ("Medic al appt"); a dot
  measured 724 (free). Owner kept the word and the cost. Dead ends, don't retry:
  word at END of remark saved nothing (759); turning off mid-word breaking saved
  nothing (757). Don't "fix" the split by shrinking the badge — its WIDTH is the
  whole cost. (These measurements live only here.)
- **It CAN be dropped per input** (owner, 21 Aug 26; REPLACED the 20 Aug global
  "Hide LATE marks" button, now removed). A session-only forgiven registry
  (`LATEOFF` in `state/view.ts`) with the way back = tapping the same chip again.
  Three fixed parts: the board's LIVE rows (Personal Inputs + Unavailable) always
  draw a clickable `latechip` (solid shown, dim ghost dropped); the GATE is at the
  passive printers (`ui/html.ts lateShown`, `isLateInput` still answers); the Inputs
  page KEEPS printing it (paperwork record). Admin-only at the write path, cleared
  on login/logout. Don't move the gate into the engine, don't bring back the global
  button, don't silence the Inputs page.

### Board behaviour
- **No warning / advisory / note counts in the top bar** (owner, 20 Aug 26). The
  three `pillbtn` counts are gone (each day leads with its own "N issues · N
  warning · tap to review"). `openWarns` KEPT with no caller (reference behaviour /
  future "expand everything"). Pinned in `app.test.tsx` — don't put the sum back.
- **Nothing on the board re-orders itself** (owner, 10 Aug 26 — "prevent … the line
  jumps"). Auto sort / Sort all are the only reorderers, by START TIME not role
  rank. Don't add an automatic sort to any board list. (The Ground Programme's
  render-time time sort predates this and stays — time-less rows sink to append.)
- **MAIN/SPARE on a standalone line is a clickable BADGE in the remarks cell; a click
  flips the line** (owner, 24 Aug 26; supersedes the 10 Aug ghost-text). `saRoleHTML`
  draws it week+board (button in edit mode, chip read-only); placeholder is plain
  `Remarks`. The flip is ENGINE-VISIBLE: `a.spare`+`a.role` flip together,
  `scSpare`/`saExempt`/shift count follow, marks the `st:` key pending (rides next
  AL). Next-week peek keeps role as fallback text (`saRoleText`). Don't restore the
  placeholder or make the flip label-only. Pins: `sarole.test.tsx`.
- **On SC, the B box is an IN-TIME; its sortie furniture is gone** (owner, 24 Aug 26,
  SC only; AVALON/BB untouched). Board drops the SC header "in-time · N ac" note; SC
  lines lose the blue suggested-brief ghost but KEEP the empty B box; the engine
  reads a typed SC `f.br` as the crew's in-time (`events.ts` → `intime`,
  `insOf` anchors crew rest on the earlier of it and shift start). Blank = byte-
  identical to before. Extended same day: an early B on a MAIN starts the long-day/
  duty-hours span (`workSpan` = `min(report,start)`), and anything cutting into the
  B→start window raises the amber `SC_INTIME` advisory (already-overlapping events
  stay with the hard-clash loop). SPARE rows have no event stream (both MAIN-only).
  Known seam (documented, don't silently "fix"): the SC in-time shows on the board
  only — the desktop week renders SC as SHIFT/START/END with no B, so a board value
  isn't mirrored to the week. Owner said it'll be rare; raise mirroring only if he
  asks. Pins: `scintime.test.ts`, `scboard.test.tsx`. Rules: `engine-rules.md`;
  placement: `ui-contracts.md` §The B box.
- **Amendment marks are a PUBLISHED-day thing — a draft day shows none** (owner,
  25 Aug 26). `alAttr` emits `data-alp`/`data-aln` only when `dayApproved`; a draft
  edit emits nothing but is still tracked in `SCHED.pending` (count/publish/History
  unchanged — History finds cells by key+edit log). Don't re-add a draft-day mark.
  Pinned in `publish.test.ts`/`interact.test.tsx`; `ui-contracts.md` §Amendment marks.
- **A new flying line comes up blank** (owner, 10 Aug 26) — `+ Line` no longer copies
  the previous callsign/mission/times. **`+ Wave` follows the same rule** (owner,
  25 Aug 26): a plain flying wave's first line is blank (`cs/msn/to/ld` empty),
  byte-identical to a `+ Line` add — no more `NEW / 12:00 / 13:00` seed and its green
  suggested-brief. Standalone waves (`makeStandalone`) keep their kind-specific
  structure (the `!kind` branch only). Pinned in `board.test.tsx`.

### Waves & duties — templates and defaults
- **Duties are decoupled from waves** (owner, 13 Aug 26; supersedes 10 Aug "AVALON
  auto-creates its desk"). No wave auto-creates a duty desk (AVALON included);
  deleting a wave leaves duty blocks alone. Every desk comes from the `+ Block`
  template picker (`engine/dutytpl.ts`, persisted): a chosen template copies onto the
  day as a block carrying the template's WAVE, if it names one (owner, 7 Sep 26 —
  "a duty role that falls under AVALON will not have any warning … unless OL, HL,
  OML, ATT C, OD" / "sc duties desk … not … the same time as main or spare"): the
  editor's "For wave" picker (None / SC / AVALON / BB) → `blockFromTpl` mints `sa`
  (+ `noconf` for AVALON and BB — BB is AVALON's twin, owner 7 Sep 26), so that
  desk is exempt as its wave is and earns no
  OIL, an SC desk is an SC seat for the spare rule, and a template with no wave
  mints the PLAIN block it always did. This amends the 13 Aug "always plain"
  clause; the decoupling itself stands. Seed week carries no template desk, parity
  untouched. Editor `ui/DutyTplModal.tsx`. Do NOT re-add
  `SAWAVE.autoDuty` or the wave-delete → `saDutyIx` linkage (`waveDutyBlock`/
  `saDutyIx` remain in `waves.ts` only for old AL snapshots).
- **Flying-wave templates + a + Wave show/hide list** (owner, 25 Aug 26). Sibling of
  duty templates one level up: `engine/wavetpl.ts` library, `ui/WaveTplModal.tsx`
  editor (from the + Wave pencil, `WAVEEDIT`), `+ Wave` lists templates beside its 4
  built-in kinds. Template `{id,title,kind,lines}`: **one rule-set per template** —
  exactly one of `fly`/`sc`/`avalon`/`bb`. Placing one mints an ordinary wave
  (`waveFromTpl`) whose OWN kind flags drive checking — `validate.ts` never reads a
  template, parity untouched. **A STANDBY-kind template mints the built-in's SHAPE**
  (owner, 26 Aug 26): consecutive same-shift lines become ONE formation with a crew
  row per line (like `makeStandalone`); a fly line stays one formation per line —
  don't return the standby mint to 1:1. Times store raw, normalise on blur/mint/load
  (`waveTime`, colon form `07:00`). Show/hide via a `WAVEHIDE` set (default all-shown,
  deleted template drops its flag), persisted (`wavetpl`+`wavehide`), boot-loaded,
  untrusted storage clamped. Don't seed built-in templates (library starts empty),
  don't make `validate` read a template, don't move the gate off `WAVEHIDE`. Pins:
  `wavetpl.test.ts`, `WaveTplModal.test.tsx`, `wavepicker.test.tsx`.
  - **Manage + edit are ONE sheet, ONE gear** (owner, 30 Aug 26; folds in the 29 Aug
    "remove it from Admin"). The + Wave menu carries a single ⚙ (`data-wvedit`) +
    "N hidden · Manage", both opening the unified `WaveTplModal.tsx` (edits templates
    AND shows/hides/deletes: a "Wave types" list with an EYE per built-in kind,
    `setWaveHidden`; per template an EYE + footer Delete; built-ins hide but never
    delete). Old `WaveManageSheet.tsx`/`WAVEMANAGE` DELETED — don't re-add it or a
    second button. Admin's `WaveVisibility` stays REMOVED; Admin keeps only the
    template-editor button (same sheet). Don't strand a hidden wave — the "N hidden"
    line + eyes are the way back.
  - **Kind-picker rule notes have ONE source, on the picker AND the Logic page**
    (owner, 30 Aug 26). `wavetpl.kindNote(k)` is the single count-free summary of
    each kind's checking rule, verified against `validate.ts`/`events.ts`; the editor
    and the Logic "Wave types at a glance" group render the SAME strings. Deliberately
    NOT `SAWAVE.note` (keeps its "2 MAIN + 2 SPARE" count for the built-in popup where
    the count is real). When a kind's rule changes, update rule + `kindNote` + the
    Logic "standby lines" row together (`logic.test.tsx` pins it).
  - **The leave/absence "what each type costs" sentence has ONE source too** (owner,
    30 Aug 26), shared by the Inputs "?" legend and the Logic type matrix.
    `inputs.ts inputRuleText(t)`, derived from the enforced flags; both `InputsPage`
    and `logic-html.ts` read it (they'd drifted). `inputs.test.tsx`/`logic.test.tsx`
    guard it. Deliberately still separate, don't "helpfully" merge: `SAWAVE.note`,
    the `satag` caption, the OIL-confirm prose (different jobs/voices).
- **The DEFAULT arrangement is admin-set; the wave half is "new schedules only"**
  (owner, 29 Aug 26 pt.2). Admin → Squadron config → **Default arrangement** panel,
  two ▲▼ lists persisted on the `wavehide` footing: **section order**
  (`engine/order.ts SEC_DEFAULT`, `secdefault`) is display-only fallback `secOrder`
  uses for un-arranged sections (hand-arranged day still wins, canonical baseline
  keeps 728/0); **wave order** (`engine/reorder.ts WAVE_DEFAULT`, `wavedefault`,
  default OFF) applies ONLY at wave-add on a not-signed-off day. DON'T make the wave
  default reorder existing/published days, and DON'T give it a display-only layer (it
  would fight `sortWaves`). Unset = append. Pins: `arrdefaults.test.ts`,
  `wavedefault-add.test.tsx`, `admin.test.tsx`.

### Drag-reordering (sections, waves, dense rows)
Contract: `docs/ui-contracts.md` §Dragging sections and waves, §Dense row reorder.
- **Sections and waves re-order by IN-PLACE DRAG, not a sheet** (owner, 30 Aug 26;
  the 29 Aug `⇅ Arrange` sheet is DELETED). One machine `ui/rowdrag.ts` (board wrap +
  edit-week root) tells section/wave/row apart by the grip pressed and validates via
  `applyMove`'s same-container rule. Grips draggable at EVERY width. The SECTION grip
  is the SAME dotted `⠿` (owner, 31 Aug 26, reversing the 30 Aug drawn-rail),
  placed INLINE at the panel header (not an overlay rail); headers set
  `user-select:none`. Don't turn it back into a rail or drop the no-select. Don't add
  arrows back to any of them.
  - **Overall Notes and Common Programme are two SEPARATE draggable board sections**
    (owner, 31 Aug 26). On the EDIT WEEK day notes still print inside the Common
    Programme block, so the week keeps 'notes' EMPTY/skipped (keeps view week ==
    reference byte-identical). The week's Flying-waves `.wv-sech` header stays (its
    grip needs a header; stripped in the reference compare).
  - **The four CREW WORKING-AID panels join the SAME draggable list** (owner, 31 Aug
    26): Personal Inputs, Available crew, SANS availability, Unavailable are ordinary
    section keys, each a `.sb-sec` card. **They drag on the EDIT SCHEDULER too** —
    `dayHTML` in EDIT mode emits all ten sections through the SAME `secOrder` loop, so
    a drag on either surface drives the ONE per-day order. The VIEW week is UNTOUCHED
    and parity-locked (four not draggable, only Unavailable prints in its fixed tail;
    whole change gated on `ed`, 728/0). This is a scheduler WORKSPACE arrangement, not
    a published property. A SECTION drag is display-only (`moveSectionTo` →
    `reorderSectionTo`, histPush, no markEdit) then offers the "Set default order?"
    snackbar (`SecDefaultSnackbar.tsx`, promotes via the SAME `setSecDefault` as
    Admin). A WAVE drag is a real amendment (`applyMove('mv:w…')` → `moveWave`). A held
    drag AUTO-SCROLLS at screen edges (`pointermove` on the DOCUMENT — don't move it
    back onto the container). Don't fold the crew panels into a fixed tail or let
    their order reach the VIEW week. The old one-week "Apply to all days" is GONE.
    Pins: `rowdrag.test.tsx`, `SecDefaultSnackbar.test.tsx`, `board.test.tsx`,
    `html.test.ts`.
- **Dense ROW reorder is by DRAG too — the ▲▼ nudge is GONE** (owner, 31 Aug 26;
  reverses the 8 Aug "phone hides grip, shows ▲▼"). Every dense row shows its dotted
  `⠿` at ALL widths; `sbNudge` returns '' (also ~2 nodes/row off the board budget);
  phone grid gains a leading marker track, first box shortens by it, each box stays
  under its heading. A same-week follow-up widened the lane 13→20px (glyph
  left-aligned, handle clear of the first box) and shifted puck containers to span
  track 1 (`.sb-line .sb-seatpair 1/4`, `.sb-arow.c6r>.ppl 1/3`) so crew pucks go
  flush-left and a two-wide box's second puck clears the remarks. No puck resized.
  **ALIGNMENT is a HARD RULE**: every grip's centre measured to delta-0 against the
  box beside it (flying-line grip bottom-aligns `align-self:end;height:24px`; c6r/
  notes centre naturally). Don't re-add ▲▼, don't hide the row grip on a phone, don't
  move a grip without re-measuring delta-0. `boardMbtn mv:up/dn` stays as inert guard.
  Pins: `rowdrag.test.tsx`, `board.test.tsx`.
- **No ⋯ collapse of the phone row control strips** (owner, 16 Aug 26 — built +
  rolled back same day). Row `▲▼/CX/■/✕` strips stay always-visible on a phone; the
  `CTLOPEN` implementation is one `git revert` away — don't rebuild or re-propose.
  Sibling touches from that batch (aircrew-tab gutter, plural warnings, week's faded
  `Remarks` placeholder) STAND; only the ⋯ collapse was undone (and the batch's
  4-digit board input times were later reversed by the 30 Aug hh:mm decision).

### Time format
- **EVERY time in the app reads `08:00` — colon, 24-hour, everywhere** (owner, 30 Aug
  26, reversing their own 29 Aug "just 0800"). hh:mm is native and the reference gate
  PINS it (`tfin.js`). Three layers: **Display** wraps every stored time in
  `engine/time.ts fmtHM` (the ONE display fold; board renderers, week already folds
  via `fmtT`); **Minting** `dutytpl.tplTime`/`DUTYTPL_STD`/`waveDutyBlock`/"+ In time"
  now mint `07:00` (old templates refold on load); **Typing** every box accepts
  `800`/`0800`/`8:00`/`08:00` (`parseHM`), shows hh:mm after commit (user never types
  the colon). Engine untouched (readers go through `parseHM`), parity **728/0**. ONE
  deliberate 4-digit survivor: the AREA window token (`0800-0900`, `atimeText`) — the
  reference pins it compact; changing it needs owner sign-off. Don't add a second
  display formatter — `fmtHM` is the one. `ui-contracts.md` §Every time reads hh:mm.

### Week navigation & cross-week continuity
- **The phone board's top bar is ONE row; the day is STEPPED BY ARROWS** (owner,
  11–12 Aug 26). Getting the bar from 166→70px was the whole point — don't add a
  control to its FIRST LINE without taking one off (the geometry gate counts ROWS).
  History added an 8th button and stayed at 70px only because the same change fixed
  `.sb-title` to shrink (`flex:1 1 0`) — that was the last free 33px; the next control
  must displace one (the changes list is the worked alternative — it went to the
  checks panel). `+ Line` is off the bar (every wave header has one); labels icon-only
  under 820px. **The swipe is GONE (12 Aug); do not rebuild it** — `#sbPrevDay`/
  `#sbNextDay` call `boardDayStep(±1)`, CONTINUOUS across weeks since 22 Aug
  (`loadWeek`+`boardTab`; don't re-add the end-of-week `disabled`). `#sbCal` opens the
  week picker in 'board' context. Arrows flank the DAY STRIP (bar 70→75px). Above
  820px they aren't drawn (desktop has 7 day chips). The Mon–Sun chips became dots
  then LEFT the phone bar on 23 Aug (freed row carries `#searchB`+`#sbHl`), removal is
  CSS `display:none` so `dayTabsHTML`/`wireDayDots`/jsdom tests untouched. **Day name
  is THREE letters on a phone** (12 Aug — split `Wed`+`.bl` tail, desktop still reads
  `Wednesday` off one path; don't restore/ shorten). The DESKTOP scrub survives
  (Mon–Sun chips): every chip keeps its footprint whatever is selected — don't grow
  the current one. `boardTab` is view-only (must not validate; its board lane must not
  wake EditWeek/EditRoster). `ui-contracts.md` §The board on a phone is ONE window.
- **Week navigation is a rolling window + a calendar, and it is CONTINUOUS** (owner,
  22–25 Aug 26). The fixed 5-chip `WEEKS` strip is gone; `weekWindow(CURWEEK)`
  (`ui/weeknav.ts`) draws four `data-wk` buttons (prev·current·+1·+2, re-centring).
  `WeekCal` (single-date, whole-week highlight) jumps to any day's week; it's a DAY
  picker (loads that week AND lands that exact day) — don't turn it back into a
  week-row picker. All week/Monday math lives in `weeknav.ts` (one drift seam). The
  big `#vTitle`/`#vSub` were removed as clutter (cards carry dates) — don't re-add
  them, the fixed chips, or the end-of-week clamp. `WEEKS` kept for probe-bridge/
  reference only. Pinned mechanics:
  - **Phone**: view/edit stepped day-to-day by SWIPE, continuous across weeks
    (`pan.ts` edge-overswipe + `WEEKJUMP`). A wave-dense day no longer traps it
    (owner, 23 Aug) — the `.go` block's ownership is decided at touch-END (a wave
    already at its edge lets the gesture fall through); don't restore the touch-start
    `.go` bail. The cross GLIDES (owner, 23 Aug, `ui/weekglide.ts:beginGlide`,
    phone-only ≤820px, reduced-motion-aware, no-ops without layout). It slides TWO
    FROZEN CLONES (owner, 24 Aug) — outgoing frozen on the finger's day, incoming
    frozen on its landing day, both `overflow:hidden` so neither scrolls/flings, real
    week painted but COVERED behind them (`pointer-events:none`, never
    `visibility:hidden` — 5 Sep 26, a hidden week came back unpainted). Load-bearing
    details: landing day derives from cross DIRECTION (`fwd?0:weekScrollMax`), NOT
    live `scrollLeft`; the clone box is as TALL as the taller of the two weeks,
    measured on both sides of the swap (5 Sep 26 — sized from the leaving week it
    clipped a tall arriving Monday at a short week's height: the "split" / "lower
    half black"); each clone is ONE opaque day card (`snapshot()`), inserted
    ON-SCREEN under the leaving one and left two frames to paint before it slides;
    real week covered so its fling/snap never shows. Clones at `z-index` 40/41,
    BELOW the sticky `.topbar` (60) — keep them under the chrome. Don't slide the
    live week, use a single clone, size a clone from one week, clone the whole
    week, or start a clone off-screen.
    Swipe NOT locked to one day (owner kept this) — no `scroll-snap-stop`; within-week
    swipes never glide, desktop instant.
  - **Desktop arrows are continuous across weeks** (owner, 23 Aug), landings instant.
    They **walk EVERY live day incl Sat/Sun to the FRONT before crossing** —
    `weekScrollMax` = "last live day at the front" (`(liveDays−1)×dayStep` clamped),
    the next-week peek's real columns are the runway; the JS-sized trailing spacer
    stays (`.week::after`/`--week-tail`, desktop only). Don't reintroduce a fixed
    `calc()` spacer or the flush-right ceiling. **One press = one day even mid-glide,
    both directions** — `panDays` counts from the COMMANDED target (`panTgt`) via a
    BURST CORRIDOR anchored at the burst start (`panAnchor`→`panTgt` = `panBase`); a
    manual pan or new week drops it. Don't narrow the corridor back to the last step.
    **A park NEAR a boundary counts as ON it, and a plain horizontal wheel drops the
    corridor** (owner, 24 Aug) — `PARK_TOL` 0.35-of-a-day decides step counting and
    edge-cross; `onWheel` drops `panWk` on any plain horizontal tick. Don't shrink
    `PARK_TOL` to a hairline or remove the deltaX invalidation. **The glide OWNS the
    week while in flight** (owner, 24–25 Aug) — the proxy scrollbar and any repaint
    are pure FOLLOWERS: `panDays` arms a short `glideEnd`/`GLIDE_MS` window (cleared on
    land or manual pan) during which `onTrackScroll` never drives the week, and a
    mid-glide repaint holds the glide's TARGET (`panHold` in EditWeek/ViewWeek), not
    the captured mid-glide position; `mirrorToTrack` records `trkEcho`. Don't remove
    the `glideEnd` guard, revert `panHold` to pinning live `sl`, or let `onTrackScroll`
    write unconditionally. Pinned `pan.test.tsx`; `ui-contracts.md` §desktop arrow
    glide + §spacer; `performance.md` §Single-writer during a glide.
  - **The desktop scheduler BOARD now has week navigation** (owner, 23 Aug) — `‹ ›`
    week-jump chips inside `#sbDays` (`data-sbweek`, `boardWeekStep`, one press = a
    week keeping the open day); `#sbDays` is `display:none` on a phone so the phone
    board keeps its edge arrows. The `.crew-hint` edge hint stays RETIRED.
- **Personal INPUTS are GLOBAL, not week-scoped** (owner, 22 Aug 26). `loadWeek` swaps
  DAYS/DATES but NOT `INPUTS`; every authored week's inputs merge into one `INPUTS`
  at boot (idempotent, boot-only → parity 728/0). Each week's schedule shows only its
  own (builders match by DATE). Gotcha kept in `loadWeek`: it clears every input's
  `acc` so `autoAcceptSeedInputs` re-lands date-matching rows on the fresh days. Don't
  re-add the `INPUTS` swap or move the `acc` clear. Flow: `feature-impact.md` §Flow E.
- **The flagging engine reads across week boundaries** (owner, 23 Aug 26). Two rules
  fixed to look past the loaded week: `DAYS_RUN` (`VCONF.maxRun`) walks in seeded days
  before Monday; Monday's `CREW_REST`/`CREW_TIGHT` runs against the previous week's
  Sunday (so `REST[0]` is real). Bounded to those lookbacks + one lookahead day (the
  midnight-tail sliver past Sunday + the forward crew-rest trace); nothing else looks
  further — don't widen either window without a named case. **A flag still lands on the
  day it BREAKS**: next Monday's breach only becomes clickable when next week is
  loaded. **The forward "Breaks Monday" trace** (owner asked from the deployed site,
  23 Aug, reversing this entry's old "don't build a same-page hint") — a loaded week's
  Sunday whose late finish busts next Monday draws the same trace box, off
  `nextMondaySeed` + a phantom pass of the real `crewRestDay` (one body, two callers,
  can't drift); it carries `di:null` (no jump target) and writes no second warning.
  `CREW_TIGHT` never traces. Session edits ARE read now via the stash
  (`weekctx.ts:bundle()` checks it before the pure seed); `SCHED`/publish state still
  isn't read by these seed functions. Rules: `engine-rules.md` §validation/crew rest;
  screen: `ui-contracts.md` §Three crew-rest rings; `feature-impact.md` Flow F.
- **Weeks remember their edits — the per-week stash** (owner, 23 Aug 26).
  `engine/weekstash.ts` remembers, per week-start key, the last snapshot `loadWeek`
  handed it on the way OUT — decided parts:
  - **SUPERSEDED 17 Sep 26 — stashed weeks PERSIST.** The 23 Aug "session memory
    only, a reload forgets" decision (and its reasoning that `INPUTS` and Leave War
    forget in lockstep) was overtaken by the 8 Sep 26 storage work: `persistAll`
    writes `inputs`, `people`, the `plan` layer AND every stashed week to the
    whiteboard, and a built site runs on the Browser backend. A reload KEEPS them.
    Memory-only is now just dev/tests/`?fresh=1`. Don't "fix" this by removing the
    persistence — it is deliberate; see `docs/data-schema.md`.
  - **Pristine weeks are deliberately NOT stashed** (a persisted byte-copy of the seed
    would outrank a later demo-week update forever). Stashed on the way out only if
    changed since load or already carrying an entry. Don't re-add the unconditional
    stash.
  - **Publish state rides the restore** — the stash shares its SCHED field list with
    `history.ts:schedFields` (the two serializers can't drift).
  - **Seeds read the stash first** — cross-week reads go through `weekctx.ts:bundle()`,
    stash ahead of the pure seed.
  - **The "Sync" chip stays decorative** — per-browser, no server; don't present the
    stash as shared/multi-device or move storage off `HOOKS.storeBackend`.
  - Undo still re-baselines per week; the edit log stays session-only — the stash is
    additive to both. Flow: `feature-impact.md` Flow E.

### Inputs & Admin
- **Manage users lives on the Admin tab** (owner, 23 Aug 26). Topbar `#manageUsers`
  and `#userModal` gone; same fields/list/mutations on `ui/AdminPage.tsx` (7th nav
  tab, ALWAYS LAST, admin-hidden like Edit — but the PAGE is the gate, `#admDeny`).
  Don't put the button back on the topbar or add a tab after Admin.
- **No repeat-weeks on inputs** (owner, 22 Aug 26). The "Repeat wks" field, Recurring
  column and `recur` write are deleted — the feature never actually repeated (one
  span stored, `recur` a label nothing expanded). A truly repeating input is a real
  future feature (build only if he asks); a member files the same absence per week.
  Pinned in `inputs.test.tsx`. (`reference/` keeps its own Repeat field — test-only.)
  Moving an `Other` row to Ground/Unavailable is the `→ Ground`/`→ Unavail` buttons
  in `html.ts` (week + board); don't add drop targets to `drag.ts` (pucks only).
- **The calendar day popover — five owner asks** (23 Aug 26, all in `InputsCal.tsx`/
  `scheduler.css`; `ui-contracts.md` §The Inputs month calendar):
  - A SANS input reads its F/O/A letters on the popover row too (`isSansAvail ?
    (sansLetters||'F/O/A') : inpLabel`) — don't put "SANS Availability" back.
  - The day TITLE matches the date number's size (15px/700) — don't drop the explicit
    size (without it the input takes the 16px UA default).
  - A cell NOTE is plain text, no box (the dashed accent border is gone); on a phone a
    muted `--edge-2` bar so it stays visible — don't re-add the dashed border.
  - The cell mini-pucks (`.ic-pk`) are standard-olive (`--fcp`), CAT a right-edge line
    (`--pk-cat` via `::after`), a SANS person a purple LEFT line — pseudo-elements not
    inline box-shadows. Not the old full CAT-tint fill.
  - `+ Pucks` opens the MULTI-SELECT picker (`.ic-pick`): category buttons
    (`personMatchesCat` — the SAME predicate as the highlight chips, one body) light a
    category, ✓ Add batches the ticks (`addPuckRow`/`addPuckPeople`, dedupe). A seated
    puck removes 3 ways (✕, right-click, drag off its row). Don't restore the
    per-person `<select>`; keep `personMatchesCat` the one predicate.

### Leave War grid & scheduler render/drag performance
Full detail for this whole group: `docs/performance.md` (Part 1 invariants + Part 2
ledger). Read it before any layout/render/drag-touching change.
- **The board DOM ceiling of 1150 is settled** (owner, 28 Aug 26). Raised 960→1150 in
  PR #333 (board ~1051 nodes; timings held at 0.57× reference). Lives at
  `DOM_CEILING` in `perf-port.cjs`. Don't re-litigate the raise or trim the board to
  the old ceiling. (`performance.md`.)
- **The Leave War grid — the open-bidding box, opening on the war being bid on, the window of months** — moved 24 Sep 26, whole, to `../.claude/rules/decisions/leave-war.md` §Settled before this list → Leave War grid performance.
- **A dragged puck's ghost rides its own compositor layer, moved by ONE transform;
  cell hover highlights are off in flight** (6 Sep 26). `.dragimg`/`.tdghost` carry
  `will-change:transform` with left/top pinned at 0; `drag.ts ghostXf` writes
  `translate(x,y)…` — no page paint (left/top moves relaid the whole page, ~170ms/
  move). The three cell-hover rules are scoped `body:not(.tdrag)` (`.dragover` is the
  drag's feedback). Don't move the ghost by left/top or re-enable hover under a drag.
  Measured-and-done dead ends (don't chase): toggling ghost `pointer-events`,
  any cursor rule on body, every re-layering ghost variant (the cost is the page's
  ~230 compositor layers, not the ghost); the `translateZ(0)` "fix" was a broken-
  experiment artefact, retracted. Nothing here touches native drag. `performance.md`
  §Drag; `ui-contracts.md` §the mouse rides the pointer machine.
- **A changed day rewrites only its changed BLOCKS, and a drop hit-tests before it
  takes the ghost down** (6 Sep 26). `ui/dayswap.ts` parses the new day into a
  `<template>` and replaces only the top-level/`.day-body` blocks whose CANONICAL
  markup (freshly parsed, never the live decorated node) differs from the last write;
  any shape mismatch falls back to whole-day replace (the old `outerHTML=`). Both
  weeks use it (keep `prev.chunks`). `drag.ts onPointerUp` hit-tests with the ghost
  still up, removes it after, leaves `body.tdrag/.mdrag` to `tdClear()`. Drop long
  task ~600→400–490ms at 4×. Dead ends (don't retry): every paint-isolation variant on
  the week (equal/worse — the ~60–100ms repaint records the two visible days); a
  "quiet path" in `refreshHighlights` (2× slower). Don't swap by live `outerHTML`
  comparison, match blocks by index across a count mismatch, or take the ghost down
  before the hit-test. Pins `ui/dayswap.test.ts` (`drag.test.tsx` unchanged).
  `performance.md`; `ui-contracts.md` §Rendering (per-block swap).








