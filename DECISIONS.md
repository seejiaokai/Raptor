# DECISIONS — every ruling the owner has made, and where each one lives

**Why this file exists.** The owner, 21 Sep 26: *"This is a recurring problem. Whenever we
discussed something important to note down. I dont see u noting them down. How do we fix your
behavior lapse?"* He was right, and the cause is not forgetting. A ruling stated mid-task gets
absorbed into the WORK — executed correctly — and that feels like it has been handled. The work
absorbing a ruling is not the record keeping it. Because the work comes out right, nothing feels
missing, which is why it kept happening.

**So this is the artefact that would not exist if the step were skipped.** Append-only. Newest at
the top. One entry per ruling, and **every entry names the file that now carries it** — this file
is the INDEX of decisions, never the only home of one. A ruling recorded only here is still lost,
because nobody reads an index while building.

**The rule, in one line:** the moment he states a decision, preference, correction or ruling that
outlives the current task, it is appended here BEFORE the work it implies is done — and the closing
report carries a `Rulings:` line. Full rule: `.claude/rules/record-decisions.md`.

**Parallel branches (owner, D70):** when several chats run at once, each reaches `main` only on his
"merge live", ONE AT A TIME; whichever merges later merges `main` in first, and if a D-number
clashes, the later branch renumbers ITS OWN rows — never the ones already on `main`. So numbers
may SKIP (a parallel branch can hold a range); a number is never reused and never lost.

**What counts.** A ruling ("do it this way from now on"), a product decision ("green only where it
counted"), a correction to how I work, a preference, a "leave it", a supersession of an earlier
ruling, or an explicit no. **Not** ordinary task instructions ("run the tests", "check that file")
— those die with the task and belong nowhere.

---

## 23 Sep 2026

| # | His ruling, in his words where short enough | What it means | Where it lives now |
|---|---|---|---|
| D76 | **"Ok do it"** — after being told what `[DOCS-GUARD]` step 4 is and what it buys | **STEP 4 IS DONE NOW, NOT AT THE NEXT ARCHIVE PASS.** Supersedes the timing in D30 and his own earlier "leave step 4 for when an archive pass is actually due" the same day; the scope is unchanged (F5's mover + F7). The agent had recommended waiting, then agreed on a reason it had missed: done on the same branch, it shares the one full gate run the merge already pays for, where a separate later branch would bill a second | `raptor-port/docs/superpowers/specs/2026-09-22-backlog-process-attack.md` (the closing section) |
| D75 | *"Yes an award and a worked day add up. So it's 4. The auto oil credits don't get affected by manual OIL inputs."* (21 Sep 26; recorded 23 Sep under [DOCS-GUARD] F6) | **AN OIL AWARD AND A WORKED DAY ADD UP.** A 3-day award on a Saturday he then works is worth 4; the schedule's credit and a typed award never change each other. BUILT and merged (PR #423) | `raptor-port/docs/superpowers/specs/2026-09-20-one-absence-behaviour-register.md` N16 |
| D74 | *"Yes i want a warning."* — after a Sunday duty desk with no times earned nobody anything (20 Sep 26; recorded 23 Sep under F6) | **A WORKED WEEKEND THAT EARNS NOBODY ANY OIL SAYS SO** — on the day and at publish, with the reason (e.g. the desk has no times). BUILT 21 Sep 26 | `raptor-port/docs/superpowers/specs/2026-09-20-one-absence-behaviour-register.md` N14 |
| D73 | *"i want an award of an OIL to stop flagging a leave day. That makes sense."* (20 Sep 26; recorded 23 Sep under F6) | **AN OIL AWARD DOES NOT FLAG A LEAVE DAY** — it says a man is owed a day, not where he was. He did NOT rule on whether an award counts him on duty for manning; ask before changing that. BUILT 21 Sep 26 | `raptor-port/docs/superpowers/specs/2026-09-20-one-absence-behaviour-register.md` N13 |
| D72 | *"We should enable that even on any day."* (20 Sep 26; recorded 23 Sep under F6) | **OIL MAY BE CREDITED BY HAND ON ANY DAY.** The weekend/holiday limit belongs to the AUTOMATIC pass, which reads the published schedule — not to a credit the squadron records itself. BUILT | `raptor-port/docs/superpowers/specs/2026-09-20-one-absence-behaviour-register.md` N11 |
| D71 | *"at the end of this work just summarise or clean up or optimise the saved context in the repo. Don't wanna be bloating it"* · clarified: *"I mean if your context is bloating and u decide to save the context in the repo"* | **WHEN A SESSION SAVES ITS CONTEXT INTO THE REPO, IT SAVES A SUMMARY.** Decisions, state and next step — never the conversation. D14 applied to handoffs and context docs | `raptor-port/docs/doc-budget.md` §2 |
| D70 | *"Two other chats are running in parallel … Each reaches main only on my "merge live", one at a time; whichever goes later merges main in first, and if a D-number clashes, renumber yours."* | **PARALLEL CHATS MERGE ONE AT A TIME, AND THE LATER ONE ADAPTS.** Each branch still needs his own "merge live" (D60 unchanged). The branch that merges second first merges `main` into itself, so it is checked against what is already live; a clashing D-number is fixed by renumbering the LATER branch's rows, so a number already on `main` never changes meaning. Given while starting `[DOCS-GUARD]` beside `claude/all-avail-window` and `claude/skill-review` | this file's header (the rule, where D-numbers are assigned) |
| D60 | **"2"** — choosing, from three standing options, *push freely to a branch, always ask before `main`*, after being told that a private repo meters its Actions minutes so every push now costs him | **PUSHING A BRANCH NEEDS NO PERMISSION; `main` ALWAYS DOES.** A branch push is how he SEES a thing — it produces a Vercel link of its own and changes nothing official — so the agent does it without asking. `main` stays gated behind his **"merge live"**, exactly as before. **This EXTENDS the standing merge rule rather than replacing it:** that rule only ever covered merging, because while the repo was PUBLIC pushes were free and unlimited and there was nothing to ration. D59 made them metered, which is what created the question. **The two words are not synonyms and the agent must not blur them:** "push" puts work on GitHub on its own branch; "merge live" makes it the app. **The cost he accepted by choosing this:** the careful route (branch push, look, then merge) is TWO billed runs against one for a direct merge — he chose the look. **He rejected** "ask every time" (too many interruptions) and "ask only when it is more than one push" (cheapest, least oversight) | this file; `raptor-port/CLAUDE.md` §Stable decisions → Pipeline & repo invariants; memory `push-branch-freely-ask-before-main` |
| D59 | *"ok my visibility is private"* · *"in the future i would like to share my private repo so that developers can work on it"* · *"in order to see my app in the future i just need to generate a vercel link? ... As in see my app on my phone"* | **THE REPO IS PRIVATE AS OF 23 Sep 26 — DONE, BY HIM. `[REPO-PRIVATE]` IS NO LONGER PARKED; IT IS TAKEN.** This reverses the 23 Sep *"nvm disregard this first"*. **Three consequences, all confirmed rather than predicted:** (1) the GitHub Pages site is GONE — the API returns 404 — so the old public URL is dead, which is the point; (2) the publish job in the deploy workflow would now FAIL on every push and still bill its minutes, so it is **switched off in the same change**, with the gates left running so a red merge is still blocked; (3) Actions minutes are METERED on a private repo at a measured ~37 billed minutes a push. **The app is now viewed on VERCEL**, which is his alone behind Vercel's own sign-in — that is how he reaches it on his phone, and it is unaffected by the repo going private so long as Vercel keeps its GitHub access. **SHARING WITH DEVELOPERS IS HIS STATED INTENTION, NOT YET DONE:** the route is Settings → Collaborators → add by GitHub username with Write, and they run it locally (`cd raptor-port && npm install && npm run dev`) — they do not need Vercel, whose free tier is single-person. **The open question that comes with it:** a collaborator on THIS repo sees everything — the uploaded original, the full history, every agent-facing document. If that matters, the fresh-single-commit-repo route is the answer; his call, unmade | this file; `.github/workflows/deploy.yml` (the publish job, disabled with its reason); `OUTSTANDING.md` `[REPO-PRIVATE]`; `raptor-port/README.md` (the live-site line, now stale) |
| D58 | Ruling on what the app may be branded with, after a check of the public repo found the unit named in the browser title, the printed schedule header and the sign-in page. **His exact words are deliberately not reproduced here, because they contain the two strings this ruling removes** | **THE BARE NUMBER STAYS; THE SQUADRON ABBREVIATION AND THE SERVICE ABBREVIATION GO, EVERYWHERE.** `142` on its own is fine anywhere — it is already the export filenames (`142-schedule.csv`, `142-inputs.csv`, `142-LoX-*.csv`) and those are untouched. What must not appear is the number PAIRED with the squadron abbreviation (that pairing is what names a real unit), or the service abbreviation in any form — including code comments, docs, the archive and the uploaded original. **Why it is worth the churn:** the 63 people in the app are invented callsigns with no real names, ID numbers, emails or phone numbers, so the demo data is harmless on its own. The unit designation was the single thing turning it into a named squadron's internal tool — and the repo is PUBLIC with a live public site. **DONE the same hour, 11 files:** the browser tab title, the printed schedule's own tab title and its heading, the test that pinned that heading, the React app's guide file, three code comments, an e2e comment, a sample export and the uploaded original. **The gate risk was checked and is nil:** `npm run test:reference` RUNS the original app and asserts its behaviour — it does not compare text, and none of its 728 assertions touches the branding | `raptor-port/index.html`; `raptor-port/src/ui/printpdf.ts` and `printpdf.test.ts`; `raptor-port/CLAUDE.md`; `src/leavewar/ui/Chrome.tsx`; `src/ui/Login.tsx`; `raptor-port/e2e/leavewar.spec.ts`; `raptor-port/docs/img/flag-export-sample-new.html`; `raptor-port/reference/scheduler.html`; `HANDOFF-ARCHIVE.md`; `OUTSTANDING.md` `[REPO-PRIVATE]` |
| D57 | *"the RAPTOR-Command-Brief.pptx is ok"* — asked, while reviewing what a stranger can read in the public repo, whether the commander briefing deck was a concern | **THE DECK IS HIS AND STAYS. NOT A CONCERN, NOT TO BE WIPED.** He made it himself; it is not material anyone else uploaded and nothing in it worries him. **Recorded so a later clean-up does not "find" it in the history and propose removing it again** — it was raised here only because it is the one presentation file the history carries (added `d53ce954`, edited twice, already gone from the current files). **What this does NOT settle:** the two things the same check did find — the repo is PUBLIC with a live public site, and the app was branded with the SQUADRON ABBREVIATION AND THE SERVICE NAME in its browser title, its printed schedule header and its export filename, which is the one thing tying invented callsign demo data to a real unit. The branding half is now settled by D58 above and DONE; the public-repo half remains open and is his call | this file; `OUTSTANDING.md` `[REPO-PRIVATE]` (the measured findings block) |
| D56 | *"Make sure the bug checks dont waste time catching these bugs in the future. It will not happen because ill clear all the demo data anyway before shiping this app into a real database. Remember this"* | **A PROBLEM THAT LIVES ONLY IN DATA ALREADY STORED IS NOT A FINDING. DO NOT SPEND A REVIEWER, A WALK STEP OR A FIX ON IT.** D54 said such a thing is not worth designing around; this says the CHECKING must not go looking for it either. The reviewers are to be TOLD so up front, in the brief, so the finding is never produced — that is where the time actually goes. **The measured cost that produced this ruling: ~25 minutes of one session building a sentence that can never fire again once the data is cleared** (`OIL_OLD_BLOCK`), on a finding a reviewer raised and the owner asked to be fixed before he gave the reason that made it moot. **THE GUARD, so this can never be used to wave a real defect away: BOTH must be true — (i) the harm exists only in data already stored, AND (ii) the code is already correct going forward.** If the app would do it again to NEW data, it is a real finding and the rule does not apply. A "pre-existing" label alone is NOT this rule — the standing order's own warning stands, that the dangerous category is old code a new feature has just made reachable | `raptor-port/docs/bug-check-order.md` (new §2b, the brief wording in §4, and a named anti-pattern in §10); `.claude/rules/bug-check.md` (the always-loaded copy, so it is in force before the order is opened); memory `dev-phase-reset-demo-data-not-migrate` |
| D55 | **"merge live"**, then — asked whether his five-minute look was done or waived — **"Merge now, look after"** | **`[OIL-SEATS-CAN-EARN]` GOES LIVE, AND HIS LOOK IS WAIVED, NOT DONE.** The standing order's §12 wants that box ticked or EXPLICITLY waived; it is waived. **Record it because a later session must not assume the day was eyeballed** — this branch's evidence is the walk, the gates and the two rounds of code reads, not an owner sighting. Same shape as D34 (22 Sep 26) and recorded for the same reason. He looks at the LIVE site afterwards; anything he finds there is a new finding and, by §11, costs the method a line. Everything else that gates a merge was already true: own branch, 49 commits, mergeable with no conflicts, all six gates green, the FULL-tier walk done with its evidence sheet, both providers read the code twice, blind, and every finding dispositioned. **The agent flagged the divergence from its own recommendation before acting, as the standing advisor rule requires, and he did not change his mind** | this file; `raptor-port/docs/handpass/2026-09-22-oil-seats.md` §8a (the checklist row now reads WAIVED, with its date); `OUTSTANDING.md` `[OIL-SEATS-CAN-EARN]` |
| D54 | *"leave it as it is. any way this app is going to get wiped of data before its being brought into a database as these are demo data anyway"* — asked whether an already-issued weekend carrying a placeholder on a duty desk, a sim seat or an extras line should raise the pending mark the moment `[OIL-SEATS-CAN-EARN]` ships, or stay silent | **THE DAY RAISES THE MARK. LEAVE IT.** Such a day reads "1 pending", he republishes it once, and the men behind that puck get the OIL the issued day never paid them — D48's correction-and-republish path, working as he wrote it. The alternative was silence, which would have left those men unpaid for that seat unless the day were republished for some other reason, and quietly withholding earned leave is the thing this whole job exists to stop. **No code change; the behaviour stands.** What WAS built before he answered is the day's explanation of itself (`OIL_OLD_BLOCK`), because the day used to say "1 pending" with no cell marked and nothing in History while the chip beside the puck said "?" — he asked for that separately ("ok fix this first") and it is right under either answer. **THE REASON IS THE BIGGER HALF AND IT REACHES PAST THIS JOB:** the whole store is demo data and **will be wiped before the database step**, so a one-off amendment on days that already exist costs nothing and is not worth designing around. That is the 13 Sep 26 dev-phase rule and D22 **strengthened** — those said clearing is APPROVED when it is the simpler path; this says the wipe is PLANNED and certain, so "it only affects data that already exists" is now a reason to stop worrying rather than a reason to weigh. Apply it as a test to any future finding: if the harm lives only in stored demo data and is prevented going forward, it is not a defect worth building around | this file; `raptor-port/docs/superpowers/specs/2026-09-22-oil-seats-behaviour-register.md` (the row now states the settled answer); `OUTSTANDING.md` `[OIL-READ-LEFTOVERS]` item 3 (closed as ruled) and `[DB-STEP]` (which now carries the wipe as a stated intention); memory `dev-phase-reset-demo-data-not-migrate` |

## 22 Sep 2026

| # | His ruling, in his words where short enough | What it means | Where it lives now |
|---|---|---|---|
| D53 | *“God dam why u need me to tell u 3 times to find the mock up? Is it because u didn’t save this to outstanding?”* | **NO — THE RECORD WAS FINE; THE AGENT NEVER READ IT.** The mock-up was saved (`docs/mock/allavail-window.html`), ruled on four times (D38–D41), and filed in `OUTSTANDING.md` as job 2. All three places were correct and current. The agent answered “there is no design” from a CODE COMMENT that had gone stale within the same afternoon, put an already-settled choice to him as an open question, and only searched the record after being pushed three times. **So the writing half of the rule worked and the READING half was missing.** Three specific errors, named so they can be checked for: (1) the session was handed “read DECISIONS.md D48–D49” and grepped for exactly those two instead of reading the file; (2) `OUTSTANDING.md`’s read-trigger was scoped to “resuming parked work”, and this was classified as “continuing a build”, so it was skipped; (3) when he asked about the mock-up the agent searched for the phrase the stale comment used — a search that could only CONFIRM, never disconfirm. **The standing fix, added to the rule file:** before telling him anything is undecided, before putting any choice to him, and before calling anything missing, search `DECISIONS.md` and `OUTSTANDING.md` FIRST. A code comment is dated the moment it is written and is never evidence that something is unruled. | `.claude/rules/record-decisions.md` (new §“Read it before you ask him anything”); the memory entry for `OUTSTANDING.md` (read-trigger widened) |
| D52 | *“If a ground crew is placed on a weekend duty they earn oil. Another scenario If all avail is placed onto a weekend duty, they earn oil. Unless the admin toggles them to not earn. All avail or all pucks don’t include ground crew. … How would ground crew ever join that window if they are never included in the first place?”* — answering a walk finding that was put to him as “two answers on one desk” | **ALL THREE BEHAVIOURS ARE CORRECT AS BUILT, AND THEY ARE NOT IN CONFLICT. LEAVE THEM.** A ground crewman NAMED on a weekend duty earns OIL. A placeholder on a weekend duty earns for the men it stands for, unless the admin switches it off. And ALL / ALL AVAIL do not include ground crew. **He rejected the framing, not the facts** — the two behaviours are different ACTS, not two answers to one question: naming a man is deliberate, the placeholder is a sweep of the available AIRCREW. Asking whether ground crew should be “in the window” was really asking to redefine what ALL AVAIL means, and the agent failed to say so. **Nothing to build; the walk finding is closed as not-a-defect.** The agent’s “option 1” (widen the placeholder to sweep ground crew) is DROPPED, and with it the open consequence D51 flagged — a crowd holds only aircrew, so pilots-left / WSOs-right needs no third column. | `raptor-port/docs/superpowers/specs/2026-09-22-oil-seats-behaviour-register.md`; `raptor-port/docs/handpass/parts/2026-09-22-oil-seats-leavewar.md` (F3, closed); `raptor-port/docs/handpass/2026-09-22-oil-seats.md` §7 |
| D51 | *“Yes 2 pucks a row, pilot left wso right”* — shown a comp of the real app at phone width with a crowd of 27 opened behind one placeholder, one man per row, running about 600 pixels (three quarters of the screen) for a single row | **INSIDE THE EARN MODE, A CROWD OPENS TWO ACROSS ON A PHONE, AND THE TWO COLUMNS CARRY SEAT IDENTITY — PILOTS LEFT, WSOs RIGHT.** Not a wrapped list that fills left-to-right: the LEFT column is the pilots and the RIGHT column is the WSOs, so the split says something rather than just saving space. That is the same pairing the sim seat grid already uses (front-seat left / rear-seat right, owner 13 Aug 26) and the same seat colours the app already paints (pilot olive, WSO green). **Consequence he accepted by choosing it:** the two columns are independent lists, so they are usually UNEQUAL in length and the row is as tall as the longer one — taller than a naive wrap would be, and worth it because a glance now shows how many of each. Desktop is unchanged (there is room there). **Open consequence, tied to his ground-crew answer:** a man who is neither pilot nor WSO has no column yet; if ground crew are ever swept into a crowd, where they sit has to be decided. | **CORRECTED THE SAME HOUR — this is not new, it is D38–D41.** The agent put the two-column question to him as an open choice without having read `DECISIONS.md` or `OUTSTANDING.md`, where the answer was already settled and APPROVED: D38 replaces the name bubble with a movable, resizable, non-blocking window of real pucks, pilots left and WSOs right, and **the SAME window serves both counters — who is available, and who is credited OIL, where he switches individual pucks off.** D39/D40 set one puck per row and a skinny default; D41 approved the mock-up as the design of record. So this ruling ADDS NOTHING NEW and is **not built here**: the surface it describes is `[ALL-AVAIL-WINDOW]`, already filed as job 2 in `OUTSTANDING.md`, straight after this one. Patching two-a-row into the current in-row crowd would be thrown away by that job. | `OUTSTANDING.md` `[ALL-AVAIL-WINDOW]` (job 2, where it is actually built); the approved mock-up `raptor-port/docs/mock/allavail-window.html` (D41); `raptor-port/docs/handpass/2026-09-22-oil-seats.md` §7 |
| D50 | *“Keep one spare seat showing”* — asked, during the walk, what to do about a sim row that is already full: it has nowhere to drop an extra person, so the drag a scheduler would naturally make lands on a seated man and replaces him with no message. Every other kind of row carries a thin drop strip under its people; the sim seat grid deliberately does not, to save a row of screen height | **A SIM ROW ALWAYS SHOWS ONE SPARE SEAT, EVEN WHEN IT IS FULL.** That empty seat IS the door for an instructor, an observer or a placeholder — exactly as a sim row with an odd number of people already shows one today. It costs one row of height on sim rows that are full, and he accepted that cost knowingly against the alternative of filing it. **The gap is PRE-EXISTING** (identical on `main`); what changed is that [OIL-SEATS-CAN-EARN] step 5 made that seat pay real people, so the promise that a placeholder on a sim row counts the men it stands for could not be exercised at all through the screen. He was offered “leave it and file it” and “only show the strip while dragging” and chose neither — the second because the tap-a-seat-then-pick-a-name order would still have had no door. **This NARROWS the 26 Aug 26 reasoning behind `.schedboard .ppl.fcprcp .addz{display:none}`** (“the seat grid auto-adds rows as pucks land, so it never packs edge-to-edge and swaps”), which is true for odd counts and false for even ones — fix the stale comment in the same change. | `raptor-port/src/ui/board-html.ts` (the sim seat cell); `raptor-port/src/ui/scheduler.css` (the `.fcprcp .addz` rule and its comment); `raptor-port/docs/ui-contracts.md`; `raptor-port/docs/handpass/2026-09-22-oil-seats.md` §6 finding 3 |
| D49 | *"It should still earn — leave it as it is"* — asked whether a flying line typed with the SAME take-off and landing minute should keep paying half a day, after being shown that the half day comes entirely from the three hours of report and two hours of debrief the app adds around it | **A FLYING LINE WHOSE WRITTEN TAKE-OFF AND LANDING ARE THE SAME STILL EARNS.** The man reported and debriefed, so he was at work whatever the times say — the squadron’s fact, which the code could not supply. **This OVERRULES the approved plan’s §5 step 8 (finding C2) and BOTH reviewers**, who read the zero-length interval as a typing slip the app should refuse to pay. The rule change is dropped: nothing about what such a line earns changes. **What is kept from that step:** the day must SAY the times look wrong, so somebody fixes them — a warning, not a refusal. **It also makes D48 moot for this case** (no rule changes under an issued day, so no issued day’s money moves); D48 stands as the principle for the next rule change, not as work to do now. **Third time the owner’s knowledge of the squadron corrected the agent’s reading of the code** — after D36 (the availability window) and D44 (membership frozen on every day). The plan’s own §5a lesson says it: a disagreement between two bodies of code is a QUESTION, not a verdict. | `raptor-port/docs/superpowers/specs/2026-09-22-oil-seats-can-earn-plan.md` §5 step 8 (marked overruled); `raptor-port/docs/superpowers/specs/2026-09-22-oil-seats-behaviour-register.md`; `raptor-port/docs/engine-rules.md` §the warning that replaces it |
| D48 | *"keep what’s published until corrected"* — asked what should happen to a day that has ALREADY been published carrying a flying line whose take-off and landing are the same minute, once that line stops earning | **AN ISSUED DAY KEEPS THE MONEY IT WENT OUT WITH WHEN A RULE CHANGES UNDER IT.** A day already published is held to the rule it was published under; the credit it carries stays until somebody corrects the times and re-publishes, and the correction then shows as an ordinary amendment. This is D45 applied to a RULE change rather than an availability change — nothing on a published schedule may change without the scheduler acknowledging it. **SUPERSEDED IN PRACTICE THE SAME HOUR BY D49**, which dropped the rule change this was given about — so no issued day’s money moves and there is nothing to guard. The ruling stands as the answer for the NEXT rule change that would move published money; nothing is built for it now. **Two of his earlier rulings pointed the other way and are narrowed, not overturned:** the 13 Sep 26 dev-phase rule (a problem living only in stored demo data is cleared, not migrated) and D22 (clearing demo data is approved whenever it is the simpler path) still hold for stored data that is merely WRONG — they do not cover money on a published record silently changing amount. The 7 Aug 26 "no rule versioning" invariant is untouched: this is one marker written onto a day as it is published so the app can tell a day issued before the rule from one issued after, not a versioned rulebook. | `raptor-port/docs/superpowers/specs/2026-09-22-oil-seats-can-earn-plan.md` §5 step 8; `raptor-port/docs/superpowers/specs/2026-09-22-oil-seats-behaviour-register.md`; pinned by the step-8 tests |
| D47 | *“Keep the refusal”* — asked whether a placeholder puck on a JET seat should be refused outright or merely warned about, after being shown that the two options differ in where the bugs land | **D33 STANDS WHOLE, AND IT BEATS THE 13 AUG 26 “PLACEHOLDER PUCK IS A SHORTCUT” RULE ON COCKPIT SEATS ONLY.** ALL and ALL AVAIL are refused on flying-line cockpit seats, with the reason on screen at every door. **The 13 Aug shortcut is NOT retired** — a placeholder puck still arms its seat everywhere a placeholder is legal (duty desks, sim seats and passengers, ground rows, the Common Programme, an accepted request row), and tapping an EMPTY cockpit seat still narrows the crew list as it always did. Only the cockpit instance of the shortcut is gone, because the puck can no longer get there. **What settled it:** the two lists are not the same question. Tapping an empty seat asks “who can fly THIS seat” — seat, qualification, SC currency, crew rest, busy, absences. The puck asks only “who has nothing else on at that time”, so on a front seat it would include WSOs, men not SC current and men who would break crew rest — and would then pay every one of them for a sortie they could not have flown. The two also use different windows (availability step→dekit; the flying rule report→debrief, D36). **The risk argument he asked for:** a refusal removes the state, so roughly ten downstream readers never have to handle it, and its failure mode is LOUD (a placeholder blocked where it should be allowed). A warning keeps the state, needs a cockpit branch in every one of those readers, and its failure mode is SILENT and about money. **Not covered, and belted separately:** a placeholder arriving from a copied day template, a parked plan or older saved data bypasses both doors — a jet seat credits nobody whatever sits in it. | `raptor-port/docs/ui-contracts.md` §Arm-and-plant; `raptor-port/docs/superpowers/specs/2026-09-22-oil-seats-can-earn-plan.md` §2 (clash list); pinned by `raptor-port/src/ui/interact.test.tsx` and `src/ui/oilseat-refusal.test.tsx` |
| D46 | *"Allow it crediting by default. But if u remove it the crediting will be removed right"* — on a placeholder puck dropped into ONE man’s accepted request row | **THE PLACEHOLDER IS ALLOWED ON AN ACCEPTED REQUEST ROW AND CREDITS BY DEFAULT. NO EXCEPTION TO D43 ANYWHERE.** He was offered three ways and took the one with no carve-outs: a request row behaves like every other seat. **Rejects Fable R2-4’s recommendation** (refuse the puck there) and the agent’s, which agreed with it. **His condition, checked before this was recorded, and it holds:** removing the puck removes the crediting. On a day not yet published the credits go at once; on a published day the issued version keeps paying the people it was issued with, the day reads pending, and re-issuing it sweeps the cells out of the Leave War — the reverse sweep clears any generated credit no published work still earns. **That is D44 and D45 working as he designed them**, which is why the answer is yes rather than a qualified maybe. **One thing the sweep never touches, worth knowing:** a hand-typed award is the admin’s and is never removed automatically. **What it costs the build:** step 6 keeps its third freeze path instead of losing it, and must cover BOTH the request row’s primary name box AND its extras (Codex OSE-R2-04 — the rewrite had covered only the extras) | `raptor-port/docs/superpowers/specs/2026-09-22-oil-seats-can-earn-plan.md` §5 step 6 and §7; the review log (R2-4 closed); on build: `raptor-port/docs/ui-contracts.md` |
| D45 | *"It should be like any other weekday, it shouldn’t. I sign my published schedule and just show pending"* — asked whether an availability change should invalidate a signature · and, stated as the principle underneath it: *"Things shouldn’t change silently without the scheduler acknowledging it."* | **A CHANGE IN WHO WAS AVAILABLE NEVER INVALIDATES A SIGNATURE. THE PENDING MARK IS THE WHOLE MECHANISM, AND EVERY DAY BEHAVES THE SAME.** He signs the published schedule; a later change in availability shows as pending and nothing more; he clears it by amending or by issuing the EOD version. **Rejects the signature-binding half of Codex OSE-05** while keeping the freeze whole (D44) — the freeze is about the document not changing under the reader, the signature is about authorising the schedule, and he has separated them. **It also rejects the agent’s own narrower proposal** (bind the signature only on a day that earns): he wants ONE behaviour for every day, not a money/no-money split, and the simpler rule is his to choose. **THE PRINCIPLE IS THE BIGGER HALF AND IT REACHES BEYOND THIS FEATURE:** nothing on a published schedule may change without the scheduler acknowledging it. That is WHY the freeze exists and WHY the pending mark exists — together they are the acknowledgement mechanism, and neither works alone. **Apply it as a test to any future change that touches an issued day:** if a reader could see a different answer tomorrow with nobody having acknowledged it, the design is wrong | `raptor-port/docs/superpowers/specs/2026-09-22-oil-seats-can-earn-plan.md` §4; `OUTSTANDING.md` `[EOD]` and `[OIL-SEATS-CAN-EARN]`; on build: `raptor-port/docs/ui-contracts.md` §Amendment marks, and `raptor-port/CLAUDE.md` §Stable decisions as a standing test |
| D44 | *"I think what we discussed just now is better to put it frozen. Because the scheduler will then see the change if someone takes a leave and they will then publish a latest amendment of the schedule or publish an EOD schedule version to show all that’s happened on the day itself as the truth. Which is in our process. Someone will review all the changes end of day and publishes a EOD version of the schedule"* | **WHO WAS BEHIND A PLACEHOLDER PUCK IS FROZEN AT PUBLICATION ON EVERY DAY, EARNING OR NOT.** The issued version keeps the people it was issued with; the working copy shows the live answer; the difference raises the ordinary pending mark, and the scheduler amends or publishes the EOD version. **OVERTURNS THE AGENT’S ARBITRATION** — it had chosen live-on-a-non-earning-day and argued that a pending mark for a number owing nobody anything would devalue a mark that does carry money. **His reasoning is better and the agent could not have reached it from the code:** that pending mark is not noise, it is the SIGNAL their end-of-day process runs on. Someone reviews every change at the close of the day and issues an EOD version as the record of what actually happened — and a change in who was available IS something that happened. **This makes Codex right and the agent wrong** (Codex OSE-05 wanted membership frozen on every publication and bound into the signature and the amendment comparison; Fable M6 wanted the split the agent took). **It also supersedes Fable’s R2-3 fix 1**, which would have recorded the membership only where it is money-bearing. **SECOND TIME his knowledge of the PROCESS corrected the agent’s reasoning about the code** — D36 was the first (the debrief window). The pattern is now named in the plan §5a: a disagreement between two bodies of code is a QUESTION, not a verdict, and only someone who runs the squadron can settle it | `raptor-port/docs/superpowers/specs/2026-09-22-oil-seats-can-earn-plan.md` §4 (the arbitration, now reversed) and §5 step 9; the review log; `OUTSTANDING.md` `[OIL-SEATS-CAN-EARN]` and `[EOD]`, which this leans on; on build: `raptor-port/docs/ui-contracts.md` |
| D43 | *"Duty desk, sim, any extra people line, including all that i’ve not mentioned should be turned on by default to credit OIL. All avail and all is not allowed to go to flying line anyway. So its not required"* — asked whether the two seats that pay today should keep paying once the broken ones are fixed | **THE PLACEHOLDER PUCKS BEHAVE EXACTLY LIKE NAMED PEOPLE: ON BY DEFAULT, EVERYWHERE THEY CAN LAND.** A crowd behind ALL / ALL AVAIL on a weekend or PH event earns, by default, the same as a man whose name is typed there — because they are doing the same work. Duty desks, sims, sim passengers, the extra-people line beside any row, ground rows, Common Programme rows: all ON. Switch one off deliberately if you do not want it. **SUPERSEDES the "default OFF for the placeholder" reading of D27**, and supersedes the agent’s plan (§5 step 3, "the placeholder seats default off") and BOTH reviewers’ recommendations — Fable wanted off everywhere, the agent wanted the two existing seats left paying and the rest off. **His answer is simpler than either and removes the worst finding in the whole review:** Fable’s M1 warned that switching those two off would recompute already-issued Saturdays and silently DELETE landed credits from the Leave War, with no amendment. Nothing is switched off, so that cannot happen, and no data reset is needed. **What it does NOT touch: D24 stands.** The four exempt KINDS — SC SPARE, AVALON lines, AVALON duty desks, BB lines, and a block minted from an AVALON template (D35) — still default OFF with the switch offered. Those are about the KIND of work, not about who is standing on it. **And the flying line does not arise**, in his words: D33 refuses the puck there, so it needs no default. The engine keeps a non-expanding belt on that branch anyway, for data that arrived by copy before the refusal existed (Fable M4). **Simplifies the build:** the span default is ON for everything except the four exempt kinds, so the placeholder needs no default of its own — it inherits the seat’s | `OUTSTANDING.md` `[OIL-SEATS-CAN-EARN]`; the review log (M1 closed by this); the D27 row below, marked superseded in part; on build: `raptor-port/docs/engine-rules.md` and the OIL behaviour register |
| D42 | Asked which day an overnight AVALON/BB line earns, he chose **the day the line sits on**, and corrected the agent: *“first option but ur description is incorrect. if its a sunday night line 1900-0700. It means that monday doesnt earn”* | **AN OVERNIGHT LINE EARNS ON THE DAY IT SITS ON, AND THE HOURS SPILLING PAST MIDNIGHT EARN THE NEXT DAY NOTHING.** A Sunday-night line 19:00–07:00 earns **Sunday**; **Monday earns nothing from it**. The day a line is drawn on is the day it pays — the same way every other reading in the app treats a day, and the same way it reads on screen. **New ground:** until now AVALON and BB could not earn at all, so no overnight line had ever reached the OIL rules; D24 opens that door and this is the first question it raises. **The agent stated the rule wrongly when putting it to him** — it gave the Sunday half and a muddying Friday example, and never said what happens to the day the hours spill INTO, which is the half he had to supply himself. The rule is recorded in HIS framing, not the agent’s | `OUTSTANDING.md` `[OIL-SEATS-CAN-EARN]`; the review log’s S5 (`…/specs/2026-09-22-oil-seats-can-earn-review-log.md`); on build: `raptor-port/docs/engine-rules.md` §Weekend/PH work earns OIL, and the OIL behaviour register |
| D41 | *"that mock up looks good"* — after three rounds of corrections | **THE MOCK-UP IS APPROVED AND IS NOW THE DESIGN OF RECORD FOR `[ALL-AVAIL-WINDOW]`.** `raptor-port/docs/mock/allavail-window.html` (also an Artifact) is no longer a sketch to argue with — it is what gets built, and a change to it now needs his word the same as any other settled design. **Everything it demonstrates is settled:** movable by the six-dot grip, resizable, does not block the schedule behind it; one puck per row, pilots left and WSOs right; 212px default and a 186px floor; flag reasons under their pucks, moving beside them when widened; the counter with no "free"; and the two-tab shape where the OIL half exists only while OIL Earn is on. **Recorded because “he saw it” and “he approved it” are different facts** and nothing else in the repo would tell them apart later. **What is NOT settled by this** — the mock is a comp, not the build: it uses invented people and one invented event, it does not read the real availability rule, and it has not been through a walk. The build still owes its own FULL-tier checks | `OUTSTANDING.md` `[ALL-AVAIL-WINDOW]` (now marked design-approved); the mock itself; on build: `raptor-port/docs/ui-contracts.md` |
| D40 | *"Why it still shows the window so wide by default. I should be skinny like my previous screen shot. And I prefer the 2nd picture 6 nicely placed neat dots to indicate that this window is movable. Not the dashed on the first picture top left"* | **THE WINDOW OPENS SKINNY, AND ITS DRAG HANDLE IS THE APP’S OWN SIX-DOT GRIP.** (1) **SKINNY BY DEFAULT** — tall and narrow, just wide enough for the two puck columns, not a wide panel with dead space beside each list. He can drag it wider; the DEFAULT is the narrow one. **Follows from D39**: once each man has his own row, the window only ever needs two puck-widths, and a puck is a fixed 74px. **He asked for skinnier twice** — 332px was still too wide — so the settled default is **212px**, which is the two 74px pucks plus their gap, the body padding and about 16px of slack per column. That is close to the floor: below ~186px a puck starts to clip, so 186 is the minimum the user can drag it to. **Build it to those numbers rather than re-deriving them.** **What this costs and how it is handled:** a flagged man’s reason will not fit beside his puck at that width, so it WRAPS onto its own line under him, and sits beside him only when the user drags the window wider. The reason never disappears. (2) **THE GRIP IS `⠿`** — the six-dot braille grip the app already uses on every draggable row and section header, not a dashed or hamburger glyph. **This is not only taste: it is the app’s existing vocabulary for “drag me”**, so the window is recognisable as movable without being explained. The mock had drifted off it while the file was being made encoding-proof | `OUTSTANDING.md` `[ALL-AVAIL-WINDOW]`; the mock-up (Artifact, rebuilt to this); on build: `raptor-port/docs/ui-contracts.md` |
| D39 | On the mock-up: *"it should show like this by default. Dont need to put the word free there."* — with a picture of the window drawn NARROW, each puck on its own row | **TWO FIXES TO THE WINDOW’S DEFAULT, BOTH FROM HIS OWN PICTURE.** (1) **ONE PUCK PER ROW.** Each column is a LIST running down, not a wrapping grid — pilots down the left, WSOs down the right. The mock-up let them wrap two and three to a row on a wide window, which is what the crew palette does; he saw the narrow version, where they stack, and chose that. So the stacking is the DEFAULT at every width, not a phone fallback. **Why it matters beyond taste:** a puck per row gives each man a full row for his flag and his figure, which is what makes them scannable — the whole point of replacing the name bubble. (2) **THE COUNTER CHIP DROPS THE WORD "free".** It reads `ALL AVAIL · 27`, not `ALL AVAIL · 27 free`. **Consistent with D37**, which said the count must read as what it is rather than as a promise — "free" is exactly the kind of claim D37 warns about, since the window’s own job is to show the men who are flagged rather than assert they are clear | `OUTSTANDING.md` `[ALL-AVAIL-WINDOW]`; the mock-up (Artifact, rebuilt to this); on build: `raptor-port/docs/ui-contracts.md` |
| D38 | *"the current interface to show just names on a bubble … is not intuitive. I think we should have a window opened that is movable and … resizable and a user can still click and edit/scroll the schedule behind while that window is still opened … show the pucks just like how the placeholder shows the personnel and I can click on the flagging as well … intuitive for the user to click on his puck and know that the ops brief he’s scheduled for as all avail is inbetween the standard debrief time … similar to the oil earned when I click on the counter … I can individually select the pucks on who doesn’t earn oil in that interface too … pilot then wso, left right column … Perhaps make a mock up before we execute this."* | **THE NAME BUBBLE IS REPLACED BY A MOVABLE, RESIZABLE, NON-BLOCKING WINDOW OF REAL PUCKS — ONE WINDOW, TWO JOBS.** Not a popup and not a sheet: it stays open while the scheduler scrolls and EDITS the schedule behind it, he can move it and resize it, and it draws the people as PUCKS in the placeholder panel’s own layout — pilots in the left column, WSOs in the right — carrying the same warning flags the rest of the app draws, clickable. **The SAME window serves both counters:** who is available behind an ALL AVAIL / ALL puck, and who is credited OIL — and in the OIL one he switches individual pucks off. **The reason it must SHOW rather than decide, in his own example:** a man whose ops brief sits inside his standard debrief should appear, flagged, so the scheduler sees the overlap and judges it. **This is the other half of D36 and explains it** — the availability window stays narrow (the man IS available) precisely because the app’s job is to surface the clash, not to remove him from the list. **MOCK-UP BEFORE BUILD, his ask** — and the house rule already says a visual direction gets a picture in the app’s own stylesheet at phone and desktop before any product code | `OUTSTANDING.md` `[ALL-AVAIL-WINDOW]` (new item, carries the design and the mock-up); on build: `raptor-port/docs/ui-contracts.md`. Sits AFTER `[OIL-SEATS-CAN-EARN]`, which builds the counters this window opens from |
| D37 | Having asked *"should I not have this all avail counter? It just makes things more in accurate and confusing? … maybe we don't need this function?"*, and been given the case both ways, he chose **"Keep it, worded honestly"** | **THE COUNT STAYS, ON EVERY SEAT THE PUCK CAN LAND ON — BUT IT MUST READ AS WHAT IT IS, NOT AS A PROMISE.** It counts who has nothing else on the programme at that time. It is a starting point a scheduler can correct, and the wording on screen has to say so; a bare number reads as a fact and this one is not. It taps through to the names, as it does today. **CONFIRMS D27's display half rather than withdrawing it**, and adds the wording requirement on top. **The argument that decided it, and the one to re-use if this is ever reopened:** because D28/D32 let the puck EARN, the app must resolve it to named men whether or not a number is shown — earned leave lands on people, not on a placeholder. So removing the count would not remove the guess, only hide it, and an invisible guess that banks leave is worse than a visible one. The missing count is also how the silent-credit defect was found at all: his Sunday desk showed no number, and underneath it the puck was crediting nobody. **He was right about the risk** — the fix is the wording, not the removal | `OUTSTANDING.md` `[OIL-SEATS-CAN-EARN]` half three; `raptor-port/docs/superpowers/specs/2026-09-22-oil-seats-can-earn-plan.md` §4–§5; on build: `raptor-port/docs/ui-contracts.md` (the exact wording) |
| D36 | *"sometimes we will schedule a brief much earlier to cater for a brief for all available to join after the brief. So like 1400 is the brief, a brief ends around 1520 and then ops brief commences at 1520 till step time of 1620. So technically the person is available to join the ops brief. There’s also scenarios in which when someone lands, instead of debriefing immediately within the 2 hours the management wants them to attend a ops brief then debrief after."* | **THE AVAILABILITY WINDOW STAYS NARROW — STEP BEFORE TAKE-OFF TO DEKIT AFTER LANDING. DO NOT WIDEN IT TO THE REPORT OR THE DEBRIEF.** A flying man counts as free up to his step and again from dekit; the 3-hour nominal report and the 2-hour flight debrief do NOT block him from an event. **His two scenarios are the proof, and they are why this is recorded rather than left to be re-derived:** an ops brief deliberately slotted between the flight brief and step (1520–1620 against a 1620 step) is a man who IS available, and a debrief that management moves to AFTER an ops brief is a man who is available straight after dekit. Widening the window breaks both — it would mark men unavailable for the very events the squadron schedules around them. **This settles a real disagreement rather than hiding it:** the warning list DOES treat land+2h as an obligation (it raises "Not enough time to attend the debrief"), and the OIL earning rules DO use report→debrief as the working day. Those two are correct for what they each decide; availability is a different question and keeps its own, narrower answer. **The agent recommended widening it and was wrong** — it read the disagreement as a defect without knowing how the squadron actually schedules. **Nothing to build; this exists so a later session does not "find" the gap and close it.** Money is unaffected either way: a man in debrief earns from his own sortie regardless, and OIL is measured across the whole day, not per event | `raptor-port/docs/engine-rules.md` (the availability window, on build); `raptor-port/docs/superpowers/specs/2026-09-22-oil-seats-can-earn-plan.md` §6; `OUTSTANDING.md` `[ALL-AVAIL-REDEF]`, which settled WHO counts but never how wide a flying man's tasking is |
| D35 | *"I remember we talked about SC Spare, Avalon lines, Avalon duty, BB lines, that are created from the templates will show OIL earned off by default. But the admin can still click to enable it"* — recalled unprompted while the merge ran | **RE-CONFIRMS D24 AND MAKES ITS TEMPLATE HALF EXPLICIT.** The ruling itself is not new: D24 already says the four exempt kinds OFFER the switch, defaulting OFF, and D20's second half already says a duty block MADE FROM AN AVALON TEMPLATE is treated the same. **What was only IMPLIED and is now written down: the SWITCH reaches the template-minted block too**, not just the no-earn default. D20 settled the default for the minting path because a test had watched the engine and not the mint; D24 opened the door on the four kinds; nobody had joined the two sentences, so a build could have given the switch to a hand-built AVALON desk and not to one made from the template — the same seat, two answers. **Nothing to re-decide** — this closes a wording gap, and `[OIL-SEATS-CAN-EARN]` half one now names the minting path in its own words | `OUTSTANDING.md` `[OIL-SEATS-CAN-EARN]` half one (now names the template path); the D24 and D20 rows below |
| D34 | **"Merge live now"** — chosen over the agent's recommendation that he look first | **`[OIL-AUTO-REMOVE]` GOES LIVE, AND HIS FIVE-MINUTE LOOK IS WAIVED.** The handoff named TWO conditions: his look at the walked Saturday, and the phrase. He gave the phrase and declined the look. **Record it because a later session must not assume the day was eyeballed** — the branch's evidence is the walk and the gates, not an owner sighting. Everything else that gates a merge was already true: own branch, 63 commits, mergeable, no conflicts, every check green, FULL-tier walk done with its evidence sheet. **The agent flagged the divergence before acting**, as the standing advisor rule requires, and he did not change his mind | this file; `HANDOFF-OIL-WALK.md` (now closed out); `OUTSTANDING.md` `[OIL-NEXT-TWO]` item 2, which was his own look and is now moot |
| D33 | Given three placement rules for the ALL / ALL AVAIL pucks, he chose **"Only flying lines refuse it"** — over the agent's recommendation that every ONE-PERSON seat refuse it | **THE TWO PLACEHOLDER PUCKS ARE REFUSED ON FLYING LINES ONLY — EVERYWHERE ELSE THEY LAND.** Refused: both cockpit seats on every flying line, whatever the wave (Standard, SC MAIN, SC SPARE, AVALON, BB), with the reason on screen. Allowed: the sim's own two seats, a duty desk's own position, sim passenger slots, the extra-people line beside any row, ground rows and common programme rows. **With D32 this is also the EARNING list** — those same places offer the OIL switch, off by default. **What the agent recommended and he set aside:** that every seat holding ONE NAMED BODY refuse it (cockpit seats, sim seats AND a duty desk's own position), because qualifications, crew pairing and the manning count all read such a seat. **The consequence he accepted, flagged to him before building:** a duty desk can now answer "who holds this desk?" with a crowd rather than a name, so its manning count reads a placeholder. That is defensible for a weekend duty anyone available covers, which is why it is his call. **Corrects his own premise:** he said "obviously flying lines can't have all avail" — checked, and the app does NOT stop it today; the puck drops into a cockpit seat with no refusal and no warning, the aircraft looks crewed and nobody is on it **— AMENDED 22 Sep 26 BY D43:** the placeholder pucks are ON by default wherever they can land, like named people; only the four exempt KINDS (D24/D35) default off. Read this row with that correction. | `OUTSTANDING.md` `[OIL-SEATS-CAN-EARN]`; on build: `raptor-port/docs/ui-contracts.md` (the refusal) and the OIL behaviour register |
| D32 | *"Then for places which all avail or all pucks can land, it can also have the option to earn oil"* — said while deciding whether to restrict where the two placeholder pucks may be dropped | **ONE LIST, NOT TWO: wherever a placeholder puck is ALLOWED TO LAND, it must also OFFER the OIL switch.** Landing and earning stop being separate questions — settle where ALL / ALL AVAIL may be dropped, and the earning door follows that answer exactly, defaulting to OFF like every other seat under D28. **Why it matters more than it looks:** the alternative was two lists that could drift apart, which is precisely the defect he found on his phone — a seat that ACCEPTED the puck but paid nobody. A seat that takes the puck and cannot offer the switch is the same silent hole in a new place. **So the placement decision now carries the money decision with it**, and any later request to allow the puck somewhere new automatically carries the switch. Applies to both sentinels equally — ALL and ALL AVAIL are byte-for-byte the same semantics **— AMENDED 22 Sep 26 BY D43:** the placeholder pucks are ON by default wherever they can land, like named people; only the four exempt KINDS (D24/D35) default off. Read this row with that correction. | `OUTSTANDING.md` `[OIL-SEATS-CAN-EARN]` (half three now states the one-list rule); on build: `raptor-port/docs/ui-contracts.md` and the OIL behaviour register |
| D31 | Asked whether a seat the rules genuinely cannot measure — no times, zero length, cancelled, ⓘ — is switchable too or stays refused, he chose **"Refuse, with the reason shown"** | **A SEAT WITH NOTHING TO MEASURE OFFERS NO OIL SWITCH — IT SAYS WHY INSTEAD.** No times on it, zero length, cancelled, or ⓘ: no switch, and a short line on screen giving the reason. **This is the one boundary on D28.** D28 says every seat can earn and the default decides — but that assumes a stretch of time the rules can measure. Where there is no window, a credit would be invented rather than earned, so the answer is refusal WITH ITS REASON, never a silent absence. **Closes the question D28 left open**, which is the last thing that stood between `[OIL-SEATS-CAN-EARN]` and being buildable. **It does not weaken D28:** every seat that CAN be measured still offers the switch, including the three exempt kinds and both sentinel pucks. Two rejected alternatives, for the record: a greyed-out switch on every seat (same information, more drawing, and a dead control invites a press), and fully switchable everywhere (the day would have to invent a length to credit against) | `OUTSTANDING.md` `[OIL-SEATS-CAN-EARN]` (the open question is now answered there); on build: `raptor-port/docs/ui-contracts.md` (the refusal wording) and the OIL behaviour register |
| D30 | *"Nvm I'll do what fable recommend"* — on how much of the backlog-process work to do, after the agent had offered three different recommendations in three messages | **FABLE'S ORDER IS THE PLAN OF RECORD FOR `[DOCS-GUARD]`. Do not re-decide the scope or the sequence.** All of it, in its order: **(1) F1 + F3** — the inventory check and the gate that can never demand a trim inside a code change, one docs/scripts pass, ~1 h; these make everything after them enforceable. **(2) F2 + F6** — the docs-only correction pass, ~45 min, done UNDER the new gate: the four live texts that still order trim-on-touch, and the rulings with no D-number. **(3) F4 items 1/3/4 and F5 items 1–2**, ~30 min. **(4) F5's archive mover and F7** when the next archive pass is actually due. Total ~2–2½ h, none of it touching `raptor-port/src`. **Why his call and not the agent's:** the agent recommended F1+F3 first, then reversed to F2-only as the cheap win, then reversed back to F1+F3 — three positions on the same question, which is precisely the condition the bug-check order §4 rank 4 describes (the builder defending, and re-arguing, the process that missed the defect). The owner ended it by taking the outsider's ranking whole. **The lesson for the agent is not the ordering, it is that a settled outside ranking beats a third re-derivation of its own.** | `OUTSTANDING-ARCHIVE.md` `[DOCS-GUARD]` (all four steps done and the item archived, 23 Sep 26); the findings and exact steps are `raptor-port/docs/superpowers/specs/2026-09-22-backlog-process-attack.md`; `HANDOFF-OIL-WALK.md` §3 |
| D29 | *"Seems like compressing is causing more harm?"* — then, given three options, **"1"** (do the archive split now) | **THREE STANDING CHANGES TO HOW THE BACKLOG IS KEPT.** (1) **FINISHED WORK LEAVES `OUTSTANDING.md`** for `OUTSTANDING-ARCHIVE.md` — searched, never read whole, never appended to by hand — the same shape `HANDOFF-ARCHIVE.md` already has. Measured before the split: 1,186 lines, of which **345 across 20 items were finished work** — 29% of a file every session reads. (2) **A RULING NEVER LIVES ONLY IN THE BACKLOG.** Every one gets its home in `DECISIONS.md`. The trigger was a near-miss the same hour: the agent was one step from retiring `[OIL-AWARD-ADD-RULING]` — *"an award and a worked day add up. So it's 4"* — which has no D-number to this day. **CORRECTED 22 Sep 26 by Fable's attack on this very entry:** the agent wrote that the ruling "existed in no other file", and that was WRONG — it is N16 in the one-absence behaviour register. The real point survives and is narrower: its only homes are a tier-3 spec and the backlog, so no session reads it, and the backlog is the file being trimmed. (3) **NEVER TRIM DOCS IN THE SAME CHANGE AS A FIX.** A blocked docsize gate means the file needs its own pass, NOT that old entries get carved up to make room. **Why this is structural and not a promise to be careful:** the agent read the bounded-edit rule the same day and still destroyed two filed items with an unbounded span replace, because the gate forced the trim to happen under pressure inside a money change. The coupling was the fault, not the carelessness | `OUTSTANDING-ARCHIVE.md` (**DONE** — 8 items, 94 lines, every item verified into exactly one file). **THE OTHER TWO HOMES THIS ROW ORIGINALLY CLAIMED WERE NOT TRUE WHEN IT WAS WRITTEN, and Fable's attack caught it — anti-pattern #4, the comment that vouches, committed by the entry that exists to stop exactly this.** `OUTSTANDING.md` §Maintaining this file still says "move the item to Done", and `raptor-port/scripts/docsize.mjs` is unmodified. **The ceiling must NOT be ratcheted down** — Fable measured seven of eight gated files at ZERO headroom, so ratcheting the eighth to zero would recreate the pressure this ruling exists to remove; that clause is withdrawn. **DONE 23 Sep 26 under `[DOCS-GUARD]` (D30):** the three rules are in `OUTSTANDING.md` §Maintaining; the four texts that ordered trim-on-touch are corrected (memory `outstanding-tasks-file`, `.claude/rules/record-decisions.md`, `raptor-port/docs/doc-budget.md` §3, and the gate's failure message); and it is STRUCTURAL — `raptor-port/scripts/docsize.mjs` now fails a lost, duplicated or truncated record, never demands a trim inside a code change, and checks every row's home. Fable's report: `raptor-port/docs/superpowers/specs/2026-09-22-backlog-process-attack.md` |
| D28 | *"Currently what doesnt earn oil no matter what when I put the puck there? I remember I just put default to turn the oil credit off but it still can be turned on. If everywhere in the schedule can earn oil, then the all avail or all puck should also be able to earn oil"* | **ONE PRINCIPLE INSTEAD OF AN ALLOW-LIST: every seat can earn, the DEFAULT decides whether it does, and the admin can always override.** He reached this himself from D24 — if SC spare, AVALON and BB become switchable-but-off, then singling out ALL AVAIL / ALL is inconsistent, so those pucks earn on the same footing as everything else. **This SUPERSEDES his own earlier lean the same day** (*"it doesn't make sense for all avail and all to be on duty"*, recorded under D27) and it also supersedes the agent's recommendation, which was a short per-seat earning list — his rule is simpler and removes the whole class of "this seat silently pays nobody" defects rather than enumerating around it. **It does NOT change what is owed by default:** nothing earns that does not earn today; what changes is that everything can be turned on deliberately. **Merges D24 and `[OIL-SEATS-CAN-EARN]` into ONE change** — both are now the same question, in the same engine file. **Still open and his to answer:** whether a seat the rules genuinely cannot measure (no times, zero length, cancelled, ⓘ) is also switchable, or stays refused with its reason on screen — the agent's view is that those stay refused, because there is no window to measure and a credit would be invented rather than earned **— AMENDED 22 Sep 26 BY D43:** the placeholder pucks are ON by default wherever they can land, like named people; only the four exempt KINDS (D24/D35) default off. Read this row with that correction. | `OUTSTANDING.md` `[OIL-SEATS-CAN-EARN]` (now the single home for the combined change) and `[OIL-SEATS-CAN-EARN]`; `raptor-port/docs/handpass/2026-09-22-oil-walk.md` §11a (the display-versus-earning cost, which still holds) |
| D27 | *"my intent for all avail should also display who is avail even when the oil earn is not selected. So by default when that puck lands on an area in the schedule. It will auto calculate who would be available to attend. And the counter would show."* · and, as his current LEAN not yet a ruling: *"it doesn't make sense for all avail and all to be on duty… but I want the flexibility to do so in the event that happens"* | **ALL AVAIL / ALL ARE A SCHEDULING FEATURE, NOT AN OIL FEATURE.** Dropping one on any area of the schedule works out who would be available to attend and SHOWS THE COUNT — by default, with OIL Earn switched OFF. Today the count exists only inside the OIL mode's decoration, so it appears only where OIL is being decided; this makes it an ordinary property of the puck. **It EXTENDS `[ALL-AVAIL-REDEF]` (21 Sep) rather than reversing it:** that ruling settled WHO counts as available (no ground crew, a SANS man only when planned with us, ATT B in, a time clash out) and this one settles WHERE the answer is shown. Same membership rule, one body. **SEPARATES DISPLAY FROM EARNING**, which had been conflated: showing who is available is a read and owes nobody anything; earning is money. His lean is that neither puck should EARN on a duty desk, and he asked whether allowing them everywhere would cost bugs — the agent's answer and recommendation are in `[OIL-SEATS-CAN-EARN]`; the earning list is NOT settled until he says so **— AMENDED 22 Sep 26 BY D43:** the placeholder pucks are ON by default wherever they can land, like named people; only the four exempt KINDS (D24/D35) default off. Read this row with that correction. | `OUTSTANDING.md` `[OIL-SEATS-CAN-EARN]` (the two halves, and the seat list awaiting his word) and `[ALL-AVAIL-REDEF]`; on build: `raptor-port/docs/ui-contracts.md` and the OIL behaviour register |
| D26 | *"I think i choose 3. Because I can still settle OIL on my phone easily from my point of view."* — on the OIL mode's phone tap targets (hand-pass finding 12) | **LEAVE THE TAP TARGETS AS THEY ARE. Settled; do not re-open, and do not re-file it as a defect.** The measurement stands as a fact: at 390px the mode draws 71 tappable things, median 15px tall, all 71 under the 44px comfortable-finger figure. **What was WRONG was the agent's inference from it** — option 3 was put to him as "the mode stays a desktop job, meaning you cannot settle OIL from your phone", and he corrected that directly: he does it on his phone and finds it easy. So the number never made the mode unusable; that was a guess dressed as a consequence. He is the one who uses it, on his own phone, and his judgment governs. **Nothing to build** — no taller rows, no phone list view. `[OIL-PHONE-TARGETS]` is closed as RULED, with its date, so it is never re-fixed later as a bug (standing order §7.6). If a real user ever complains about mis-taps, that is new evidence and a new question, not this one re-asked | `OUTSTANDING.md` (`[OIL-PHONE-TARGETS]`, closed as ruled); `raptor-port/docs/handpass/2026-09-22-oil-walk.md` §6 item 12 and §7 |
| D25 | *"Actually I think u deviate from what OIL CREDITS means. It just means that this person earns OIL which means off in lieu. Which means that they earn a day of leave to be used next time."* | **OIL IS EARNED LEAVE, NOT PAY — say it that way.** A credit is a day (or half-day) of time off banked for the man to take later; it lands on the Leave War as an FO/HO cell and moves his OIL balance. Nothing is disbursed, nothing touches salary. **A correction to the agent's vocabulary, not to the build:** checked the same hour — every word the app puts ON SCREEN is already right (*earns*, *credit*, *OIL balance*, *lands on the Leave War as an HO cell*), and no user-facing string anywhere says paid, pay or money. What was wrong was the agent's reports, which said "pays a man" and "money defects" throughout, and the same shorthand in code comments. **What it does NOT change:** the bug-check order's tier question 1 still fires on OIL, because it asks what a man is OWED as well as paid, and earned leave is owed — that wording is now made explicit so a later reader cannot conclude OIL is out of scope. Code comments carrying the old shorthand are left alone on this branch (churn at its last gate) and filed | `raptor-port/docs/bug-check-order.md` §5 question 1 (made explicit); `raptor-port/docs/superpowers/specs/2026-09-21-oil-behaviour-register.md` (the definition, added at the head); `OUTSTANDING.md` `[OIL-WORDS]` for the comment sweep |
| D24 | *"is it too late to revert that SC spare, Avalon and BB could also earn OIL? Reason is because they could be activated and if they are, the admin can just easily click credit OIL with an amendment. But by default they are not going to earn OIL."* | **SUPERSEDES D15 AND D20 IN PART — the three exempt kinds become CREDITABLE, but still earn nothing by default.** SC SPARE, AVALON flying lines, AVALON duty desks and BB flying lines must OFFER the OIL switch in the mode, defaulting to OFF, so that when one of them is actually activated the admin can credit it — on a published day through an ordinary amendment. What D15 and D20 settled still holds for the DEFAULT (none of them earns unless someone says so); what changes is that today they are wholly inert, with no switch and no door, which is the one case the data allows and no screen offers. **Not a one-line change:** all three are skipped BEFORE anyone enters the calculation (`oil.ts` — `saExemptKind` for AVALON/BB, `f.spare`/`ac.spare` for the spare), so no item key and no person-window exist for a credit to attach to; the engine has to let them in while defaulting off. **Sequencing CONFIRMED BY HIM the same day** (*"ok put that in outstanding?"* — accepting the recommendation below): NOT on `claude/oil-auto-remove-design`, which is at its last gate — it lands next, on its own branch, at FULL tier. Nothing here needs unpicking, because the new default reproduces today's behaviour exactly | `OUTSTANDING.md` (queued, with its place in the order); `docs/superpowers/specs/2026-09-21-oil-behaviour-register.md` (OIL19/OIL21a, where D15 is pinned, now marked superseded-in-part); the D15 and D20 rows below |
| D23 | *"Work Codex's open findings before the remaining jobs — start with its Rank 4"* · *"Each one red first"* · *"I'll be sleep and u can try to finish all the bug jobs"* | **A REVIEWER'S OPEN FINDINGS OUTRANK THE PLANNED JOB LIST.** The settled plan's §2 order is set aside until every open finding from Codex's read is closed — its ranks 4, 2, 3, 5 and 6, in that order, Rank 4 first because it is the unfinished half of a repair already reported as done. Only then do the remaining numbered jobs (3, 4, 5, 6, 9) resume in their own order. Each fix is proved RED first through its own production function, and nothing merges without his "merge live" | `raptor-port/docs/superpowers/specs/2026-09-22-oil-fixplan-settled.md` §2 (the job list now opens with the findings queue); `…/2026-09-22-oil-fix-handoff.md` |
| D22 | *"if it's easier to reset why not"* (on Fable's advice to clear stored data rather than reconstruct a missing field) | **Clearing the demo data is APPROVED whenever it is the simpler path** — a re-confirmation of the dev-phase stance that this app is pre-promulgation and a defect living only in existing data is cleared, not migrated. It was NOT taken this time, because the reconstruction was already built and proved by test, so resetting would have been more churn and lost his data for no gain. The fallback stands if the reconstruction is ever shown wrong | `raptor-port/docs/superpowers/specs/2026-09-22-oil-fix-handoff.md`; memory `dev-phase-reset-demo-data-not-migrate` |
| D21 | *"1 nope."* — then, after asking for the full context and being given both sides: *"My answer nope still stays."* — on whether a day tagged **"Off day"** should earn OIL | **AN "OFF DAY" DOES NOT EARN OIL. Settled; do not reopen.** It is time GIVEN to a man, not a public holiday he was called in on, so working it earns nothing — only "PH" turns a day into one that can earn. This closes hand-pass finding 16, which both reviewers had already read as a stale sentence in a written contract rather than a bug: **no code changes, the contract wording does.** It also means the Leave War's own second event type stays deliberately inert for OIL, and leave charging is untouched. **RE-CONFIRMED the same night after he asked what the question actually was and was given both sides** — so this is a settled answer, not an off-hand one | `raptor-port/docs/ui-contracts.md` (the stale sentence, corrected); `docs/superpowers/specs/2026-09-21-oil-behaviour-register.md`; evidence `docs/handpass/parts/blocks-g.md` finding 16 |
| D20 | *"Likewise for Avalon flight and duty and BB flight too. No oil"* · *"Or when someone creates a new block using the Avalon rules it will also not get OIL credited"*. | **SUPERSEDED IN PART 22 Sep 26 by D24** — the DEFAULT stands (none of them earns unless someone says so), but they stop being inert: the mode must offer the switch so an activated one can be credited. **RE-CONFIRMS D15 and EXTENDS it to the creation path.** AVALON flying lines, AVALON duty desks and BB flying lines earn no OIL — and neither does a duty block a user MAKES from a template whose "For wave" is AVALON. The second half is the new part: D15 was pinned by a test that hand-built an AVALON desk, so it watched the engine and not the mint. Driven in the app and now pinned by a test with a control, all three refuse to earn even when the strongest possible green is forced onto them, because the exempt wave is skipped before anyone enters the calculation | `src/engine/oil.test.ts` (the new template-mint test, named for D15); evidence in `docs/superpowers/specs/2026-09-22-oil-fixplan-settled.md` §2 item 8 |
| D19 | *"6. Perhaps indicate that the leave war period doesn't exist, create it"* | **A weekend or holiday that no Leave War period covers must NAME the reason and offer the way out, not just refuse.** Today the day shows a full green bar, promises "earns a full day of OIL", tells the scheduler to publish it, and afterwards reports "No conflicts flagged for this day ✓" — while no period exists for that date, so nothing can ever land. The fix is no longer "say it cannot earn": the day says **the leave war period for that year does not exist**, and offers to create it. Creating a period is a real Leave War record with bidding dates and a stage, so it is never made silently from a schedule screen — the offer creates it and hands the scheduler to the Leave War to set the window. **This widens fix 6 from a wording change into one that reaches the Leave War**, which is the owner's call and he has made it | `docs/handpass/2026-09-21-oil.md` §6 row 6; `docs/superpowers/specs/2026-09-22-oil-fixplan-settled.md` §2 item 5; on build: `docs/ui-contracts.md` and the OIL behaviour register |

## 21 Sep 2026

| # | His ruling, in his words where short enough | What it means | Where it lives now |
|---|---|---|---|
| D18 | *"I agree with all so far. 1-5. for 2 he should earn."* | **All five hand-pass findings accepted as real, and finding 2 settled his way: a second man the scheduler puts on a member's landed request row EARNS from it, the same as the man who filed it.** This CHANGES the input-row rule (OIL31), which until now let the claim own the row so the schedule half skipped everyone else standing on it. The other four are his approval to fix: the shift switch drawn on empty lines, the missing "not in force yet" mark on a puck, the silent placeholder on a desk, and the missing line when a day pays nobody | `docs/engine-rules.md` §Weekend/PH work earns OIL (the rule itself); the OIL behaviour register (OIL31 revised); `docs/ui-contracts.md` §The green edge; evidence `docs/handpass/parts/blocks-ab.md` |
| D17 | *"Isint playwright the same as browser test? Why didn't u choose that in the first place?"* | **For a long hand pass, drive the app with a SCRIPTED real browser from the start**, not the in-app browser panel. Both run the real bundle, but the panel is one click per message, resizes under you and times out on screenshots, so the pictures the standing order requires come out unusable. The scripted driver replays a whole day in about a minute, saves full-size pictures to disk, and is what makes handing the work to parallel agents possible. Looking at the pictures is still the agent's job — the tool changed, the looking did not | `raptor-port/docs/bug-check-order.md` §7.2; the reusable driver `raptor-port/scripts/handpass/` |
| D16 | *"Should we run agents to expedite this?"* → *"But if u spawn agents that are opus would that be better?"* | **A long hand-test pass is FANNED OUT across parallel Opus agents, not walked serially.** Partition by WORLD (its own port, its own browser tab, its own copy of the demo data) so no two agents share a fixture; hand each the fixture recipe rather than letting each rediscover it. The standing caution is unchanged and is the price of the speed: an agent's "found nothing" is weaker evidence than the host's own look, so each returns PICTURES and a filled table, and the host REPRODUCES every finding before it enters the evidence sheet | `raptor-port/docs/bug-check-order.md` §4 (a new paragraph under "where to spend the other models"); this session's evidence sheet `raptor-port/docs/handpass/2026-09-21-oil.md` |
| D15 | *"SC MAIN should be earning OIL. SC Spare not earning"* · *"Flying waves should be earning OIL"* · *"Common programme should be earning oil"* · *"Avalon and BB not earning OIL is ok. The duties for Standard. SC shift should be earning OIL, but Avalon duties should not"* | **SUPERSEDED IN PART 22 Sep 26 by D24** — for SC SPARE, AVALON and BB only, and only on the DOOR: they still earn nothing by default, but a switch must be offered. Every other clause of D15 stands. **The acceptance criteria for the whole feature, in his words** — said while looking at a real Saturday. EARN: flying lines, SC MAIN, Common Programme, Standard and SC-shift duty desks, ground rows. DO NOT: SC SPARE, AVALON/BB lines, AVALON/BB desks, ⓘ rows. Confirms what the engine already did; now pinned by tests rather than assumed | register OIL19/OIL21a; `ui/oilmode.test.tsx` §the green strip reaches every kind of seat; verified by hand on the board AND the week |
| D14 | *"theres going to be alot of context for the AI to read ... you record down word for word on what i mentioned. Not sure if u can summarise it ... reading so much context as an AI it starts to hallucinate"* | Records state the DECISION, not the transcript. Quote him only where the exact words are load-bearing; otherwise one line + date + a pointer. Docs are tiered by how often they must be read, and the always-read tier has a budget that can only go DOWN | `raptor-port/docs/doc-budget.md`; gate `npm run docsize`; `OUTSTANDING.md` → `[DOC-TRIM]` |
| D13 | *"How do we fix your behavior lapse?"* (on rulings not being written down) | Record a ruling the moment it is made, in `DECISIONS.md`, before doing the work it implies — enforced by a hook on every message and a `Rulings:` line in every report | `.claude/rules/record-decisions.md`; `.claude/hooks/record-decisions.sh`; this file |
| D12 | *"Just to confirm the bug check is not designed only for this OIL check scenario right? ... it can be interface, rules, cross platform and more"* | The bug-check order must adapt to any kind of work, not just this feature | `raptor-port/docs/bug-check-order.md` §2a |
| D11 | *"how does it integrate with the Claudex review workflow. Make sure that's covered"* | Claudex owns the plan and the code read; the order owns the running app. The walk goes BEFORE Claudex's final inspection, and money work needs both providers, not one | `…/bug-check-order.md` §4a; `.claude/rules/bug-check.md` |
| D10 | *"The previous methods I told I to bug check. I think this is the most comprehensive one. So just follow this instead"* | The bug-check order SUPERSEDES the 16 Sep scenario rule, the 20 Sep rules sweep and the 21 Sep test-like-a-human rule. One method, not four | `…/bug-check-order.md` §0a; the three superseded memories now point at it |
| D9 | *"the bug check order auto automatically kick in based on the right scenario and will recommend what checks to do ... So that i dont need to figure out what kind of bug checks to execute"* | The order fires itself; the agent states the tier and the checks; the owner never picks them | `…/bug-check-order.md` §0a; `.claude/rules/bug-check.md` |
| D8 | *"Can u ask fable and codex to come up with a robust way to bug check, I want to save that as a standing order"* | Both providers proposed independently; the merged result is the adopted order | `…/bug-check-order.md`, plus both proposals verbatim in `…/superpowers/briefs/` |
| D7 | *"there are also different way to execute bug tests ... Do we think we should execute all 3 to be robust?"* — answered yes, proportionately | Code read, automated tests and a hand pass each catch what the others cannot; run all three, scaled by what the change touches | `…/bug-check-order.md` §3 and §5 |
| D6 | *"U can ask fable and codex to think through all the possible scenarios to test for bugs and opus be the executor"* | The other providers DESIGN the scenarios; Opus executes them in the running app | `…/bug-check-order.md` §4 rank 1; the two lists in `…/specs/2026-09-21-oil-scenarios-{fable,codex}.md` |
| D5 | *"Remember this way of testing henceforth when a bug test is made"* | Driving the running app IS the bug test; a code review plus green tests is not one | `…/bug-check-order.md` §0; memory `bug-tests-drive-the-app-like-a-human` |
| D4 | *"all the previous bug tests we did there will be bugs not captured ... perhaps the next session we can do that after this task is truely completed"* | Every earlier feature checked the review-plus-tests way is carrying the same class of defect; sweep them after OIL closes | `OUTSTANDING.md` → `[HUMAN-RETEST]`, priority 1a |
| D3 | **R-2** — the two pre-existing money bugs are fixed on this branch, not filed | They share the root cause with R-1, so they are one coherent change | `…/specs/2026-09-21-oil-bugcheck-fixplan.md`; `docs/engine-rules.md` §Weekend/PH work earns OIL |
| D2 | **R-1** — only the issued schedule pays, BOTH directions | A holiday declared after publication waits for a republication and the day says so; revoking one no longer sweeps money silently | `docs/engine-rules.md`; `…/specs/2026-09-21-oil-bugcheck-fixplan.md`; tests in `leavewar/oilsync.test.ts` |
| D1 | **O-1** — the green bar shows only on the events that COUNTED towards a man's day | Supersedes §2.10 / OIL21 ("repeated on every puck he wears"). The ALL AVAIL count chip agrees with it again (O-3 re-examined) | register OIL21 + OIL21a/b; `docs/ui-contracts.md` §The green edge; `ui/oilmode.ts` |

### Carried, still open for him

| # | The question | Why it is his |
|---|---|---|
| Q1 | On a published day, an OIL decision not yet published changes the green bar on the scheduler's own screen while the Leave War still pays the issued figure — and nothing on the puck says "pending". A typed edit gets a dotted amendment mark; an OIL decision gets only the day's aggregate "1 change". | Product direction: whether a pending money change should look different from one in force. Raised by Fable's S4; to be shown to him in the app first. |

---

## Before this file existed

Rulings made before 21 Sep 26 are not listed here — they are in `raptor-port/CLAUDE.md`
§Stable decisions, in the per-feature behaviour registers under `docs/superpowers/specs/`, and in
the memory index. **Do not back-fill them wholesale**; add an old ruling here only when it is
re-confirmed, superseded or found to be stale, so this file stays a record of live decisions rather
than a second copy of the archive.
